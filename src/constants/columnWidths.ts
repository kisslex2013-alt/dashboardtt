/**
 * 🔧 Дефолтные значения ширины столбцов
 *
 * Этот файл содержит дефолтные значения для всех пользователей.
 * При сохранении "как дефолт" эти значения обновляются автоматически.
 *
 * ВАЖНО: Эти значения автоматически обновляются при сохранении столбцов в dev режиме.
 * После деплоя эти значения используются для всех пользователей (включая инкогнито).
 *
 * Последнее обновление: 2025-11-09 (текущие дефолтные значения)
 */

import type { GridColumnWidths, TableColumnWidths } from '../types'

export const DEFAULT_GRID_COLUMN_WIDTHS: GridColumnWidths = {
  percentMargin: 34, // Расстояние от даты до процентов
  insightsMargin: 303, // Расстояние до инсайтов
  totalMargin: 158, // Расстояние до итого
}

export const DEFAULT_TABLE_COLUMN_WIDTHS: TableColumnWidths = {
  checkbox: 40, // Фиксированная ширина чекбокса
  time: 150, // Время работы (13:36—16:30)
  category: 444, // Название категории
  hours: 91, // Часы (2.90ч)
  income: 100, // Доход (1000₽)
}
