# PaymentDatesSettingsModal

Модальное окно для настройки дат выплат с календарным видом и расширенными возможностями управления.

## 📋 Описание

Компонент позволяет пользователям:
- Добавлять новые даты выплат
- Редактировать существующие выплаты
- Удалять выплаты
- Изменять порядок выплат (drag & drop)
- Настраивать даты, периоды и цвета
- Выбирать даты через календарный интерфейс

## 🏗️ Структура компонента

```
PaymentDatesSettingsModal/
├── index.tsx                    # Главный компонент
├── PaymentCalendar.tsx          # Календарь с логикой выбора
├── PaymentDateItem.tsx          # Отдельная карточка выплаты
├── PaymentDateForm.tsx          # Форма добавления/редактирования
├── PeriodSelector.tsx           # Селектор периода
├── hooks/
│   ├── usePaymentCalendar.js   # Логика календаря
│   ├── usePaymentSelection.js   # Логика выбора дат
│   └── usePaymentValidation.js  # Валидация
└── utils/
    ├── calendarHelpers.js       # Вспомогательные функции календаря
    └── paymentFormatters.js     # Форматирование дат
```

## 📦 Props

```typescript
interface PaymentDatesSettingsModalProps {
  /** Открыто ли модальное окно */
  isOpen: boolean
  /** Callback при закрытии модального окна */
  onClose: () => void
}
```

## 🎯 Основные функции

### Управление выплатами
- **Добавление**: Создание новой выплаты с настройкой даты, периода и цвета
- **Редактирование**: Изменение существующих параметров выплаты
- **Удаление**: Удаление выплаты с подтверждением
- **Переупорядочивание**: Drag & drop для изменения порядка

### Календарный выбор
- Выбор дня выплаты через календарь
- Выбор периода (начало и конец) через календарь
- Навигация по месяцам
- Визуальная индикация выбранных дат

### Валидация
- Проверка корректности дат
- Проверка периодов (начало < конец)
- Проверка уникальности дат
- Визуальная индикация ошибок

## 🔧 Используемые хуки

### usePaymentCalendar
Управление состоянием календаря:
- Текущий месяц и год
- Навигация по месяцам
- Генерация календарной сетки

### usePaymentSelection
Логика выбора дат и периодов:
- Выбор дня выплаты
- Выбор периода (начало и конец)
- Обработка изменений

### usePaymentValidation
Валидация данных выплат:
- Проверка корректности дат
- Проверка периодов
- Проверка уникальности

## 📊 Состояние компонента

```typescript
{
  paymentDates: PaymentDate[]      // Массив выплат
  editingId: string | null          // ID редактируемой выплаты
  draggedId: string | null          // ID перетаскиваемой выплаты
}
```

## 🔄 Интеграция со store

Использует следующие селекторы из `useSettingsStore`:
- `usePaymentDates()` - получение сохраненных выплат
- `useAddPaymentDate()` - добавление новой выплаты
- `useUpdatePaymentDate()` - обновление выплаты
- `useDeletePaymentDate()` - удаление выплаты
- `useReorderPaymentDates()` - изменение порядка выплат

## 💡 Примеры использования

### Базовое использование

```tsx
import { PaymentDatesSettingsModal } from './components/modals/PaymentDatesSettingsModal'

function App() {
  const [isOpen, setIsOpen] = useState(false)

  return (
    <>
      <button onClick={() => setIsOpen(true)}>
        Настроить даты выплат
      </button>
      <PaymentDatesSettingsModal
        isOpen={isOpen}
        onClose={() => setIsOpen(false)}
      />
    </>
  )
}
```

## 🎨 Стилизация

Компонент использует Tailwind CSS классы и поддерживает:
- Темную и светлую тему
- Адаптивный дизайн для мобильных устройств
- Анимации открытия/закрытия через BaseModal

## ⚠️ Известные ограничения

1. Максимальное количество выплат не ограничено, но рекомендуется не более 50 для оптимальной производительности
2. Календарь показывает только текущий месяц (навигация по месяцам доступна)
3. Drag & drop работает только на десктопе (на мобильных используется кнопочное управление)

## 🐛 Отладка

Для отладки используйте:
- React DevTools для проверки состояния
- Console логи через `logger` утилиту
- Проверка store через Zustand DevTools

## 📝 Связанные компоненты

- `BaseModal` - базовый компонент модального окна
- `PaymentCalendar` - календарный компонент
- `PaymentDateItem` - карточка выплаты
- `PaymentDateForm` - форма редактирования

## 🔗 Связанные файлы

- `src/store/useSettingsStore.ts` - store для настроек
- `src/components/ui/BaseModal.tsx` - базовый модальный компонент
- `src/utils/uuid.ts` - генерация ID

---

**Последнее обновление:** 2025-11-17  
**Версия:** 1.0

