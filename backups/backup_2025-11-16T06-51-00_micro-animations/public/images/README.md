# 📁 Папка для изображений

## 📸 QR код для поддержки

Чтобы использовать ваш собственный QR код вместо автоматически сгенерированного:

1. **Сохраните изображение QR кода** в эту папку с именем: `qr-code-alexey.png`
   - Поддерживаемые форматы: `.png`, `.jpg`, `.jpeg`, `.svg`
   - Рекомендуемый размер: 200x200 пикселей или больше

2. **Обновите файл** `src/components/modals/AboutModal.jsx`:
   - Найдите функцию `getQRCodeUrl()`
   - Раскомментируйте строку: `return '/images/qr-code-alexey.png';`
   - Закомментируйте или удалите код генерации через API

3. **Пересоберите проект:**
   ```bash
   npm run build
   ```

---

## 📝 Пример использования

Если ваш QR код называется `my-qr-code.png`:

1. Переименуйте его в `qr-code-alexey.png`
2. Положите в папку `public/images/`
3. Обновите `AboutModal.jsx` как описано выше

---

**Примечание:** Если изображение не найдено, будет использован автоматически сгенерированный QR код через API.
