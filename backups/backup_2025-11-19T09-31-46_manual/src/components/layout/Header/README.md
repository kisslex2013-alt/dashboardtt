# Header

Главный компонент шапки приложения с навигацией, переключателем темы и быстрыми действиями.

## 📋 Описание

Компонент Header является основным элементом навигации приложения и включает:
- Логотип и название приложения
- Переключатель темы (светлая/тёмная)
- Выбор цветовой схемы
- Панель быстрого старта
- Контролы сравнения периодов
- Кнопки экспорта/импорта данных
- Мобильное меню (hamburger menu)

## 🏗️ Структура компонента

```
Header/
├── index.tsx                    # Главный компонент
├── components/
│   ├── AppLogo.tsx             # Логотип приложения
│   ├── ThemeToggle.tsx         # Переключатель темы
│   ├── ColorSchemeSelector.tsx # Выбор цветовой схемы
│   ├── QuickStartPanel.tsx     # Панель быстрого старта
│   ├── ComparisonControls.tsx  # Контролы сравнения
│   └── AINotificationsButton.tsx # Кнопка AI уведомлений
└── README.md                    # Документация
```

## 📦 Props

```typescript
interface HeaderProps {
  /** Callback для показа обучалки */
  onShowTutorial: () => void
  /** Callback для показа окна "О приложении" */
  onShowAbout: () => void
  /** Callback для показа настроек звука */
  onShowSoundSettings: () => void
  /** Включен ли режим сравнения */
  compareMode: boolean
  /** Callback для переключения режима сравнения */
  onToggleCompare: () => void
  /** Callback для открытия категорий */
  onOpenCategories: () => void
  /** Callback для открытия резервных копий */
  onOpenBackups: () => void
  /** Callback для экспорта данных */
  onExport: () => void
  /** Callback для импорта данных */
  onImport: () => void
}
```

## 🎯 Основные функции

### Управление темой
- Переключение между светлой и тёмной темой
- Выбор цветовой схемы (default, claymorphism, soft-pop)
- Плавные переходы между темами

### Быстрый старт
- Панель для быстрого начала работы
- Выбор категории и запуск таймера
- Интеграция с основным таймером приложения

### Сравнение периодов
- Включение/выключение режима сравнения
- Выбор периода для сравнения (сегодня, неделя, месяц, год)
- Отображение статистики за выбранный период

### Мобильная адаптация
- Автоматическое переключение на hamburger menu на мобильных устройствах
- Адаптивная верстка для разных размеров экрана
- Оптимизация для touch-интерфейсов

## 🔧 Используемые хуки

### useIsMobile
Определение мобильного устройства для адаптивной верстки.

### useTheme, useSetTheme
Управление темой приложения через Zustand store.

## 📊 Состояние компонента

```typescript
{
  comparePeriod: 'today' | 'week' | 'month' | 'year'  // Выбранный период
  isMobileMenuOpen: boolean                            // Открыто ли мобильное меню
}
```

## 🔄 Интеграция со store

Использует следующие селекторы из `useSettingsStore`:
- `useTheme()` - получение текущей темы
- `useSetTheme()` - установка темы
- `useThemeTransitionType()` - тип перехода между темами

## 💡 Примеры использования

### Базовое использование

```tsx
import { Header } from './components/layout/Header'

function App() {
  const handleShowTutorial = () => {
    // Показать обучалку
  }

  return (
    <Header
      onShowTutorial={handleShowTutorial}
      onShowAbout={() => {}}
      onShowSoundSettings={() => {}}
      compareMode={false}
      onToggleCompare={() => {}}
      onOpenCategories={() => {}}
      onOpenBackups={() => {}}
      onExport={() => {}}
      onImport={() => {}}
    />
  )
}
```

## 🎨 Стилизация

Компонент использует:
- Tailwind CSS классы
- Glassmorphism эффекты (`glass-effect`, `glass-button`)
- Адаптивные классы для мобильных устройств
- Темная и светлая тема

## ⚠️ Известные ограничения

1. Мобильное меню открывается только на экранах < 640px (sm breakpoint)
2. Некоторые функции могут быть недоступны в мобильной версии
3. Переключение темы требует перезагрузки страницы для применения некоторых стилей

## 🐛 Отладка

Для отладки используйте:
- React DevTools для проверки состояния
- Проверка store через Zustand DevTools
- Console логи через `logger` утилиту

## 📝 Связанные компоненты

- `MobileMenu` - мобильное меню
- `StatisticsDashboard` - панель статистики
- `ThemeToggle` - переключатель темы
- `QuickStartPanel` - панель быстрого старта

## 🔗 Связанные файлы

- `src/store/useSettingsStore.ts` - store для настроек
- `src/hooks/useIsMobile.ts` - хук определения мобильного устройства
- `src/components/layout/MobileMenu.tsx` - мобильное меню

---

**Последнее обновление:** 2025-11-17  
**Версия:** 1.0

