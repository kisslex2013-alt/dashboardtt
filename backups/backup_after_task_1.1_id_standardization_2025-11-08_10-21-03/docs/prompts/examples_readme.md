# 📚 Примеры кода для AI

> **ВАЖНО:** Эта папка содержит эталонные примеры кода для Cursor AI.  
> Файлы здесь НЕ импортируются в приложение - они только для обучения AI.

---

## 🎯 Назначение

Эта папка содержит идеальные примеры кода, которые Cursor AI использует как основу при создании новых файлов.

**Для разработчиков:**
- Изучайте эти примеры чтобы понять best practices проекта
- Используйте как reference при написании кода

**Для Cursor AI:**
- Читай соответствующий пример перед созданием нового файла
- Адаптируй структуру и стиль под конкретную задачу
- Следуй всем паттернам и правилам из примера

---

## 📁 Структура файлов

### `component.example.jsx`
**Когда использовать:**
- При создании любого React компонента
- Команды: "компонент [название]", "создай компонент"

**Что содержит:**
- Правильная структура импортов
- JSDoc документация
- Обучающие комментарии 🎓
- useState, useEffect примеры
- Glass-effect стили
- Анимации Tailwind
- Accessibility (a11y)
- Примеры использования

**Паттерны:**
- Named export (`export function Name`)
- Деструктуризация пропсов
- Условная стилизация через template literals
- Обработчики событий с префиксом `handle`
- Состояние загрузки (isLoading)

---

### `hook.example.js`
**Когда использовать:**
- При создании кастомных React хуков
- Команды: "хук [название]", "создай хук"

**Что содержит:**
- useState, useEffect, useCallback, useRef
- Cleanup функции в useEffect
- Оптимизация через useMemo/useCallback
- Возврат объекта с методами
- Обучающие комментарии о каждом хуке

**Паттерны:**
- Имя начинается с `use`
- Используй useCallback для функций
- Используй useRef для значений без ререндера
- Всегда добавляй cleanup в useEffect
- Документируй все возвращаемые значения

---

### `store.example.js`
**Когда использовать:**
- При создании Zustand хранилищ
- Команды: "store [название]", "создай store"

**Что содержит:**
- Конфигурация Zustand с persist
- CRUD операции (Create, Read, Update, Delete)
- Геттеры для вычисляемых значений
- Фильтры и сортировка
- Batch операции

**Паттерны:**
- Named export (`export const useName`)
- Иммутабельные обновления (spread, map, filter)
- НИКОГДА не изменяй state напрямую
- Группировка по категориям: STATE, CRUD, GETTERS
- Возврат полезных значений (id, boolean, count)

---

### `utility.example.js`
**Когда использовать:**
- При создании утилит и helper функций
- Команды: "утилита [название]", "создай утилиту"

**Что содержит:**
- Чистые функции (pure functions)
- Валидация входных параметров
- Обработка ошибок
- Форматирование данных
- Примеры использования

**Паттерны:**
- Named export для каждой функции
- ВСЕГДА валидируй параметры
- Выбрасывай понятные ошибки
- Переиспользуй другие утилиты
- Документируй формат данных

---

### `validation.example.js`
**Когда использовать:**
- При создании функций валидации форм и данных
- Команды: "валидация [название]", "создай валидацию"

**Что содержит:**
- Проверка обязательных полей
- Проверка формата (regex)
- Проверка диапазонов
- Кастомные правила
- Возврат `{ isValid, errors }`

**Паттерны:**
- Имя начинается с `validate`
- Возврат объекта с isValid и errors
- errors - объект где ключ = имя поля
- Понятные сообщения об ошибках на русском

---

### `calculation.example.js`
**Когда использовать:**
- При создании функций расчета времени/денег
- Команды: "расчет [название]", "создай расчет"

**Что содержит:**
- Валидация входных данных
- Парсинг времени
- Математические операции
- Округление результатов
- Обработка edge cases

**Паттерны:**
- Имя начинается с `calculate`
- Полная валидация перед расчетом
- Проверка корректности данных
- Понятные ошибки для пользователя
- Округление до 2 знаков (деньги)

---

## 🤖 Инструкции для Cursor AI

### Процесс создания нового файла:

1. **Определить тип файла:**
   - Компонент → `component.example.jsx`
   - Хук → `hook.example.js`
   - Store → `store.example.js`
   - Утилита → `utility.example.js`
   - Валидация → `validation.example.js`
   - Расчет → `calculation.example.js`

2. **Прочитать соответствующий пример:**
   ```
   Используй MCP filesystem для чтения файла:
   await fs.readFile('.examples/[тип].example.[ext]')
   ```

3. **Проверить существующие файлы:**
   ```
   Используй MCP filesystem для проверки:
   await fs.readDir('src/[целевая-папка]')
   
   Если похожий файл существует:
   - Предложить переиспользовать
   - Спросить создавать ли новый
   - Показать различия
   ```

4. **Адаптировать пример:**
   - Заменить имена на конкретные из задачи
   - Адаптировать логику под требования
   - Сохранить все паттерны и структуру
   - Сохранить обучающие комментарии

5. **Проверить перед созданием:**
   - [ ] Соответствие `.cursorrules`
   - [ ] Все паттерны из примера применены
   - [ ] Есть JSDoc документация
   - [ ] Есть обучающие комментарии 🎓
   - [ ] Есть примеры использования
   - [ ] Валидация входных данных
   - [ ] Обработка ошибок

6. **Создать файл:**
   ```
   Используй MCP filesystem:
   await fs.writeFile('[путь]', [содержимое])
   ```

7. **Показать результат:**
   - Путь к созданному файлу
   - Краткое описание что создано
   - Пример использования
   - Следующие шаги

---

## 📋 Чеклист качества кода

При создании ЛЮБОГО файла проверь:

### Общие требования:
- [ ] Named export (не default)
- [ ] JSDoc комментарии
- [ ] Обучающие комментарии 🎓
- [ ] Примеры использования в JSDoc
- [ ] Валидация входных данных
- [ ] Обработка ошибок
- [ ] Понятные имена переменных/функций
- [ ] Комментарии на русском языке

### Для компонентов:
- [ ] Деструктуризация пропсов
- [ ] Tailwind классы (не inline стили)
- [ ] Glass-effect стили где уместно
- [ ] Анимации (hover, transition)
- [ ] Accessibility (aria-label, role)
- [ ] Адаптивность (md:, lg: префиксы)
- [ ] Состояние загрузки если нужно

### Для хуков:
- [ ] Имя начинается с `use`
- [ ] useCallback для функций
- [ ] useMemo для вычислений
- [ ] Cleanup в useEffect
- [ ] Зависимости указаны правильно
- [ ] Возврат объекта с методами

### Для Zustand stores:
- [ ] Используется persist если нужно
- [ ] Иммутабельные обновления
- [ ] CRUD операции полные
- [ ] Геттеры для вычислений
- [ ] Группировка по категориям
- [ ] Batch операции где уместно

### Для утилит:
- [ ] Чистые функции (pure)
- [ ] Валидация всех параметров
- [ ] Throw Error при невалидных данных
- [ ] Округление чисел (toFixed)
- [ ] Переиспользование других утилит

---

## 💡 Примеры запросов

### Правильные запросы к AI:

```
✅ Создай компонент EarningsCard для отображения заработка
   (AI прочитает component.example.jsx и адаптирует)

✅ Создай хук useTimer для управления таймером
   (AI прочитает hook.example.js и адаптирует)

✅ Создай store для хранения настроек приложения
   (AI прочитает store.example.js и адаптирует)

✅ Создай утилиту для расчета налогов
   (AI прочитает utility.example.js и адаптирует)
```

### Неправильные запросы:

```
❌ Создай компонент
   (Недостаточно информации)

❌ Сделай что-нибудь для таймера
   (Неясная задача)

❌ Нужен store
   (Нет деталей что хранить)
```

---

## 🔄 Обновление примеров

Если вы обновляете примеры:

1. **Обновите README.md** с описанием изменений
2. **Протестируйте** что AI использует новые паттерны
3. **Обновите .cursorrules** если нужно
4. **Сделайте коммит** с понятным сообщением

```bash
git add .examples/
git commit -m "docs: обновлены примеры - добавлен паттерн XYZ"
```

---

## 🎓 Для начинающих разработчиков

Эта папка - ваш **учебник по best practices** проекта.

**Как использовать:**

1. **Изучите каждый пример файл**
   - Читайте обучающие комментарии 🎓
   - Разбирайте каждый паттерн
   - Пробуйте воспроизвести сами

2. **Сравнивайте с реальным кодом**
   - Откройте файл из `src/`
   - Сравните с примером
   - Найдите применённые паттерны

3. **Задавайте вопросы AI**
   ```
   Объясни мне паттерн [название] из component.example.jsx
   Почему используется useCallback в hook.example.js?
   Зачем нужна иммутабельность в store.example.js?
   ```

4. **Практикуйтесь**
   - Создавайте компоненты по примерам
   - Модифицируйте под свои задачи
   - Экспериментируйте с кодом

---

## ⚠️ Важно

- ❌ **НЕ импортируйте** файлы из `.examples/` в приложение
- ❌ **НЕ изменяйте** примеры без обновления README
- ✅ **ВСЕГДА используйте** примеры как основу для нового кода
- ✅ **ВСЕГДА читайте** комментарии и объяснения

---

## 📞 Вопросы?

Если что-то непонятно - спросите у Cursor AI:

```
Объясни мне как работает [паттерн] в .examples/[файл]
Покажи примеры использования [функция] из .examples/
Почему в примерах используется [техника]?
```

AI прочитает примеры и объяснит простым языком!