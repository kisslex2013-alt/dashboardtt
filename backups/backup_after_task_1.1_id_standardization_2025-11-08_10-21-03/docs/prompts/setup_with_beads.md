# 🚀 Полная установка автоматизации для Zero Coder

## 🎯 Что мы устанавливаем

### 1. **MCP Memory** (Запоминание контекста)
- Запоминает ваши предпочтения
- Помнит структуру проекта
- Помнит решения между сессиями

### 2. **Beads** (Управление задачами)
- AI автоматически создает задачи
- Отслеживает зависимости
- Находит что делать дальше
- Планирует долгосрочные фичи

### 3. **Улучшенный .cursorrules**
- Инструкции для AI
- Шаблоны кода
- Быстрые команды

### 4. **Примеры кода** (.examples/)
- Эталонные шаблоны
- Обучающие материалы

---

## 📦 Шаг 1: Установка Beads

**Запрос в Cursor Terminal (`Ctrl+` `):**

```bash
# Установка через официальный скрипт
curl -fsSL https://raw.githubusercontent.com/steveyegge/beads/main/install.sh | bash

# Проверка установки
bd --version
```

**Если ошибка "bd: command not found":**

```bash
# Добавить в PATH (для Mac/Linux)
echo 'export PATH="$PATH:$(go env GOPATH)/bin"' >> ~/.zshrc
source ~/.zshrc

# Или для bash
echo 'export PATH="$PATH:$(go env GOPATH)/bin"' >> ~/.bashrc
source ~/.bashrc
```

---

## 📦 Шаг 2: Инициализация Beads в проекте

**Запрос в Cursor Chat (`Cmd+L`):**

```
Инициализируй Beads в моем проекте:

1. Запусти команду: bd init --prefix ttm
   (ttm = Time Tracker Module)

2. Настрой Git hooks для автоматической синхронизации:
   - pre-commit hook для экспорта issues
   - post-merge hook для импорта issues
   - post-checkout hook для синхронизации

3. Добавь в .gitignore:
   .beads/*.db
   .beads/*.db-*
   
4. Добавь в Git (НЕ игнорируй):
   .beads/issues.jsonl

5. Проверь что все работает:
   bd list
   bd quickstart

Объясни что делает каждый hook и зачем он нужен.
```

---

## 📦 Шаг 3: Обновление .cursorrules с поддержкой Beads

**Запрос в Cursor Composer (`Cmd+I`):**

```
Обнови мой .cursorrules файл, добавив раздел про Beads:

ДОБАВЬ в конец файла:

## 🎯 УПРАВЛЕНИЕ ЗАДАЧАМИ ЧЕРЕЗ BEADS

### AI должен использовать Beads для:

1. **Долгосрочное планирование** (задачи на несколько дней/недель):
   - Разбивка больших фич на подзадачи
   - Отслеживание зависимостей
   - Планирование архитектуры

2. **Автоматическое создание задач во время работы**:
   - Обнаружил баг → создать issue
   - Нашел TODO → создать issue
   - Придумал улучшение → создать issue

3. **Поиск следующей задачи**:
   - `bd ready --json` показывает что можно делать
   - Только задачи без блокирующих зависимостей

### Workflow с Beads:

#### Когда пользователь просит создать большую фичу:

1. **Разбить на подзадачи и создать в Beads:**
   ```bash
   # Создать эпик
   bd create "Добавить экспорт в Excel" -t epic -p 1 --json
   
   # Создать подзадачи с зависимостями
   bd create "Установить библиотеку xlsx" -t task -p 1 --json
   bd create "Создать функцию экспорта" -t task -p 1 --json
   bd create "Добавить кнопку в UI" -t task -p 2 --json
   bd create "Написать тесты" -t task -p 2 --json
   
   # Настроить зависимости
   bd dep add ttm-3 ttm-2 --type blocks  # UI зависит от функции
   bd dep add ttm-4 ttm-3 --type blocks  # Тесты зависят от UI
   ```

2. **Показать план пользователю:**
   ```bash
   bd dep tree ttm-1
   bd ready --json
   ```

3. **Начать с первой готовой задачи:**
   ```bash
   READY=$(bd ready --limit 1 --json)
   echo "Начинаем с задачи: $READY"
   bd update ttm-2 --status in_progress
   ```

#### Когда обнаружил новую работу во время выполнения:

```bash
# Создать issue для обнаруженной проблемы
bd create "Исправить баг с валидацией дат" -t bug -p 0 --json

# Связать с текущей задачей
bd dep add ttm-5 ttm-2 --type discovered-from
```

#### Когда завершил задачу:

```bash
bd close ttm-2 --reason "Реализовано и протестировано"
bd ready --json  # Показать что стало доступно
```

### Команды Beads для AI:

**Создание:**
- `bd create "Название" -t [bug|feature|task|epic|chore] -p [0-4] --json`

**Поиск работы:**
- `bd ready --json` - задачи без блокеров
- `bd ready --limit 5 --json` - топ-5 приоритетных
- `bd blocked` - заблокированные задачи

**Управление:**
- `bd update <id> --status [open|in_progress|closed]`
- `bd close <id> --reason "Описание"`
- `bd show <id> --json`
- `bd list --json`

**Зависимости:**
- `bd dep add <from> <to> --type [blocks|related|parent-child|discovered-from]`
- `bd dep tree <id>` - визуализация графа
- `bd dep cycles` - поиск циклических зависимостей

### Когда НЕ использовать Beads:

- ❌ Быстрые правки (одна команда)
- ❌ Простые вопросы пользователя
- ❌ Рефакторинг одного файла
- ❌ Исправление опечатки

### Когда ОБЯЗАТЕЛЬНО использовать Beads:

- ✅ Фича займет >1 часа работы
- ✅ Есть зависимости между задачами
- ✅ Работа на несколько дней
- ✅ Архитектурные изменения
- ✅ Обнаружил проблему во время другой задачи

### Интеграция с MCP Memory:

**Memory хранит:**
- Предпочтения пользователя
- Структуру проекта
- Принятые решения

**Beads хранит:**
- Конкретные задачи
- Зависимости
- План работы

**Пример связки:**
```
Memory: "Пользователь хочет экспорт в Excel с фильтрами"
Beads: ttm-15 "Добавить экспорт в Excel" (эпик с 4 подзадачами)
```

### Ежедневная проверка для AI:

При начале каждой сессии:
1. `bd ready --json` - что можно делать сегодня
2. Показать пользователю список готовых задач
3. Спросить с чего начать или предложить приоритетную

---

Сохрани обновленный .cursorrules и проверь что все корректно.
```

---

## 📦 Шаг 4: Создание примеров и структуры

**Запрос в Composer (`Cmd+I`):**

```
Выполни полную установку системы автоматизации:

1. ЗАМЕНИ .cursorrules на Enhanced Version из артефакта

2. СОЗДАЙ папку .examples/ со всеми примерами:
   - component.example.jsx
   - hook.example.js
   - store.example.js  
   - utility.example.js
   - README.md

3. СОЗДАЙ первые задачи в Beads для ознакомления:
   bd create "Изучить систему автоматизации" -t task -p 1 -d "Прочитать SETUP-GUIDE.md и попробовать создать компонент" --json
   bd create "Создать первый компонент с помощью AI" -t task -p 2 -d "Попробовать команду: компонент TestCard" --json

4. СОЗДАЙ SETUP-GUIDE.md в корне с полной инструкцией:
   - Что установлено (MCP Memory + Beads + примеры)
   - Как работает Memory (запоминание)
   - Как работает Beads (задачи)
   - Быстрые команды для создания кода
   - Команды Beads для управления задачами
   - Примеры использования обеих систем
   - Ежедневный workflow
   - Troubleshooting

5. ПРОВЕРЬ установку:
   - bd list показывает созданные задачи
   - .examples/ содержит все файлы
   - .cursorrules обновлен
   - Git hooks работают

После всего покажи краткую шпаргалку по использованию.
```

---

## 🎯 Шаг 5: Первое использование

### Протестируйте систему:

**1. Проверка Beads:**
```
Покажи мне готовые задачи в Beads:
bd ready --json
```

**2. Проверка Memory:**
```
Сохрани в memory что я предпочитаю:
- Tailwind для стилей
- Zustand для state
- Обучающие комментарии обязательны
```

**3. Создание кода с автоматическими задачами:**
```
Создай фичу "Экспорт данных в PDF":

1. Разбей на подзадачи в Beads
2. Покажи граф зависимостей
3. Начни с первой задачи
4. По ходу работы создавай issues если что-то найдешь
```

**4. Проверка автоматизации:**
```
компонент ExportButton
(должен использовать примеры из .examples/)
```

---

## 📋 Ежедневный Workflow

### Утро (Начало работы):

```
Доброе утро! Покажи:
1. Что мы делали вчера (memory)
2. Готовые задачи на сегодня (bd ready)
3. С чего лучше начать
```

### В процессе работы:

**AI автоматически:**
- Создает issues когда находит проблемы
- Связывает их с текущей задачей
- Обновляет статусы
- Закрывает выполненные задачи

**Вы просто работаете:**
```
Продолжи работу над экспортом в PDF
(AI сам проверит Beads, найдет текущую задачу, продолжит)
```

### Вечер (Конец работы):

```
Подведи итоги дня:
1. Что сделано (закрытые issues)
2. Что осталось (open issues)
3. Что будем делать завтра (ready tasks)
```

---

## 🔧 Полезные команды

### Memory (используется автоматически):
```
# AI сохраняет автоматически, но можно явно:
Запомни что я использую [технологию/подход]
Сохрани это решение в memory
```

### Beads (AI использует автоматически):
```
# Просмотр задач
bd list                    # Все задачи
bd ready                   # Готовые к работе
bd blocked                 # Заблокированные
bd show ttm-5              # Детали задачи

# Граф зависимостей  
bd dep tree ttm-1          # Дерево зависимостей
bd dep cycles              # Найти циклы

# Статистика
bd stats                   # Общая статистика
```

### Быстрые команды для кода:
```
компонент [Название]       # Создать компонент
хук [название]             # Создать хук
store [название]           # Создать store
утилита [название]         # Создать утилиту
```

---

## 💡 Примеры реальных сценариев

### Сценарий 1: Добавление новой фичи

**Вы:**
```
Добавь фильтрацию по датам в таблицу записей
```

**AI автоматически:**
1. Создаст эпик в Beads: "Добавить фильтрацию по датам"
2. Разобьет на подзадачи с зависимостями
3. Покажет граф: UI → Store → Utils
4. Начнет с первой задачи (Utils)
5. В процессе найдет баг → создаст issue
6. Закроет задачу, покажет следующую

---

### Сценарий 2: Продолжение работы на следующий день

**Вы:**
```
Привет! С чего начнем сегодня?
```

**AI:**
```
Привет! Вчера мы работали над фильтрацией по датам.

Memory показывает:
- Предпочитаешь date-fns для дат
- Используем Zustand для фильтров

Beads показывает:
✅ Готово: Утилиты для дат (ttm-23)
🔄 В работе: Store для фильтров (ttm-24, 60% готово)
⏳ Ждут: UI компонент (ttm-25, зависит от ttm-24)

Продолжим ttm-24 (Store для фильтров)?
```

---

### Сценарий 3: Обнаружение проблемы

**В процессе работы AI находит баг:**

```
🐛 Обнаружил проблему: валидация дат не работает для високосных годов

Создал issue:
ttm-26: "Исправить валидацию для високосных годов"
- Тип: bug
- Приоритет: 1 (высокий)
- Связано с: ttm-24 (discovered-from)

Продолжить текущую задачу или исправить баг сейчас?
```

---

## 🎓 Для Zero Coder

### Что делает Memory:
🧠 **Память между сессиями**
- Помнит ваши предпочтения
- Помнит как вы любите писать код
- Помнит решения которые приняли

### Что делает Beads:
📋 **Умная доска задач**
- AI создает задачи автоматически
- Отслеживает что от чего зависит
- Знает что делать дальше
- Не забывает ничего

### Вместе они:
🚀 **Супер-мозг для разработки**
- Memory помнит "КАК" вы работаете
- Beads помнит "ЧТО" нужно сделать
- Вы просто говорите что хотите
- AI делает правильно и по плану

---

## ⚠️ Troubleshooting

### Beads не находит задачи:
```bash
bd list  # Должно показать задачи
# Если пусто - задачи еще не созданы
# Попросите AI создать первую задачу
```

### Memory не работает:
```
# Проверьте mcp.json
cat mcp.json | grep memory

# Должно быть:
"memory": {
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-memory"]
}
```

### Git hooks не работают:
```bash
# Проверьте права
ls -la .git/hooks/

# Должны быть executable
chmod +x .git/hooks/pre-commit
chmod +x .git/hooks/post-merge
```

### AI не использует примеры:
```
# Проверьте что папка существует
ls -la .examples/

# Должны быть файлы:
# component.example.jsx
# hook.example.js  
# store.example.js
# utility.example.js
# README.md
```

---

## 📞 Нужна помощь?

Спросите у AI:

```
Объясни как работает [Memory/Beads/примеры]
Покажи примеры использования [система]
Почему [что-то] не работает?
```

AI прочитает эту инструкцию и поможет! 🤖