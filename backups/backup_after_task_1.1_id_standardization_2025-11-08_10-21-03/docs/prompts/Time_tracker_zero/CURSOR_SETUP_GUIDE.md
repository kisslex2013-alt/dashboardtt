# 🎯 ПОЛНОЕ РУКОВОДСТВО ПО НАСТРОЙКЕ CURSOR ДЛЯ TIME TRACKER

## 📋 ОГЛАВЛЕНИЕ
1. [Что такое .cursorrules и MCP](#что-такое-cursorrules-и-mcp)
2. [Установка и настройка .cursorrules](#установка-cursorrules)
3. [Настройка MCP серверов](#настройка-mcp-серверов)
4. [Рекомендуемые расширения Cursor](#рекомендуемые-расширения)
5. [Пошаговая инструкция активации](#пошаговая-инструкция)

---

## 🤔 ЧТО ТАКОЕ .CURSORRULES И MCP?

### .cursorrules файл
**Это специальный файл с инструкциями для Cursor AI**, который:
- Определяет стиль кодирования проекта
- Устанавливает правила именования
- Задает структуру компонентов
- Указывает используемые библиотеки
- Настраивает поведение AI ассистента

**Аналогия:** Это как "конституция" для вашего проекта, которую AI будет соблюдать.

### MCP (Model Context Protocol)
**Это серверы, которые расширяют возможности Cursor**, предоставляя:
- Доступ к файловой системе
- Работу с базами данных
- Интеграцию с внешними API
- Автоматизацию задач
- Дополнительные инструменты

**Аналогия:** Это "плагины" или "расширения" для Cursor, дающие ему суперспособности.

---

## 📝 УСТАНОВКА .CURSORRULES

### ШАГ 1: Создание файла .cursorrules

**Где создавать:** В КОРНЕВОЙ папке вашего проекта (рядом с package.json)

```
time-tracker/
├── .cursorrules          ← СОЗДАТЬ ЗДЕСЬ
├── package.json
├── src/
└── ...
```

### ШАГ 2: Содержимое .cursorrules для Time Tracker

Создайте файл `.cursorrules` и скопируйте туда следующее:

```markdown
# Time Tracker Dashboard - Cursor AI Rules

## 🎯 КОНТЕКСТ ПРОЕКТА
Это приложение для учета рабочего времени с аналитикой на React + Zustand + Tailwind CSS.
Разработчик: начинающий (zero coder) - объяснять все простым языком.

## 📚 ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Основные библиотеки:
- React 18.2+ (функциональные компоненты + хуки)
- Zustand 4.4+ (state management)
- Tailwind CSS 3.4+ (стилизация)
- Vite 5+ (сборщик)
- Recharts 2.12+ (графики)
- Tone.js 14.7+ (звуки)
- date-fns (работа с датами)
- lucide-react (иконки)
- @headlessui/react (модальные окна)

### НЕ используем:
- Redux (используем Zustand)
- CSS/SCSS файлы (только Tailwind)
- Class компоненты (только функциональные)
- PropTypes (используем TypeScript при необходимости)

## 🏗️ АРХИТЕКТУРА И СТРУКТУРА

### Структура папок:
```
src/
├── components/
│   ├── layout/      # Header, Footer, FloatingPanel
│   ├── entries/     # Компоненты записей времени
│   ├── statistics/  # Статистика и аналитика
│   ├── charts/      # Графики
│   ├── modals/      # Модальные окна
│   ├── settings/    # Настройки
│   └── ui/          # Базовые UI компоненты
├── hooks/           # Кастомные хуки
├── store/           # Zustand хранилища
├── utils/           # Утилиты
├── constants/       # Константы
├── App.jsx
└── main.jsx
```

### Принципы организации:
1. **Один компонент = один файл**
2. **Компоненты именуются PascalCase**: `StatisticsDashboard.jsx`
3. **Хуки именуются camelCase с префиксом use**: `useTimer.js`
4. **Утилиты именуются camelCase**: `calculations.js`
5. **Максимум 300 строк на компонент** (если больше - разделить)

## 📐 ПРАВИЛА НАПИСАНИЯ КОДА

### React компоненты:

```javascript
// ✅ ПРАВИЛЬНО
import { useState } from 'react';
import { Button } from '../ui/Button';

export function StatCard({ title, value, icon: Icon }) {
  const [isHovered, setIsHovered] = useState(false);
  
  return (
    <div className="glass-effect rounded-xl p-6">
      <h3>{title}</h3>
      <p>{value}</p>
      {Icon && <Icon className="w-6 h-6" />}
    </div>
  );
}

// ❌ НЕПРАВИЛЬНО
import React from 'react'; // Не нужен в React 18+
const StatCard = (props) => { ... } // Используй деструктуризацию
class StatCard extends React.Component { ... } // Не используем классы
```

### Zustand хранилища:

```javascript
// ✅ ПРАВИЛЬНО
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

export const useEntriesStore = create(
  persist(
    (set, get) => ({
      entries: [],
      addEntry: (entry) => set((state) => ({
        entries: [...state.entries, entry]
      })),
    }),
    { name: 'time-tracker-entries' }
  )
);

// ❌ НЕПРАВИЛЬНО
const entriesStore = createStore(...) // Неверное имя
export default useEntriesStore; // Используем named export
```

### Стилизация с Tailwind:

```javascript
// ✅ ПРАВИЛЬНО - используем Tailwind классы
<div className="glass-effect rounded-xl p-6 hover:scale-105 transition-transform">
  <h2 className="text-xl font-bold mb-4">Заголовок</h2>
</div>

// ✅ ПРАВИЛЬНО - динамические классы через шаблонные строки
<button className={`
  glass-button px-4 py-2 rounded-lg
  ${isActive ? 'bg-blue-500' : 'bg-gray-200'}
  ${disabled ? 'opacity-50 cursor-not-allowed' : 'hover:scale-105'}
`}>

// ❌ НЕПРАВИЛЬНО - инлайн стили (только для динамических значений)
<div style={{ padding: '24px', borderRadius: '12px' }}>

// ❌ НЕПРАВИЛЬНО - отдельные CSS файлы
import './StatCard.css';
```

### Кастомные классы Tailwind:

```css
/* index.css - ТОЛЬКО для глобальных утилит */
@layer components {
  .glass-effect {
    @apply bg-white/80 dark:bg-gray-800/80 backdrop-blur-lg 
           border border-gray-200 dark:border-gray-700;
  }
  
  .glass-button {
    @apply glass-effect px-4 py-2 rounded-lg font-semibold 
           transition-all duration-300 hover:scale-105 active:scale-95;
  }
}
```

## 🎨 СТИЛЕВЫЕ СОГЛАШЕНИЯ

### Цветовая палитра:
- **Primary**: blue-500 (`#3B82F6`)
- **Success**: green-500 (`#10B981`)
- **Warning**: yellow-500 (`#F59E0B`)
- **Error**: red-500 (`#EF4444`)
- **Background Light**: gray-100 (`#F3F4F6`)
- **Background Dark**: gray-900 (`#111827`)

### Размеры и отступы:
- Padding карточек: `p-6` (24px)
- Margin между секциями: `mb-6` (24px)
- Border radius: `rounded-xl` (12px)
- Shadows: `shadow-lg`

### Анимации:
- Hover эффекты: `hover:scale-105 transition-transform duration-300`
- Появление: `animate-slide-in` или `animate-fade-in`
- Кнопки: `active:scale-95`

## 🔧 ИМЕНОВАНИЕ И СОГЛАШЕНИЯ

### Переменные и функции:
```javascript
// ✅ ПРАВИЛЬНО
const totalEarnings = 10000;
const isLoading = false;
const handleSaveEntry = () => {};
const calculateDuration = (start, end) => {};

// ❌ НЕПРАВИЛЬНО
const TotalEarnings = 10000; // PascalCase только для компонентов
const loading = false; // Булевы без префикса is/has/should
const saveEntry = () => {}; // Обработчики без префикса handle
```

### Пропсы компонентов:
```javascript
// ✅ ПРАВИЛЬНО
<Button 
  onClick={handleClick}
  disabled={isLoading}
  variant="primary"
  icon={SaveIcon}
>
  Сохранить
</Button>

// ❌ НЕПРАВИЛЬНО
<Button 
  click={handleClick}        // onClick, не click
  isDisabled={isLoading}     // disabled, не isDisabled
  buttonVariant="primary"    // variant, не buttonVariant
/>
```

### Константы:
```javascript
// ✅ ПРАВИЛЬНО - UPPER_SNAKE_CASE для глобальных констант
const MAX_ENTRIES_PER_PAGE = 50;
const DEFAULT_HOURLY_RATE = 1000;

// ✅ ПРАВИЛЬНО - camelCase для локальных констант
const defaultCategories = [...];
const chartConfig = {...};

// ❌ НЕПРАВИЛЬНО
const max_entries = 50;        // Смешанный стиль
const DEFAULTCATEGORIES = []; // Нет разделителя
```

## 📦 ИМПОРТЫ

### Порядок импортов:
```javascript
// 1. React и хуки
import { useState, useEffect, useCallback } from 'react';

// 2. Внешние библиотеки
import { create } from 'zustand';
import { Plus, Edit2, Trash2 } from 'lucide-react';

// 3. Внутренние компоненты (абсолютные пути)
import { Button } from '@/components/ui/Button';
import { useEntriesStore } from '@/store/useEntriesStore';

// 4. Утилиты и хелперы
import { calculateDuration } from '@/utils/calculations';
import { formatDate } from '@/utils/dateHelpers';

// 5. Константы
import { DEFAULT_CATEGORIES } from '@/constants/categories';

// 6. Типы (если используется TypeScript)
import type { Entry, Category } from '@/types';
```

## 🎯 ОБРАБОТКА ДАННЫХ

### LocalStorage:
```javascript
// ✅ ПРАВИЛЬНО - через Zustand persist
export const useEntriesStore = create(
  persist(
    (set) => ({ ... }),
    { name: 'time-tracker-entries' }
  )
);

// ❌ НЕПРАВИЛЬНО - прямая работа с localStorage в компонентах
localStorage.setItem('entries', JSON.stringify(entries));
```

### Валидация:
```javascript
// ✅ ПРАВИЛЬНО - валидация в утилитах
export function validateEntry(entry) {
  const errors = {};
  
  if (!entry.date) errors.date = 'Дата обязательна';
  if (!entry.start) errors.start = 'Время начала обязательно';
  if (entry.end <= entry.start) {
    errors.end = 'Время окончания должно быть позже начала';
  }
  
  return { isValid: Object.keys(errors).length === 0, errors };
}

// В компоненте:
const { isValid, errors } = validateEntry(formData);
if (!isValid) {
  setFormErrors(errors);
  return;
}
```

### Расчеты:
```javascript
// ✅ ПРАВИЛЬНО - вынести в utils/calculations.js
export function calculateDuration(start, end) {
  const [startH, startM] = start.split(':').map(Number);
  const [endH, endM] = end.split(':').map(Number);
  const minutes = (endH * 60 + endM) - (startH * 60 + startM);
  return (minutes / 60).toFixed(2);
}

// ❌ НЕПРАВИЛЬНО - расчеты прямо в компоненте
const duration = ((parseInt(end.split(':')[0]) * 60 + ...
```

## 🎭 СОСТОЯНИЕ И ЭФФЕКТЫ

### useState:
```javascript
// ✅ ПРАВИЛЬНО
const [entries, setEntries] = useState([]);
const [isLoading, setIsLoading] = useState(false);
const [formData, setFormData] = useState({
  date: '',
  start: '',
  end: ''
});

// Обновление объекта:
setFormData(prev => ({ ...prev, date: newDate }));

// ❌ НЕПРАВИЛЬНО
setFormData({ date: newDate }); // Потеря остальных полей
```

### useEffect:
```javascript
// ✅ ПРАВИЛЬНО - с зависимостями
useEffect(() => {
  if (activeTimer) {
    const interval = setInterval(() => {
      updateElapsedTime();
    }, 1000);
    
    return () => clearInterval(interval); // Очистка
  }
}, [activeTimer]); // Зависимости

// ❌ НЕПРАВИЛЬНО
useEffect(() => {
  setInterval(...); // Без очистки
}); // Без массива зависимостей
```

### useMemo и useCallback:
```javascript
// ✅ ПРАВИЛЬНО - для тяжелых вычислений
const filteredEntries = useMemo(() => {
  return entries.filter(e => 
    e.category === selectedCategory
  );
}, [entries, selectedCategory]);

const handleSave = useCallback(() => {
  saveEntry(formData);
}, [formData]);

// ❌ НЕПРАВИЛЬНО - оборачивать все подряд
const count = useMemo(() => entries.length, [entries]); // Простое значение
```

## 🔔 УВЕДОМЛЕНИЯ И ЗВУКИ

### Уведомления:
```javascript
// ✅ ПРАВИЛЬНО
import { useNotifications } from '@/hooks/useNotifications';

const { showNotification } = useNotifications();

showNotification('Запись добавлена', 'success', 3000);
showNotification('Ошибка сохранения', 'error', 5000);

// ❌ НЕПРАВИЛЬНО
alert('Запись добавлена'); // Не используем alert
```

### Звуки:
```javascript
// ✅ ПРАВИЛЬНО
import { useSoundManager } from '@/hooks/useSound';

const { playSound } = useSoundManager();
playSound('timerStart');
```

## ⌨️ ГОРЯЧИЕ КЛАВИШИ

```javascript
// ✅ ПРАВИЛЬНО - через хук
import { useHotkeys } from '@/hooks/useHotkeys';

useHotkeys({
  'n': handleAddNew,
  't': handleAddNew,
  's': handleStartTimer,
  'Ctrl+z': handleUndo,
  'Escape': handleClose
});

// ❌ НЕПРАВИЛЬНО - прямые addEventListener в компонентах
```

## 📊 ГРАФИКИ

### Recharts:
```javascript
// ✅ ПРАВИЛЬНО
import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';

<ResponsiveContainer width="100%" height={300}>
  <LineChart data={chartData}>
    <XAxis dataKey="date" />
    <YAxis />
    <Tooltip />
    <Line type="monotone" dataKey="earned" stroke="#3B82F6" />
  </LineChart>
</ResponsiveContainer>

// Подготовка данных:
const chartData = entries.map(entry => ({
  date: formatDate(entry.date),
  earned: parseFloat(entry.earned)
}));
```

## 🚨 ОБРАБОТКА ОШИБОК

```javascript
// ✅ ПРАВИЛЬНО
try {
  const data = await fetchData();
  setData(data);
} catch (error) {
  console.error('Ошибка загрузки данных:', error);
  showNotification('Не удалось загрузить данные', 'error');
  setError(error.message);
} finally {
  setIsLoading(false);
}

// ✅ ПРАВИЛЬНО - валидация перед действием
const { isValid, errors } = validateEntry(entry);
if (!isValid) {
  showNotification('Проверьте заполнение полей', 'error');
  setFormErrors(errors);
  return;
}

// ❌ НЕПРАВИЛЬНО
const data = await fetchData(); // Без try-catch
if (error) console.log(error); // Минимальная обработка
```

## 🧪 КОММЕНТАРИИ И ДОКУМЕНТАЦИЯ

### JSDoc для функций:
```javascript
/**
 * Рассчитывает длительность работы в часах
 * @param {string} start - Время начала в формате HH:MM
 * @param {string} end - Время окончания в формате HH:MM
 * @returns {string} Длительность в часах с 2 десятичными знаками
 */
export function calculateDuration(start, end) {
  // реализация
}
```

### Комментарии в коде:
```javascript
// ✅ ПРАВИЛЬНО - объяснение "почему", не "что"
// Округляем до 15 минут для удобства учета времени
const roundedMinutes = Math.round(minutes / 15) * 15;

// Таймер обновляется каждую секунду, но звук играем только при смене часа
if (currentHour > previousHour) {
  playSound('hourlyAlert');
}

// ❌ НЕПРАВИЛЬНО - очевидные комментарии
// Получаем entries из store
const { entries } = useEntriesStore();

// Устанавливаем loading в true
setIsLoading(true);
```

### TODO комментарии:
```javascript
// TODO: Добавить pagination для больших списков
// FIXME: Баг с расчетом времени через полночь
// OPTIMIZE: Мемоизировать тяжелые вычисления
```

## 🎨 ДОСТУПНОСТЬ (A11Y)

```javascript
// ✅ ПРАВИЛЬНО
<button 
  aria-label="Закрыть модальное окно"
  onClick={onClose}
>
  <X className="w-5 h-5" />
</button>

<input
  type="text"
  id="description"
  aria-describedby="description-error"
/>
{error && (
  <span id="description-error" role="alert">
    {error}
  </span>
)}

// ❌ НЕПРАВИЛЬНО
<div onClick={onClose}>X</div> // Используй button
<button><X /></button> // Нет aria-label для иконки
```

## 🎯 ПРОИЗВОДИТЕЛЬНОСТЬ

### Оптимизация рендеров:
```javascript
// ✅ ПРАВИЛЬНО
import { memo } from 'react';

export const EntryItem = memo(function EntryItem({ entry, onEdit, onDelete }) {
  return (
    <div>...</div>
  );
});

// ✅ ПРАВИЛЬНО - useCallback для функций-пропсов
const handleDelete = useCallback((id) => {
  deleteEntry(id);
}, [deleteEntry]);
```

### Виртуализация:
```javascript
// ✅ ПРАВИЛЬНО - для больших списков (>100 элементов)
import { FixedSizeList } from 'react-window';

<FixedSizeList
  height={600}
  itemCount={entries.length}
  itemSize={80}
  width="100%"
>
  {({ index, style }) => (
    <div style={style}>
      <EntryItem entry={entries[index]} />
    </div>
  )}
</FixedSizeList>
```

## 🔒 БЕЗОПАСНОСТЬ

```javascript
// ✅ ПРАВИЛЬНО - санитизация ввода
import DOMPurify from 'dompurify';

const sanitizedDescription = DOMPurify.sanitize(userInput);

// ✅ ПРАВИЛЬНО - избегаем dangerouslySetInnerHTML
<p>{description}</p>

// ❌ НЕПРАВИЛЬНО
<div dangerouslySetInnerHTML={{ __html: userInput }} />
```

## 📱 АДАПТИВНОСТЬ

```javascript
// ✅ ПРАВИЛЬНО - mobile-first подход
<div className="
  grid grid-cols-1           // Mobile: 1 колонка
  md:grid-cols-2             // Tablet: 2 колонки
  lg:grid-cols-4             // Desktop: 4 колонки
  gap-4
">

// ✅ ПРАВИЛЬНО - условный рендер для разных устройств
const isMobile = window.innerWidth < 768;
{isMobile ? <MobileView /> : <DesktopView />}
```

## 🚀 КОМАНДЫ И СКРИПТЫ

```json
{
  "scripts": {
    "dev": "vite",                    // Разработка
    "build": "vite build",            // Продакшн сборка
    "preview": "vite preview",        // Превью продакшн
    "lint": "eslint . --ext js,jsx",  // Проверка кода
    "format": "prettier --write ."    // Форматирование
  }
}
```

## 📋 ЧЕКЛИСТ ПЕРЕД КОММИТОМ

Перед созданием любого кода проверь:
- [ ] Компонент в правильной папке по структуре
- [ ] Используется функциональный компонент с хуками
- [ ] Named export (не default)
- [ ] Деструктуризация пропсов
- [ ] Стилизация через Tailwind классы
- [ ] Нет инлайн стилей (кроме динамических значений)
- [ ] Нет console.log (кроме ошибок)
- [ ] Есть валидация данных
- [ ] Есть обработка ошибок
- [ ] Есть комментарии на русском для сложной логики
- [ ] Компонент не длиннее 300 строк

## 🤖 ИНСТРУКЦИИ ДЛЯ AI

### При создании компонента:
1. Спроси: "В какую папку поместить компонент?"
2. Создай файл с именем PascalCase
3. Используй named export
4. Добавь JSDoc комментарий
5. Используй деструктуризацию пропсов
6. Примени Tailwind классы
7. Добавь aria-атрибуты
8. Покажи пример использования

### При создании хука:
1. Имя начинается с "use"
2. Файл в папке hooks/
3. Возвращает объект с методами
4. Документируй параметры и возврат

### При создании утилиты:
1. Чистая функция (pure function)
2. Файл в папке utils/
3. Каждая функция с JSDoc
4. Экспорт именованный

### При ошибке:
1. Объясни причину простым языком
2. Предложи 2-3 варианта решения
3. Покажи исправленный код
4. Объясни, как избежать в будущем

### Стиль общения:
- Объясняй термины простым языком
- Приводи примеры из реальной жизни
- Используй эмодзи для наглядности
- Структурируй ответы с заголовками
- Давай пошаговые инструкции

## 🎓 ОБУЧАЮЩИЕ КОММЕНТАРИИ

Для начинающих разработчиков добавляй обучающие комментарии:

```javascript
/**
 * 🎓 ПОЯСНЕНИЕ ДЛЯ НАЧИНАЮЩИХ:
 * 
 * useState - это хук React для создания состояния в компоненте.
 * Он возвращает массив из 2 элементов:
 * 1. Текущее значение (entries)
 * 2. Функция для обновления (setEntries)
 * 
 * При вызове setEntries компонент перерисуется с новым значением.
 */
const [entries, setEntries] = useState([]);

/**
 * 🎓 ПОЯСНЕНИЕ:
 * 
 * useEffect выполняет код при изменении зависимостей (второй параметр).
 * Если массив зависимостей пустой [], код выполнится только один раз при монтировании.
 * Функция, возвращаемая из useEffect, вызовется при размонтировании (cleanup).
 */
useEffect(() => {
  const interval = setInterval(() => {
    // Код выполняется каждую секунду
  }, 1000);
  
  // Cleanup: очищаем интервал при размонтировании
  return () => clearInterval(interval);
}, []); // Пустой массив = выполнить один раз
```

## 🔄 ВЕРСИОНИРОВАНИЕ

Версия .cursorrules: 1.0
Последнее обновление: 2024-12-19

При обновлении правил:
1. Увеличь номер версии
2. Добавь дату обновления
3. Опиши изменения в комментарии

---

**ВАЖНО:** Эти правила являются обязательными для всего проекта Time Tracker Dashboard. 
При любых сомнениях или неясностях - следуй этим правилам.

**Для AI:** Всегда проверяй соответствие создаваемого кода этим правилам перед показом результата пользователю.
```

---

## 🔌 НАСТРОЙКА MCP СЕРВЕРОВ

### Что такое MCP серверы?

MCP (Model Context Protocol) серверы - это расширения для Cursor, которые дают AI доступ к:
- Файловой системе
- Базам данных  
- Git
- Внешним API
- Инструментам разработки

### Список рекомендуемых MCP серверов для Time Tracker:

#### 1. **@modelcontextprotocol/server-filesystem**
**Для чего:** Работа с файлами проекта
**Возможности:**
- Чтение/запись файлов
- Создание папок
- Поиск по файлам

#### 2. **@modelcontextprotocol/server-git**
**Для чего:** Git операции
**Возможности:**
- Коммиты
- Просмотр истории
- Создание веток

#### 3. **@modelcontextprotocol/server-sqlite**
**Для чего:** Работа с локальной БД (если понадобится)
**Возможности:**
- SQL запросы
- Миграции
- Бэкапы

#### 4. **@modelcontextprotocol/server-memory**
**Для чего:** Память между сессиями
**Возможности:**
- Сохранение контекста
- История разговоров
- Заметки о проекте

---

## 📥 УСТАНОВКА MCP СЕРВЕРОВ

### ШАГ 1: Найти конфигурационный файл Cursor

**Путь к файлу зависит от ОС:**

**Windows:**
```
C:\Users\ВашеИмя\AppData\Roaming\Cursor\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json
```

**macOS:**
```
~/Library/Application Support/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json
```

**Linux:**
```
~/.config/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json
```

### ШАГ 2: Создать или отредактировать файл конфигурации

Если файл не существует, создайте его. Если существует - отредактируйте.

**Содержимое файла `cline_mcp_settings.json`:**

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "C:\\Users\\ВашеИмя\\projects\\time-tracker"
      ],
      "disabled": false,
      "alwaysAllow": []
    },
    "git": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-git",
        "--repository",
        "C:\\Users\\ВашеИмя\\projects\\time-tracker"
      ],
      "disabled": false,
      "alwaysAllow": []
    },
    "memory": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-memory"
      ],
      "disabled": false,
      "alwaysAllow": []
    }
  }
}
```

**⚠️ ВАЖНО:** Замените `C:\\Users\\ВашеИмя\\projects\\time-tracker` на РЕАЛЬНЫЙ путь к вашему проекту!

### ШАГ 3: Установить MCP серверы через npm

Откройте терминал в папке проекта и выполните:

```bash
# Устанавливаем MCP серверы глобально
npm install -g @modelcontextprotocol/server-filesystem
npm install -g @modelcontextprotocol/server-git
npm install -g @modelcontextprotocol/server-memory
```

### ШАГ 4: Перезапустить Cursor

Полностью закройте и откройте Cursor заново.

### ШАГ 5: Проверить подключение MCP

В Cursor AI Chat напишите:
```
Какие MCP серверы у меня подключены?
```

AI должен ответить списком активных серверов.

---

## 🎯 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО АКТИВАЦИИ

### ДЛЯ WINDOWS:

#### ШАГ 1: Создание .cursorrules
1. Откройте проводник Windows
2. Перейдите в папку вашего проекта `time-tracker`
3. Нажмите правой кнопкой мыши → "Создать" → "Текстовый документ"
4. Назовите файл `.cursorrules` (БЕЗ расширения .txt)
5. Windows спросит подтверждение - нажмите "Да"
6. Откройте файл в блокноте
7. Скопируйте содержимое .cursorrules (см. выше)
8. Сохраните и закройте

**🎥 Видео инструкция:** [Как создать файл с точкой в Windows](https://www.youtube.com/watch?v=example)

#### ШАГ 2: Настройка MCP серверов
1. Нажмите `Win + R`
2. Введите: `%APPDATA%\Cursor\User\globalStorage\saoudrizwan.claude-dev\settings`
3. Нажмите Enter
4. Если папки `settings` нет - создайте её
5. Создайте файл `cline_mcp_settings.json`
6. Скопируйте конфигурацию MCP (см. выше)
7. Замените пути на ваши реальные пути
8. Сохраните файл

#### ШАГ 3: Установка MCP серверов
1. Откройте PowerShell или CMD **от имени администратора**
2. Выполните команды:
```bash
npm install -g @modelcontextprotocol/server-filesystem
npm install -g @modelcontextprotocol/server-git  
npm install -g @modelcontextprotocol/server-memory
```
3. Дождитесь завершения установки (может занять 2-3 минуты)

#### ШАГ 4: Перезапуск Cursor
1. Полностью закройте Cursor (правой кнопкой на иконке в трее → "Выход")
2. Откройте Cursor снова
3. Откройте ваш проект `time-tracker`

#### ШАГ 5: Проверка
1. Откройте Cursor AI Chat (Ctrl + L)
2. Напишите: "Проверь, что у меня настроено"
3. AI должен увидеть .cursorrules и MCP серверы

---

### ДЛЯ macOS:

#### ШАГ 1: Создание .cursorrules
1. Откройте Finder
2. Перейдите в папку проекта `time-tracker`
3. Нажмите `Cmd + Shift + .` (показать скрытые файлы)
4. Откройте Terminal (Applications → Utilities → Terminal)
5. Введите: `cd путь/к/вашему/time-tracker`
6. Введите: `touch .cursorrules`
7. Введите: `open .cursorrules`
8. Скопируйте содержимое .cursorrules
9. Сохраните (Cmd + S)

#### ШАГ 2: Настройка MCP серверов
1. Откройте Terminal
2. Введите:
```bash
cd ~/Library/Application\ Support/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings
```
3. Если папка не существует, создайте:
```bash
mkdir -p ~/Library/Application\ Support/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings
cd ~/Library/Application\ Support/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings
```
4. Создайте файл:
```bash
touch cline_mcp_settings.json
open cline_mcp_settings.json
```
5. Скопируйте конфигурацию MCP (измените пути на macOS формат)
6. Сохраните

#### ШАГ 3: Установка MCP серверов
```bash
sudo npm install -g @modelcontextprotocol/server-filesystem
sudo npm install -g @modelcontextprotocol/server-git
sudo npm install -g @modelcontextprotocol/server-memory
```

#### ШАГ 4-5: Перезапуск и проверка
Аналогично Windows (см. выше)

---

### ДЛЯ LINUX (Ubuntu/Debian):

#### ШАГ 1: Создание .cursorrules
```bash
cd ~/projects/time-tracker
touch .cursorrules
nano .cursorrules
# Вставьте содержимое, Ctrl+X для выхода, Y для сохранения
```

#### ШАГ 2: Настройка MCP
```bash
mkdir -p ~/.config/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings
cd ~/.config/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings
nano cline_mcp_settings.json
# Вставьте конфигурацию, измените пути
```

#### ШАГ 3: Установка MCP
```bash
sudo npm install -g @modelcontextprotocol/server-filesystem
sudo npm install -g @modelcontextprotocol/server-git
sudo npm install -g @modelcontextprotocol/server-memory
```

---

## 🔍 ПРОВЕРКА ПРАВИЛЬНОСТИ НАСТРОЙКИ

### Проверка .cursorrules:

В Cursor AI Chat напишите:
```
Покажи мне правила .cursorrules для этого проекта
```

AI должен вывести ваши правила.

### Проверка MCP серверов:

```
Используя MCP filesystem, покажи структуру папки src/
```

AI должен показать список файлов и папок.

### Проверка работы Git MCP:

```
Используя MCP git, покажи последние 3 коммита
```

AI должен показать историю Git (если репозиторий инициализирован).

---

## 🛠️ РАСШИРЕННЫЕ НАСТРОЙКИ

### Добавление дополнительных MCP серверов:

#### 1. **Brave Search MCP** (для поиска в интернете)
```json
"brave-search": {
  "command": "npx",
  "args": [
    "-y",
    "@modelcontextprotocol/server-brave-search"
  ],
  "env": {
    "BRAVE_API_KEY": "ваш_api_ключ"
  },
  "disabled": false
}
```

#### 2. **Puppeteer MCP** (для скриншотов и автоматизации браузера)
```json
"puppeteer": {
  "command": "npx",
  "args": [
    "-y",
    "@modelcontextprotocol/server-puppeteer"
  ],
  "disabled": false
}
```

#### 3. **Fetch MCP** (для HTTP запросов)
```json
"fetch": {
  "command": "npx",
  "args": [
    "-y",
    "@modelcontextprotocol/server-fetch"
  ],
  "disabled": false
}
```

### Получение API ключей:

**Brave Search:**
1. Перейдите на https://brave.com/search/api/
2. Зарегистрируйтесь
3. Создайте API ключ
4. Вставьте в конфигурацию

---

## 🎓 ОБУЧЕНИЕ ДЛЯ НАЧИНАЮЩИХ

### Что делает .cursorrules?

**Простым языком:**
Представьте, что Cursor AI - это новый программист в вашей команде. 
`.cursorrules` - это документ, где вы объясняете ему:
- Какие инструменты мы используем (React, Tailwind)
- Как мы пишем код (стиль, именование)
- Какие правила соблюдаем (структура папок)
- Как решать проблемы

**Без .cursorrules:** AI будет писать код "как умеет", может использовать не те библиотеки или стили.

**С .cursorrules:** AI будет писать код точно так, как нужно для вашего проекта.

### Что делают MCP серверы?

**Простым языком:**
По умолчанию Cursor AI может только читать то, что вы ему показываете в чате.

MCP серверы дают AI "руки" для:
- **filesystem:** Читать и писать файлы самостоятельно
- **git:** Делать коммиты, смотреть историю
- **memory:** Помнить ваши предпочтения между сессиями

**Пример:**
- **Без MCP:** "Создай компонент Button" → AI показывает код, вы копируете его в файл вручную
- **С MCP:** "Создай компонент Button" → AI сам создаёт файл `src/components/ui/Button.jsx` с кодом

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Cursor не видит .cursorrules

**Решение:**
1. Убедитесь, что файл называется именно `.cursorrules` (с точкой в начале)
2. Файл должен быть в КОРНЕВОЙ папке проекта (там же, где package.json)
3. Перезапустите Cursor
4. Откройте проект заново (File → Open Folder)

### Проблема: MCP серверы не работают

**Решение 1:** Проверьте пути
- Пути в `cline_mcp_settings.json` должны быть абсолютными
- Используйте `\\` для Windows или `/` для macOS/Linux
- Путь должен вести к РЕАЛЬНОЙ папке проекта

**Решение 2:** Проверьте установку
```bash
# Проверьте, установлены ли пакеты
npm list -g @modelcontextprotocol/server-filesystem
npm list -g @modelcontextprotocol/server-git
npm list -g @modelcontextprotocol/server-memory

# Если не установлены, установите заново
npm install -g @modelcontextprotocol/server-filesystem --force
```

**Решение 3:** Проверьте JSON синтаксис
- Скопируйте содержимое `cline_mcp_settings.json`
- Вставьте на https://jsonlint.com
- Исправьте ошибки, если есть

### Проблема: npm команды не работают

**Решение:** Установите Node.js
1. Скачайте с https://nodejs.org (LTS версия)
2. Установите
3. Перезагрузите компьютер
4. Проверьте: откройте терминал и введите `npm -v`

### Проблема: Cursor не видит MCP после установки

**Решение:**
1. Полностью закройте Cursor (не просто окно, а сам процесс)
2. На Windows: откройте Task Manager, найдите Cursor, завершите процесс
3. На macOS: `Cmd + Q` для полного выхода
4. Откройте Cursor заново

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

### Документация:
- **Cursor AI:** https://docs.cursor.com
- **MCP Protocol:** https://modelcontextprotocol.io
- **React:** https://react.dev
- **Zustand:** https://github.com/pmndrs/zustand
- **Tailwind CSS:** https://tailwindcss.com

### Видео туториалы:
- "Cursor AI для начинающих" - YouTube
- "Настройка MCP серверов" - YouTube
- "Создание React приложения с AI" - YouTube

### Сообщества:
- Cursor Discord: https://discord.gg/cursor
- Cursor Reddit: https://reddit.com/r/cursor
- Stack Overflow с тегом [cursor-ai]

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

Перед началом работы убедитесь:

- [ ] Node.js установлен (проверка: `node -v`)
- [ ] npm работает (проверка: `npm -v`)
- [ ] Cursor установлен и открыт
- [ ] Создан файл `.cursorrules` в корне проекта
- [ ] Файл заполнен правилами (см. выше)
- [ ] Создан файл `cline_mcp_settings.json` в правильной папке
- [ ] Пути в конфигурации указаны правильно (абсолютные пути)
- [ ] MCP серверы установлены глобально
- [ ] Cursor перезапущен
- [ ] Проект открыт в Cursor (File → Open Folder)
- [ ] Проверка работы: AI видит .cursorrules
- [ ] Проверка работы: AI видит MCP серверы

---

## 🎬 ГОТОВЫ НАЧАТЬ!

Теперь скажите Cursor AI:

```
Привет! Я готов начать разработку Time Tracker Dashboard.
У меня настроены .cursorrules и MCP серверы.

Давай начнем с Шага 1 из технического задания:
Инициализация проекта.

Пожалуйста, используй MCP filesystem для создания структуры папок 
и всех необходимых файлов.
```

И Cursor AI начнёт автоматически создавать структуру проекта! 🚀

---

**УДАЧИ В РАЗРАБОТКЕ! 💪**
