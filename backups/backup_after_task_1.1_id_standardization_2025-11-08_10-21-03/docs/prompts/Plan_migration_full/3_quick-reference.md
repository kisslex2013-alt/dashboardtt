# 🚀 **БЫСТРАЯ ШПАРГАЛКА - МИГРАЦИЯ TIME TRACKER**

## ⚡ **КРИТИЧЕСКИЕ ПРОМПТЫ (копируй и вставляй в Cursor)**

### **1️⃣ СТАРТ ПРОЕКТА (5 минут)**

```
Создай новый React проект с Vite для миграции Time Tracker:

1. Выполни: npm create vite@latest time-tracker-react -- --template react
2. Перейди в папку: cd time-tracker-react
3. Установи все зависимости:
   npm install
   npm install -D tailwindcss postcss autoprefixer
   npm install recharts tone lucide-react
   
4. Настрой Tailwind: npx tailwindcss init -p

5. Создай структуру папок:
   src/
     components/
       ui/
       modals/
       charts/
     hooks/
     contexts/
     utils/
     services/
     constants/
     styles/
     assets/

6. В tailwind.config.js добавь:
   content: ["./index.html", "./src/**/*.{js,jsx}"]

7. В src/index.css добавь:
   @tailwind base;
   @tailwind components;
   @tailwind utilities;

8. Запусти: npm run dev
```

---

### **2️⃣ ПЕРВЫЙ КОМПОНЕНТ (10 минут)**

```
Создай простой компонент Button в src/components/ui/Button.jsx:

1. Используй glassmorphism стили:
   - backdrop-blur-md
   - bg-white/10 dark:bg-white/5
   - border border-white/20
   - rounded-lg
   - transition-all duration-300
   - hover:bg-white/20

2. Варианты цветов:
   - primary: синий (text-blue-400)
   - success: зеленый (text-green-400)
   - danger: красный (text-red-400)
   - gray: серый (text-gray-400)

3. Props: variant, onClick, disabled, children, className

4. Экспортируй компонент

Протестируй в App.jsx
```

---

### **3️⃣ GLASSMORPHISM СТИЛИ (15 минут)**

```
Создай файл src/styles/glassmorphism.css с базовыми стилями:

1. Скопируй из HTML все стили для:
   .glass-panel { ... }
   .glass-button { ... }
   .glass-input { ... }
   .glass-modal { ... }

2. Все цветовые варианты:
   .glass-button-blue { ... }
   .glass-button-green { ... }
   .glass-button-red { ... }
   .glass-button-gray { ... }

3. Анимации:
   @keyframes fadeIn { ... }
   @keyframes slideIn { ... }
   @keyframes pulse-slow { ... }

4. Импортируй в main.jsx:
   import './styles/glassmorphism.css'

Проверь что стили применяются к Button компоненту
```

---

### **4️⃣ ЛОКАЛЬНОЕ ХРАНИЛИЩЕ (20 минут)**

```
Создай хук useLocalStorage в src/hooks/useLocalStorage.js:

1. Скопируй логику из HTML файла (строка ~3667)

2. Основные функции:
   - Чтение из localStorage
   - Запись с автосохранением
   - Интеграция с IndexedDB для бэкапов
   - Обработка ошибок

3. Экспортируй хук

4. Создай тестовый компонент для проверки:
   - Сохранение текста
   - Чтение после перезагрузки
   - Проверка бэкапов в IndexedDB

Это критический хук! Проверь тщательно!
```

---

### **5️⃣ ТАЙМЕР - БАЗОВАЯ ВЕРСИЯ (30 минут)**

```
Создай простую версию таймера в src/components/Timer.jsx:

1. State для:
   - isRunning (bool)
   - elapsedTime (number в секундах)
   - category (string)

2. Функции:
   - startTimer() - запуск
   - pauseTimer() - пауза
   - stopTimer() - остановка и сохранение записи

3. UI:
   - Большой дисплей времени (HH:MM:SS)
   - Кнопки Play/Pause/Stop
   - Выбор категории (простой select)

4. useEffect для тика таймера каждую секунду

Сделай простую рабочую версию без звуков и уведомлений пока
```

---

### **6️⃣ СПИСОК ЗАПИСЕЙ (40 минут)**

```
Создай компонент EntriesList в src/components/EntriesList.jsx:

1. Получи записи из useLocalStorage('entries', [])

2. Отобрази список:
   - Группировка по дням
   - Цветной бейдж категории
   - Время записи (HH:MM:SS)
   - Описание
   - Кнопки Редактировать/Удалить

3. Функции:
   - deleteEntry(id) - с confirm
   - editEntry(id) - открытие модального окна

4. Общая статистика сверху:
   - Всего записей
   - Суммарное время
   - Время сегодня

Используй glassmorphism стили для карточек
```

---

### **7️⃣ КАТЕГОРИИ (30 минут)**

```
Создай управление категориями в src/components/CategoryManager.jsx:

1. Получи категории из useLocalStorage('categories', defaultCategories)

2. Дефолтные категории:
   Work: #3b82f6 (синий)
   Study: #8b5cf6 (фиолетовый)
   Sport: #10b981 (зеленый)
   Rest: #f59e0b (оранжевый)
   Other: #6b7280 (серый)

3. Интерфейс:
   - Список категорий с цветными бейджами
   - Кнопка "Добавить категорию"
   - Редактирование (название + выбор цвета)
   - Удаление (с проверкой использования)

4. Валидация:
   - Уникальные названия
   - Макс 20 символов
   - Обязательный цвет

Используй color picker для выбора цвета
```

---

### **8️⃣ ГРАФИКИ СТАТИСТИКИ (45 минут)**

```
Создай компонент Statistics в src/components/Statistics.jsx:

1. Используй Recharts для графиков

2. График 1 - Line Chart (время по дням):
   <LineChart data={dailyData}>
     <XAxis dataKey="date" />
     <YAxis />
     <Tooltip />
     <Line type="monotone" dataKey="hours" stroke="#3b82f6" />
   </LineChart>

3. График 2 - Pie Chart (категории):
   <PieChart>
     <Pie data={categoryData} dataKey="value" nameKey="name" />
     <Tooltip />
   </PieChart>

4. Фильтры:
   - Сегодня / Эта неделя / Этот месяц / Весь период
   - По категориям

5. Вычисления:
   - Группировка записей
   - Суммирование времени
   - Форматирование для графиков

ResponsiveContainer для адаптива
```

---

### **9️⃣ ТЕМНАЯ ТЕМА (15 минут)**

```
Добавь переключение темной темы:

1. Создай контекст ThemeContext в src/contexts/ThemeContext.jsx:
   - State: isDark (bool)
   - toggleTheme() функция
   - Сохранение в localStorage

2. В App.jsx оберни все в ThemeProvider

3. В корневом div добавь класс dark при isDark=true:
   <div className={isDark ? 'dark' : ''}>

4. Используй dark: префиксы в Tailwind:
   bg-white dark:bg-gray-900
   text-gray-900 dark:text-white

5. Кнопка переключения темы в хедере:
   - Иконка солнца/луны
   - Плавная анимация

Tailwind автоматически применит темные стили
```

---

### **🔟 ЗВУКИ И УВЕДОМЛЕНИЯ (25 минут)**

```
Добавь звуки через Tone.js:

1. Создай SoundService в src/services/SoundService.js:
   
   import * as Tone from 'tone';
   
   class SoundService {
     constructor() {
       this.synth = new Tone.Synth().toDestination();
     }
     
     playStart() {
       this.synth.triggerAttackRelease("C4", "8n");
     }
     
     playStop() {
       this.synth.triggerAttackRelease("A3", "8n");
     }
     
     playNotification() {
       this.synth.triggerAttackRelease("E4", "16n");
     }
   }
   
   export default new SoundService();

2. Используй в Timer компоненте:
   - При startTimer() -> playStart()
   - При stopTimer() -> playStop()

3. Настройки звука:
   - Включить/выключить (в localStorage)
   - Громкость (0-100)

Проверь что звуки проигрываются корректно
```

---

## 🎯 **БЫСТРАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ ДЕЙСТВИЙ**

### **День 1-2: Базовая структура**
1. ✅ Создать проект Vite + React
2. ✅ Настроить Tailwind CSS
3. ✅ Создать структуру папок
4. ✅ Перенести glassmorphism стили
5. ✅ Создать базовые UI компоненты (Button, Input)

### **День 3-4: Основная функциональность**
6. ✅ Хук useLocalStorage
7. ✅ Базовый таймер
8. ✅ Список записей
9. ✅ Управление категориями

### **День 5-6: Улучшения**
10. ✅ Графики и статистика (Recharts)
11. ✅ Темная тема
12. ✅ Звуки (Tone.js)

### **День 7-8: Продвинутые функции**
13. ✅ Модальные окна
14. ✅ Резервное копирование (IndexedDB)
15. ✅ Экспорт/импорт JSON
16. ✅ Настройки приложения

### **День 9-10: Оптимизация**
17. ✅ Тестирование всех функций
18. ✅ Исправление багов
19. ✅ Оптимизация производительности
20. ✅ Адаптив для мобильных

---

## 🚨 **ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ**

### **Проблема: Glassmorphism не работает**
```
Решение:
1. Проверь что backdrop-filter: blur() поддерживается браузером
2. Добавь в CSS: -webkit-backdrop-filter: blur(10px);
3. Убедись что есть фоновый элемент с цветом/градиентом
```

### **Проблема: localStorage не сохраняет**
```
Решение:
1. Проверь console на ошибки
2. Проверь что данные сериализуются в JSON правильно
3. Проверь лимит localStorage (5-10MB)
4. Используй try/catch для обработки ошибок
```

### **Проблема: Recharts графики не отображаются**
```
Решение:
1. Проверь что data в правильном формате (массив объектов)
2. Оберни в <ResponsiveContainer width="100%" height={300}>
3. Проверь что есть числовые значения в dataKey
4. Проверь CSS height родительского элемента
```

### **Проблема: Tone.js не проигрывает звуки**
```
Решение:
1. Убедись что Tone.start() вызван после user interaction
2. Проверь что autoplay разрешен в браузере
3. Добавь кнопку "Включить звук" с await Tone.start()
4. Проверь mute состояние в AudioContext
```

---

## 📱 **МИНИМАЛЬНЫЙ РАБОЧИЙ ПРОТОТИП (2-3 часа)**

```
Самый быстрый путь к работающему приложению:

1. Создать проект + Tailwind (15 мин)
2. Один файл App.jsx с:
   - useState для entries, isRunning, elapsedTime
   - Таймер с setInterval
   - Кнопки Start/Stop
   - Список записей
   - localStorage для сохранения
3. Базовые стили (30 мин)
4. Тестирование (15 мин)

Получишь работающий базовый таймер!
Дальше можно итеративно улучшать.
```

---

## 💡 **СОВЕТЫ ДЛЯ CURSOR PRO**

### **Лучшие практики промптов:**

1. **Будь конкретен:**
   ❌ "Создай компонент таймера"
   ✅ "Создай компонент Timer.jsx с состояниями isRunning, elapsedTime, кнопками Start/Pause/Stop и отображением времени в формате HH:MM:SS"

2. **Указывай файлы и пути:**
   ✅ "Создай файл src/components/Timer.jsx"
   ✅ "Импортируй в src/App.jsx"

3. **Проси сохранять стили:**
   ✅ "Используй glassmorphism: backdrop-blur-md bg-white/10 border border-white/20"

4. **Проси тестировать:**
   ✅ "После создания добавь console.log для проверки"
   ✅ "Создай тестовую кнопку в App.jsx"

5. **Итеративный подход:**
   ✅ Сначала базовая версия
   ✅ Потом добавляй функции одну за другой
   ✅ Тестируй после каждого добавления

---

## 🎨 **ЦВЕТОВАЯ ПАЛИТРА**

```css
/* Категории */
Work:   #3b82f6 (синий)
Study:  #8b5cf6 (фиолетовый)
Sport:  #10b981 (зеленый)
Rest:   #f59e0b (оранжевый)
Other:  #6b7280 (серый)

/* Glassmorphism */
Background: bg-white/10 dark:bg-white/5
Border:     border-white/20
Shadow:     shadow-xl shadow-black/5

/* Кнопки */
Primary:  blue-500
Success:  green-500
Danger:   red-500
Cancel:   gray-500

/* Темы */
Light bg:  gray-100
Dark bg:   gray-900
```

---

## ⚡ **КОГДА ЗАСТРЯЛ - БЫСТРЫЕ ДЕЙСТВИЯ**

1. **Проверь консоль** - 90% проблем там
2. **Проверь импорты** - забыли импортировать компонент/хук?
3. **Проверь props** - правильные ли названия и типы?
4. **Упрости** - уменьши компонент до минимума, который работает
5. **Сравни с оригиналом** - посмотри как сделано в HTML
6. **Погугли ошибку** - Stack Overflow в помощь
7. **Спроси Claude** - опиши проблему подробно

---

## 📝 **ЧЕКЛИСТ ПЕРЕД КОММИТОМ**

- [ ] Код компилируется без ошибок
- [ ] Нет warnings в консоли
- [ ] Протестировал основные функции вручную
- [ ] Стили применяются корректно
- [ ] Темная тема работает
- [ ] localStorage сохраняет данные
- [ ] Код отформатирован (Prettier)
- [ ] Компоненты названы понятно
- [ ] Добавлены комментарии для сложной логики

---

**Используй эту шпаргалку как quick reference во время работы!** 🚀
