# 🎯 **CURSOR PRO ДЛЯ ZERO CODER - ULTIMATE GUIDE**

**Версия:** v1.0  
**Дата:** 27.10.2025  
**Для:** Zero Coders - тех кто не пишет код сам

---

## 🚀 **ФИЛОСОФИЯ ZERO CODING В CURSOR**

### **Главный принцип:**
Ты - **архитектор и тестировщик**, Cursor - **строитель**

**Твоя роль:**
- Четко описываешь ЧТО нужно
- Проверяешь результат
- Указываешь на проблемы
- Принимаешь решения

**Роль Cursor:**
- Пишет код
- Предлагает решения
- Исправляет ошибки
- Реализует детали

---

## ⚙️ **НАСТРОЙКА CURSOR - ПЕРВЫЕ ШАГИ**

### **1. Базовые настройки (обязательно!):**

```
Settings (Cmd/Ctrl + ,) → Cursor:

✅ Enable "Cursor Tab" - автодополнение кода
✅ Enable "AI Chat" - чат с AI
✅ Model: Claude 3.5 Sonnet (самый умный)
✅ Enable "Command K" - редактирование кода
✅ Enable "Composer" - мультифайловое редактирование
```

---

### **2. Горячие клавиши (запомни их!):**

```bash
# САМЫЕ ВАЖНЫЕ:
Cmd/Ctrl + K     - Редактировать код в текущем файле
Cmd/Ctrl + L     - Открыть AI чат
Cmd/Ctrl + I     - Composer (работа с несколькими файлами)
Cmd/Ctrl + /     - Добавить в контекст (важно!)
Tab              - Принять автодополнение

# ПОЛЕЗНЫЕ:
Cmd/Ctrl + Shift + K - Удалить строку
Cmd/Ctrl + D     - Выделить следующее вхождение
Alt + Up/Down    - Переместить строку
Cmd/Ctrl + P     - Быстрый поиск файлов
```

---

### **3. Настройка workspace (один раз):**

Создай файл `.cursorrules` в корне проекта:

```
# .cursorrules

# Мой проект: Time Tracker React App
# Стек: React 18, Vite, Tailwind CSS, Recharts, Tone.js

## Правила кодирования:
- Использовать функциональные компоненты (не классы)
- Использовать хуки (useState, useEffect, useMemo, useCallback)
- Glassmorphism дизайн обязателен
- Темная тема через Tailwind dark: классы
- JSDoc комментарии для всех функций

## Стиль кода:
- Имена компонентов: PascalCase
- Имена функций: camelCase
- Файлы компонентов: PascalCase.jsx
- Файлы утилит: camelCase.js

## Запреты:
- НЕ использовать inline styles
- НЕ использовать var (только const/let)
- НЕ использовать классы CSS (только Tailwind)

## Приоритеты:
1. Читаемость кода
2. Производительность
3. Соответствие дизайну
```

Теперь Cursor ВСЕГДА будет помнить эти правила! 🎯

---

## 💡 **CURSOR FEATURES - ЧТО ИСПОЛЬЗОВАТЬ**

### **Feature 1: Cursor Tab (автодополнение)**

**Что это:** AI предсказывает следующий код

**Как использовать:**
```javascript
// Ты начинаешь писать комментарий:
// Create a function to format time

// Cursor предлагает (серым текстом):
function formatTime(seconds) {
  const hours = Math.floor(seconds / 3600);
  const minutes = Math.floor((seconds % 3600) / 60);
  const secs = seconds % 60;
  return `${hours}:${minutes}:${secs}`;
}

// Жми TAB чтобы принять!
```

**Лайфхаки:**
- ✅ Пиши четкие комментарии перед кодом
- ✅ Используй описательные имена переменных
- ✅ Начинай новую строку - Cursor допишет логику
- ❌ Не принимай вслепую - проверяй предложения

---

### **Feature 2: Chat (Cmd+L)**

**Что это:** Разговор с AI о коде

**Базовое использование:**
```
Ты: Создай компонент Button с glassmorphism стилями
AI: [генерирует код]
Ты: Добавь hover эффект
AI: [обновляет код]
Ты: Теперь сделай 4 варианта: primary, success, danger, cancel
AI: [добавляет варианты]
```

**Продвинутое использование:**

```
# Добавление контекста через @
Ты: @Button.jsx сделай похожий компонент Input

# Поиск в кодовой базе через @codebase
Ты: @codebase где используется useTimer?

# Ссылка на документацию через @docs
Ты: @docs как использовать Recharts LineChart?

# Добавление файла через @
Ты: @Timer.jsx @useTimer.js интегрируй хук в компонент
```

**Золотые правила чата:**
1. Один вопрос - одно сообщение
2. Показывай примеры кода
3. Уточняй если не понятно
4. Проси объяснить решение
5. Итерируй постепенно

---

### **Feature 3: Command K (Cmd+K)**

**Что это:** Редактирование кода inline

**Как использовать:**
```
1. Выдели код который хочешь изменить
2. Нажми Cmd+K
3. Опиши что изменить
4. Cursor покажет diff (красное = удалено, зеленое = добавлено)
5. Accept или Reject
```

**Примеры промптов для Cmd+K:**

```
# Выдели функцию:
"Добавь обработку ошибок через try-catch"

# Выдели компонент:
"Оберни в React.memo для оптимизации"

# Выдели стили:
"Замени на glassmorphism стили"

# Выдели несколько строк:
"Вынеси это в отдельную функцию"

# Выдели useState:
"Добавь localStorage sync для этого state"
```

**Лайфхак:** Cmd+K идеален для **точечных изменений** в коде!

---

### **Feature 4: Composer (Cmd+I)**

**Что это:** Работа с НЕСКОЛЬКИМИ файлами одновременно

**Когда использовать:**
- Создание связанных компонентов
- Рефакторинг across files
- Создание feature с несколькими файлами

**Пример использования:**

```
Ты в Composer (Cmd+I):

"Создай feature 'категорий' с файлами:
1. src/components/CategoryManager.jsx - главный компонент
2. src/components/CategoryInput.jsx - инпут для создания
3. src/utils/categories.js - утилиты
4. src/hooks/useCategories.js - хук для работы с категориями

Стек: React, Tailwind, glassmorphism дизайн
Функционал: CRUD категорий с цветами, сохранение в localStorage"

Cursor создаст ВСЕ 4 файла одновременно! 🚀
```

**Золотое правило Composer:**
Описывай ВСЕЙ feature целиком, а не по файлу

---

### **Feature 5: Add to Context (Cmd+/)**

**Что это:** Добавить файл в "память" AI

**Как работает:**
```
1. Открой нужный файл
2. Нажми Cmd+/ (файл добавлен в контекст)
3. Теперь AI "видит" этот файл в чате

Пример:
- Открой Button.jsx → Cmd+/
- Открой Input.jsx → Cmd+/
- В чате: "Сделай Select похожий на Button и Input"
- AI видит ОБА файла и сделает консистентный стиль!
```

**Лайфхак:** Добавляй в контекст:
- Существующие похожие компоненты
- Utility файлы
- Конфиг файлы (tailwind.config.js)
- Стили (.css файлы)

---

## 🎯 **ПРОМПТЫ ДЛЯ ZERO CODER**

### **Шаблон идеального промпта:**

```
КОНТЕКСТ: [что уже есть]

ЗАДАЧА: [что нужно сделать]

ТРЕБОВАНИЯ:
- [требование 1]
- [требование 2]
- [требование 3]

СТИЛЬ: [как должно выглядеть]

ПРИМЕР: [покажи похожий код если есть]
```

---

### **20 ГОТОВЫХ ПРОМПТОВ:**

#### **1. Создание компонента:**
```
Создай React компонент TimerDisplay в src/components/TimerDisplay.jsx

ФУНКЦИОНАЛ:
- Отображение времени в формате HH:MM:SS
- Кнопки Start, Pause, Stop
- Визуальная индикация когда активен

СТИЛЬ:
- Glassmorphism: backdrop-blur-md bg-white/10 border border-white/20
- Темная тема: dark:bg-white/5
- Анимация: pulse когда таймер активен

PROPS:
- isRunning: boolean
- elapsedTime: number (секунды)
- onStart: function
- onPause: function
- onStop: function

ИСПОЛЬЗУЙ: Lucide React для иконок (Play, Pause, Square)
```

---

#### **2. Создание хука:**
```
Создай кастомный хук useTimer в src/hooks/useTimer.js

ФУНКЦИОНАЛ:
- Управление таймером (start, pause, stop)
- Подсчет времени каждую секунду
- Сохранение записи при stop

ВОЗВРАЩАЕТ:
{
  isRunning: boolean,
  isPaused: boolean,
  elapsedTime: number,
  startTimer: function,
  pauseTimer: function,
  stopTimer: function
}

ИСПОЛЬЗУЕТ:
- useState для состояний
- useEffect для интервала (не забудь cleanup!)
- useLocalStorage для сохранения

ВАЖНО: 
- Интервал должен очищаться
- Использовать функциональное обновление в setInterval
```

---

#### **3. Исправление бага:**
```
ПРОБЛЕМА: Таймер не обновляется

КОД: [вставь проблемный код]

ОШИБКА: [копируй из консоли если есть]

ЧТО ОЖИДАЮ: Таймер должен обновляться каждую секунду

ЧТО ПРОИСХОДИТ: Показывает 00:00:00 и не меняется

Исправь эту проблему и объясни что было не так
```

---

#### **4. Добавление функции:**
```
К существующему компоненту Timer добавь:

НОВАЯ ФУНКЦИЯ: Pause/Resume функциональность

ТРЕБОВАНИЯ:
- Кнопка меняется с Pause на Resume
- При паузе интервал останавливается
- При resume продолжается с того же времени
- Сохранять isPaused в state

НЕ ТРОГАЙ:
- Существующие функции start и stop
- Стили компонента
- Props interface
```

---

#### **5. Рефакторинг:**
```
Отрефактори этот код используя лучшие практики:

КОД: [вставь код]

ПРИМЕНИ:
- React.memo если нужно
- useMemo для дорогих вычислений
- useCallback для функций в props
- Разбей на меньшие функции если большой

СОХРАНИ:
- Всю функциональность
- Все props
- Визуал
```

---

#### **6. Интеграция библиотеки:**
```
Интегрируй Recharts LineChart в компонент Statistics

ДАННЫЕ: массив entries с полями {date, duration, category}

ГРАФИК:
- X axis: даты
- Y axis: часы (duration / 3600)
- Line цвет: #3b82f6
- Tooltip: показывать дату и время
- Responsive: width 100%, height 300px

СТИЛЬ:
- Оберни в glassmorphism контейнер
- Темная тема для графика

ПОКАЖИ: Полный код компонента с графиком
```

---

#### **7. Создание формы:**
```
Создай форму EntryEditForm с валидацией

ПОЛЯ:
- category: select (Work, Study, Sport, Rest)
- description: textarea
- duration: number input (минуты)
- date: date input

ВАЛИДАЦИЯ:
- category: обязательно
- duration: > 0 и < 1440 (24 часа)
- date: не в будущем

НА SUBMIT:
- Валидировать все поля
- Показать ошибки красным под полями
- Если ок - вызвать onSave(data)
- Очистить форму

СТИЛЬ: Glassmorphism инпуты
```

---

#### **8. Добавление стилей:**
```
К компоненту Button добавь hover эффекты

ЭФФЕКТЫ:
- Hover: transform scale(1.05)
- Hover: bg opacity увеличить на 0.1
- Active: scale(0.95)
- Transition: 300ms cubic-bezier(0.4, 0, 0.2, 1)

СОХРАНИ:
- Все существующие стили
- Все варианты (primary, success, danger)
- Glassmorphism эффект
```

---

#### **9. Оптимизация:**
```
Оптимизируй компонент EntriesList для больших списков

ПРОБЛЕМА: Лагает при 1000+ записях

ПРИМЕНИ:
- React.memo на EntryCard
- useMemo для фильтрации и сортировки
- Виртуализация если нужно (react-window)

ИЗМЕРЬ:
- Добавь console.log для подсчета ререндеров
- Добавь performance.now() для измерения времени рендера

ЦЕЛЬ: Плавный скролл без лагов
```

---

#### **10. Дебаг режим:**
```
Добавь debug режим в компонент Timer

РЕЖИМ:
- Включается через const DEBUG = true
- Console.log при:
  * Монтировании: "Timer mounted"
  * Start: "Timer started"
  * Pause: "Timer paused"  
  * Stop: "Timer stopped, duration: X seconds"
  * Каждом тике: "Timer tick: X seconds"
- Цветные логи: ⏱️ эмодзи для таймера

ДОЛЖНО: Легко включаться/выключаться
```

---

#### **11. Адаптив:**
```
Сделай компонент Dashboard адаптивным

BREAKPOINTS:
- Mobile: < 640px (1 колонка)
- Tablet: 640px-1024px (2 колонки)
- Desktop: > 1024px (3 колонки)

ИЗМЕНЕНИЯ:
- На mobile: stack вертикально
- На tablet: 2 колонки grid
- На desktop: 3 колонки grid
- Кнопки на mobile: full width

ИСПОЛЬЗУЙ: Tailwind responsive классы
```

---

#### **12. Темная тема:**
```
Добавь поддержку темной темы в App

ТРЕБОВАНИЯ:
- Переключатель Sun/Moon иконки в header
- Сохранение выбора в localStorage
- Применение dark: классов ко всем элементам

ЭЛЕМЕНТЫ:
- Background: bg-gray-100 dark:bg-gray-900
- Text: text-gray-900 dark:text-white
- Borders: border-gray-300 dark:border-gray-700

БЕЗ МИГАНИЯ: Проверять localStorage до первого рендера
```

---

#### **13. Загрузка данных:**
```
Добавь состояния загрузки в Statistics компонент

СОСТОЯНИЯ:
- loading: показать skeleton loader
- error: показать сообщение об ошибке
- empty: показать "Нет данных"
- success: показать графики

SKELETON:
- 3 прямоугольника glassmorphism
- Анимация пульсации
- Такого же размера как графики

СТИЛЬ: Консистентный с остальным UI
```

---

#### **14. Добавление звуков:**
```
Интегрируй Tone.js для звуковых уведомлений

ЗВУКИ:
- Start: C4 нота, 8n длительность
- Stop: A3 нота, 8n длительность
- Notification: E4 нота, 16n длительность

УПРАВЛЕНИЕ:
- Инициализация после user interaction
- Включение/выключение через настройки
- Проверка что AudioContext started

СЕРВИС: Создай SoundService.js класс
```

---

#### **15. Экспорт данных:**
```
Добавь кнопку Export в Statistics

ФУНКЦИОНАЛ:
- Экспорт entries в JSON
- Имя файла: time-tracker-YYYY-MM-DD.json
- Красивое форматирование (indent 2)
- Download через blob URL

КНОПКА:
- Иконка Download из lucide-react
- Glassmorphism стиль
- Tooltip "Экспорт данных"

ФОРМАТ JSON:
{
  "version": "1.0",
  "exportDate": "2025-10-27",
  "entries": [...]
}
```

---

#### **16. Горячие клавиши:**
```
Добавь keyboard shortcuts в Timer

SHORTCUTS:
- Space: Start/Pause
- Escape: Stop
- R: Reset

ТРЕБОВАНИЯ:
- Работать только когда таймер в фокусе
- Не конфликтовать с инпутами
- Показать подсказки (? кнопка)

ИСПОЛЬЗУЙ: useEffect с addEventListener('keydown')
НЕ ЗАБУДЬ: Cleanup в return
```

---

#### **17. Анимации:**
```
Добавь анимации в Modal компонент

АНИМАЦИИ:
- Open: fade in + scale from 0.95 to 1
- Close: fade out + scale to 0.95
- Backdrop: fade in/out
- Duration: 200ms

ИСПОЛЬЗУЙ:
- CSS transitions
- Conditional rendering с задержкой
- Transform для scale

SMOOTH: cubic-bezier(0.4, 0, 0.2, 1)
```

---

#### **18. Валидация форм:**
```
Добавь валидацию в CategoryInput

ПРАВИЛА:
- Название: 1-20 символов
- Название: уникальное (нет дубликатов)
- Название: только буквы/цифры/пробелы
- Цвет: обязателен

ОШИБКИ:
- Показывать под инпутом красным
- Disable кнопку Save если ошибки
- Проверять onChange и onBlur

СТИЛЬ: border-red-500 для невалидных полей
```

---

#### **19. localStorage sync:**
```
Добавь автосохранение в useTimer

ТРЕБОВАНИЯ:
- Сохранять state каждые 5 секунд если isRunning
- Восстанавливать при загрузке
- Очищать при stop

СОХРАНЯТЬ:
{
  isRunning: boolean,
  startTime: timestamp,
  elapsedTime: number,
  category: string
}

ИСПОЛЬЗУЙ: useEffect с интервалом
```

---

#### **20. Error boundary:**
```
Создай ErrorBoundary компонент

ФУНКЦИОНАЛ:
- Отлавливать ошибки в дочерних компонентах
- Показывать fallback UI с сообщением
- Логировать ошибку в console
- Кнопка "Попробовать снова"

FALLBACK UI:
- Glassmorphism контейнер
- Иконка AlertTriangle
- Текст ошибки
- Кнопка reload

ИСПОЛЬЗУЙ: componentDidCatch (class component)
```

---

## 🧠 **CURSOR AI - КАК ОН ДУМАЕТ**

### **Понимание контекста:**

AI видит:
✅ Открытые файлы
✅ Файлы добавленные через Cmd+/
✅ Файлы упомянутые через @
✅ .cursorrules файл
✅ package.json (зависимости)
✅ Файлы в той же папке

AI НЕ видит:
❌ Закрытые файлы (пока не упомянешь @filename)
❌ Файлы в других папках (пока не добавишь)
❌ Твои мысли (описывай явно!)

---

### **Как улучшить контекст:**

```
# ПЛОХОЙ промпт (мало контекста):
"Сделай таймер"

# ХОРОШИЙ промпт (много контекста):
"Создай компонент Timer в src/components/Timer.jsx

Контекст:
- Проект: Time Tracker (React + Vite + Tailwind)
- Дизайн: Glassmorphism (см. @Button.jsx)
- Хуки: используем @useTimer.js
- Стили: @glassmorphism.css

Требования: [...]"
```

---

## 🎮 **WORKFLOW ДЛЯ ZERO CODER**

### **Паттерн 1: Создание нового компонента**

```bash
1. ПЛАНИРОВАНИЕ (в голове или на бумаге):
   - Что должен делать компонент?
   - Какие props нужны?
   - Как должен выглядеть?

2. ПОДГОТОВКА КОНТЕКСТА:
   - Открой похожие компоненты
   - Добавь их в контекст (Cmd+/)
   - Открой стили (glassmorphism.css)

3. ПРОМПТ В CHAT (Cmd+L):
   "Создай компонент [название] похожий на @ExistingComponent.jsx
   
   Отличия: [что другое]
   Дополнительно: [новые функции]"

4. ПРОВЕРКА:
   - Открой созданный файл
   - Читаешь код (понимаешь ли что делает?)
   - Запусти npm run dev
   - Проверь визуально

5. ИТЕРАЦИЯ:
   - Cmd+K на куске кода → "Измени это: [...]"
   - Или в чате: "Теперь добавь [...]"
   
6. ФИНАЛИЗАЦИЯ:
   - Все работает? Git commit!
   - git add src/components/NewComponent.jsx
   - git commit -m "feat: create NewComponent"
```

---

### **Паттерн 2: Исправление бага**

```bash
1. ВОСПРОИЗВЕДЕНИЕ:
   - Найди шаги для воспроизведения бага
   - Запиши что происходит
   - Копируй ошибку из консоли

2. ЛОКАЛИЗАЦИЯ:
   - Открой подозрительный файл
   - Добавь console.log для debugging
   - Определи где именно проблема

3. ПРОМПТ:
   "ПРОБЛЕМА: [описание]
   КОД: [проблемный код]
   ОШИБКА: [из консоли]
   
   Исправь и объясни что было не так"

4. ПРИМЕНЕНИЕ ФИКСА:
   - Cursor предложит изменения
   - Проверь что исправлено
   - Удали debug console.log

5. ТЕСТИРОВАНИЕ:
   - Воспроизведи баг снова
   - Проверь что больше не происходит
   - Проверь что ничего не сломалось

6. COMMIT:
   - git commit -m "fix: resolve [problem]"
```

---

### **Паттерн 3: Рефакторинг**

```bash
1. ЗАЧЕМ?
   - Код сложный/дублируется/неоптимальный
   - Определи конкретную цель рефакторинга

2. BACKUP:
   - git commit -m "chore: before refactoring"
   - Теперь можно безопасно экспериментировать

3. ПРОМПТ:
   - Выдели код (весь файл или кусок)
   - Cmd+K: "Отрефактори используя [техника]"
   
4. ПРОВЕРКА:
   - Функциональность не изменилась?
   - Тесты проходят?
   - Код читабельнее?

5. ЕСЛИ ЧТО-ТО СЛОМАЛОСЬ:
   - git diff (посмотри что изменилось)
   - git checkout -- file.jsx (откати файл)
   - Попробуй другой подход
```

---

## 🔥 **ADVANCED ЛАЙФХАКИ**

### **Лайфхак 1: Cursor как документация**

```
Вместо гугления:

Ты: @codebase как работает useTimer?
AI: [объясняет весь код хука]

Ты: @codebase покажи все места где используется formatTime
AI: [список всех использований]

Ты: объясни этот код построчно:
[вставляешь код]
AI: [детальное объяснение]
```

**Преимущество:** Контекст твоего проекта!

---

### **Лайфхак 2: Множественные изменения**

```
В Chat (Cmd+L):

"Обнови все компоненты в src/components/:
- Добавь PropTypes валидацию
- Добавь JSDoc комментарии
- Оберни экспорты в React.memo где нужно

Начни с @Button.jsx @Input.jsx @Select.jsx"

Cursor обработает несколько файлов!
```

---

### **Лайфхак 3: Генерация тестов**

```
"Создай тесты для @Button.jsx

Используй: Vitest + Testing Library

Протестируй:
- Рендер всех вариантов (primary, success, danger)
- onClick вызывается
- disabled состояние
- Правильные стили применяются"

Cursor создаст Button.test.jsx!
```

---

### **Лайфхак 4: Code review**

```
"Сделай code review для @Timer.jsx

Проверь:
- Есть ли баги?
- Правильно ли используются хуки?
- Есть ли утечки памяти?
- Можно ли оптимизировать?
- Соответствует ли best practices?"

Cursor даст детальный feedback!
```

---

### **Лайфхак 5: Миграция кода**

```
"Переведи этот код из HTML в React:

HTML КОД:
[вставляешь HTML/JS]

ТРЕБОВАНИЯ:
- Функциональный компонент
- Хуки вместо глобальных переменных
- JSX вместо createElement
- Tailwind вместо inline styles"
```

---

### **Лайфхак 6: Автофикс ESLint**

```
В Chat:

"@codebase найди все ESLint ошибки и исправь их"

Или Cmd+K на файле:

"Исправь все ESLint warnings"
```

---

### **Лайфхак 7: Создание boilerplate**

```
"Создай boilerplate для нового feature:

СТРУКТУРА:
feature/timer/
  ├── Timer.jsx (компонент)
  ├── useTimer.js (хук)
  ├── timer.utils.js (утилиты)
  ├── Timer.test.jsx (тесты)
  └── index.js (экспорты)

Все файлы со скелетом кода"
```

---

### **Лайфхак 8: Объяснение ошибок**

```
Когда видишь ошибку в консоли:

"Объясни эту ошибку и как исправить:

[вставляешь ошибку из консоли]

Код где ошибка:
[вставляешь код]"

AI даст понятное объяснение!
```

---

## ⚠️ **ЧАСТЫЕ ОШИБКИ ZERO CODERS**

### **Ошибка 1: Расплывчатые промпты**

❌ **ПЛОХО:**
```
"Сделай лучше"
"Исправь"
"Добавь функции"
```

✅ **ХОРОШО:**
```
"Добавь обработку ошибок через try-catch"
"Исправь утечку памяти в useEffect (не очищается интервал)"
"Добавь функцию pause которая останавливает интервал но сохраняет elapsedTime"
```

---

### **Ошибка 2: Не проверять результат**

❌ **ПЛОХО:**
```
Cursor: [генерирует код]
Ты: Ok! [применяешь не глядя]
```

✅ **ХОРОШО:**
```
Cursor: [генерирует код]
Ты: [читаешь код]
Ты: [запускаешь npm run dev]
Ты: [проверяешь в браузере]
Ты: [если ок - git commit]
```

---

### **Ошибка 3: Не добавлять контекст**

❌ **ПЛОХО:**
```
"Создай компонент Timer"
[AI не знает про твой стиль кода]
```

✅ **ХОРОШО:**
```
"Создай компонент Timer похожий на @Button.jsx
Используй стили из @glassmorphism.css
Интегрируй хук @useTimer.js"
```

---

### **Ошибка 4: Менять всё сразу**

❌ **ПЛОХО:**
```
"Переделай весь App.jsx: добавь роутинг, стили, новые компоненты, тесты"
[Всё сломается]
```

✅ **ХОРОШО:**
```
Итерация 1: "Добавь роутинг"
[проверка]
Итерация 2: "Обнови стили"
[проверка]
Итерация 3: "Добавь компоненты"
[проверка]
```

---

### **Ошибка 5: Не использовать Git**

❌ **ПЛОХО:**
```
[Много изменений без коммитов]
[Что-то сломалось]
[Не можешь откатиться]
```

✅ **ХОРОШО:**
```
[Каждый компонент - коммит]
[Что-то сломалось]
[git log → git checkout previous-commit]
```

---

## 🎓 **ОБУЧЕНИЕ ДЛЯ ZERO CODER**

### **Что изучать чтобы быть эффективнее:**

1. **Базовое понимание React (2-3 дня):**
   - Что такое компонент
   - Что такое props
   - Что такое state
   - Базовые хуки (useState, useEffect)
   
   **Ресурс:** React.dev tutorial (читай, не пиши код)

---

2. **Чтение кода (постоянно):**
   - Читай что генерирует Cursor
   - Пытайся понять логику
   - Гугли непонятные вещи
   
   **Цель:** Понимать что работает, что нет

---

3. **Git basics (1 день):**
   - git add, commit, push
   - git log, diff
   - git checkout (откат)
   
   **Ресурс:** git-workflow.md из документации

---

4. **Chrome DevTools (1 день):**
   - Console для ошибок
   - Elements для стилей
   - Network для запросов
   
   **Цель:** Уметь найти проблему

---

5. **Tailwind CSS (3-4 дня):**
   - Базовые классы
   - Responsive (sm:, md:, lg:)
   - Dark mode (dark:)
   
   **Ресурс:** Tailwind docs (читай примеры)

---

## 📱 **CURSOR НА ПРАКТИКЕ - РЕАЛЬНЫЙ ПРИМЕР**

### **Задача: Создать компонент Statistics с графиком**

```bash
# ШАГ 1: Открываем контекст
- Открой src/components/Button.jsx (для стиля)
- Cmd+/ (добавить в контекст)
- Открой src/styles/glassmorphism.css
- Cmd+/ (добавить в контекст)

# ШАГ 2: Prompt в Chat (Cmd+L)
```

```
Создай компонент Statistics в src/components/Statistics.jsx

КОНТЕКСТ:
- Проект: Time Tracker (React + Vite)
- Стиль: Glassmorphism как в @Button.jsx
- Данные: entries из localStorage

ФУНКЦИОНАЛ:
1. Получить entries через useLocalStorage('entries', [])
2. Показать статистику:
   - Всего записей
   - Общее время (в часах)
   - Среднее время в день
3. График времени по дням (Recharts LineChart):
   - X axis: даты
   - Y axis: часы
   - Line цвет: #3b82f6

МАКЕТ:
┌────────────────────┐
│ Статистика         │
├────────────────────┤
│ 📊 50 записей     │
│ ⏱️ 120 часов      │
│ 📅 4 ч/день       │
├────────────────────┤
│  [График]          │
│                    │
└────────────────────┘

СТИЛЬ:
- Glassmorphism контейнер
- Темная тема (dark: классы)
- Responsive (mobile friendly)
- Иконки из lucide-react

ЗАВИСИМОСТИ:
- recharts (уже установлен)
- lucide-react (уже установлен)

ВАЖНО:
- Обработать пустой массив (показать "Нет данных")
- Группировать entries по дням
- Форматировать время красиво
```

```bash
# ШАГ 3: Cursor создает компонент
[AI генерирует полный код]

# ШАГ 4: Проверяем
npm run dev
# Открываем localhost:3000
# Смотрим на компонент

# ШАГ 5: Итерируем (если нужно)
```

```
# В Chat:
"Отлично! Теперь добавь фильтр по периодам:
- Сегодня
- Эта неделя
- Этот месяц
- Весь период

Кнопки над графиком"
```

```bash
# ШАГ 6: Финализация
git add src/components/Statistics.jsx
git commit -m "feat: create Statistics component with Recharts"
git push

# ГОТОВО! 🎉
```

---

## 🎯 **CHECKLIST ДЛЯ КАЖДОГО ПРОМПТА**

Перед отправкой промпта проверь:

```
□ Четко описал ЧТО нужно сделать?
□ Указал ГДЕ создать файл?
□ Добавил похожие файлы в контекст (@filename)?
□ Указал используемые технологии?
□ Показал примеры если нужно?
□ Указал стиль кода (.cursorrules или в промпте)?
□ Описал edge cases?
□ Указал что НЕ нужно менять?
```

---

## 💎 **ЗОЛОТЫЕ ПРАВИЛА ZERO CODER**

1. **Начинай с малого**
   - Один компонент за раз
   - Одна функция за раз
   - Итерируй постепенно

2. **Всегда проверяй результат**
   - Читай сгенерированный код
   - Тестируй в браузере
   - Проверяй консоль на ошибки

3. **Используй Git как страховку**
   - Коммить часто
   - Коммить рабочий код
   - Не бояться откатываться

4. **Давай контекст AI**
   - Открывай похожие файлы
   - Используй @ для ссылок
   - Объясняй что уже есть

5. **Учись на коде**
   - Читай что генерирует Cursor
   - Гугли непонятное
   - Со временем поймешь больше

6. **Не бойся спрашивать**
   - "Объясни этот код"
   - "Почему это работает?"
   - "Есть ли лучший способ?"

7. **Итерируй, не переделывай**
   - Маленькие изменения
   - Проверка после каждого
   - Откат если не работает

8. **Сохраняй удачные промпты**
   - Создай файл my-prompts.md
   - Копируй успешные промпты
   - Переиспользуй их

---

## 🚀 **ФИНАЛЬНЫЕ СОВЕТЫ**

### **Для максимальной продуктивности:**

1. **Утром:**
   - Открой проект в Cursor
   - git pull (обнови код)
   - Определи цель на день
   - Напиши план (в комментариях или notes)

2. **Во время работы:**
   - Один промпт = одна задача
   - Проверяй после каждого
   - Коммитить каждые 30-60 минут
   - Делай перерывы

3. **Вечером:**
   - Проверь что всё работает
   - Финальный commit
   - git push
   - Запиши что сделал (для себя)

4. **Если застрял:**
   - Сделай перерыв (15 мин)
   - Попробуй другой промпт
   - Откатись к последнему рабочему коммиту
   - Спроси у AI объяснение

---

## 🎉 **ТЫ ГОТОВ!**

Теперь у тебя есть:
- ✅ Настроенный Cursor
- ✅ Понимание всех features
- ✅ 20 готовых промптов
- ✅ Workflow для работы
- ✅ Лайфхаки и советы
- ✅ Checklist для промптов

**Главное помни:**
- Cursor - инструмент, ты - архитектор
- Начинай с малого, итерируй
- Всегда проверяй результат
- Git - твой друг
- Не бойся экспериментировать

**Удачи в разработке! Ты справишься!** 🚀

---

## 📚 **ПОЛЕЗНЫЕ РЕСУРСЫ**

- [Cursor Docs](https://docs.cursor.sh/) - официальная документация
- [React Docs](https://react.dev/) - для понимания React
- [Tailwind Docs](https://tailwindcss.com/) - для стилей
- [Git Guide](git-workflow.md) - из твоей документации

**Сохрани этот файл и возвращайся к нему когда нужна помощь!** 📖
