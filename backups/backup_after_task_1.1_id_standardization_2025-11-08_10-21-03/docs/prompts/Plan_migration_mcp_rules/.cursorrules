# ============================================
# CURSOR AI RULES для ZERO CODER
# Time Tracker - HTML → React Migration
# ============================================
# 
# Этот файл настраивает Cursor AI под твой проект.
# Cursor будет автоматически следовать этим правилам!
#
# Положи этот файл в КОРЕНЬ проекта:
# /time-tracker/.cursorrules
#
# ============================================

# ============================================
# 📋 ИНФОРМАЦИЯ О ПРОЕКТЕ
# ============================================

## Название проекта
Time Tracker Web Application

## Описание
Приложение для отслеживания времени с красивым glassmorphism дизайном.
Миграция с vanilla HTML/CSS/JS на React 18.

## Текущий стек (откуда мигрируем)
- HTML5 (index.html)
- CSS3 (glassmorphism стили)
- Vanilla JavaScript
- localStorage для данных
- Tone.js для звуков

## Целевой стек (куда мигрируем)
- React 18 (функциональные компоненты)
- Vite (сборщик)
- Tailwind CSS (стили)
- Lucide React (иконки)
- Recharts (графики)
- Tone.js (звуки)

## Уровень разработчика
Zero Coder - я не пишу код сам, использую AI для генерации.
ВАЖНО: Всегда объясняй что делаешь и почему!

# ============================================
# 🎯 ФИЛОСОФИЯ КОДИРОВАНИЯ
# ============================================

## Главные принципы
1. **Простота > Сложность** - код должен быть понятен zero-coder
2. **Объясняй всё** - добавляй комментарии и JSDoc
3. **Консистентность** - следуй одному стилю везде
4. **Итеративность** - маленькие изменения лучше больших
5. **Безопасность** - всегда проверяй входные данные

## Для zero-coder
- Добавляй комментарии к сложной логике
- Используй описательные имена переменных
- Разбивай большие функции на маленькие
- Объясняй почему выбрал такое решение
- Предупреждай о потенциальных проблемах

# ============================================
# 🏗️ СТРУКТУРА ПРОЕКТА
# ============================================

## Организация файлов
```
src/
├── components/       # React компоненты
│   ├── ui/          # UI компоненты (Button, Input, etc)
│   ├── features/    # Feature компоненты (Timer, Stats, etc)
│   └── layout/      # Layout компоненты (Header, Footer, etc)
├── hooks/           # Кастомные хуки
├── utils/           # Утилиты и хелперы
├── services/        # Сервисы (звук, storage, etc)
├── styles/          # Глобальные стили
├── assets/          # Изображения, иконки
└── App.jsx          # Главный компонент
```

# ============================================
# ⚛️ REACT ПРАВИЛА
# ============================================

## Компоненты

### ВСЕГДА используй:
- Функциональные компоненты (НИКОГДА классы)
- Хуки для состояния и эффектов
- Named exports для утилит
- Default export для компонентов
- PascalCase для имен компонентов
- Расширение .jsx для компонентов

### Шаблон компонента:
```javascript
import React from 'react';
import PropTypes from 'prop-types';

/**
 * Brief component description
 * 
 * @component
 * @param {Object} props - Component props
 * @param {string} props.title - Title text
 * @param {function} props.onClick - Click handler
 * @returns {JSX.Element} Component markup
 * 
 * @example
 * <ComponentName title="Hello" onClick={handleClick} />
 */
const ComponentName = ({ title, onClick }) => {
  // 1. Хуки (всегда в начале)
  const [state, setState] = React.useState(null);
  
  // 2. Derived state
  const derivedValue = React.useMemo(() => {
    return computeValue(state);
  }, [state]);
  
  // 3. Handlers
  const handleClick = () => {
    onClick?.();
  };
  
  // 4. Effects (если нужны)
  React.useEffect(() => {
    // Effect logic
    return () => {
      // Cleanup
    };
  }, []);
  
  // 5. Render
  return (
    <div className="component-wrapper">
      <h2>{title}</h2>
      <button onClick={handleClick}>Click</button>
    </div>
  );
};

// PropTypes для валидации (ОБЯЗАТЕЛЬНО!)
ComponentName.propTypes = {
  title: PropTypes.string.isRequired,
  onClick: PropTypes.func
};

// Default props если нужны
ComponentName.defaultProps = {
  onClick: () => {}
};

export default ComponentName;
```

## Хуки

### Порядок хуков (СТРОГО СОБЛЮДАЙ):
1. useState
2. useReducer
3. useContext
4. useMemo
5. useCallback
6. useRef
7. useEffect
8. Кастомные хуки

### Правила хуков:
- Называй хуки с префикса `use` (useTimer, useLocalStorage)
- ВСЕГДА указывай зависимости в useEffect/useMemo/useCallback
- Используй функциональное обновление в setState: `setState(prev => prev + 1)`
- Очищай эффекты через return в useEffect
- Не вызывай хуки условно (if, loops)

### Шаблон кастомного хука:
```javascript
/**
 * Custom hook description
 * 
 * @param {*} initialValue - Initial value
 * @returns {Object} Hook return value
 * 
 * @example
 * const { value, increment } = useCustomHook(0);
 */
export const useCustomHook = (initialValue) => {
  const [value, setValue] = React.useState(initialValue);
  
  const increment = React.useCallback(() => {
    setValue(prev => prev + 1);
  }, []);
  
  return { value, increment };
};
```

## Оптимизация

### Когда использовать оптимизацию:
- `React.memo()` - если компонент рендерится часто с теми же props
- `useMemo()` - для дорогих вычислений (loops, filters, sorts)
- `useCallback()` - для функций передаваемых как props

### НЕ оптимизируй преждевременно:
- Начни с простого кода
- Оптимизируй только если есть проблемы с производительностью
- Измеряй перед оптимизацией (React DevTools Profiler)

# ============================================
# 🎨 СТИЛИ И ДИЗАЙН
# ============================================

## Tailwind CSS

### ВСЕГДА используй:
- Tailwind utility классы (НЕ inline styles)
- Responsive префиксы: `sm:`, `md:`, `lg:`, `xl:`
- Dark mode: `dark:` префикс
- Hover/focus states: `hover:`, `focus:`

### Glassmorphism стиль (основной дизайн):
```javascript
// Базовый glassmorphism контейнер
className="
  backdrop-blur-md
  bg-white/10 dark:bg-white/5
  border border-white/20
  rounded-2xl
  shadow-xl
  p-6
"

// С hover эффектом
className="
  backdrop-blur-md
  bg-white/10 dark:bg-white/5
  border border-white/20
  rounded-2xl
  shadow-xl
  p-6
  transition-all duration-300
  hover:bg-white/15 hover:scale-105
"
```

### Цветовая палитра:
```javascript
// Primary (синий)
bg-blue-500/20
border-blue-500/30
text-blue-400

// Success (зелёный)
bg-green-500/20
border-green-500/30
text-green-400

// Danger (красный)
bg-red-500/20
border-red-500/30
text-red-400

// Warning (жёлтый)
bg-yellow-500/20
border-yellow-500/30
text-yellow-400
```

### Адаптивность:
```javascript
// Mobile first approach
className="
  text-sm sm:text-base md:text-lg
  p-2 sm:p-4 md:p-6
  grid-cols-1 sm:grid-cols-2 lg:grid-cols-3
"
```

## Иконки

### Используй Lucide React:
```javascript
import { Play, Pause, Square, Trash2 } from 'lucide-react';

<Play className="w-5 h-5" />
```

### НЕ используй:
- Font Awesome
- Material Icons
- SVG файлы напрямую (только через Lucide)

# ============================================
# 🔧 УТИЛИТЫ И ХЕЛПЕРЫ
# ============================================

## Форматирование

### Время:
```javascript
/**
 * Format seconds to HH:MM:SS
 * @param {number} seconds - Time in seconds
 * @returns {string} Formatted time
 */
export const formatTime = (seconds) => {
  const hours = Math.floor(seconds / 3600);
  const minutes = Math.floor((seconds % 3600) / 60);
  const secs = seconds % 60;
  return `${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}:${String(secs).padStart(2, '0')}`;
};
```

### Дата:
```javascript
/**
 * Format date to DD.MM.YYYY
 * @param {Date|string} date - Date to format
 * @returns {string} Formatted date
 */
export const formatDate = (date) => {
  const d = new Date(date);
  return d.toLocaleDateString('ru-RU');
};
```

## Валидация

### Всегда валидируй:
- Входные данные функций
- Props компонентов (через PropTypes)
- Формы (перед submit)
- LocalStorage данные (перед парсингом)

# ============================================
# 💾 РАБОТА С ДАННЫМИ
# ============================================

## LocalStorage

### Используй кастомный хук:
```javascript
/**
 * Hook for localStorage with JSON serialization
 * @param {string} key - Storage key
 * @param {*} initialValue - Initial value
 * @returns {Array} [value, setValue]
 */
export const useLocalStorage = (key, initialValue) => {
  const [value, setValue] = React.useState(() => {
    try {
      const item = window.localStorage.getItem(key);
      return item ? JSON.parse(item) : initialValue;
    } catch (error) {
      console.error(`Error loading ${key} from localStorage:`, error);
      return initialValue;
    }
  });

  const setStoredValue = React.useCallback((newValue) => {
    try {
      setValue(newValue);
      window.localStorage.setItem(key, JSON.stringify(newValue));
    } catch (error) {
      console.error(`Error saving ${key} to localStorage:`, error);
    }
  }, [key]);

  return [value, setStoredValue];
};
```

### Структура данных:
```javascript
// entries - массив записей таймера
{
  id: 'uuid-v4',
  category: 'Work',
  description: 'Task description',
  startTime: '2025-10-27T10:00:00Z',
  endTime: '2025-10-27T11:30:00Z',
  duration: 5400, // seconds
  date: '2025-10-27'
}

// categories - массив категорий
{
  id: 'uuid-v4',
  name: 'Work',
  color: '#3b82f6',
  icon: 'briefcase'
}
```

# ============================================
# 🎵 ЗВУКИ (Tone.js)
# ============================================

## Сервис звуков

### Создай SoundService:
```javascript
import * as Tone from 'tone';

class SoundService {
  constructor() {
    this.enabled = true;
    this.synth = null;
  }

  async init() {
    await Tone.start();
    this.synth = new Tone.Synth().toDestination();
  }

  play(note, duration = '8n') {
    if (!this.enabled || !this.synth) return;
    this.synth.triggerAttackRelease(note, duration);
  }

  playStart() {
    this.play('C4', '8n');
  }

  playStop() {
    this.play('A3', '8n');
  }

  toggle() {
    this.enabled = !this.enabled;
  }
}

export const soundService = new SoundService();
```

# ============================================
# 📊 ГРАФИКИ (Recharts)
# ============================================

## Использование:
```javascript
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from 'recharts';

// Подготовка данных
const data = entries.map(entry => ({
  date: formatDate(entry.date),
  hours: entry.duration / 3600
}));

// Компонент
<ResponsiveContainer width="100%" height={300}>
  <LineChart data={data}>
    <CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.1)" />
    <XAxis dataKey="date" stroke="rgba(255,255,255,0.5)" />
    <YAxis stroke="rgba(255,255,255,0.5)" />
    <Tooltip 
      contentStyle={{ 
        backgroundColor: 'rgba(0,0,0,0.8)', 
        border: '1px solid rgba(255,255,255,0.2)' 
      }} 
    />
    <Line type="monotone" dataKey="hours" stroke="#3b82f6" strokeWidth={2} />
  </LineChart>
</ResponsiveContainer>
```

# ============================================
# ⚠️ ОБРАБОТКА ОШИБОК
# ============================================

## Принципы:
1. Всегда используй try-catch для:
   - localStorage операций
   - JSON.parse/stringify
   - Async операций
   - Audio API

2. Логируй ошибки в консоль (для разработки)

3. Показывай понятные сообщения пользователю

## Пример:
```javascript
const handleSave = () => {
  try {
    const data = JSON.stringify(entries);
    localStorage.setItem('entries', data);
    showNotification('Saved successfully', 'success');
  } catch (error) {
    console.error('Save error:', error);
    showNotification('Failed to save', 'error');
  }
};
```

# ============================================
# 🚫 ЗАПРЕТЫ
# ============================================

## НИКОГДА не используй:
- ❌ `var` (только `const` и `let`)
- ❌ Классовые компоненты React
- ❌ Inline styles в JSX (только Tailwind классы)
- ❌ `any` тип в PropTypes
- ❌ Прямые мутации state (`state.push()` ❌, `setState([...state, item])` ✅)
- ❌ `==` для сравнения (только `===`)
- ❌ Глобальные переменные
- ❌ `eval()`, `Function()` constructor
- ❌ `document.write()`
- ❌ Синхронные XMLHttpRequest

## Избегай:
- ⚠️ Длинные функции (>50 строк - разбей на части)
- ⚠️ Дублирование кода (используй функции)
- ⚠️ Магические числа (используй константы)
- ⚠️ Излишняя оптимизация (сначала работающий код)

# ============================================
# ✅ ЧЕКЛИСТ ДЛЯ КАЖДОГО КОМПОНЕНТА
# ============================================

Перед тем как считать компонент готовым, проверь:

□ JSDoc комментарии добавлены
□ PropTypes определены
□ Именование соответствует conventions
□ Стили используют Tailwind (не inline)
□ Glassmorphism дизайн применён
□ Dark mode поддержка добавлена (dark: классы)
□ Responsive на всех экранах (sm:, md:, lg:)
□ Обработка ошибок присутствует
□ useEffect имеет cleanup (если нужно)
□ Нет прямых мутаций state
□ Нет предупреждений в консоли
□ Компонент работает как ожидается

# ============================================
# 🎯 МИГРАЦИЯ HTML → REACT
# ============================================

## Стратегия миграции:

### 1. Анализ HTML:
- Найди все интерактивные элементы
- Определи что нужно вынести в state
- Найди event listeners

### 2. Создание компонентов:
- Один HTML блок = один React компонент
- Timer section → Timer.jsx
- Stats section → Statistics.jsx
- Entries list → EntriesList.jsx

### 3. Конвертация стилей:
- inline styles → Tailwind классы
- CSS classes → Tailwind utility классы
- Сохрани glassmorphism эффекты

### 4. Логика:
- Global variables → useState/useContext
- Event listeners → onClick/onChange handlers
- setInterval → useEffect с cleanup
- localStorage calls → useLocalStorage hook

### 5. Тестирование:
- Проверь каждую функцию
- Сравни с оригинальным HTML
- Убедись что всё работает

## Порядок миграции:

1. **ЭТАП 1: Настройка**
   - Создай Vite проект
   - Установи зависимости
   - Настрой Tailwind
   - Создай базовую структуру

2. **ЭТАП 2: UI компоненты**
   - Button
   - Input
   - Select
   - Modal

3. **ЭТАП 3: Layout**
   - Header
   - Footer
   - Container

4. **ЭТАП 4: Хуки и сервисы**
   - useTimer
   - useLocalStorage
   - SoundService

5. **ЭТАП 5: Feature компоненты**
   - Timer
   - EntriesList
   - Statistics

6. **ЭТАП 6: Интеграция**
   - App.jsx
   - Роутинг (если нужен)
   - Final testing

# ============================================
# 💬 КОММЕНТАРИИ И ДОКУМЕНТАЦИЯ
# ============================================

## JSDoc для функций (ОБЯЗАТЕЛЬНО):
```javascript
/**
 * Brief function description
 * 
 * Detailed explanation if needed
 * 
 * @param {Type} paramName - Parameter description
 * @returns {Type} Return value description
 * 
 * @example
 * const result = functionName(param);
 */
```

## Inline комментарии:
```javascript
// Используй для объяснения ПОЧЕМУ, не ЧТО
// ✅ Хорошо:
// We use functional update because state might be stale in async context
setState(prev => prev + 1);

// ❌ Плохо:
// Increment state by 1
setState(prev => prev + 1);
```

## TODO комментарии:
```javascript
// TODO: Add validation for negative values
// FIXME: Memory leak in useEffect cleanup
// HACK: Temporary solution until API is ready
```

# ============================================
# 🔍 ОТЛАДКА
# ============================================

## Console.log для разработки:
```javascript
// Используй в development
if (process.env.NODE_ENV === 'development') {
  console.log('Debug info:', data);
}

// Или создай debug флаг
const DEBUG = false; // Поменяй на true для отладки

if (DEBUG) {
  console.log('State:', state);
}
```

## React DevTools:
- Используй для просмотра state
- Проверяй props компонентов
- Анализируй ререндеры

# ============================================
# 📦 ИМПОРТЫ
# ============================================

## Порядок импортов (СТРОГО):
```javascript
// 1. React и библиотеки
import React from 'react';
import PropTypes from 'prop-types';
import { LineChart } from 'recharts';

// 2. UI компоненты
import Button from './ui/Button';
import Input from './ui/Input';

// 3. Хуки
import { useTimer } from '../hooks/useTimer';
import { useLocalStorage } from '../hooks/useLocalStorage';

// 4. Утилиты
import { formatTime, formatDate } from '../utils/formatters';

// 5. Сервисы
import { soundService } from '../services/soundService';

// 6. Стили (если есть)
import './Component.css';
```

# ============================================
# 🎨 NAMING CONVENTIONS
# ============================================

## Файлы:
- Компоненты: `PascalCase.jsx` (Timer.jsx, Button.jsx)
- Хуки: `camelCase.js` (useTimer.js, useLocalStorage.js)
- Утилиты: `camelCase.js` (formatters.js, validators.js)
- Сервисы: `camelCase.js` (soundService.js, storageService.js)

## Переменные и функции:
```javascript
// camelCase для всего
const userName = 'John';
const isActive = true;
const handleClick = () => {};
const fetchData = async () => {};

// PascalCase только для компонентов и классов
const Timer = () => {};
class SoundService {}

// UPPERCASE для констант
const MAX_RETRIES = 3;
const API_URL = 'https://api.example.com';
```

## Boolean переменные:
```javascript
// Префиксы: is, has, should, can
const isLoading = true;
const hasError = false;
const shouldUpdate = true;
const canDelete = false;
```

# ============================================
# 🎯 КОНКРЕТНЫЕ ПРАВИЛА ДЛЯ TIME TRACKER
# ============================================

## Категории по умолчанию:
```javascript
const DEFAULT_CATEGORIES = [
  { id: '1', name: 'Work', color: '#3b82f6', icon: 'briefcase' },
  { id: '2', name: 'Study', color: '#10b981', icon: 'book' },
  { id: '3', name: 'Sport', color: '#f59e0b', icon: 'dumbbell' },
  { id: '4', name: 'Rest', color: '#8b5cf6', icon: 'coffee' }
];
```

## Формат времени:
- Всегда HH:MM:SS
- Всегда 2 цифры (01:05:03, не 1:5:3)

## Формат даты:
- ISO 8601 для хранения: '2025-10-27T10:00:00Z'
- DD.MM.YYYY для отображения: '27.10.2025'

## localStorage ключи:
```javascript
const STORAGE_KEYS = {
  ENTRIES: 'time-tracker-entries',
  CATEGORIES: 'time-tracker-categories',
  SETTINGS: 'time-tracker-settings'
};
```

# ============================================
# 🚀 PRODUCTION ГОТОВНОСТЬ
# ============================================

Перед production build проверь:

□ Удалены все console.log (кроме errors)
□ Удалены все TODO/FIXME (или запланированы)
□ Все PropTypes определены
□ Нет unused imports/variables
□ Нет предупреждений ESLint
□ Build проходит без ошибок
□ Приложение работает в production mode
□ Responsive на всех разрешениях
□ Dark mode работает корректно
□ localStorage не переполняется

# ============================================
# 💡 ДОПОЛНИТЕЛЬНЫЕ СОВЕТЫ ДЛЯ ZERO CODER
# ============================================

## Когда не знаешь как сделать:
1. Спроси Cursor AI: "Как лучше реализовать [задачу]?"
2. Попроси показать несколько вариантов
3. Попроси объяснить плюсы/минусы каждого
4. Выбери понятный тебе вариант

## Когда код не работает:
1. Скопируй ошибку из консоли
2. Скажи Cursor: "Вот ошибка: [ошибка]. Что не так?"
3. Cursor объяснит проблему и предложит решение

## Когда нужно что-то изменить:
1. Используй Cmd+K на конкретном участке кода
2. Опиши конкретно что изменить
3. Проверь результат
4. Если не то - попробуй переформулировать

## Учись на ходу:
- Читай сгенерированный код
- Гугли непонятные вещи
- Спрашивай у Cursor объяснения
- Со временем начнёшь понимать больше!

# ============================================
# 📚 РЕСУРСЫ
# ============================================

## Документация (открывай при необходимости):
- React: https://react.dev
- Tailwind CSS: https://tailwindcss.com
- Vite: https://vitejs.dev
- Recharts: https://recharts.org
- Tone.js: https://tonejs.github.io

## Инструменты:
- React DevTools (browser extension)
- Vite DevTools
- Tailwind CSS IntelliSense (VSCode extension)

# ============================================
# 🎉 ФИНАЛ
# ============================================

Эти правила помогут Cursor AI генерировать идеальный код
для твоего проекта. Cursor будет автоматически следовать
всем этим правилам без необходимости повторять их в каждом промпте!

Если нужно что-то изменить - просто отредактируй этот файл.

Удачи с миграцией! 🚀

# ============================================
