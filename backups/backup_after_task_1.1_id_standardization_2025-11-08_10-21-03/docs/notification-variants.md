# 🎨 Визуальные варианты уведомлений

## Описание

В проекте реализовано **5 визуальных вариантов** уведомлений в стиле проекта. Все варианты используют:
- Tailwind CSS для стилизации
- Glass-effect или современные эффекты
- Иконки из `lucide-react`
- Анимации появления (`animate-slide-in-right`)
- Индикатор прогресса (если указана длительность)

## Варианты

### Вариант 1: Glass Effect (Стеклянный эффект) ⭐ По умолчанию
**Стиль:** Минималистичный с glass-effect и цветным акцентом слева

**Особенности:**
- Использует класс `glass-effect` из проекта
- Цветная левая граница (4px) для типа уведомления
- Иконка слева с цветным акцентом
- Компактный индикатор прогресса
- Кнопка закрытия в стиле `glass-button`

**Размеры:**
- Минимальная ширина: 300px
- Максимальная ширина: 400px
- Padding: 16px (p-4)
- Скругление: rounded-xl

---

### Вариант 2: Bordered (С рамкой)
**Стиль:** С цветной рамкой со всех сторон и иконкой в контейнере

**Особенности:**
- Цветной фон по типу уведомления
- Рамка 2px со всех сторон
- Иконка в отдельном контейнере с glass-effect
- Более заметный индикатор прогресса
- Кнопка закрытия с hover-эффектом

**Размеры:**
- Минимальная ширина: 300px
- Максимальная ширина: 400px
- Padding: 16px (p-4)
- Скругление: rounded-xl

---

### Вариант 3: Gradient (Градиент)
**Стиль:** С градиентным фоном и современным дизайном

**Особенности:**
- Градиентный фон (`bg-gradient-to-r`)
- Иконка в контейнере с полупрозрачным фоном
- Белый текст для контраста
- Плавный индикатор прогресса
- Современный дизайн с тенями

**Размеры:**
- Минимальная ширина: 300px
- Максимальная ширина: 400px
- Padding: 16px (p-4)
- Скругление: rounded-2xl

---

### Вариант 4: Compact (Компактный)
**Стиль:** Компактный стиль с иконкой слева и минимальными отступами

**Особенности:**
- Минимальные отступы (p-3)
- Иконка и текст в одной строке
- Тонкий индикатор прогресса (h-0.5)
- Маленькая кнопка закрытия
- Идеален для множественных уведомлений

**Размеры:**
- Минимальная ширина: 280px
- Максимальная ширина: 380px
- Padding: 12px (p-3)
- Скругление: rounded-lg

---

### Вариант 5: Modern (Современный)
**Стиль:** Современный стиль с глубокими тенями и скругленными углами

**Особенности:**
- Глубокие тени (shadow-2xl)
- Большие скругления (rounded-2xl)
- Иконка в контейнере с backdrop-blur
- Толстый индикатор прогресса (h-2)
- Увеличенные отступы (p-5)

**Размеры:**
- Минимальная ширина: 320px
- Максимальная ширина: 420px
- Padding: 20px (p-5)
- Скругление: rounded-2xl

---

## Цветовая схема

Все варианты используют единую цветовую схему по типам:

### Success (Успех)
- Цвет: Green-500/600
- Иконка: `CheckCircle2`
- Используется для: успешных операций

### Error (Ошибка)
- Цвет: Red-500/600
- Иконка: `AlertCircle`
- Используется для: ошибок и предупреждений

### Warning (Предупреждение)
- Цвет: Yellow-500/600
- Иконка: `AlertTriangle`
- Используется для: предупреждений

### Info (Информация)
- Цвет: Blue-500/600
- Иконка: `Info`
- Используется для: информационных сообщений

---

## Расположение

Все уведомления отображаются в **правом верхнем углу** экрана:
- Позиция: `fixed top-4 right-4`
- Z-index: `z-50`
- Отступ между уведомлениями: `space-y-2` (8px)

---

## Настройка варианта

Вариант уведомления настраивается в `useSettingsStore`:

```javascript
notifications: {
  variant: 1, // 1-5 (по умолчанию 1 - Glass Effect)
  // ... другие настройки
}
```

### Изменение варианта программно:

```javascript
import { useSettingsStore } from '@/store/useSettingsStore';

const { updateSettings } = useSettingsStore();

// Установить вариант 2
updateSettings({
  notifications: {
    ...notifications,
    variant: 2
  }
});
```

---

## Примеры использования

### Показать уведомление (используется текущий вариант):

```javascript
import { useNotifications } from '@/hooks/useNotifications';

const { showSuccess, showError, showWarning, showInfo } = useNotifications();

// Успех
showSuccess('Таймер запущен', 3000);

// Ошибка
showError('Не удалось сохранить запись', 5000);

// Предупреждение
showWarning('Проверьте заполнение полей', 4000);

// Информация
showInfo('Новая версия доступна', 5000);
```

---

## Анимации

Все варианты используют анимацию появления:
- Класс: `animate-slide-in-right`
- Длительность: 300ms
- Easing: `ease-out`
- Направление: справа налево

---

## Индикатор прогресса

Индикатор прогресса отображается только если:
- `notification.duration > 0`
- Автоматически уменьшается каждые 100ms
- Исчезает при достижении 0%

---

## Доступность (A11Y)

Все варианты включают:
- `aria-label` для кнопки закрытия
- Семантические цвета для типов уведомлений
- Контрастный текст для читаемости
- Поддержка темной темы

---

## Рекомендации по выбору варианта

- **Вариант 1 (Glass Effect)** - рекомендуется для большинства случаев, минималистичный и в стиле проекта
- **Вариант 2 (Bordered)** - для важных уведомлений, требующих внимания
- **Вариант 3 (Gradient)** - для современного, яркого дизайна
- **Вариант 4 (Compact)** - когда нужно показать много уведомлений одновременно
- **Вариант 5 (Modern)** - для премиум-ощущения, с глубокими тенями

---

## Технические детали

- **Компоненты:** `NotificationVariants.jsx`, `Notification.jsx`
- **Хранилище:** `useSettingsStore` → `notifications.variant`
- **Иконки:** `lucide-react` (CheckCircle2, AlertCircle, AlertTriangle, Info)
- **Стили:** Tailwind CSS + кастомные классы проекта

