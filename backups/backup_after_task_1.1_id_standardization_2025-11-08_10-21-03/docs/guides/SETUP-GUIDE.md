# 🚀 Полная установка автоматизации для Zero Coder

**Проект:** Time Tracker Dashboard  
**Дата:** 2 ноября 2025

---

## 🎯 Что установлено

### ✅ 1. **MCP Memory** (Запоминание контекста)
- Запоминает ваши предпочтения
- Помнит структуру проекта
- Помнит решения между сессиями
- **Статус:** Требует настройки mcp.json (см. ниже)

### ✅ 2. **Beads** (Управление задачами)
- AI автоматически создает задачи
- Отслеживает зависимости
- Находит что делать дальше
- Планирует долгосрочные фичи
- **Статус:** Требует установки (см. ниже)

### ✅ 3. **Улучшенный .cursorrules**
- Инструкции для AI
- Шаблоны кода
- Быстрые команды
- Раздел про Beads
- **Статус:** ✅ Установлено

### ✅ 4. **Примеры кода** (.examples/)
- Эталонные шаблоны компонентов
- Примеры хуков
- Примеры Zustand stores
- Примеры утилит
- **Статус:** ✅ Установлено

---

## 📦 Шаг 1: Установка Beads

### ⚠️ ВАЖНО: Для Windows

Beads требует Go для установки. Если Go не установлен, у вас есть два варианта:

#### Вариант A: Установить Go (рекомендуется)

1. Скачайте Go: https://go.dev/dl/
2. Установите Go для Windows
3. Добавьте Go в PATH (обычно делается автоматически)
4. Откройте новый терминал и выполните:

```powershell
# Проверка установки
go version

# Установка Beads
go install github.com/steveyegge/beads@latest

# Проверка установки
bd --version
```

#### Вариант B: Использовать без Beads (временно)

Если установка Go проблематична, можно работать без Beads. AI все равно может управлять задачами через комментарии и память (MCP Memory).

---

## 📦 Шаг 2: Инициализация Beads в проекте

**Если Beads установлен:**

Откройте Cursor Chat (`Ctrl+L`) и попросите:

```
Инициализируй Beads в моем проекте:

1. Запусти команду: bd init --prefix ttm
   (ttm = Time Tracker Module)

2. Настрой Git hooks для автоматической синхронизации:
   - pre-commit hook для экспорта issues
   - post-merge hook для импорта issues
   - post-checkout hook для синхронизации

3. Добавь в .gitignore:
   .beads/*.db
   .beads/*.db-*
   
4. Добавь в Git (НЕ игнорируй):
   .beads/issues.jsonl

5. Проверь что все работает:
   bd list
   bd quickstart

Объясни что делает каждый hook и зачем он нужен.
```

---

## 📦 Шаг 3: Настройка MCP серверов

### Что такое MCP?

MCP (Model Context Protocol) - это протокол, который позволяет AI подключаться к внешним источникам данных:
- Официальная документация (React, Tailwind, Vite)
- Поиск в интернете (Brave Search)
- Память между сессиями (Memory)
- Улучшенное мышление (Sequential Thinking)

### Установка MCP Configuration

1. **Создайте файл `mcp.json` в корне проекта `time-tracker/`**

2. **Используйте готовый шаблон из `Claude_promt/Plan_migration_mcp_rules/mcp.json`**

3. **Активируйте MCP в Cursor:**
   - Cursor Settings → Features → Enable "Model Context Protocol"
   - Cursor Settings → MCP → Add Configuration File
   - Выберите файл `time-tracker/mcp.json`
   - Перезапустите Cursor

4. **Протестируйте:**
   ```
   В Cursor Chat: "@docs-search как использовать useState в React?"
   ```

---

## 📦 Шаг 4: Что уже установлено и работает

### ✅ .cursorrules
- Файл настроен в `time-tracker/.cursorrules`
- Содержит все правила кодирования
- Включает раздел про Beads
- Cursor автоматически использует эти правила

### ✅ .examples/ папка
- `component.example.jsx` - шаблон компонента
- `hook.example.js` - шаблон хука
- `store.example.js` - шаблон Zustand store
- `utility.example.js` - шаблон утилиты
- `README.md` - документация по использованию

### ✅ .gitignore обновлен
- Игнорирует Beads базы данных (`.beads/*.db`)
- НЕ игнорирует файл задач (`.beads/issues.jsonl`)

---

## 🎯 Как работает Memory (MCP Memory)

**Что делает:**
🧠 **Память между сессиями**
- Помнит ваши предпочтения
- Помнит как вы любите писать код
- Помнит решения которые приняли

**Примеры:**
```
Вы: "Запомни что я использую Tailwind для стилей"
AI: [Сохраняет в Memory]

Через день:
Вы: "Создай компонент кнопки"
AI: [Вспоминает из Memory что использовать Tailwind]
AI: [Создает компонент с Tailwind классами]
```

**Как использовать:**
- AI сохраняет автоматически во время работы
- Можно явно попросить: "Запомни это решение"
- AI вспоминает при начале новой сессии

---

## 🎯 Как работает Beads

**Что делает:**
📋 **Умная доска задач**
- AI создает задачи автоматически
- Отслеживает что от чего зависит
- Знает что делать дальше
- Не забывает ничего

**Пример использования:**

```
Вы: "Добавь экспорт в Excel"

AI автоматически:
1. Создает эпик: "Добавить экспорт в Excel"
2. Разбивает на подзадачи:
   - Установить библиотеку xlsx
   - Создать функцию экспорта
   - Добавить кнопку в UI
   - Написать тесты
3. Настраивает зависимости:
   - UI зависит от функции
   - Тесты зависят от UI
4. Показывает план: bd dep tree
5. Начинает с первой задачи: bd ready
```

**Вместе с Memory:**
- Memory помнит "КАК" вы работаете
- Beads помнит "ЧТО" нужно сделать
- Вы просто говорите что хотите
- AI делает правильно и по плану

---

## 📋 Быстрые команды для создания кода

### Компоненты:
```
компонент EarningsCard
(AI создаст компонент по шаблону из .examples/component.example.jsx)
```

### Хуки:
```
хук useExport
(AI создаст хук по шаблону из .examples/hook.example.js)
```

### Stores:
```
store useExportStore
(AI создаст store по шаблону из .examples/store.example.js)
```

### Утилиты:
```
утилита exportToExcel
(AI создаст утилиту по шаблону из .examples/utility.example.js)
```

---

## 📋 Команды Beads для управления задачами

**Создание:**
```bash
bd create "Название задачи" -t task -p 1 --json
```

**Просмотр:**
```bash
bd list                    # Все задачи
bd ready                   # Готовые к работе (без блокеров)
bd blocked                 # Заблокированные задачи
bd show ttm-5              # Детали задачи
```

**Управление:**
```bash
bd update ttm-1 --status in_progress  # Начать работу
bd close ttm-1 --reason "Готово"      # Закрыть задачу
```

**Зависимости:**
```bash
bd dep add ttm-2 ttm-1 --type blocks  # Задача 2 зависит от задачи 1
bd dep tree ttm-1                     # Показать дерево зависимостей
```

---

## 💡 Примеры реальных сценариев

### Сценарий 1: Добавление новой фичи

**Вы:**
```
Добавь фильтрацию по датам в таблицу записей
```

**AI автоматически:**
1. Создаст эпик в Beads: "Добавить фильтрацию по датам"
2. Разобьет на подзадачи с зависимостями
3. Покажет граф: UI → Store → Utils
4. Начнет с первой задачи (Utils)
5. В процессе найдет баг → создаст issue
6. Закроет задачу, покажет следующую

---

### Сценарий 2: Продолжение работы на следующий день

**Вы:**
```
Привет! С чего начнем сегодня?
```

**AI:**
```
Привет! Вчера мы работали над фильтрацией по датам.

Memory показывает:
- Предпочитаешь date-fns для дат
- Используем Zustand для фильтров

Beads показывает:
✅ Готово: Утилиты для дат (ttm-23)
🔄 В работе: Store для фильтров (ttm-24, 60% готово)
⏳ Ждут: UI компонент (ttm-25, зависит от ttm-24)

Продолжим ttm-24 (Store для фильтров)?
```

---

### Сценарий 3: Обнаружение проблемы

**В процессе работы AI находит баг:**

```
🐛 Обнаружил проблему: валидация дат не работает для високосных годов

Создал issue:
ttm-26: "Исправить валидацию для високосных годов"
- Тип: bug
- Приоритет: 1 (высокий)
- Связано с: ttm-24 (discovered-from)

Продолжить текущую задачу или исправить баг сейчас?
```

---

## 📋 Ежедневный Workflow

### Утро (Начало работы):

```
Доброе утро! Покажи:
1. Что мы делали вчера (memory)
2. Готовые задачи на сегодня (bd ready)
3. С чего лучше начать
```

### В процессе работы:

**AI автоматически:**
- Создает issues когда находит проблемы
- Связывает их с текущей задачей
- Обновляет статусы
- Закрывает выполненные задачи

**Вы просто работаете:**
```
Продолжи работу над экспортом в PDF
(AI сам проверит Beads, найдет текущую задачу, продолжит)
```

### Вечер (Конец работы):

```
Подведи итоги дня:
1. Что сделано (закрытые issues)
2. Что осталось (open issues)
3. Что будем делать завтра (ready tasks)
```

---

## ⚠️ Troubleshooting

### Beads не находит задачи:
```bash
bd list  # Должно показать задачи
# Если пусто - задачи еще не созданы
# Попросите AI создать первую задачу
```

### Memory не работает:
```
# Проверьте mcp.json
# Должно быть:
"memory": {
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-memory"]
}
```

### Git hooks не работают:
```bash
# Проверьте права (для Linux/Mac)
ls -la .git/hooks/

# Должны быть executable
chmod +x .git/hooks/pre-commit
chmod +x .git/hooks/post-merge
```

### AI не использует примеры:
```
# Проверьте что папка существует
ls -la .examples/

# Должны быть файлы:
# component.example.jsx
# hook.example.js  
# store.example.js
# utility.example.js
# README.md
```

---

## 📞 Нужна помощь?

Спросите у AI:

```
Объясни как работает [Memory/Beads/примеры]
Покажи примеры использования [система]
Почему [что-то] не работает?
```

AI прочитает эту инструкцию и поможет! 🤖

---

## ✅ Чеклист установки

- [ ] `.cursorrules` установлен в `time-tracker/.cursorrules`
- [ ] Папка `.examples/` создана со всеми файлами
- [ ] `.gitignore` обновлен для Beads
- [ ] Go установлен (для Beads) или пропущено
- [ ] Beads установлен и инициализирован (или пропущено)
- [ ] `mcp.json` создан в `time-tracker/`
- [ ] MCP активирован в Cursor Settings
- [ ] Cursor перезапущен после настройки MCP
- [ ] Протестированы все MCP серверы
- [ ] `SETUP-GUIDE.md` прочитан и понят

---

**Дата создания:** 2 ноября 2025  
**Версия:** 1.0

