# Руководство по анимации модальных окон

**Версия:** 1.0  
**Дата создания:** 2024-12-19  
**Последнее обновление:** 2024-12-19

## 📋 Обзор

Это руководство описывает, как правильно реализовать анимацию изменения размера модальных окон в проекте Time Tracker Dashboard. Используйте это руководство при создании новых модальных окон или при необходимости добавить анимацию к существующим.

---

## 🎯 Типы анимаций

В проекте реализованы три типа анимаций для модальных окон:

### 1. Анимация открытия/закрытия (BaseModal)
- **Компонент:** `BaseModal.jsx`
- **Статус:** ✅ Автоматически работает для всех модальных окон
- **Описание:** Плавное появление модального окна снизу вверх при открытии и исчезновение вниз при закрытии

### 2. Анимация изменения размера окна браузера (BaseModal)
- **Компонент:** `BaseModal.jsx`
- **Статус:** ✅ Автоматически работает для всех модальных окон
- **Описание:** Плавное изменение размера модального окна при изменении размера окна браузера (window resize)

### 3. Анимация изменения содержимого (AnimatedModalContent)
- **Компонент:** `AnimatedModalContent.jsx`
- **Статус:** ⚠️ Требует ручной интеграции
- **Описание:** Плавное изменение размера модального окна при изменении содержимого (например, при переключении вкладок или шагов)

---

## 📦 Компоненты

### BaseModal

**Путь:** `src/components/ui/BaseModal.jsx`

**Описание:** Базовый компонент модального окна с автоматической анимацией открытия/закрытия и изменения размера при window resize.

**Использование:**
```javascript
import { BaseModal } from '../ui/BaseModal';

<BaseModal
  isOpen={isOpen}
  onClose={onClose}
  title="Заголовок"
  size="large"
>
  {/* Контент модального окна */}
</BaseModal>
```

**Особенности:**
- ✅ Автоматическая анимация открытия/закрытия
- ✅ Автоматическая анимация при изменении размера окна браузера
- ✅ Не требует дополнительной настройки

### AnimatedModalContent

**Путь:** `src/components/ui/AnimatedModalContent.jsx`

**Описание:** Компонент-обертка для анимации изменения размера контента модального окна при изменении содержимого.

**Использование:**
```javascript
import { AnimatedModalContent } from '../ui/AnimatedModalContent';

<BaseModal
  isOpen={isOpen}
  onClose={onClose}
  title="Заголовок"
>
  {/* Контент С анимацией */}
  <AnimatedModalContent contentKey={currentStep}>
    {currentStepData.content}
  </AnimatedModalContent>
</BaseModal>
```

**Параметры:**
- `children` (React.ReactNode) - Контент для отображения
- `contentKey` (string | number) - **ОБЯЗАТЕЛЬНО** - Ключ для отслеживания смены контента
- `fadeOutDuration` (number, по умолчанию 250) - Длительность затухания в миллисекундах
- `heightDuration` (number, по умолчанию 350) - Длительность изменения высоты в миллисекундах

**Особенности:**
- ✅ Плавная анимация fade-out → изменение высоты → fade-in
- ✅ Работает корректно при увеличении и уменьшении размера
- ✅ Защита от быстрых переключений
- ✅ Оптимизация для Safari

---

## 🚀 Когда использовать AnimatedModalContent

### ✅ Используйте AnimatedModalContent, если:

1. **Модальное окно имеет динамический контент:**
   - Переключение вкладок (tabs)
   - Переключение шагов (steps)
   - Условный рендеринг контента
   - Любое изменение содержимого, которое меняет размер окна

2. **Размер контента может изменяться:**
   - Разные вкладки имеют разный объем контента
   - Шаги имеют разную высоту
   - Контент загружается динамически

3. **Нужна плавная анимация:**
   - Пользователь должен видеть плавный переход между размерами
   - Резкое изменение размера выглядит как баг

### ❌ НЕ используйте AnimatedModalContent, если:

1. **Контент статичен:**
   - Размер контента не меняется
   - Нет переключения между разными вариантами контента

2. **Размер контента всегда одинаковый:**
   - Все вкладки имеют одинаковую высоту
   - Контент не меняется динамически

3. **Анимация не нужна:**
   - Резкое изменение размера приемлемо
   - Производительность важнее плавности

---

## 📝 Примеры использования

### Пример 1: Модальное окно с вкладками (Tabs)

**Сценарий:** Модальное окно с несколькими вкладками, каждая из которых имеет разный объем контента.

**Решение:** Используйте `AnimatedModalContent` с `contentKey={activeTab}`

```javascript
import { BaseModal } from '../ui/BaseModal';
import { AnimatedModalContent } from '../ui/AnimatedModalContent';

export function MyModal({ isOpen, onClose }) {
  const [activeTab, setActiveTab] = useState('tab1');
  
  const tabs = {
    tab1: <div>Контент вкладки 1 (маленький)</div>,
    tab2: <div>Контент вкладки 2 (большой) ... много контента ...</div>,
    tab3: <div>Контент вкладки 3 (средний)</div>,
  };
  
  return (
    <BaseModal
      isOpen={isOpen}
      onClose={onClose}
      title="Модальное окно с вкладками"
      size="large"
    >
      {/* Навигация вкладок - БЕЗ анимации */}
      <div className="flex gap-2 mb-4">
        <button onClick={() => setActiveTab('tab1')}>Вкладка 1</button>
        <button onClick={() => setActiveTab('tab2')}>Вкладка 2</button>
        <button onClick={() => setActiveTab('tab3')}>Вкладка 3</button>
      </div>
      
      {/* Контент С анимацией */}
      <AnimatedModalContent contentKey={activeTab}>
        {tabs[activeTab]}
      </AnimatedModalContent>
    </BaseModal>
  );
}
```

**Важно:** 
- `contentKey={activeTab}` - ключ должен изменяться при смене вкладки
- Навигация вкладок находится ВНЕ `AnimatedModalContent` (не анимируется)

---

### Пример 2: Модальное окно с шагами (Steps)

**Сценарий:** Модальное окно с несколькими шагами (например, Tutorial или Wizard).

**Решение:** Используйте `AnimatedModalContent` с `contentKey={currentStep}`

```javascript
import { BaseModal } from '../ui/BaseModal';
import { AnimatedModalContent } from '../ui/AnimatedModalContent';

export function TutorialModal({ isOpen, onClose }) {
  const [currentStep, setCurrentStep] = useState(0);
  
  const steps = [
    { title: 'Шаг 1', content: <div>Контент шага 1</div> },
    { title: 'Шаг 2', content: <div>Контент шага 2 ... много контента ...</div> },
    { title: 'Шаг 3', content: <div>Контент шага 3</div> },
  ];
  
  const currentStepData = steps[currentStep];
  
  return (
    <BaseModal
      isOpen={isOpen}
      onClose={onClose}
      title={currentStepData.title}
      size="large"
    >
      {/* Индикатор шагов - БЕЗ анимации */}
      <div className="flex gap-2 mb-4">
        {steps.map((_, index) => (
          <div
            key={index}
            className={`flex-1 h-2 rounded-full ${
              index === currentStep ? 'bg-blue-500' : 'bg-gray-300'
            }`}
          />
        ))}
      </div>
      
      {/* Контент С анимацией */}
      <AnimatedModalContent contentKey={currentStep}>
        {currentStepData.content}
      </AnimatedModalContent>
      
      {/* Навигация - БЕЗ анимации */}
      <div className="flex justify-between mt-4">
        <button onClick={() => setCurrentStep(currentStep - 1)}>Назад</button>
        <button onClick={() => setCurrentStep(currentStep + 1)}>Далее</button>
      </div>
    </BaseModal>
  );
}
```

**Важно:**
- `contentKey={currentStep}` - ключ должен изменяться при смене шага
- Индикатор шагов и навигация находятся ВНЕ `AnimatedModalContent` (не анимируются)

---

### Пример 3: Условный рендеринг контента

**Сценарий:** Модальное окно с условным рендерингом контента в зависимости от состояния.

**Решение:** Используйте `AnimatedModalContent` с `contentKey={state}`

```javascript
import { BaseModal } from '../ui/BaseModal';
import { AnimatedModalContent } from '../ui/AnimatedModalContent';

export function ConditionalModal({ isOpen, onClose, hasData }) {
  return (
    <BaseModal
      isOpen={isOpen}
      onClose={onClose}
      title="Условное модальное окно"
    >
      {/* Контент С анимацией */}
      <AnimatedModalContent contentKey={hasData ? 'with-data' : 'without-data'}>
        {hasData ? (
          <div>
            <h3>Данные загружены</h3>
            <p>Много контента с данными...</p>
          </div>
        ) : (
          <div>
            <h3>Нет данных</h3>
            <p>Мало контента</p>
          </div>
        )}
      </AnimatedModalContent>
    </BaseModal>
  );
}
```

**Важно:**
- `contentKey` должен быть уникальным для каждого варианта контента
- Используйте строковые ключи для лучшей читаемости

---

## ⚙️ Настройка параметров анимации

### Изменение длительности анимации

По умолчанию:
- `fadeOutDuration = 250ms` - длительность затухания
- `heightDuration = 350ms` - длительность изменения высоты

**Пример с кастомными параметрами:**

```javascript
<AnimatedModalContent 
  contentKey={currentStep}
  fadeOutDuration={200}  // Быстрее fade-out
  heightDuration={400}   // Медленнее изменение высоты
>
  {currentStepData.content}
</AnimatedModalContent>
```

### Отключение анимации (для тестирования)

```javascript
<AnimatedModalContent 
  contentKey={currentStep}
  fadeOutDuration={0}   // Без fade-out
  heightDuration={0}    // Без анимации высоты
>
  {currentStepData.content}
</AnimatedModalContent>
```

---

## 🎨 Визуальная структура

### Правильная структура модального окна:

```
<BaseModal>
  {/* Статичные элементы - БЕЗ анимации */}
  <Header />              // Заголовок, кнопки
  <Navigation />          // Навигация вкладок/шагов
  <Indicators />          // Индикаторы прогресса
  
  {/* Динамический контент - С анимацией */}
  <AnimatedModalContent contentKey={key}>
    {dynamicContent}
  </AnimatedModalContent>
  
  {/* Статичные элементы - БЕЗ анимации */}
  <Footer />              // Футер, кнопки действий
</BaseModal>
```

### ❌ Неправильная структура:

```javascript
// ❌ НЕПРАВИЛЬНО: Вся структура внутри AnimatedModalContent
<AnimatedModalContent contentKey={key}>
  <Header />              // Будет анимироваться (не нужно)
  <Navigation />          // Будет анимироваться (не нужно)
  {dynamicContent}
  <Footer />              // Будет анимироваться (не нужно)
</AnimatedModalContent>
```

---

## 🔧 Технические детали

### Как работает анимация:

1. **Фиксация текущей высоты** (мгновенно)
   - Измеряется текущая высота через `getBoundingClientRect()`
   - Устанавливается фиксированная высота в пикселях

2. **Fade-out** (250ms по умолчанию)
   - Контент плавно затухает (opacity: 1 → 0)
   - `overflow: hidden` включается для предотвращения прокрутки

3. **Смена контента** (мгновенно, пока opacity: 0)
   - Новый контент рендерится при opacity: 0
   - Пользователь не видит смену контента

4. **Измерение новой высоты** (через requestAnimationFrame)
   - Временно устанавливается `height: auto`
   - Измеряется новая высота
   - Возвращается старая высота для начала анимации

5. **Анимация высоты** (350ms по умолчанию)
   - Высота плавно анимируется от старой к новой
   - Используется `cubic-bezier(0.4, 0, 0.2, 1)` для плавности

6. **Fade-in** (50ms задержка после начала анимации высоты)
   - Контент плавно появляется (opacity: 0 → 1)

7. **Возврат к auto** (после завершения анимации высоты)
   - Высота возвращается к `auto`
   - `overflow: visible` возвращается

### Порядок выполнения:

```
0ms:    Фиксация текущей высоты
0ms:    Начало fade-out (opacity: 1 → 0)
250ms:  Смена контента (пока opacity: 0)
250ms:  Измерение новой высоты (requestAnimationFrame)
250ms:  Анимация высоты (height: old → new, 350ms)
300ms:  Начало fade-in (opacity: 0 → 1)
600ms:  Возврат height: auto + overflow: visible
```

---

## ⚠️ Важные замечания

### 1. Обязательный параметр `contentKey`

**❌ НЕПРАВИЛЬНО:**
```javascript
<AnimatedModalContent>
  {content}
</AnimatedModalContent>
```

**✅ ПРАВИЛЬНО:**
```javascript
<AnimatedModalContent contentKey={currentStep}>
  {content}
</AnimatedModalContent>
```

**Почему:** `contentKey` необходим для отслеживания изменений контента. Без него анимация не будет работать.

### 2. Уникальность ключа

**❌ НЕПРАВИЛЬНО:**
```javascript
// Ключ не меняется при смене контента
<AnimatedModalContent contentKey="static">
  {dynamicContent}
</AnimatedModalContent>
```

**✅ ПРАВИЛЬНО:**
```javascript
// Ключ меняется при смене контента
<AnimatedModalContent contentKey={activeTab}>
  {tabs[activeTab]}
</AnimatedModalContent>
```

### 3. Размещение элементов

**❌ НЕПРАВИЛЬНО:**
```javascript
// Статичные элементы внутри AnimatedModalContent
<AnimatedModalContent contentKey={key}>
  <Header />              // Не должно быть здесь
  {dynamicContent}
  <Footer />              // Не должно быть здесь
</AnimatedModalContent>
```

**✅ ПРАВИЛЬНО:**
```javascript
// Только динамический контент внутри AnimatedModalContent
<Header />                // Вне AnimatedModalContent
<AnimatedModalContent contentKey={key}>
  {dynamicContent}        // Только динамический контент
</AnimatedModalContent>
<Footer />                // Вне AnimatedModalContent
```

### 4. Производительность

- Не используйте `AnimatedModalContent` для статичного контента
- Не оборачивайте весь контент модального окна в `AnimatedModalContent`
- Используйте только для динамического контента, который меняет размер

---

## 🧪 Тестирование

### Чеклист для тестирования:

1. **Базовая функциональность:**
   - [ ] Анимация работает при изменении контента
   - [ ] Fade-out плавный (250ms)
   - [ ] Изменение высоты плавное (350ms)
   - [ ] Fade-in плавный (50ms задержка)

2. **Работа в обе стороны:**
   - [ ] Анимация работает при увеличении размера
   - [ ] Анимация работает при уменьшении размера
   - [ ] Нет резких рывков или скачков

3. **Быстрое переключение:**
   - [ ] Анимация корректно обрабатывает быстрое переключение
   - [ ] Нет конфликтов между анимациями
   - [ ] Нет зависаний или артефактов

4. **Разные браузеры:**
   - [ ] Chrome - работает корректно
   - [ ] Firefox - работает корректно
   - [ ] Safari - работает корректно (нет мерцания)
   - [ ] Edge - работает корректно

5. **Разные размеры контента:**
   - [ ] Маленький → Большой - плавная анимация
   - [ ] Большой → Маленький - плавная анимация
   - [ ] Одинаковый размер - нет лишних анимаций

---

## 📚 Дополнительные ресурсы

### Связанные файлы:

- `src/components/ui/BaseModal.jsx` - Базовый компонент модального окна
- `src/components/ui/AnimatedModalContent.jsx` - Компонент анимации контента
- `src/components/modals/TutorialModal.jsx` - Пример использования
- `time-tracker/code_anim.md` - Полный код анимации
- `time-tracker/problems/MODAL_TABS_ANIMATION_ISSUE.md` - История решения проблемы

### Технологии:

- **React 18.3.1** - React hooks (useState, useEffect, useRef)
- **ResizeObserver API** - Для отслеживания изменений размера (в BaseModal)
- **requestAnimationFrame** - Для синхронизации с браузером
- **CSS Transitions** - Для плавных переходов

---

## 🎯 Итоговая рекомендация

**При создании нового модального окна:**

1. ✅ Используйте `BaseModal` - автоматически получаете анимацию открытия/закрытия и window resize
2. ⚠️ Если контент динамический и меняет размер - используйте `AnimatedModalContent`
3. ✅ Передавайте уникальный `contentKey` для отслеживания изменений
4. ✅ Размещайте только динамический контент внутри `AnimatedModalContent`
5. ✅ Тестируйте в разных браузерах и при разных размерах контента

---

**Версия документа:** 1.0  
**Дата создания:** 2024-12-19  
**Последнее обновление:** 2024-12-19

