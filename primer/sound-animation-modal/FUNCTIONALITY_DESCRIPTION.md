# Описание функционала окна "Звуки и анимация"

## Общая информация

**Название окна:** "Звуки и анимация" (Sound and Animation Settings Modal)

**Тип компонента:** Модальное окно настроек с табами

**Стиль:** Glassmorphism (стеклянный эффект с размытием фона)

**Структура:** Модальное окно с тремя вкладками (табами) и футером с кнопками управления

---

## Структура окна

### Заголовок
- **Название:** "Звуки и анимация"
- **Иконка:** Volume2 (из lucide-react)
- **Размер:** large (max-w-2xl)

### Футер
- **Кнопка "Отмена":** Закрывает окно без сохранения изменений
- **Кнопка "Сохранить":** Сохраняет все изменения и закрывает окно

---

## Вкладки (Tabs)

Окно содержит **3 вкладки**:

1. **"Уведомления"** (Notifications) - иконка Bell
2. **"Pomodoro"** - иконка Timer
3. **"Фавикон"** (Favicon) - иконка Palette

**Поведение табов:**
- Активный таб выделяется синим фоном (bg-blue-500) и белым текстом
- Неактивные табы имеют серый текст и становятся белыми при наведении
- Переключение между табами происходит по клику
- Контент табов анимируется при переключении (AnimatedModalContent)

---

## ВКЛАДКА 1: "Уведомления" (Notifications)

### 1.1. Звуковые уведомления

**Переключатель (Toggle Switch):**
- **Название:** "Звуковые уведомления"
- **Состояние:** `soundNotificationsEnabled` (boolean)
- **По умолчанию:** true
- **Визуал:** Toggle switch (w-11 h-6) с анимацией переключения

**Когда включено, отображаются настройки:**

#### 1.1.1. Интервал уведомлений
- **Тип:** Dropdown select + опциональное текстовое поле
- **Параметр:** `notificationInterval` (number, минуты)
- **Варианты:**
  - 15 минут
  - 30 минут (по умолчанию)
  - 45 минут
  - 60 минут (1 час)
  - 120 минут (2 часа)
  - Кастомное значение (от 1 до 1440 минут)
- **Кастомный интервал:**
  - При выборе "Кастомное" появляется input[type="number"]
  - Минимум: 1 минута
  - Максимум: 1440 минут (24 часа)
  - Валидация при потере фокуса (onBlur)

#### 1.1.2. Тип звука
- **Тип:** Grid из кнопок (grid-cols-3)
- **Параметр:** `notificationSound` (string)
- **Доступные звуки:**
  1. **chime** - "Мелодия" (Мелодичное уведомление)
  2. **alert** - "Предупреждение" (Короткое предупреждение)
  3. **phone** - "Звонок" (Звук телефонного звонка)
  4. **doorbell** - "Дверной звонок" (Многотональный звонок)
  5. **alarm** - "Тревога" (Повторяющийся сигнал)
  6. **notification** - "Уведомление" (Стандартное уведомление)
  7. **bell** - "Колокол" (Колокольный звон)
  8. **beep** - "Сигнал" (Короткий сигнал)
  9. **ping** - "Пинг" (Мягкий короткий сигнал)
- **Визуал:** Каждая кнопка содержит название звука и иконку Play для прослушивания
- **Выбранный звук:** Выделяется синей рамкой и фоном (border-blue-500 bg-blue-500/10)
- **Функция прослушивания:** При клике на иконку Play воспроизводится звук через `playSound(sound.value)`

### 1.2. Напоминания о перерывах

**Переключатель (Toggle Switch):**
- **Название:** "Напоминания о перерывах"
- **Состояние:** `breakRemindersEnabled` (boolean)
- **По умолчанию:** true

**Когда включено, отображаются настройки:**

#### 1.2.1. Интервал напоминаний
- **Тип:** Dropdown select
- **Параметр:** `breakReminderInterval` (number, часы)
- **Варианты:**
  - Каждый час (1)
  - Каждые 2 часа (2)
  - Каждые 3 часа (3)
  - Каждые 4 часа (4)
  - Каждые 6 часов (6)
- **По умолчанию:** 2 часа
- **Описание:** "Напоминание появится после непрерывной работы указанное количество часов"

#### 1.2.2. Кнопка "Пример"
- **Функция:** Показывает тестовое уведомление
- **Текст уведомления:** "⏸️ Пора сделать перерыв! Вы работаете уже [X] [час/часа/часов]. Рекомендуется сделать перерыв для поддержания продуктивности."
- **Тип уведомления:** warning
- **Длительность:** 10 секунд
- **Отображение:** Через React Portal поверх модального окна (z-index: 99999999)

### 1.3. Предупреждения о переработке

**Переключатель (Toggle Switch):**
- **Название:** "Предупреждения о переработке"
- **Состояние:** `overtimeAlertsEnabled` (boolean)
- **По умолчанию:** true

**Когда включено, отображаются настройки:**

#### 1.3.1. Порог предупреждения
- **Тип:** Range slider (input[type="range"])
- **Параметр:** `overtimeWarningThreshold` (number, множитель от дневной нормы)
- **Диапазон:** 0.5 - 2.0
- **Шаг:** 0.1
- **По умолчанию:** 1.0
- **Визуал:** 
  - Slider с синим градиентом (bg-blue-500)
  - Отображается текущее значение в часах: `(dailyHours * overtimeWarningThreshold).toFixed(1) ч`
  - Подсказки: 50%, 100%, 200%
- **Описание:** "Предупреждение появится при превышении [X] часов работы за день"

#### 1.3.2. Порог критического предупреждения
- **Тип:** Range slider (input[type="range"])
- **Параметр:** `overtimeCriticalThreshold` (number, множитель от дневной нормы)
- **Диапазон:** 1.0 - 3.0
- **Шаг:** 0.1
- **По умолчанию:** 1.5
- **Визуал:**
  - Slider с красным градиентом (bg-red-500)
  - Отображается текущее значение в часах: `(dailyHours * overtimeCriticalThreshold).toFixed(1) ч`
  - Подсказки: 100%, 200%, 300%
- **Описание:** "Критическое предупреждение появится при превышении [X] часов работы за день"

#### 1.3.3. Звуковое уведомление
- **Тип:** Toggle Switch
- **Параметр:** `overtimeSoundAlert` (boolean)
- **По умолчанию:** true
- **Описание:** "Воспроизводить звук при предупреждении о переработке"

#### 1.3.4. Кнопки тестирования
- **Кнопка "Пример предупреждения":**
  - Цвет: желтый (bg-yellow-500)
  - Функция: Показывает тестовое предупреждение (тип: warning)
  - Текст: "⚠️ Переработка! Вы работаете уже [X] [час/часа/часов] (норма: [dailyHours] ч). Превышение: [X] [час/часа/часов]. Рекомендуется сделать перерыв."
  - Длительность: 12 секунд

- **Кнопка "Пример критического":**
  - Цвет: красный (bg-red-500)
  - Функция: Показывает тестовое критическое предупреждение (тип: error)
  - Текст: "🚨 Критическая переработка! Вы работаете уже [X] [час/часа/часов] (норма: [dailyHours] ч). Превышение: [X] [час/часа/часов]. Рекомендуется сделать перерыв и отдохнуть."
  - Длительность: 15 секунд

---

## ВКЛАДКА 2: "Pomodoro"

### 2.1. Pomodoro таймер

**Переключатель (Toggle Switch):**
- **Название:** "Pomodoro таймер"
- **Состояние:** `pomodoroEnabled` (boolean)
- **По умолчанию:** false

**Описание:** "Техника управления временем: работа разбивается на интервалы по 25 минут (помодоро), разделенные перерывами."

**Когда включено, отображаются настройки:**

#### 2.1.1. Автоматически запускать перерывы
- **Тип:** Toggle Switch
- **Параметр:** `pomodoroAutoStartBreaks` (boolean)
- **По умолчанию:** true
- **Описание:** "После завершения работы автоматически запускается перерыв"

#### 2.1.2. Автоматически запускать работу
- **Тип:** Toggle Switch
- **Параметр:** `pomodoroAutoStartWork` (boolean)
- **По умолчанию:** false
- **Описание:** "После завершения перерыва автоматически запускается работа"

#### 2.1.3. Звук при завершении интервала
- **Тип:** Toggle Switch
- **Параметр:** `pomodoroSoundOnComplete` (boolean)
- **По умолчанию:** true
- **Описание:** "Воспроизводить звук при завершении работы или перерыва"

#### 2.1.4. Показывать уведомления
- **Тип:** Toggle Switch
- **Параметр:** `pomodoroShowNotifications` (boolean)
- **По умолчанию:** true
- **Описание:** "Показывать визуальные уведомления при завершении интервалов"

#### 2.1.5. Кнопка "Пример"
- **Функция:** Показывает тестовое уведомление Pomodoro
- **Текст уведомления:** "🍅 Pomodoro завершен! Пора сделать перерыв."
- **Тип уведомления:** success
- **Длительность:** 10 секунд
- **Отображение:** Через React Portal поверх модального окна

---

## ВКЛАДКА 3: "Фавикон" (Favicon)

### 3.1. Анимация фавикона

**Переключатель (Toggle Switch):**
- **Название:** "Анимация фавикона"
- **Состояние:** `faviconAnimationEnabled` (boolean)
- **По умолчанию:** true

**Когда включено, отображаются настройки:**

#### 3.1.1. Стиль анимации
- **Тип:** Grid из карточек предпросмотра (grid-cols-4)
- **Параметр:** `faviconAnimationStyle` (string)
- **По умолчанию:** 'pulse'
- **Доступные стили:**
  1. **pulse** - "Пульсация" (Плавное увеличение и уменьшение)
  2. **blink** - "Мигание" (Быстрое мигание)
  3. **rotate** - "Вращение" (Вращение с индикатором)
  4. **breathe** - "Дыхание" (Плавное дыхание)
  5. **wave** - "Волна" (Расходящиеся волны)
  6. **gradient** - "Градиент" (Изменение цвета)
  7. **data-pulse** - "Data Pulse" (Пульсация данных и времени)
  8. **particles** - "Частицы" (Вращающиеся частицы)
- **Визуал карточек:**
  - Canvas 32x32 пикселя с предпросмотром анимации
  - Анимация запускается при наведении (onMouseEnter)
  - Выбранный стиль выделяется синей рамкой (border-blue-500 bg-blue-500/10)
  - Под карточкой отображается название стиля (text-[10px])

**Детали анимаций:**
- **pulse:** Быстрое увеличение и уменьшение радиуса (8-16px) с внешним кольцом
- **blink:** Мигание с изменением прозрачности (1 ↔ 0.2)
- **rotate:** Вращение круга с белым треугольником-индикатором
- **breathe:** Медленное плавное дыхание с изменением радиуса (10-16px) и прозрачности
- **wave:** Концентрические кольца, расходящиеся от центра (5 волн)
- **gradient:** Радиальный градиент с изменением оттенка (HSL)
- **data-pulse:** Сложная анимация с концентрическими кругами, волной данных, цветными точками и часами со стрелками
- **particles:** Круг с 6 вращающимися частицами вокруг него
- **morph:** Морфинг формы: квадрат → круг → треугольник → квадрат

#### 3.1.2. Цвет
- **Тип:** Grid из цветных кнопок (grid-cols-8) + color picker
- **Параметр:** `faviconAnimationColor` (string, hex цвет)
- **По умолчанию:** '#3b82f6' (синий)
- **Предустановленные цвета:**
  1. #3b82f6 - Синий (bg-blue-500)
  2. #22c55e - Зеленый (bg-green-500)
  3. #f97316 - Оранжевый (bg-orange-500)
  4. #ef4444 - Красный (bg-red-500)
  5. #8b5cf6 - Фиолетовый (bg-purple-500)
  6. #06b6d4 - Голубой (bg-cyan-500)
  7. #fbbf24 - Желтый (bg-yellow-500)
- **Кастомный цвет:**
  - Последняя кнопка - градиентная (from-blue-500 via-purple-500 to-pink-500)
  - При клике открывается нативный color picker (input[type="color"])
  - Выбранный цвет выделяется синей рамкой и кольцом (ring-2 ring-blue-500/50)

#### 3.1.3. Скорость
- **Тип:** Grid из кнопок (grid-cols-3)
- **Параметр:** `faviconAnimationSpeed` (string)
- **По умолчанию:** 'normal'
- **Варианты:**
  1. **slow** - "Медленно (4000ms)" - 4000ms на цикл
  2. **normal** - "Обычно (2000ms)" - 2000ms на цикл
  3. **fast** - "Быстро (1000ms)" - 1000ms на цикл
- **Визуал:** Кнопки с текстом (только первое слово: "Медленно", "Обычно", "Быстро")
- **Выбранная скорость:** Выделяется синей рамкой и фоном

---

## Состояние и управление данными

### Инициализация состояния
При открытии модального окна (`isOpen === true`):
- Все состояния инициализируются из текущих настроек через `getInitialState()`
- Если настройка отсутствует, используется значение по умолчанию
- Для кастомного интервала: если значение не входит в стандартные (15, 30, 45, 60, 120), оно помечается как кастомное

### Сохранение настроек
При нажатии кнопки "Сохранить":
1. Вычисляется финальный интервал уведомлений (если выбран кастомный, используется `customIntervalMinutes`)
2. Вызывается `updateSettings()` с объектом:
   ```javascript
   {
     notifications: {
       soundNotificationsEnabled,
       notificationInterval: finalInterval,
       notificationSound,
       faviconAnimationEnabled,
       faviconAnimationStyle,
       faviconAnimationColor,
       faviconAnimationSpeed,
       breakRemindersEnabled,
       breakReminderInterval,
       overtimeAlertsEnabled,
       overtimeWarningThreshold,
       overtimeCriticalThreshold,
       overtimeSoundAlert,
     },
     pomodoro: {
       enabled: pomodoroEnabled,
       autoStartBreaks: pomodoroAutoStartBreaks,
       autoStartWork: pomodoroAutoStartWork,
       soundOnComplete: pomodoroSoundOnComplete,
       showNotifications: pomodoroShowNotifications,
     }
   }
   ```
3. Показывается уведомление об успехе: "Настройки звуков и анимации сохранены"
4. Окно закрывается (`onClose()`)

### Отмена изменений
При нажатии кнопки "Отмена":
- Все изменения отбрасываются
- Окно закрывается без сохранения

---

## Визуальные компоненты

### Toggle Switch
- **Размер:** w-11 h-6 (44px × 24px)
- **Состояния:**
  - Включено: bg-blue-500, переключатель справа (translate-x-5)
  - Выключено: bg-gray-300 dark:bg-gray-600, переключатель слева (translate-x-0)
- **Анимация:** transition-transform, transition-colors
- **Переключатель:** w-5 h-5, bg-white, rounded-full

### Glass Card
- **Класс:** glass-card
- **Стиль:** Glassmorphism с backdrop-filter: blur(24px)
- **Фон:** rgba(17, 24, 39, 0.7)
- **Рамка:** 1px solid rgba(255, 255, 255, 0.1)
- **Тень:** 0 8px 32px rgba(0, 0, 0, 0.4)
- **Скругление:** rounded-xl
- **Отступы:** p-4

### Кнопки
- **Основные кнопки (Сохранить):** bg-blue-500 hover:bg-blue-600, text-white, rounded-lg, px-4 py-2
- **Вторичные кнопки (Отмена):** border border-gray-700, text-gray-300, hover:bg-gray-800
- **Кнопки тестирования:** Различные цвета в зависимости от типа (blue, yellow, red)
- **Кнопки выбора:** border-2, hover:scale-105, transition-all

### Sliders
- **Стиль:** Кастомный range input
- **Градиент:** Зависит от типа (синий для предупреждения, красный для критического)
- **Высота:** h-2
- **Скругление:** rounded-lg
- **Подсказки:** Текстовые метки под слайдером (50%, 100%, 200% и т.д.)

### Dropdown Select
- **Стиль:** Кастомный select с SVG стрелкой
- **Фон:** bg-gray-800 dark:bg-gray-700
- **Рамка:** border border-gray-700 dark:border-gray-600
- **Стрелка:** SVG через background-image (data URI)
- **Фокус:** focus:ring-2 focus:ring-blue-500

---

## Анимации и переходы

### Переключение табов
- **Анимация контента:** AnimatedModalContent с key={`${activeTab}-${...}`}
- **Переходы:** transition-all для кнопок табов

### Тестовые уведомления
- **Отображение:** React Portal в document.body
- **Позиция:** fixed top-4 right-4
- **Z-index:** 99999999 (поверх всего)
- **Автоскрытие:** Через setTimeout (10-15 секунд в зависимости от типа)

### Предпросмотр анимаций фавикона
- **Запуск:** При наведении на карточку (onMouseEnter)
- **Остановка:** При уходе курсора (onMouseLeave)
- **Технология:** Canvas API с requestAnimationFrame
- **Размер canvas:** 32×32 пикселя

---

## Зависимости и интеграции

### Хуки и сторы
- `useNotificationsSettings()` - получение настроек уведомлений
- `useDailyHours()` - получение дневной нормы часов
- `usePomodoroSettings()` - получение настроек Pomodoro
- `useUpdateSettings()` - обновление настроек
- `useSoundManager()` - управление звуками (playSound)
- `useUIStore()` - показ уведомлений об успехе (showSuccess)
- `useNotifications()` - показ уведомлений (showWarning, showError)

### Компоненты
- `BaseModal` - базовое модальное окно
- `AnimatedModalContent` - анимированный контент
- `Notification` - компонент уведомления
- `FaviconPreviewCard` - карточка предпросмотра анимации фавикона

### Иконки (lucide-react)
- Volume2 - иконка заголовка
- Bell - иконка таба "Уведомления"
- Timer - иконка таба "Pomodoro"
- Palette - иконка таба "Фавикон"
- Play - иконка прослушивания звука

---

## Технические детали

### Валидация
- **Кастомный интервал:** Проверка на диапазон 1-1440 минут
- **При потере фокуса:** Автоматическая коррекция некорректных значений
- **Пустое значение:** Разрешается для ввода, но при сохранении используется значение по умолчанию (30)

### Производительность
- **Атомарные селекторы:** Используются для минимизации ре-рендеров
- **Ленивая инициализация:** Состояния инициализируются через функции
- **Очистка:** useEffect с cleanup для таймеров уведомлений

### Доступность
- **Клавиатурная навигация:** Поддержка Enter/Space для кнопок
- **ARIA labels:** Для кнопок прослушивания звука
- **Семантическая разметка:** Использование правильных HTML элементов

---

## Примеры использования настроек

### Звуковые уведомления
- При достижении заданного интервала воспроизводится выбранный звук
- Звук генерируется через Tone.js синтезатор
- Громкость регулируется через общие настройки приложения

### Напоминания о перерывах
- После непрерывной работы указанное количество часов показывается уведомление
- Уведомление имеет тип "warning" и содержит рекомендацию сделать перерыв

### Предупреждения о переработке
- При превышении порога предупреждения показывается уведомление типа "warning"
- При превышении критического порога показывается уведомление типа "error"
- Если включено звуковое уведомление, дополнительно воспроизводится звук

### Pomodoro
- При включении Pomodoro таймера применяются настройки автозапуска
- При завершении интервала (если включено) воспроизводится звук и показывается уведомление

### Анимация фавикона
- Когда таймер активен и анимация включена, фавикон в браузере анимируется
- Анимация использует Canvas API для отрисовки на canvas 32×32
- Стиль, цвет и скорость анимации определяются настройками
- Анимация останавливается, когда вкладка неактивна (для экономии ресурсов)

---

## Заключение

Окно "Звуки и анимация" представляет собой комплексный интерфейс для настройки всех звуковых и визуальных уведомлений приложения. Оно включает:

- **3 основные категории настроек** (Уведомления, Pomodoro, Фавикон)
- **15+ различных параметров** для тонкой настройки поведения
- **9 типов звуков** для уведомлений
- **8 стилей анимации** фавикона
- **7 предустановленных цветов** + кастомный цвет
- **3 скорости анимации**
- **Интерактивные предпросмотры** для звуков и анимаций
- **Тестовые функции** для проверки настроек перед сохранением

Все настройки сохраняются в централизованном хранилище (Zustand store) и применяются глобально во всем приложении.

