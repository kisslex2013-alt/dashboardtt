# 🚀 АКТУАЛЬНАЯ инструкция: MCP в Cursor (новая версия)

**Версия Cursor:** Обновленная (с разделом "Tools & MCP")  
**Время проверки:** 5 минут

---

## ✅ ХОРОШИЕ НОВОСТИ: MCP УЖЕ АКТИВЕН!

По вашим скриншотам видно, что:

1. ✅ **Раздел "Tools & MCP" существует** - значит MCP уже установлен
2. ✅ **Серверы видны в списке** - они уже подключены
3. ✅ **Переключатели включены (зеленые)** - серверы активны

---

## 📍 Где находится конфигурация MCP?

### Правильное расположение:

**Глобальная конфигурация (для всех проектов):**

```
C:\Users\kissl\.cursor\mcp.json
```

**Или может быть:**

```
C:\Users\kissl\AppData\Roaming\Cursor\User\settings\mcp.json
```

**Локальная конфигурация (только для проекта):**

```
H:\Backup\Zero-Coding\Cursor AI\TTM_module\time-tracker\mcp.json
```

---

## 🎯 Что нужно проверить

### 1. Проверьте статус серверов в "Tools & MCP"

Откройте: `Ctrl + ,` → раздел **"Tools & MCP"**

**Что должно быть:**

- ✅ `docs-search` - зеленый индикатор или "Error - Show Output" (если ошибка)
- ✅ `sequential-thinking` - зеленый индикатор, "1 tools enabled"
- ✅ `memory` - зеленый индикатор, "9 tools enabled"
- ✅ Другие серверы по желанию

**Если видите красный индикатор "Error":**

1. Нажмите "Show Output"
2. Посмотрите на ошибку
3. Возможно нужно обновить конфигурацию

---

### 2. Если `docs-search` показывает ошибку

**Возможные причины:**

1. Не установлен `@modelcontextprotocol/server-docs`
2. Проблема с npx
3. Неправильная конфигурация в `mcp.json`

**Решение:**

1. Откройте `c:\Users\kissl\.cursor\mcp.json`
2. Проверьте секцию `docs-search`
3. Убедитесь что там:

```json
{
  "mcpServers": {
    "docs-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-docs"],
      "env": {
        "DOCS_URLS": "https://react.dev,https://tailwindcss.com,https://vitejs.dev,https://recharts.org,https://lucide.dev,https://tonejs.github.io,https://developer.mozilla.org/en-US/docs/Web/JavaScript"
      }
    }
  }
}
```

---

### 3. Нужен ли файл `mcp.json` в проекте?

**Короткий ответ: НЕ ОБЯЗАТЕЛЬНО, если глобальный файл работает.**

**Когда нужен локальный файл:**

- Если хотите разные настройки для разных проектов
- Если хотите специфичные пути (например, filesystem указывает на конкретный проект)

**Когда НЕ нужен:**

- Если глобальный `mcp.json` в `c:\Users\kissl\.cursor\` работает для всех проектов
- Если настройки одинаковые для всех проектов

**Рекомендация:**

- Используйте глобальный файл в `c:\Users\kissl\.cursor\mcp.json`
- Файл в проекте (`time-tracker/mcp.json`) оставьте как резервную копию или документацию

---

## 🧪 Как проверить что MCP работает?

### Тест 1: Docs Search

В Cursor Chat (`Ctrl + L`):

```
@docs-search как использовать useState в React?
```

**Если работает:** AI найдет информацию из React.dev  
**Если не работает:** Попробуйте обновить конфигурацию или перезапустить Cursor

### Тест 2: Проверка серверов

В разделе "Tools & MCP":

- Все нужные серверы должны иметь **зеленый индикатор**
- Статус должен быть "X tools enabled" (не "Error")

---

## 🔧 Что делать если не работает?

### Вариант 1: Обновить глобальный mcp.json

1. Откройте `c:\Users\kissl\.cursor\mcp.json`
2. Сравните с файлом из проекта: `time-tracker/mcp.json`
3. Если есть отличия - обновите глобальный файл
4. Перезапустите Cursor

### Вариант 2: Использовать локальный файл

1. В разделе "Tools & MCP" нажмите "New MCP Server"
2. Выберите "Add Configuration File"
3. Укажите путь к `time-tracker/mcp.json`
4. Перезапустите Cursor

### Вариант 3: Проверить ошибки

1. В "Tools & MCP" найдите сервер с ошибкой (красный индикатор)
2. Нажмите "Show Output"
3. Прочитайте ошибку и исправьте проблему

---

## ✅ ИТОГО

**Ваша ситуация:**

- ✅ MCP уже активен (раздел "Tools & MCP" существует)
- ✅ Серверы подключены и видны
- ✅ `mcp.json` в `c:\Users\kissl\.cursor\` - это правильное место для глобальной конфигурации
- ⚠️ Нужно проверить почему `docs-search` показывает ошибку (если показывает)

**Что делать:**

1. Проверьте статус `docs-search` в "Tools & MCP"
2. Если ошибка - нажмите "Show Output" и посмотрите что не так
3. Протестируйте: `@docs-search как использовать useState в React?`
4. Если работает - все готово! 🎉

---

## 📚 Дополнительная информация

Полная инструкция: `time-tracker/SETUP-GUIDE.md`  
Статус всех систем: `time-tracker/SYSTEM_STATUS_REPORT.md`
