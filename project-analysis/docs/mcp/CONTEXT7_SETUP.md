# 🔧 Настройка Context7 MCP сервера в Cursor

## ✅ Правильный формат установки

Согласно [официальной документации Context7](https://github.com/upstash/context7), Context7 использует **npm пакет** `@upstash/context7-mcp`, а не HTTP URL.

**Правильный формат конфигурации:**

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp", "--api-key", "YOUR_API_KEY"],
      "env": {}
    }
  }
}
```

**Или через переменную окружения:**

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "env": {
        "CONTEXT7_API_KEY": "YOUR_API_KEY"
      }
    }
  }
}
```

---

## ✅ Решение

### Вариант 1: Добавить через UI Cursor (РЕКОМЕНДУЕТСЯ)

1. Откройте настройки Cursor: `Ctrl + ,`
2. Перейдите в раздел **"Tools & MCP"**
3. Нажмите кнопку **"Add a Custom MCP Server"** (внизу списка)
4. Выберите **"Add Configuration"** или **"Add from JSON"**
5. Вставьте следующую конфигурацию:
   ```json
   {
     "command": "npx",
     "args": ["-y", "@upstash/context7-mcp", "--api-key", "ctx7sk-*****7ba2"],
     "env": {}
   }
   ```
6. Сохраните и перезапустите Cursor

---

### Вариант 2: Добавить в глобальную конфигурацию (РЕКОМЕНДУЕТСЯ)

**Шаг 1:** Найдите глобальный файл конфигурации MCP

**Windows:**

```
C:\Users\kissl\.cursor\mcp.json
```

**Или:**

```
C:\Users\kissl\AppData\Roaming\Cursor\User\settings\mcp.json
```

**Шаг 2:** Откройте файл и добавьте конфигурацию context7

**ПРАВИЛЬНЫЙ ФОРМАТ** (согласно официальной документации):

#### Вариант A: С API ключом через флаг (РЕКОМЕНДУЕТСЯ)

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp", "--api-key", "ctx7sk-*****7ba2"],
      "env": {}
    }
  }
}
```

#### Вариант B: С API ключом через переменную окружения

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "env": {
        "CONTEXT7_API_KEY": "ctx7sk-*****7ba2"
      }
    }
  }
}
```

**Примечание:** Замените `ctx7sk-*****7ba2` на ваш полный API ключ. Получить ключ можно на [context7.com/dashboard](https://context7.com/dashboard)

**Шаг 3:** Сохраните файл и перезапустите Cursor

---

### Вариант 3: Использование без API ключа (с ограничениями)

Context7 можно использовать без API ключа, но с ограничениями по rate limit:

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "env": {}
    }
  }
}
```

**Рекомендация:** Получите бесплатный API ключ на [context7.com/dashboard](https://context7.com/dashboard) для полного доступа.

---

## 🔍 Диагностика

### Проверка 1: Где находится глобальная конфигурация?

1. Откройте Cursor
2. Нажмите `Ctrl + Shift + P` (Command Palette)
3. Введите: `MCP: Open Configuration`
4. Это откроет правильный файл конфигурации

### Проверка 2: Проверьте формат существующих серверов

Посмотрите на формат других серверов в настройках:

- Все используют `command` + `args` (не `url`)
- Это означает, что Cursor может не поддерживать HTTP-based серверы напрямую

### Проверка 3: Логи ошибок

1. В настройках "Tools & MCP" найдите context7 (если он есть)
2. Если есть красный индикатор - нажмите "Show Output"
3. Прочитайте ошибку

---

## 📝 Рекомендации

1. **Сначала попробуйте Вариант 1** (через UI) - это самый простой способ
2. Если UI не поддерживает HTTP серверы - используйте **Вариант 3** (npm пакет)
3. Если ничего не помогает - проверьте документацию Context7 на наличие официального MCP сервера

---

## 🔗 Полезные ссылки

- [Cursor MCP Documentation](https://docs.cursor.com/context/mcp)
- [Model Context Protocol Specification](https://modelcontextprotocol.io)
- Context7 API: `context7.com/api/v1`

---

**Версия:** 1.0  
**Дата:** 2025-01-XX
