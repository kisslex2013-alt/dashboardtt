# Инструкция по установке Browser Tools MCP

## ✅ Шаг 1: Конфигурация MCP (уже сделано)

Конфигурация уже добавлена в `mcp.json`. Cursor автоматически установит пакет при первом использовании.

## 📦 Шаг 2: Установка расширения Chrome

### Вариант A: Установка из GitHub (рекомендуется)

1. Перейди на GitHub: https://github.com/AgentDeskAI/browser-tools-mcp
2. Найди раздел "Releases" или "Releases" в правой части страницы
3. Скачай последнюю версию расширения (обычно это `.zip` файл)
4. Распакуй архив в удобную папку

### Вариант B: Клонирование репозитория

Если у тебя установлен Git:

```bash
git clone https://github.com/AgentDeskAI/browser-tools-mcp.git
cd browser-tools-mcp
```

## 🔧 Шаг 3: Загрузка расширения в Chrome

1. Открой Chrome
2. Перейди в `chrome://extensions/` (вставь в адресную строку)
3. Включи **"Режим разработчика"** (Developer mode) - переключатель в правом верхнем углу
4. Нажми **"Загрузить распакованное расширение"** (Load unpacked)
5. Выбери папку с расширением (ту, которую распаковал или клонировал)
6. Расширение должно появиться в списке

## 🔌 Шаг 4: Подключение к MCP серверу

1. Открой любую веб-страницу в Chrome
2. Нажми `F12` или `Ctrl+Shift+I` чтобы открыть DevTools
3. Найди вкладку **"BrowserToolsMCP"** в DevTools
4. Проверь статус подключения - должно быть **"Connected to server"**

Если статус не "Connected":

- Убедись, что Cursor запущен
- Перезапусти Cursor
- Проверь, что расширение активно в `chrome://extensions/`

## 🚀 Шаг 5: Перезапуск Cursor

1. Закрой Cursor полностью
2. Запусти Cursor снова
3. MCP сервер `browser-tools` должен автоматически подключиться

## ✅ Проверка работы

После перезапуска Cursor, я смогу:

- Видеть ошибки в консоли браузера
- Анализировать сетевые запросы
- Делать скриншоты страниц
- Проверять производительность сайтов

## 🆘 Если что-то не работает

1. **Расширение не загружается:**
   - Проверь, что включен "Режим разработчика"
   - Убедись, что выбрана правильная папка с `manifest.json`

2. **Не подключается к серверу:**
   - Проверь, что Cursor запущен
   - Перезапусти Cursor
   - Проверь вкладку "BrowserToolsMCP" в DevTools

3. **MCP сервер не виден в Cursor:**
   - Проверь файл `mcp.json` - должна быть секция `browser-tools`
   - Перезапусти Cursor
   - Проверь Output в Cursor на наличие ошибок

## 📝 Примечания

- Расширение работает только с Chrome/Chromium браузерами
- Нужно держать DevTools открытыми для работы расширения
- MCP сервер автоматически установится при первом использовании через `npx`
