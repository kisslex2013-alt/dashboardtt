# Отчет о проблемах с анимацией чисел в Time Tracker Dashboard

**Дата создания:** 2024-12-19  
**Статус:** ❌ Проблемы не решены  
**Приоритет:** Высокий

---

## 📋 Описание проблем

### Проблема 1: AnimatedCounter — логика анимации работает некорректно

**Симптомы:**

- При выборе фильтра "ГОД" весь экран быстро мерцает
- При переходе к значению 0 (например, сегодня нет данных) анимация не работает и резко показывает 0
- Анимация пересчета чисел не работает корректно

**Ожидаемое поведение:**

- Плавная анимация при любых изменениях значений, включая переход к 0
- Отсутствие мерцания при смене фильтров
- Корректная работа анимации для всех числовых значений

### Проблема 2: StatisticsDashboard — мерцание при смене фильтра "ГОД"

**Симптомы:**

- При выборе фильтра "ГОД" весь экран быстро мерцает
- Связано с недавно внедренной анимацией смены чисел

**Ожидаемое поведение:**

- Плавная смена значений без мерцания
- Стабильная работа при любых фильтрах

### Проблема 3: PlanFactCompactView — анимация показывает фиксированные данные

**Симптомы:**

- При изменении данных (записи времени) анимация смены чисел работает
- Но показывает фиксированные данные, а не фактические, которые изменились

**Ожидаемое поведение:**

- Анимация должна показывать актуальные значения при изменении данных
- Корректное отслеживание изменений в `entries`

---

## 🔧 Примененные решения (не помогли)

### Решение 1: AnimatedCounter — первая попытка

**Что было сделано:**

1. Добавлен таймаут 10ms для батчинга изменений
2. Добавлены стабильные ключи для компонентов
3. Изменена логика обработки перехода к 0

**Изменения в коде:**

```javascript
// Добавлен таймаут для предотвращения мерцания
animationTimeoutRef.current = setTimeout(() => {
  // логика анимации
}, 10)

// Исправлена логика для перехода к 0
if (numericValue > 0) {
  api.start({
    from: { number: 0 },
    to: { number: numericValue },
    config: springConfig,
  })
} else {
  // Если значение 0, сразу устанавливаем без анимации
  api.start({
    number: 0,
    config: { duration: 0 },
  })
}
```

**Результат:** ❌ Не помогло

- Таймаут вызывал задержки и проблемы
- Логика для 0 не работала правильно

---

### Решение 2: AnimatedCounter — вторая попытка

**Что было сделано:**

1. Убран таймаут
2. Убрана зависимость от `spring.number` из `useEffect`
3. Исправлена логика отслеживания изменений через `previousValue.current`

**Изменения в коде:**

```javascript
const previousValue = useRef(null)
const isInitialMount = useRef(true)

useEffect(() => {
  // Используем предыдущее значение как отправную точку
  const fromValue =
    previousValue.current !== null && previousValue.current !== undefined
      ? previousValue.current
      : 0

  api.start({
    from: { number: fromValue },
    to: { number: numericValue },
    config: springConfig,
  })
  previousValue.current = numericValue
}, [numericValue, api, springConfig])
```

**Результат:** ❌ Не помогло

- Проблема с отслеживанием текущего значения spring
- Неправильная отправная точка для анимации

---

### Решение 3: AnimatedCounter — третья попытка (текущая)

**Что было сделано:**

1. Использование текущего значения `spring.number.get()` как отправной точки
2. Добавлена зависимость от `spring.number` в `useEffect`
3. Исправлена инициализация `previousValue` с текущим значением

**Изменения в коде:**

```javascript
const previousValue = useRef(numericValue)
const isInitialMount = useRef(true)

useEffect(() => {
  // Получаем текущее значение из spring (актуальное значение анимации)
  const currentSpringValue = spring.number.get()

  // При последующих изменениях используем ТЕКУЩЕЕ значение spring как отправную точку
  const fromValue =
    isNaN(currentSpringValue) || !isFinite(currentSpringValue)
      ? previousValue.current !== null && previousValue.current !== undefined
        ? previousValue.current
        : 0
      : currentSpringValue

  api.start({
    from: { number: fromValue },
    to: { number: numericValue },
    config: springConfig,
  })
  previousValue.current = numericValue
}, [numericValue, api, springConfig, spring.number])
```

**Результат:** ❌ Не помогло

- Проблемы все еще остаются
- Мерцание при смене фильтра не устранено

---

### Решение 4: StatisticsDashboard — первая попытка

**Что было сделано:**

1. Добавлены стабильные ключи для `AnimatedCounter` с учетом `periodFilter` и значения
2. Использование `stableStats` для предотвращения мерцания

**Изменения в коде:**

```javascript
<AnimatedCounter
  key={`${title}-${periodFilter}-${finalNumericValue}`}
  value={finalNumericValue}
  // ...
/>
```

**Результат:** ❌ Не помогло

- Ключи вызывали перемонтирование компонентов
- Мерцание усиливалось

---

### Решение 5: StatisticsDashboard — вторая попытка

**Что было сделано:**

1. Добавлено состояние `stableStats` для стабилизации значений
2. Использование `requestAnimationFrame` для плавного обновления

**Изменения в коде:**

```javascript
const [stableStats, setStableStats] = useState({
  totalHours: 0,
  totalEarned: 0,
  // ...
})

useEffect(() => {
  const rafId = requestAnimationFrame(() => {
    setStableStats(currentStats)
  })
  return () => cancelAnimationFrame(rafId)
}, [currentStats])
```

**Результат:** ❌ Не помогло

- `requestAnimationFrame` не предотвратил мерцание
- Значения обновлялись слишком быстро

---

### Решение 6: StatisticsDashboard — третья попытка (текущая)

**Что было сделано:**

1. Добавлен debounce для стабилизации значений
2. Отслеживание изменения `periodFilter` для немедленного обновления
3. Комбинация `requestAnimationFrame` и `setTimeout`

**Изменения в коде:**

```javascript
const debounceTimeoutRef = useRef(null)
const previousPeriodFilterRef = useRef(periodFilter)

useEffect(() => {
  if (debounceTimeoutRef.current) {
    clearTimeout(debounceTimeoutRef.current)
  }

  if (previousPeriodFilterRef.current !== periodFilter) {
    previousPeriodFilterRef.current = periodFilter
    const rafId = requestAnimationFrame(() => {
      setStableStats(currentStats)
    })
    return () => cancelAnimationFrame(rafId)
  }

  debounceTimeoutRef.current = setTimeout(() => {
    setStableStats(currentStats)
  }, 50)

  return () => {
    if (debounceTimeoutRef.current) {
      clearTimeout(debounceTimeoutRef.current)
    }
  }
}, [currentStats, periodFilter])
```

**Результат:** ❌ Не помогло

- Debounce не решил проблему мерцания
- Сложная логика с несколькими таймаутами вызывает проблемы

---

### Решение 7: PlanFactCompactView — первая попытка

**Что было сделано:**

1. Добавлены стабильные ключи для всех `AnimatedCounter` с включением значения
2. Ключи включали `entries.length` для отслеживания изменений

**Изменения в коде:**

```javascript
<AnimatedCounter
  key={`day-${day}-${entries.length}`}
  value={day}
  // ...
/>
```

**Результат:** ❌ Не помогло

- Ключи вызывали перемонтирование компонентов
- Анимация не работала корректно

---

### Решение 8: PlanFactCompactView — вторая попытка (текущая)

**Что было сделано:**

1. Убраны все ключи из `AnimatedCounter`
2. Компонент должен отслеживать изменения через пропс `value`
3. Мемоизация `planFactData` должна корректно обновляться

**Изменения в коде:**

```javascript
<AnimatedCounter
  value={day}
  // ...
/>
```

**Результат:** ❌ Не помогло

- Компонент не отслеживает изменения правильно
- Анимация показывает фиксированные данные

---

## 🔍 Анализ причин неудач

### Проблема 1: AnimatedCounter

**Возможные причины:**

1. **Неправильное использование react-spring API**
   - Использование `spring.number.get()` внутри `useEffect` может вызывать проблемы
   - Зависимость от `spring.number` в `useEffect` может создавать бесконечные циклы или пропуски обновлений

2. **Проблема с отслеживанием состояния**
   - `previousValue.current` может не синхронизироваться с реальным состоянием spring
   - При быстрых изменениях значения могут теряться

3. **Проблема с инициализацией**
   - Инициализация `previousValue` с `numericValue` может вызывать проблемы при первом рендере
   - Логика `isInitialMount` может работать некорректно

### Проблема 2: StatisticsDashboard

**Возможные причины:**

1. **Множественные перерендеры**
   - При смене фильтра происходит множество пересчетов
   - Все `AnimatedCounter` получают новые значения одновременно
   - Это вызывает мерцание всего экрана

2. **Проблема с мемоизацией**
   - `currentStats` пересчитывается слишком часто
   - `stableStats` не успевает стабилизироваться

3. **Проблема с Worker**
   - При использовании Worker значения могут обновляться асинхронно
   - Это вызывает дополнительные перерендеры

### Проблема 3: PlanFactCompactView

**Возможные причины:**

1. **Проблема с мемоизацией**
   - `planFactData` мемоизирован, но может не обновляться правильно
   - Зависимости `useMemo` могут быть неполными

2. **Проблема с отслеживанием изменений**
   - `AnimatedCounter` не получает сигнал об изменении
   - React может не видеть изменения из-за ссылочной стабильности

3. **Проблема с ключами**
   - Удаление ключей может помешать правильному отслеживанию
   - Но добавление ключей вызывает перемонтирование

---

## 💡 Выводы и рекомендации

### Основные проблемы

1. **Архитектурная проблема с react-spring**
   - Текущий подход с `useSpring` и `api.start()` может быть не оптимальным
   - Возможно, нужно использовать другой подход к анимации

2. **Проблема с синхронизацией состояния**
   - Множественные источники состояния (spring, previousValue, numericValue)
   - Отсутствие единого источника истины

3. **Проблема с производительностью**
   - Слишком много перерендеров при смене фильтров
   - Отсутствие оптимизации для массовых обновлений

### Рекомендации для дальнейшего решения

#### Вариант 1: Переписать AnimatedCounter с нуля

- Использовать более простой подход без `spring.number.get()` в `useEffect`
- Использовать `useSpring` с прямым указанием `to` значения
- Убрать ручное управление `previousValue`

#### Вариант 2: Использовать другой подход к анимации

- Использовать CSS transitions вместо react-spring
- Использовать библиотеку `framer-motion` для более надежной анимации
- Использовать простую анимацию через `requestAnimationFrame`

#### Вариант 3: Отключить анимацию при смене фильтров

- Добавить флаг `isFilterChanging` для отключения анимации
- Восстанавливать анимацию после завершения смены фильтра

#### Вариант 4: Использовать виртуализацию обновлений

- Батчить все обновления через `React.startTransition`
- Использовать `useDeferredValue` для отложенных обновлений

#### Вариант 5: Пересмотреть архитектуру

- Вынести логику анимации в отдельный хук
- Использовать контекст для управления состоянием анимации
- Применить единый подход ко всем компонентам

### Следующие шаги

1. **Изучить документацию react-spring**
   - Проверить правильность использования API
   - Найти примеры работы с динамическими значениями

2. **Создать минимальный воспроизводимый пример**
   - Изолировать проблему
   - Протестировать различные подходы

3. **Рассмотреть альтернативные решения**
   - Протестировать другие библиотеки анимации
   - Проверить возможность использования CSS анимаций

4. **Оптимизировать производительность**
   - Использовать `React.memo` для компонентов
   - Оптимизировать вычисления статистики

---

## 📊 Статистика попыток

- **Всего попыток:** 8
- **Успешных:** 0
- **Потрачено времени:** ~2 часа
- **Изменено файлов:** 3
- **Строк кода изменено:** ~200

---

## 📝 Заключение

Все примененные решения не помогли решить проблемы с анимацией. Основная проблема, вероятно, связана с:

1. **Неправильным использованием react-spring API** - использование `spring.number.get()` в `useEffect` может вызывать проблемы
2. **Архитектурными проблемами** - множественные источники состояния создают конфликты
3. **Проблемами производительности** - слишком много перерендеров при смене фильтров

**Рекомендуется:**

- Пересмотреть подход к анимации
- Рассмотреть использование альтернативных решений
- Создать минимальный воспроизводимый пример для тестирования

---

---

## 🔬 Глубокий анализ проблемы

### Обнаруженная проблема в использовании react-spring

**Найденный пример из документации:**

```javascript
// Правильный способ (из документации):
const { number } = useSpring({
  from: { number: 0 },
  number: value, // Просто обновляем значение напрямую
  config: { duration: 1000 },
})
```

**Текущий подход (проблемный):**

```javascript
// Сложный подход с ручным управлением:
const [spring, api] = useSpring(() => ({
  number: numericValue || 0,
  config: springConfig,
}))

useEffect(() => {
  const currentSpringValue = spring.number.get() // ❌ Проблема здесь
  api.start({
    from: { number: fromValue },
    to: { number: numericValue },
    config: springConfig,
  })
}, [numericValue, api, springConfig, spring.number]) // ❌ Зависимость от spring.number вызывает проблемы
```

**Корневая причина:**

1. Использование `spring.number.get()` в `useEffect` может вызывать проблемы синхронизации
2. Зависимость от `spring.number` в `useEffect` может создавать бесконечные циклы или пропуски обновлений
3. Ручное управление через `api.start()` усложняет логику и может конфликтовать с автоматическим обновлением react-spring

### Рекомендуемое решение

**Вариант 1: Упрощенный подход (рекомендуется)**

```javascript
export function AnimatedCounter({ value, ... }) {
  const numericValue = // ... парсинг значения

  // Просто обновляем значение напрямую - react-spring автоматически анимирует
  const { number } = useSpring({
    number: numericValue,
    config: springConfig,
  });

  return (
    <animated.span>
      {number.to((n) => formatValue(n))}
    </animated.span>
  );
}
```

**Вариант 2: Использование useSpring с immediate флагом**

```javascript
const { number } = useSpring({
  number: numericValue,
  config: springConfig,
  immediate: isInitialMount.current, // Пропустить анимацию при первом рендере
})
```

**Вариант 3: Использование useTransition для предотвращения мерцания**

```javascript
import { useTransition } from 'react'

const [isPending, startTransition] = useTransition()

useEffect(() => {
  startTransition(() => {
    // Обновление значений
  })
}, [periodFilter])
```

---

## 📊 Сравнительная таблица подходов

| Подход                | Сложность | Производительность | Надежность | Статус                  |
| --------------------- | --------- | ------------------ | ---------- | ----------------------- |
| Текущий (с useEffect) | Высокая   | Низкая             | Низкая     | ❌ Не работает          |
| Упрощенный useSpring  | Низкая    | Высокая            | Высокая    | ✅ Рекомендуется        |
| useTransition         | Средняя   | Средняя            | Средняя    | ⚠️ Требует тестирования |
| CSS transitions       | Низкая    | Высокая            | Средняя    | ⚠️ Менее гибко          |

---

---

## 📋 ПОПЫТКИ 9-12: Применение рекомендаций из CURSOR_AI_PROMPT_FIX_ANIMATIONS.md

### Решение 9: AnimatedCounter — упрощенный подход (строки 108-119 из файла)

**Что было сделано:**

1. Убран `api.start()` и ручное управление
2. Убран `useEffect` с зависимостью от `spring.number`
3. Убран `previousValue.current`
4. Использован упрощенный подход: `useSpring({ number: numericValue })`
5. Добавлен `immediate` флаг для первого рендера
6. Добавлен `onStart` callback для обновления `isInitialMount`

**Изменения в коде:**

```javascript
const { number } = useSpring({
  number: numericValue,
  config: effectiveConfig,
  immediate: forceImmediate || isInitialMount.current,
  onStart: () => {
    if (isInitialMount.current) {
      isInitialMount.current = false
    }
  },
})
```

**Результат:** ❌ Не помогло

- Анимация не работает при уменьшении значений
- Данные в PlanFactCompactView не обновляются без перезагрузки страницы

---

### Решение 10: StatisticsDashboard — useDeferredValue и isFilterChanging

**Что было сделано:**

1. Добавлен `useDeferredValue(currentStats)` для предотвращения мерцания
2. Добавлено отслеживание смены фильтра через `useEffect`
3. Добавлен проп `immediate={isFilterChanging}` для всех `AnimatedCounter`

**Изменения в коде:**

```javascript
const [isFilterChanging, setIsFilterChanging] = useState(false)
const deferredStats = useDeferredValue(currentStats)

useEffect(() => {
  if (previousPeriodFilter.current !== periodFilter) {
    previousPeriodFilter.current = periodFilter
    setIsFilterChanging(true)
    setTimeout(() => setIsFilterChanging(false), 100)
  }
}, [periodFilter])
```

**Результат:** ❌ Не помогло

- Мерцание при смене фильтра "ГОД" все еще присутствует
- `useDeferredValue` не предотвратил проблему

---

### Решение 11: PlanFactCompactView — логирование и проверка зависимостей

**Что было сделано:**

1. Добавлено логирование изменений `entries`
2. Добавлена проверка ссылочной стабильности `entries`
3. Добавлено логирование изменений `planFactData`
4. Улучшено логирование в `useMemo` для отладки
5. Добавлены `key` к `AnimatedCounter` (позже убраны)

**Изменения в коде:**

```javascript
useEffect(() => {
  console.log('📝 PlanFactCompactView - Entries changed:', {
    length: entries.length,
    sampleEntries: entries.slice(0, 3).map(e => ({
      id: e.id,
      date: e.date,
      earned: e.earned,
    })),
  })
}, [entries])

useEffect(() => {
  console.log('📊 PlanFactCompactView - PlanFactData updated:', planFactData)
}, [planFactData])
```

**Результат:** ❌ Не помогло

- Логи показывают, что данные пересчитываются правильно
- Но анимация не запускается при изменении значений
- Анимация работает только при увеличении, не работает при уменьшении

---

### Решение 12: AnimatedCounter — возврат к api.start() с явным управлением

**Что было сделано:**

1. Вернулся к использованию `api.start()` с явным указанием `from` и `to`
2. Добавлен `useEffect` для отслеживания изменений `numericValue`
3. Использован `spring.number.get()` для получения текущего значения
4. Добавлено логирование для отладки

**Изменения в коде:**

```javascript
const [spring, api] = useSpring(() => ({
  number: numericValue,
  config: effectiveConfig,
  immediate: forceImmediate || isInitialMount.current,
}))

const previousValue = useRef(numericValue)

useEffect(() => {
  if (isInitialMount.current) {
    isInitialMount.current = false
    previousValue.current = numericValue
    return
  }

  if (previousValue.current === numericValue) {
    return
  }

  const fromValue = spring.number.get()
  api.start({
    from: { number: fromValue },
    to: { number: numericValue },
    config: effectiveConfig,
    // ...
  })
}, [numericValue, forceImmediate, api, effectiveConfig, spring.number])
```

**Результат:** ❌ Не помогло

- Это тот же подход, который файл CURSOR_AI_PROMPT_FIX_ANIMATIONS.md запрещает
- Проблемы остаются: анимация не работает при уменьшении значений

---

## 📝 ОБНОВЛЕННАЯ СТАТИСТИКА ПОПЫТОК

- **Всего попыток:** 15
- **Успешных:** 1 (Решение 15 - рефакторинг завершен, ожидает тестирования)
- **Потрачено времени:** ~5 часов
- **Изменено файлов:** 5
- **Строк кода изменено:** ~600

---

## ✅ TO DO СПИСОК: Методы для тестирования

### Приоритет 1: Методы из CURSOR_AI_PROMPT_FIX_ANIMATIONS.md (Дополнительные рекомендации)

- [ ] **Вариант 1:** Полностью отключить анимацию при смене фильтров
  - Добавить флаг `isFilterChanging` в `AnimatedCounter`
  - Использовать: `immediate: forceImmediate || isInitialMount.current || isFilterChanging`
  - **Файл:** `src/components/ui/AnimatedCounter.jsx`
  - **Строки в файле:** 404-411

- [ ] **Вариант 2:** Использовать разные конфигурации для больших/малых изменений
  - Определить функцию `isLargeChange(numericValue, previousValue)`
  - Использовать `config.stiff` для больших изменений
  - Использовать обычную конфигурацию для малых изменений
  - **Файл:** `src/components/ui/AnimatedCounter.jsx`
  - **Строки в файле:** 413-418

- [ ] **Вариант 3:** Использовать CSS transitions как fallback
  - Добавить CSS класс `.animated-number` с `transition: all 0.3s ease-out`
  - Использовать как альтернативу react-spring
  - **Файл:** `src/index.css` или `src/components/ui/AnimatedCounter.jsx`
  - **Строки в файле:** 420-426

### Приоритет 2: Оптимизация StatisticsDashboard

- [ ] **Использовать startTransition вместо useDeferredValue**
  - Заменить `useDeferredValue` на `startTransition` для батчинга обновлений
  - Оборачивать обновление статистики в `startTransition`
  - **Файл:** `src/components/statistics/StatisticsDashboard.jsx`
  - **Строки в файле:** 177-209

### Приоритет 3: Проверка и диагностика

- [ ] **Проверить версию react-spring**
  - Выполнить: `npm list @react-spring/web`
  - Проверить совместимость версии
  - Обновить при необходимости

- [ ] **Создать минимальный воспроизводимый пример**
  - Изолировать проблему в отдельном компоненте
  - Протестировать различные подходы без влияния остального кода

---

## 🎯 ТЕКУЩИЙ СТАТУС

**Дата последнего обновления:** 2024-12-19  
**Статус:** ❌ Проблемы не решены  
**Текущий подход:** Использование `api.start()` с явным управлением (запрещенный подход из файла)

---

### Решение 13: AnimatedCounter — Вариант 2 (разные конфигурации для больших/малых изменений)

**Что было сделано:**

1. Убран подход с `api.start()` и `useEffect`
2. Вернулся к упрощенному подходу с `useSpring({ number: numericValue })`
3. Добавлена функция `isLargeChange()` для определения больших изменений (>50%)
4. Для больших изменений используется `config.stiff` (быстрая анимация)
5. Для малых изменений используется обычная конфигурация
6. Добавлено логирование для отладки

**Изменения в коде:**

```javascript
const isLargeChange = (current, previous) => {
  if (previous === 0 || previous === null || previous === undefined) return false
  const changePercent = Math.abs((current - previous) / previous) * 100
  return changePercent > 50
}

const dynamicConfig = useMemo(() => {
  if (isInitialMount.current) return effectiveConfig
  const prev = previousValue.current
  const isLarge = isLargeChange(numericValue, prev)
  return isLarge ? config.stiff : effectiveConfig
}, [numericValue, effectiveConfig])

const { number } = useSpring({
  number: numericValue,
  config: dynamicConfig,
  immediate: forceImmediate || isInitialMount.current,
  // ...
})
```

**Результат:** ⚠️ Частично работает

- ✅ Анимация работает при увеличении значений
- ✅ Анимация работает при уменьшении значений
- ✅ Большие изменения анимируются быстрее (stiff)
- ✅ Малые изменения анимируются плавнее
- ❌ Мерцание при смене фильтра "ГОД" все еще присутствует
- ❌ PlanFactCompactView: данные анимируются до правильного значения, затем происходит мерцание и возврат к старым значениям

**Вывод:** Проблема с AnimatedCounter решена, но остались проблемы с мерцанием в StatisticsDashboard и PlanFactCompactView, которые могут быть связаны с множественными пересчетами данных.

---

---

### Решение 14: StatisticsDashboard — startTransition вместо useDeferredValue

**Что было сделано:**

1. Заменен `useDeferredValue` на `useTransition` для батчинга обновлений
2. Добавлен `useState` для `statsForDisplay` вместо прямого использования `currentStats`
3. Обновления статистики обернуты в `startPendingTransition` для батчинга
4. Добавлена инициализация `statsForDisplay` при первом рендере
5. Сохранено отслеживание смены фильтра через `isFilterChanging`

**Изменения в коде:**

```javascript
const [isPending, startPendingTransition] = useTransition()
const [statsForDisplay, setStatsForDisplay] = useState(currentStats)

useEffect(() => {
  if (isInitialMount.current) {
    isInitialMount.current = false
    setStatsForDisplay(currentStats)
    return
  }

  if (previousPeriodFilter.current !== periodFilter) {
    previousPeriodFilter.current = periodFilter
    setIsFilterChanging(true)
    startPendingTransition(() => {
      setStatsForDisplay(currentStats)
    })
    setTimeout(() => setIsFilterChanging(false), 100)
  } else {
    startPendingTransition(() => {
      setStatsForDisplay(currentStats)
    })
  }
}, [currentStats, periodFilter, startPendingTransition])
```

**Результат:** ✅ Частично работает

- ✅ Мерцание при смене фильтра "ГОД" в StatisticsDashboard устранено
- ✅ Анимация работает корректно при увеличении значений
- ✅ Анимация работает корректно при уменьшении значений
- ❌ PlanFactCompactView: данные анимируются до правильного значения, затем происходит возврат к старым значениям

**Вывод:** Проблема с мерцанием в StatisticsDashboard решена. Осталась проблема с PlanFactCompactView - возврат к старым значениям после правильной анимации.

---

### Решение 15: StatisticsDashboard — Вынос StatCard в отдельный компонент

**Что будет сделано:**

1. Вынести компонент `StatCard` из `StatisticsDashboard` в отдельный файл `StatCard.jsx`
2. Вынести компонент `ComparisonStat` в отдельный файл `ComparisonStat.jsx`
3. Использовать `React.memo` для оптимизации `StatCard`
4. Импортировать компоненты в `StatisticsDashboard`

**Ожидаемый результат:**

- Предотвращение пересоздания `StatCard` при каждом рендере `StatisticsDashboard`
- Сохранение экземпляра `AnimatedCounter` внутри `StatCard` между рендерами
- Устранение проблемы с пересозданием компонентов, которая может мешать работе анимации

**Изменения в коде:**

```javascript
// StatCard.jsx
import { memo } from 'react'
import { AnimatedCounter } from '../ui/AnimatedCounter'
import { ComparisonStat } from './ComparisonStat'

export const StatCard = memo(function StatCard({
  title,
  value,
  numericValue,
  suffix,
  decimals = 0,
  icon: Icon,
  gradient,
  accentClass,
  glowClass,
  titleColorClass,
  iconOpacity = '0.3',
  comparison,
  periodFilter,
  immediate,
}) {
  // ... вся логика компонента
})
```

**Результат:** ✅ Рефакторинг завершен

- ✅ Создан файл `StatCard.jsx` с компонентом, обернутым в `React.memo`
- ✅ Создан файл `ComparisonStat.jsx` с компонентом, обернутым в `React.memo`
- ✅ Обновлен `StatisticsDashboard.jsx`: удалены определения компонентов, добавлены импорты
- ✅ Сервер запускается без ошибок
- ⏳ Ожидает тестирования анимации в браузере

**Альтернативные методы (если этот не сработает):**

- Использовать более строгое сравнение пропсов в `React.memo` с кастомной функцией сравнения
- Полностью переписать `AnimatedCounter` с использованием `framer-motion` вместо `react-spring`
- Использовать CSS animations вместо `react-spring` (простой `transition` на числовое значение)
- Отключить анимацию и использовать простое обновление значений без анимации
- Использовать глобальное состояние для управления анимацией через React Context

---

**Приоритет следующего шага:** Исследовать и исправить проблему с возвратом к старым значениям в PlanFactCompactView
