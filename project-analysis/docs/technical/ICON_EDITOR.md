# 🎨 Редактор иконок для разработки

## 📋 Описание

Система для быстрой смены иконок на кнопках во время разработки без изменения кода. Работает только в dev режиме (`import.meta.env.DEV`).

## 🚀 Как использовать

### 1. Включение режима редактирования

Нажмите **`Alt+Shift+I`** в dev режиме - откроется панель редактора иконок.

### 2. Добавление iconId к кнопкам

Чтобы кнопка отображалась в списке редактора, нужно добавить `iconId` пропс:

```jsx
import { Button } from './components/ui/Button';
import { Save } from 'lucide-react';

// ✅ ПРАВИЛЬНО - с iconId
<Button
  icon={Save}
  iconId="button-save-entry" // Уникальный ID
  onClick={handleSave}
>
  Сохранить
</Button>

// ❌ НЕПРАВИЛЬНО - без iconId (не появится в редакторе)
<Button
  icon={Save}
  onClick={handleSave}
>
  Сохранить
</Button>
```

### 3. Работа с редактором

1. Нажмите `Alt+Shift+I` - откроется панель справа
2. В списке будут все кнопки с `iconId` и иконками
3. Кликните на нужную кнопку в списке
4. Выберите новую иконку из `IconSelect` (поддерживает Lucide и Iconify)
5. Иконка заменится автоматически и сохранится в `localStorage`

### 4. Сброс замен

- Нажмите кнопку **"Сбросить все замены"** в панели редактора
- Или удалите данные из `localStorage` с ключом `icon-editor-store`

## 🔧 Технические детали

### Структура данных

Замены хранятся в Zustand store с persist middleware:

```javascript
{
  iconReplacements: {
    "button-save-entry": "iconify:mdi:content-save",
    "button-delete": "Trash2"
  }
}
```

### Поддержка иконок

- ✅ Lucide React: `"Save"`, `"Trash2"`, `"Plus"` и т.д.
- ✅ Iconify: `"iconify:mdi:heart"`, `"iconify:carbon:analytics"` и т.д.

### Примеры использования

```jsx
// Кнопка с Lucide иконкой
<Button
  icon={Save}
  iconId="button-save"
  onClick={handleSave}
>
  Сохранить
</Button>

// Кнопка с Iconify иконкой (будет заменена на любую другую)
<Button
  icon={getIcon("iconify:mdi:heart")}
  iconId="button-favorite"
  onClick={handleFavorite}
>
  Избранное
</Button>
```

## 📝 Лучшие практики

1. **Используйте уникальные ID**: `iconId` должен быть уникальным для каждой кнопки
2. **Именуйте понятно**: `"button-save-entry"`, `"button-delete-item"`, `"header-menu-toggle"`
3. **Не используйте в production**: Редактор работает только в dev режиме автоматически

## ⚠️ Важно

- Редактор иконок работает **ТОЛЬКО в dev режиме**
- Замены сохраняются в `localStorage` и будут применены при перезагрузке
- Для сброса замен используйте кнопку в редакторе или очистите `localStorage`
