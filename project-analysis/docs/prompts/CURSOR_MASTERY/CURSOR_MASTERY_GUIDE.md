# 🎓 **CURSOR MASTERY - ВСЁ ЧТО НУЖНО ЗНАТЬ**

**Финальный гайд для Zero Coder | Всё что осталось важного**

---

## 🎯 **ЧТО ЗДЕСЬ**

Этот файл покрывает важные темы которые НЕ были детально раскрыты в предыдущих гайдах:

1. 🎨 **Composer в деталях** - главный инструмент zero coder
2. 💬 **Как правильно общаться с AI** - advanced промптинг
3. 🐛 **Debugging с Cursor** - находить и исправлять баги
4. 🔄 **Работа с существующим кодом** - понимание и изменение
5. ⚡ **Cursor Tab в деталях** - предиктивное кодирование
6. 📚 **Работа с контекстом (@)** - максимальная эффективность
7. ⚠️ **Типичные ошибки zero coder** - чего избегать
8. ⚙️ **Важные настройки** - что включить/выключить
9. 💰 **Лимиты и планы** - Free vs Pro
10. 🔒 **Privacy и безопасность** - что важно знать
11. 🚀 **Продвинутые техники** - уровень мастера
12. 📈 **Отслеживание прогресса** - как становиться лучше

---

## 1️⃣ **COMPOSER - ТВОЙ ГЛАВНЫЙ ИНСТРУМЕНТ**

### **Что такое Composer:**

Composer (Cmd+I) - это режим где AI может работать с **НЕСКОЛЬКИМИ файлами одновременно**.

**Обычный Chat (Cmd+L):**

```
Ограничения:
- Работает с 1 файлом
- Показывает код в чате
- Ты копируешь → вставляешь
- Медленнее для больших задач
```

**Composer (Cmd+I):**

```
Возможности:
- Работает с 5-10 файлами сразу
- Редактирует файлы напрямую
- Показывает diff (что изменилось)
- Accept/Reject изменения
- В 10x быстрее для комплексных задач
```

---

### **Когда использовать Composer:**

#### **✅ ИСПОЛЬЗУЙ Composer для:**

1. **Создание связанных файлов**

```
"Создай feature таймера:
- Timer.jsx (компонент)
- useTimer.js (хук)
- timer.utils.js (утилиты)
- Timer.test.jsx (тесты)"

→ Composer создаст ВСЕ 4 файла с правильными импортами!
```

2. **Рефакторинг нескольких файлов**

```
"Переименуй все упоминания formatTime на formatDuration
в файлах: Timer.jsx, utils.js, App.jsx"

→ Composer изменит везде одновременно!
```

3. **Миграция компонента**

```
"Вынеси логику из Timer.jsx в хук useTimer.js
Обнови все импорты где используется"

→ Composer сделает всё сам!
```

4. **Создание feature с нуля**

```
"Создай полный feature категорий:
- CategoryManager.jsx
- CategoryInput.jsx
- CategoryList.jsx
- useCategories.js
- categories.utils.js
Всё с glassmorphism стилями"

→ Composer создаст весь feature!
```

#### **❌ НЕ ИСПОЛЬЗУЙ Composer для:**

1. **Простые вопросы**

```
"Что делает useEffect?"
→ Используй Chat (Cmd+L)
```

2. **Изменение 1 строки**

```
"Измени цвет кнопки"
→ Используй Cmd+K (inline edit)
```

3. **Объяснение кода**

```
"Объясни этот компонент"
→ Используй Chat (Cmd+L)
```

---

### **Как работать с Composer:**

#### **Шаг 1: Открой Composer**

```
Cmd+I → Composer открывается
```

#### **Шаг 2: Добавь файлы в контекст**

```
Способ 1: Drag & Drop
Перетащи файлы в Composer

Способ 2: @mentions
"@Timer.jsx @useTimer.js создай тесты"

Способ 3: Автоматически
Composer сам добавит нужные файлы
```

#### **Шаг 3: Напиши промпт**

```
Хороший промпт для Composer:

"Создай feature статистики:

ФАЙЛЫ:
1. Statistics.jsx - главный компонент
2. useStatistics.js - хук для вычислений
3. StatCard.jsx - карточка одной статистики
4. charts.utils.js - утилиты для графиков

ТРЕБОВАНИЯ:
- Glassmorphism дизайн
- Responsive layout
- Использовать Recharts
- Данные из localStorage
- Dark mode support

ДАННЫЕ:
- entries из useLocalStorage
- Вычислять: total time, avg per day, entries count
- График по дням за неделю"
```

#### **Шаг 4: Review изменения**

```
Composer покажет:
- Какие файлы будут созданы (зелёные)
- Какие файлы будут изменены (жёлтые)
- Diff для каждого файла

Просмотри ВСЕ изменения!
```

#### **Шаг 5: Accept или Reject**

```
Accept All - принять всё
Accept File - принять один файл
Reject - отклонить и попробовать снова
Edit - изменить промпт и попробовать ещё раз
```

---

### **Pro Tips для Composer:**

#### **Tip 1: Начинай с структуры**

```
ПЛОХО:
"Создай статистику"

ХОРОШО:
"Создай feature статистики со структурой:
src/
  components/
    statistics/
      Statistics.jsx
      StatCard.jsx
  hooks/
    useStatistics.js
  utils/
    statistics.utils.js"
```

#### **Tip 2: Указывай зависимости**

```
"Создай Timer.jsx который использует:
- @useTimer.js для логики
- @Button.jsx для кнопок (копируй стиль)
- @glassmorphism.css для стилей
- soundService для звуков"

Composer увидит связи и сделает правильно!
```

#### **Tip 3: Итеративно уточняй**

```
Первый запрос:
"Создай базовую структуру Timer"

После review:
"Добавь Pause функциональность"

После review:
"Добавь звуки при Start/Stop"

Маленькими шагами = меньше ошибок!
```

#### **Tip 4: Используй примеры**

```
"Создай CategorySelect похожий на @Button.jsx
Должен иметь те же стили и структуру
Но вместо onClick использовать onChange"

Composer скопирует паттерн!
```

---

## 2️⃣ **КАК ПРАВИЛЬНО ОБЩАТЬСЯ С AI**

### **Психология AI в Cursor:**

AI в Cursor работает как **очень умный джуниор разработчик**:

- ✅ Знает синтаксис и паттерны
- ✅ Может писать код быстро
- ✅ Следует инструкциям
- ❌ Не знает контекст твоего проекта (пока не скажешь)
- ❌ Не помнит прошлые разговоры (используй @files)
- ❌ Может делать предположения (нужно уточнять)

---

### **Структура идеального промпта:**

```markdown
## КОНТЕКСТ (что уже есть)

Проект: Time Tracker на React
Стек: React 18, Vite, Tailwind, Recharts
Текущие компоненты: @Button.jsx @Input.jsx
Дизайн: Glassmorphism

## ЗАДАЧА (что нужно сделать)

Создай компонент Timer с функционалом старт/пауза/стоп

## ТРЕБОВАНИЯ (детали)

Функционал:

- Отображение времени HH:MM:SS
- Кнопки: Start, Pause, Stop
- Состояния: idle, running, paused
- Сохранение при Stop в localStorage

Стили:

- Glassmorphism как в @Button.jsx
- Цифры крупные (text-4xl)
- Dark mode support
- Responsive

Технические:

- Использовать @useTimer.js хук
- PropTypes валидация
- JSDoc комментарии

## ПРИМЕРЫ (если нужно)

Похож на @Button.jsx по стилю
Использует @useTimer.js для логики

## ОГРАНИЧЕНИЯ (что НЕ делать)

- НЕ использовать inline styles
- НЕ использовать классы
- НЕ мутировать state напрямую
```

---

### **Типы промптов:**

#### **1. Творческий промпт (Generative)**

```
"Создай компонент таймера с красивой анимацией"

AI генерирует с нуля, много свободы
Используй для: новых компонентов, идей
```

#### **2. Точный промпт (Specific)**

```
"Создай компонент Timer:
- Props: isRunning, elapsedTime, onStart, onPause, onStop
- Три кнопки с иконками из lucide-react
- Glassmorphism стили: backdrop-blur-md bg-white/10"

AI следует чётким инструкциям
Используй для: точных требований
```

#### **3. Примерный промпт (Example-based)**

```
"Создай Input похожий на @Button.jsx
Скопируй структуру и стили
Но вместо onClick используй onChange"

AI копирует паттерн
Используй для: консистентности
```

#### **4. Проблемный промпт (Problem-solving)**

```
"ПРОБЛЕМА: Timer не останавливается при unmount
КОД: [вставить код]
ОШИБКА: Warning: Can't perform a React state update on unmounted component

Исправь эту проблему"

AI анализирует и исправляет
Используй для: багов
```

#### **5. Обучающий промпт (Educational)**

```
"Объясни почему useEffect нужен cleanup:

useEffect(() => {
  const interval = setInterval(...);
  return () => clearInterval(interval);
}, []);

Объясни каждую строку и что произойдёт без cleanup"

AI объясняет подробно
Используй для: обучения
```

---

### **Техники продвинутого промптинга:**

#### **Техника 1: Chain of Thought (Цепочка мыслей)**

```
"Давай пошагово:

Шаг 1: Проанализируй текущий Timer.jsx
Шаг 2: Определи что нужно изменить для добавления Pause
Шаг 3: Покажи план изменений
Шаг 4: Реализуй изменения

Начни с Шага 1"

AI будет думать пошагово
Полезно для: сложных задач
```

#### **Техника 2: Few-Shot Learning (Обучение на примерах)**

```
"Вот паттерн создания UI компонентов:

Пример 1: Button.jsx
[показать код]

Пример 2: Input.jsx
[показать код]

Теперь создай Select.jsx следуя этому паттерну"

AI учится на примерах
Полезно для: консистентности
```

#### **Техника 3: Constraint-based (Через ограничения)**

```
"Создай Timer БЕЗ использования:
- Классов (только функции)
- Inline styles (только Tailwind)
- var (только const/let)
- Прямых мутаций state

Используй:
- Функциональные компоненты
- Хуки
- PropTypes
- JSDoc"

AI работает в рамках ограничений
Полезно для: стандартов кода
```

#### **Техника 4: Role-based (Через роль)**

```
"Ты senior React developer с опытом в:
- React hooks и оптимизации
- Tailwind CSS и responsive design
- Accessibility (a11y)

Сделай code review для @Timer.jsx
Найди проблемы с:
- Производительностью
- Accessibility
- Best practices
- Потенциальными багами"

AI принимает роль эксперта
Полезно для: code review
```

---

### **Чего ИЗБЕГАТЬ в промптах:**

#### **❌ Плохие промпты:**

```
1. СЛИШКОМ КОРОТКИЕ:
"Сделай таймер"
→ AI не знает что конкретно нужно

2. СЛИШКОМ ДЛИННЫЕ:
[10 параграфов требований]
→ AI запутается в деталях

3. ПРОТИВОРЕЧИВЫЕ:
"Сделай простой компонент с 20 функциями"
→ AI не поймёт приоритет

4. БЕЗ КОНТЕКСТА:
"Добавь функцию"
→ AI не знает в какой файл

5. НЕЯСНЫЕ:
"Сделай красиво"
→ AI не знает что такое "красиво"
```

#### **✅ Хорошие промпты:**

```
1. КОНКРЕТНЫЕ:
"Создай компонент Timer в src/components/Timer.jsx
с функциями start, pause, stop"

2. С КОНТЕКСТОМ:
"К существующему @Timer.jsx добавь pause функцию"

3. С ПРИМЕРАМИ:
"Создай Input похожий на @Button.jsx"

4. СТРУКТУРИРОВАННЫЕ:
"Создай компонент:
Функционал: [...]
Стили: [...]
Технические: [...]"

5. ИЗМЕРИМЫЕ:
"Оптимизируй Timer.jsx чтобы рендерился < 5ms"
```

---

## 3️⃣ **DEBUGGING С CURSOR**

### **Уровни debugging:**

#### **Уровень 1: Syntax Errors (Синтаксические)**

```javascript
// Ошибка видна сразу (красное подчёркивание):
const [count setCount] = useState(0);
          ↑ Missing comma

// Error Lens покажет:
// ❌ ',' expected

// Исправление:
Cmd+K → "Исправь синтаксическую ошибку"
```

#### **Уровень 2: Runtime Errors (Ошибки выполнения)**

```javascript
// Ошибка в консоли браузера:
// Uncaught TypeError: Cannot read property 'map' of undefined

// Debugging с Cursor:
1. Копируй ошибку из консоли
2. Cmd+L → "ОШИБКА: [вставь ошибку]
   КОД: [вставь код]
   Что не так?"
3. AI объяснит и предложит исправление
```

#### **Уровень 3: Logic Errors (Логические)**

```javascript
// Код работает, но не так как нужно:
// Например: таймер показывает неправильное время

// Debugging strategy:
1. Добавь console.log для отладки:
   Cmd+K на функции → "Добавь console.log для отладки"

2. Проверь логи (Console Ninja покажет в редакторе!)

3. Найди проблему

4. Cmd+L → "Логика неправильная:
   Ожидаю: [что должно быть]
   Получаю: [что получается]
   КОД: [код]
   Исправь"
```

---

### **Debugging workflow:**

```
1. ВОСПРОИЗВЕДИ БАГ
   - Запиши шаги для воспроизведения
   - Определи ожидаемое поведение
   - Определи реальное поведение

2. ЛОКАЛИЗУЙ ПРОБЛЕМУ
   - Добавь console.log (Cmd+K → "добавь debugging логи")
   - Используй React DevTools
   - Проверь Network tab (если API)

3. ИЗОЛИРУЙ КОД
   - Открой файл с проблемой
   - Cmd+K на проблемном коде → "Что здесь не так?"

4. СПРОСИ AI
   Cmd+L → "
   ПРОБЛЕМА: [описание]
   КОД: [код]
   ОШИБКА: [из консоли]
   ОЖИДАЮ: [поведение]
   ПОЛУЧАЮ: [поведение]

   Найди баг и исправь"

5. ПРИМЕНИ ФИКС
   - Accept изменения
   - Проверь что работает
   - Удали debug console.log

6. ТЕСТ
   - Воспроизведи баг снова → должен исчезнуть
   - Проверь что ничего не сломалось
   - Git commit

7. ВЫВОДЫ
   - Что было не так?
   - Почему это произошло?
   - Как избежать в будущем?

   Cmd+L → "Объясни почему возник этот баг
   и как избежать подобных в будущем"
```

---

### **Debugging техники:**

#### **Техника 1: Rubber Duck Debugging с AI**

```
Cmd+L → "Я объясню свой код, ты скажи если что-то не так:

Я создаю таймер который:
1. Хранит startTime в state
2. Каждую секунду обновляет elapsedTime
3. При stop сохраняет в localStorage

Код:
[вставить код]

Видишь какие-то проблемы?"

AI часто находит баги просто слушая объяснение!
```

#### **Техника 2: Binary Search Debugging**

```
Если большой компонент не работает:

1. Закомментируй половину кода
2. Работает? → Баг в закомментированной части
3. Не работает? → Баг в оставшейся части
4. Повтори до нахождения проблемной строки

Можно попросить AI помочь:
Cmd+K → "Закомментируй нижнюю половину функции"
```

#### **Техника 3: Time Travel Debugging**

```
Используй Git для поиска когда появился баг:

git log --oneline
# Найди последний рабочий коммит
git checkout <commit-hash>
# Проверь работает ли
# Если да - баг между этим и текущим коммитом

git diff <old-commit> <new-commit>
# Скопируй diff

Cmd+L → "Вот изменения между коммитами:
[вставить diff]

Где может быть баг?"
```

---

### **Типичные баги и как их искать:**

#### **1. useEffect бесконечный цикл**

```javascript
// Проблема:
useEffect(() => {
  setCount(count + 1); // ← Изменяет count
}, [count]); // ← Зависит от count = бесконечный цикл!

// Как найти:
// Браузер зависает, консоль спамится

// Как исправить:
Cmd+L → "@docs-search React useEffect infinite loop prevention"
```

#### **2. Утечка памяти в useEffect**

```javascript
// Проблема:
useEffect(() => {
  const interval = setInterval(() => {
    setCount(c => c + 1);
  }, 1000);
  // ← Нет cleanup! Интервал продолжает работать после unmount
}, []);

// Как найти:
// Warning в консоли: "Can't perform state update on unmounted component"

// Как исправить:
Cmd+K на useEffect → "Добавь cleanup"
```

#### **3. State не обновляется**

```javascript
// Проблема:
const [count, setCount] = useState(0);
setCount(count + 1);
console.log(count); // ← Всё ещё старое значение!

// Почему:
// setState асинхронный!

// Как исправить:
Cmd+L → "Почему state не обновляется сразу после setState?"
```

---

## 4️⃣ **РАБОТА С СУЩЕСТВУЮЩИМ КОДОМ**

### **Понимание чужого кода:**

#### **Способ 1: Попроси объяснить**

```
Открой файл с кодом
Cmd+L → "Объясни что делает этот компонент:

@Timer.jsx

Объясни:
1. Какие props принимает
2. Что хранится в state
3. Какие функции есть
4. Как работает логика
5. Какие хуки используются"

AI разберёт код подробно!
```

#### **Способ 2: Построчное объяснение**

```
Cmd+L → "Объясни этот код построчно:

const formatTime = (seconds) => {
  const hours = Math.floor(seconds / 3600);
  const minutes = Math.floor((seconds % 3600) / 60);
  const secs = seconds % 60;
  return `${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}:${String(secs).padStart(2, '0')}`;
};

Объясни каждую строку как будто мне 5 лет"
```

#### **Способ 3: Диаграмма потока**

```
Cmd+L → "Создай текстовую диаграмму потока данных для @Timer.jsx

Покажи:
- Откуда приходят данные (props)
- Как обновляется state
- Какие функции вызываются
- Как данные отображаются"

AI создаст визуализацию!
```

---

### **Рефакторинг существующего кода:**

#### **Безопасный рефакторинг workflow:**

```
1. GIT COMMIT
   git add .
   git commit -m "Before refactoring Timer"

   Теперь можно безопасно экспериментировать!

2. ПОНИМАНИЕ
   Cmd+L → "Объясни как работает @Timer.jsx"

   Убедись что понимаешь ЧТО меняешь

3. ПЛАН
   Cmd+L → "Я хочу отрефакторить @Timer.jsx:
   - Вынести логику в хук useTimer
   - Упростить JSX
   - Добавить React.memo

   Составь пошаговый план рефакторинга"

4. РЕФАКТОРИНГ
   Cmd+I (Composer) → "Отрефактори @Timer.jsx:

   1. Создай useTimer.js с логикой таймера
   2. Обнови Timer.jsx чтобы использовал хук
   3. Оберни в React.memo
   4. Добавь PropTypes

   Сохрани всю функциональность!"

5. ПРОВЕРКА
   npm run dev
   Проверь что всё работает КАК РАНЬШЕ

6. ТЕСТЫ
   Если есть тесты - запусти
   Если нет - попроси AI создать:
   Cmd+L → "Создай тесты для @Timer.jsx"

7. GIT COMMIT
   git add .
   git commit -m "Refactor: extract useTimer hook"

8. ЕСЛИ ЧТО-ТО СЛОМАЛОСЬ:
   git diff # Посмотри что изменилось
   git reset --hard HEAD~1 # Откат назад
   Попробуй снова с другим подходом
```

---

### **Изменение существующего кода:**

#### **Способ 1: Cmd+K (точечные изменения)**

```
Выдели код который нужно изменить
Cmd+K → "Добавь обработку ошибок через try-catch"

Cursor покажет diff:
- const result = await fetchData();
+ try {
+   const result = await fetchData();
+ } catch (error) {
+   console.error('Error:', error);
+   showNotification('Failed to fetch', 'error');
+ }

Accept или Reject
```

#### **Способ 2: Find & Replace с AI**

```
Cmd+Shift+H (Find & Replace)

Find: formatTime
Replace with: formatDuration

Или с AI:
Cmd+L → "@codebase замени все упоминания formatTime на formatDuration
Обнови импорты и экспорты"

AI изменит везде + обновит зависимости!
```

#### **Способ 3: Multi-cursor editing**

```
1. Выдели слово
2. Cmd+D (несколько раз) - выделяет следующие вхождения
3. Редактируй все сразу

Или с AI:
Выдели несколько строк
Cmd+K → "Добавь console.log перед каждой из этих строк"
```

---

## 5️⃣ **CURSOR TAB - ПРЕДИКТИВНОЕ КОДИРОВАНИЕ**

### **Что такое Cursor Tab:**

Cursor Tab - это AI который **предсказывает следующий код** (как GitHub Copilot но лучше).

**Как работает:**

1. Ты пишешь код
2. Cursor предсказывает следующие строки (показывает серым)
3. Tab - принять, Escape - отклонить

---

### **Когда Cursor Tab полезен:**

#### **1. Повторяющийся код**

```javascript
// Ты написал:
const Button = ({ onClick, children }) => {
  return <button onClick={onClick}>{children}</button>
}

// Начинаешь следующий:
const Input = ({ onChange, value }) => {
  // Cursor предлагает (серым):
  return <input onChange={onChange} value={value} />
}

// Tab - принять!
```

#### **2. Шаблонный код**

```javascript
// Ты пишешь:
const [count, setCount] = useState(0)

// Начинаешь новую строку:
const [
  // Cursor предлагает:
  name,
  setName,
] = useState('')

// Tab - принять!
```

#### **3. Импорты**

```javascript
// Ты пишешь:
import { useState } from 'react'

// Начинаешь следующий:
import {
  // Cursor предлагает:
  useEffect,
  useMemo,
  useCallback,
} from 'react'

// Tab если нужны все, или продолжай писать сам
```

#### **4. JSX структура**

```javascript
// Ты пишешь:
<div className="container">
  <h1>Title</h1>
  // Cursor предлагает:
  <p>Description</p>
</div>

// Tab если подходит структура
```

---

### **Как использовать Cursor Tab эффективно:**

#### **Tip 1: Пиши комментарии перед кодом**

```javascript
// Ты пишешь комментарий:
// Create a function to format time in HH:MM:SS format

// Начинаешь функцию:
const formatTime =
  // Cursor предлагает ПОЛНУЮ реализацию:
  seconds => {
    const hours = Math.floor(seconds / 3600)
    const minutes = Math.floor((seconds % 3600) / 60)
    const secs = seconds % 60
    return `${String(hours).padStart(2, '0')}:${String(minutes).padStart(2, '0')}:${String(secs).padStart(2, '0')}`
  }

// Tab - и готово!
```

#### **Tip 2: Используй описательные имена**

```javascript
// ПЛОХО (Cursor не поймёт):
const f = (x) => {

// ХОРОШО (Cursor поймёт):
const calculateTotalHours = (entries) => {
  // Cursor предложит правильную логику суммирования
```

#### **Tip 3: Создавай паттерны**

```javascript
// Создай паттерн:
const Button = ({ onClick, children, variant = 'primary' }) => {
  const variants = {
    primary: 'bg-blue-500',
    success: 'bg-green-500',
  }
  return <button className={variants[variant]}>{children}</button>
}

// Начни следующий компонент:
const Input = ({ onChange, value, variant = 'primary' }) => {
  // Cursor скопирует паттерн!
  const variants = {
    primary: 'border-blue-500',
    success: 'border-green-500',
  }
  return <input className={variants[variant]} value={value} onChange={onChange} />
}
```

---

### **Настройка Cursor Tab:**

```json
// Settings.json
{
  // Включить/выключить Tab
  "cursor.enableTab": true,

  // Задержка перед показом предсказания (ms)
  "cursor.tabDelay": 300,

  // Показывать предсказания во время typing
  "cursor.showPredictionsDuringTyping": true
}
```

---

## 6️⃣ **РАБОТА С КОНТЕКСТОМ (@)**

### **Типы контекста:**

#### **1. @filename - Конкретный файл**

```
@Button.jsx - добавить Button.jsx в контекст
@useTimer.js - добавить хук
@styles/glassmorphism.css - добавить стили
```

**Когда использовать:**

- Нужно показать AI конкретный файл
- Хочешь чтобы AI скопировал паттерн
- Нужно изменить конкретный файл

**Пример:**

```
"Создай Input похожий на @Button.jsx
Скопируй структуру и стили"
```

---

#### **2. @codebase - Весь проект**

```
@codebase - поиск по всем файлам проекта
```

**Когда использовать:**

- Поиск по всему проекту
- Глобальный рефакторинг
- Анализ архитектуры

**Примеры:**

```
"@codebase найди все useState в проекте"
"@codebase где используется formatTime?"
"@codebase есть ли дублирование кода?"
"@codebase покажи все компоненты которые используют glassmorphism стили"
```

---

#### **3. @folder - Папка**

```
@src/components - все компоненты
@src/hooks - все хуки
@src/utils - все утилиты
```

**Когда использовать:**

- Работа с группой файлов
- Анализ модуля
- Рефакторинг папки

**Пример:**

```
"@src/components создай index.js с экспортами всех компонентов"
```

---

#### **4. @docs - Документация**

```
@docs-search React useState
@docs-search Tailwind responsive design
```

**Когда использовать:**

- Вопросы о библиотеках
- Нужна официальная информация
- Изучение API

---

#### **5. @web - Интернет**

```
@web-search react hooks best practices 2025
@web-search tailwind glassmorphism tutorial
```

**Когда использовать:**

- Нужна актуальная информация
- Решение проблем
- Поиск примеров

---

#### **6. @github - GitHub репозитории**

```
@github react useLocalStorage hook examples
@github recharts responsive examples
```

**Когда использовать:**

- Нужны примеры реального кода
- Изучение библиотек
- Поиск решений

---

### **Стратегии использования контекста:**

#### **Стратегия 1: Минимальный контекст**

```
Для простых задач:
"Создай компонент Button"

Cursor использует .cursorrules + общее знание
```

#### **Стратегия 2: Примерный контекст**

```
Для консистентности:
"Создай Input похожий на @Button.jsx"

Cursor видит Button и копирует паттерн
```

#### **Стратегия 3: Полный контекст**

```
Для комплексных задач:
"Создай Statistics компонент:
- Использует @useStatistics.js для данных
- Стили как в @Button.jsx
- Графики с @charts.utils.js
- Данные из @storageService.js"

Cursor видит все зависимости
```

#### **Стратегия 4: Поисковый контекст**

```
Для исследования:
"@codebase покажи все компоненты которые:
1. Используют useState
2. Имеют glassmorphism стили
3. Не имеют PropTypes

Создай список и приоритизируй что исправить"
```

---

### **Pro Tips для контекста:**

#### **Tip 1: Cmd+/ для быстрого добавления**

```
1. Открой файл
2. Нажми Cmd+/
3. Файл добавлен в контекст текущего чата!
```

#### **Tip 2: Drag & Drop в Composer**

```
Перетащи файлы в Composer окно
Все файлы добавятся в контекст
```

#### **Tip 3: Комбинируй источники**

```
"@docs-search React useEffect
Также @web-search useEffect best practices
И покажи @github примеры useEffect cleanup

Объясни полностью как работает useEffect"

AI использует ВСЕ источники для лучшего ответа!
```

---

## 7️⃣ **ТИПИЧНЫЕ ОШИБКИ ZERO CODER**

### **Ошибка 1: Не добавлять контекст**

```
❌ ПЛОХО:
"Создай компонент"

✅ ХОРОШО:
"Создай компонент Timer в @src/components/Timer.jsx
Используй стиль из @Button.jsx
Следуй правилам из .cursorrules"
```

**Почему важно:**
AI не знает твой проект без контекста!

---

### **Ошибка 2: Принимать код не глядя**

```
❌ ПЛОХО:
AI: [генерирует 100 строк кода]
Ты: Accept All [не читая]

✅ ХОРОШО:
AI: [генерирует код]
Ты: [читаешь код]
Ты: [понимаешь что он делает]
Ты: [проверяешь на ошибки]
Ты: Accept если всё ок
```

**Почему важно:**
AI может ошибаться! Ты должен понимать код.

---

### **Ошибка 3: Менять всё сразу**

```
❌ ПЛОХО:
"Переделай весь проект: добавь роутинг, API, базу данных, авторизацию, тесты"

✅ ХОРОШО:
Шаг 1: "Добавь роутинг"
→ [проверка] → работает ✓
Шаг 2: "Добавь API интеграцию"
→ [проверка] → работает ✓
Шаг 3: "Добавь базу данных"
→ [проверка] → работает ✓
```

**Почему важно:**
Маленькие шаги = легко отменить если что-то сломается.

---

### **Ошибка 4: Не использовать Git**

```
❌ ПЛОХО:
[100 изменений без коммитов]
[что-то сломалось]
[невозможно откатиться]

✅ ХОРОШО:
Каждая фича = коммит
git commit -m "feat: add Timer component"

Что-то сломалось?
git log → git reset --hard <hash>
```

**Почему важно:**
Git - твоя страховка!

---

### **Ошибка 5: Игнорировать предупреждения**

```
❌ ПЛОХО:
Console: Warning: Each child should have unique "key"
Ты: [игнорирую]

✅ ХОРОШО:
Console: Warning
Ты: Cmd+L → "Что означает эта ошибка:
[вставить warning]
Как исправить?"
```

**Почему важно:**
Warnings становятся errors позже!

---

### **Ошибка 6: Не проверять в браузере**

```
❌ ПЛОХО:
AI: [создал компонент]
Ты: Accept
Ты: git commit
[не запустил npm run dev]

✅ ХОРОШО:
AI: [создал компонент]
Ты: Accept
Ты: npm run dev
Ты: [открыл браузер]
Ты: [проверил что работает]
Ты: git commit
```

**Почему важно:**
Код может выглядеть правильно но не работать!

---

### **Ошибка 7: Копировать код из интернета напрямую**

```
❌ ПЛОХО:
[Нашёл на Stack Overflow]
[Скопировал]
[Вставил]
[Не работает в твоём проекте]

✅ ХОРОШО:
[Нашёл на Stack Overflow]
Cmd+L → "Вот решение с Stack Overflow:
[вставить код]

Адаптируй под мой проект:
- Мой стек: React 18, Tailwind
- Мой стиль: glassmorphism
- Следуй .cursorrules"

AI адаптирует под твой проект!
```

---

### **Ошибка 8: Не задавать уточняющие вопросы**

```
❌ ПЛОХО:
AI: [создал код с непонятной магией]
Ты: [использую не понимая]

✅ ХОРОШО:
AI: [создал код]
Ты: "Объясни эту строку:
const memoizedValue = useMemo(() => compute(), [dep]);

Почему здесь useMemo?
Что произойдёт без него?"

AI объяснит!
```

**Почему важно:**
Понимание > слепое копирование.

---

## 8️⃣ **ВАЖНЫЕ НАСТРОЙКИ CURSOR**

### **Обязательные настройки:**

```json
// Settings.json (Cmd+,)
{
  // AI MODEL
  "cursor.model": "claude-sonnet-4", // Самая умная модель

  // TAB COMPLETION
  "cursor.enableTab": true, // Включить Tab предсказания

  // FORMAT ON SAVE
  "editor.formatOnSave": true, // Автоформатирование
  "editor.defaultFormatter": "esbenp.prettier-vscode",

  // AUTOSAVE
  "files.autoSave": "onFocusChange", // Автосохранение при смене окна

  // MINIMAP (опционально)
  "editor.minimap.enabled": false, // Отключить minimap (больше места)

  // BREADCRUMBS
  "breadcrumbs.enabled": true, // Навигация сверху

  // LINE NUMBERS
  "editor.lineNumbers": "on", // Номера строк

  // WORD WRAP
  "editor.wordWrap": "on", // Перенос длинных строк

  // FONT SIZE
  "editor.fontSize": 14, // Размер шрифта (по вкусу)

  // TAB SIZE
  "editor.tabSize": 2, // 2 пробела для indent

  // BRACKET COLORIZATION
  "editor.bracketPairColorization.enabled": true // Цветные скобки
}
```

---

### **Рекомендуемые настройки:**

```json
{
  // SMOOTH SCROLLING
  "editor.smoothScrolling": true,

  // CURSOR BLINKING
  "editor.cursorBlinking": "smooth",

  // CURSOR STYLE
  "editor.cursorStyle": "line",

  // SUGGEST ON TRIGGER
  "editor.suggestOnTriggerCharacters": true,

  // QUICK SUGGESTIONS
  "editor.quickSuggestions": {
    "other": true,
    "comments": false,
    "strings": true
  },

  // ACCEPT SUGGESTION ON ENTER
  "editor.acceptSuggestionOnEnter": "on",

  // SEMANTIC HIGHLIGHTING
  "editor.semanticHighlighting.enabled": true
}
```

---

### **Privacy настройки:**

```json
{
  // TELEMETRY
  "telemetry.telemetryLevel": "off", // Отключить телеметрию

  // CURSOR PRIVACY MODE
  "cursor.privacyMode": false // false = отправляет код в облако для AI
  // true = не отправляет (AI работает хуже)

  // Рекомендация для zero coder:
  // privacyMode: false для лучшей работы AI
  // НО не пиши секреты в коде (API ключи, пароли)!
}
```

---

## 9️⃣ **ЛИМИТЫ И ПЛАНЫ**

### **Cursor Free:**

```
✅ Unlimited базовые функции:
- Code highlighting
- Git интеграция
- Extensions
- Basic AI chat

⚠️ Лимиты AI:
- ~50 AI requests в день
- ~500 Tab completions в день
- Базовые модели

❌ Нет доступа:
- Advanced AI models
- Unlimited requests
- Priority support
```

---

### **Cursor Pro ($20/месяц):**

```
✅ Всё из Free +
- Unlimited AI requests
- Unlimited Tab completions
- Claude Sonnet 4 (лучшая модель)
- GPT-4o доступ
- Priority support
- Longer context window
```

---

### **Рекомендация для Zero Coder:**

```
Начни с FREE:
- Учись основам
- Создай 5-10 компонентов
- Попробуй все фичи

Если упираешься в лимиты:
→ Upgrade на Pro ($20/мес)

Pro окупается если:
✓ Работаешь 1+ час в день
✓ Создаёшь много компонентов
✓ Часто спрашиваешь AI
✓ Нужны лучшие модели

Экономия времени с Pro > $20/мес!
```

---

## 🔟 **PRIVACY И БЕЗОПАСНОСТЬ**

### **Что Cursor отправляет в облако:**

```
При privacyMode = false (по умолчанию):

✅ Отправляется:
- Твой код (для AI анализа)
- Промпты в чате
- Файлы которые ты @mentions
- Контекст проекта

❌ НЕ отправляется:
- Файлы вне проекта
- История Git
- Environment variables
- Файлы из .gitignore
```

---

### **Best Practices безопасности:**

#### **1. НЕ пиши секреты в коде**

```javascript
❌ ПЛОХО:
const API_KEY = "sk-1234567890abcdef"; // ← Cursor видит это!

✅ ХОРОШО:
const API_KEY = process.env.VITE_API_KEY; // ← В .env файле

// .env
VITE_API_KEY=sk-1234567890abcdef

// .gitignore
.env
```

#### **2. Используй .cursorignore**

```
// .cursorignore (аналог .gitignore для Cursor)

# Не показывать Cursor эти файлы:
.env
.env.local
secrets/
config/production.js
```

#### **3. Privacy Mode для чувствительных проектов**

```
Settings → Privacy Mode → Enable

Но учти: AI будет работать ХУЖЕ!

Используй только для:
- Корпоративных проектов
- Работы с секретами
- Конфиденциальной информации
```

---

## 1️⃣1️⃣ **ПРОДВИНУТЫЕ ТЕХНИКИ**

### **Техника 1: Multi-step Task Decomposition**

```
Для сложных задач разбивай на шаги:

Cmd+I (Composer) → "
Задача: Создать полный feature статистики

ПЛАН:
Phase 1: Data Layer
- useStatistics.js хук
- statistics.utils.js утилиты

Phase 2: UI Components
- StatCard.jsx карточка
- StatChart.jsx график

Phase 3: Main Component
- Statistics.jsx главный

Phase 4: Integration
- Интегрировать в App.jsx

Начни с Phase 1"

После Phase 1: "Отлично! Теперь Phase 2"
```

---

### **Техника 2: Test-Driven Development с AI**

```
1. Попроси создать тесты СНАЧАЛА:

Cmd+L → "Создай тесты для будущего компонента Timer:
- должен рендерить время
- должен вызывать onStart при клике
- должен показывать правильное форматирование"

2. ЗАТЕМ создай компонент:

Cmd+L → "Создай Timer.jsx который пройдёт эти тесты:
@Timer.test.jsx"

AI создаст компонент который точно работает!
```

---

### **Техника 3: Pair Programming с AI**

```
"Давай сделаем пару программирование:

Я буду писать код, ты его review и предлагай улучшения:

Я пишу:
const Timer = () => {
  const [time, setTime] = useState(0);
  setInterval(() => setTime(time + 1), 1000);
  return <div>{time}</div>;
}

Твой turn: что не так?"

AI: "Проблемы:
1. setInterval вызывается каждый render!
2. Нет cleanup
3. time в closure устаревает

Предлагаю: [улучшенный код]"
```

---

### **Техника 4: AI Code Review**

```
Перед git commit:

Cmd+L → "Сделай code review для @Timer.jsx

Проверь:
✓ Best practices
✓ Performance
✓ Accessibility
✓ Potential bugs
✓ Security
✓ Maintainability

Оцени по шкале 1-10 и предложи улучшения"

AI даст детальный feedback!
```

---

## 1️⃣2️⃣ **ОТСЛЕЖИВАНИЕ ПРОГРЕССА**

### **Метрики для Zero Coder:**

```
НЕДЕЛЯ 1:
□ Установил Cursor
□ Настроил .cursorrules
□ Создал первый компонент
□ Использовал Cmd+L, Cmd+K
□ Понял 10% кода

НЕДЕЛЯ 2:
□ Создал 5+ компонентов
□ Использовал Composer
□ Исправил первый баг
□ Понял 30% кода
□ Начал читать документацию

НЕДЕЛЯ 4:
□ Создал 20+ компонентов
□ Могу изменять существующий код
□ Понимаю React hooks
□ Понял 70% кода
□ Могу найти баги

НЕДЕЛЯ 8:
□ Создал полный проект
□ Могу писать простой код сам
□ Понимаю паттерны
□ Понял 90% кода
□ Начал помогать другим
```

---

### **Как отслеживать:**

```
1. GIT COMMITS
   git log --oneline --since="1 week ago" | wc -l
   → Сколько фич создал за неделю

2. CREATED FILES
   find src -type f -mtime -7
   → Какие файлы создал за неделю

3. LINES OF CODE
   git diff --stat HEAD~7
   → Сколько строк написал

4. ПОНИМАНИЕ
   Ведь дневник обучения:
   - Что узнал сегодня
   - Какие концепты понял
   - Какие проблемы решил
```

---

## 🎯 **ФИНАЛЬНЫЕ СОВЕТЫ**

### **Для максимального успеха:**

```
1. ИСПОЛЬЗУЙ .CURSORRULES
   Один раз настроил → всегда правильный код

2. НАЧИНАЙ С МАЛОГО
   Простые компоненты → сложные фичи

3. ЧИТАЙ КОД
   Понимание > слепое копирование

4. ИСПОЛЬЗУЙ GIT
   Каждая фича = коммит

5. ЗАДАВАЙ ВОПРОСЫ
   AI не кусается, спрашивай всё!

6. ПРОВЕРЯЙ В БРАУЗЕРЕ
   Код выглядит ок != код работает

7. УЧИСЬ НА ОШИБКАХ
   Каждый баг = урок

8. НЕ СПЕШИ
   Качество > скорость (поначалу)

9. ИСПОЛЬЗУЙ КОНТЕКСТ
   @files делают AI умнее

10. ПРАКТИКУЙ КАЖДЫЙ ДЕНЬ
    30 минут/день > 3 часа раз в неделю
```

---

## 📚 **ЧТО ДАЛЬШЕ?**

### **Ты освоил Cursor если:**

```
✅ Понимаешь разницу между Chat, Cmd+K, Composer
✅ Используешь .cursorrules автоматически
✅ Знаешь когда добавлять @context
✅ Можешь создать компонент за 2 минуты
✅ Можешь найти и исправить баг
✅ Понимаешь 70%+ сгенерированного кода
✅ Используешь Git для безопасности
✅ Проверяешь код перед Accept
```

---

### **Следующий уровень:**

```
1. ИЗУЧИ REACT ГЛУБЖЕ
   - Lifecycle компонентов
   - Context API
   - Custom hooks
   - Performance оптимизация

2. ИЗУЧИ TYPESCRIPT
   - Типизация
   - Interfaces
   - Generics

3. ИЗУЧИ ТЕСТИРОВАНИЕ
   - Unit тесты
   - Integration тесты
   - E2E тесты

4. ИЗУЧИ BACKEND
   - Node.js
   - Express
   - Databases

5. НАЧНИ ПИСАТЬ КОД САМ
   - Используй AI как помощника
   - Не полагайся на него 100%
   - Ты архитектор, AI - строитель
```

---

## 🎉 **ПОЗДРАВЛЯЮ!**

Ты теперь знаешь **ВСЁ** что нужно Zero Coder'у для работы в Cursor!

**У тебя есть:**

- ✅ Полное понимание всех фич Cursor
- ✅ .cursorrules для автоматизации
- ✅ MCP для доступа к документации
- ✅ Расширения для продуктивности
- ✅ Техники промптинга
- ✅ Debugging strategies
- ✅ Best practices
- ✅ Safety guidelines

**Теперь:**

1. Открой Cursor
2. Создай свой первый проект
3. Используй все знания
4. Наслаждайся процессом!

---

## 💪 **ТЫ ГОТОВ СОЗДАТЬ КРУТОЕ ПРИЛОЖЕНИЕ!**

**С Cursor + эти знания = Супер сила разработчика!** ⚡

**Удачи! Ты справишься!** 🚀

---

## 📞 **ЕСЛИ НУЖНА ПОМОЩЬ**

Возвращайся к этим файлам:

- **cursor-zero-coder-guide.md** - основы Cursor
- **CURSOR_SETUP_GUIDE.md** - установка конфигурации
- **CURSOR_QUICK_REF.md** - быстрая справка
- **CURSOR_EXTENSIONS_GUIDE.md** - расширения
- **Этот файл** - продвинутые техники

**И помни: каждый Pro developer когда-то был Zero Coder!** 💪
