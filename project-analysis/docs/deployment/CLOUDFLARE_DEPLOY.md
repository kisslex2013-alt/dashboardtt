# 🚀 Автоматический деплой на Cloudflare Pages

Полностью автоматизированный процесс деплоя React-приложения на Cloudflare Pages с очисткой старых версий и включением аналитики.

## 📋 Требования

1. **Cloudflare аккаунт** с доступом к Pages
2. **API Token** с правами на Cloudflare Pages
3. **Node.js** и npm установлены
4. **Собранный проект** (папка `dist`)

## 🔧 Настройка

### 1. Установка зависимостей

```bash
npm install
```

Убедитесь, что установлены:

- `archiver` - для создания ZIP архивов
- `node-fetch` - для HTTP запросов
- `form-data` - для загрузки файлов
- `dotenv` - для работы с переменными окружения

### 2. Создание .env файла

Создайте файл `.env` в корне проекта на основе `env.example.txt`:

```env
CF_ACCOUNT_ID=your_account_id_here
CF_API_TOKEN=your_api_token_here
CF_PROJECT_NAME=dashboardtt
```

#### Где найти Account ID:

1. Откройте https://dash.cloudflare.com
2. В правом нижнем углу найдите "Account ID"

#### Как создать API Token:

1. Откройте https://dash.cloudflare.com/profile/api-tokens
2. Нажмите "Create Token"
3. Выберите шаблон "Edit Cloudflare Workers" или создайте кастомный:
   - **Permissions**: Account → Cloudflare Pages → Edit
   - **Account Resources**: Include → All accounts (или конкретный аккаунт)
4. Скопируйте созданный токен

### 3. Создание проекта Cloudflare Pages

**Вариант 1: Через веб-интерфейс (рекомендуется для первого раза)**

1. Откройте https://dash.cloudflare.com → Pages
2. Нажмите "Create a project"
3. Выберите "Upload assets directly"
4. Назовите проект: `dashboardtt` (или другое имя, укажите в `.env`)
5. Выберите папку `dist` и нажмите "Deploy site"

**Вариант 2: Через API (автоматически)**
Скрипт попытается найти проект автоматически. Если проект не существует, вы получите инструкции для ручного создания.

## 🚀 Использование

### Простой деплой

```bash
npm run deploy
```

Эта команда автоматически:

1. ✅ Обновит версию в `package.json` и `App.jsx`
2. ✅ Обновит `changelog.md` новой записью
3. ✅ Скопирует `changelog.md` в `public/changelog/changelog.md`
4. ✅ Скопирует `plans.md` в `public/plans.md`
5. ✅ Соберет проект (`npm run build`)
6. ✅ Создаст ZIP архив из `dist`
7. ✅ Задеплоит на Cloudflare Pages
8. ✅ Удалит старые деплои (кроме последнего)
9. ✅ Проверит статус Web Analytics

### Отдельные команды

```bash
# Только подготовка (обновление версии, changelog, plans)
npm run predeploy

# Только сборка
npm run build

# Только деплой (требует собранный dist)
node scripts/deploy-to-cloudflare.js
```

## 📊 Web Analytics

После первого деплоя нужно включить Web Analytics:

1. Откройте https://dash.cloudflare.com
2. Перейдите в **Web Analytics** → **Add site**
3. Выберите ваш проект (`dashboardtt`)
4. Нажмите **Activate**

Скрипт автоматически проверит статус аналитики при каждом деплое.

## 🔍 Проверка результата

После успешного деплоя вы увидите:

```
═══════════════════════════════════════════════════════
✨ ДЕПЛОЙ ЗАВЕРШЕН УСПЕШНО!
═══════════════════════════════════════════════════════
🌐 URL проекта: https://dashboardtt.pages.dev
📊 Web Analytics: Активна (ID: ...)
═══════════════════════════════════════════════════════
```

## ⚠️ Устранение проблем

### Ошибка: "dist/index.html не найден"

**Решение**: Запустите `npm run build` перед деплоем

### Ошибка: "Проект не существует"

**Решение**: Создайте проект вручную через веб-интерфейс Cloudflare (см. раздел "Создание проекта")

### Ошибка: "Cloudflare API Error"

**Проверьте**:

- Правильность `CF_ACCOUNT_ID` в `.env`
- Правильность `CF_API_TOKEN` в `.env`
- Права API токена (нужны права на Cloudflare Pages → Edit)

### Ошибка: "Не удалось создать архив"

**Решение**: Убедитесь, что установлен пакет `archiver`:

```bash
npm install archiver
```

## 📝 Структура файлов

```
time-tracker/
├── .env                    # Конфигурация (не коммитится в git)
├── env.example.txt        # Пример конфигурации
├── scripts/
│   ├── predeploy.js      # Подготовка к деплою
│   └── deploy-to-cloudflare.js  # Деплой на Cloudflare
└── package.json           # Скрипты: deploy, predeploy
```

## 🔄 Автоматическое обновление версии

При каждом деплое автоматически:

- Увеличивается patch версия (1.2.0 → 1.2.1)
- Обновляется `package.json`
- Обновляется версия в `App.jsx`
- Добавляется запись в `changelog.md`
- Копируются файлы в `public/` для загрузки в браузере

## 🎯 Что происходит при деплое

1. **predeploy.js**:
   - Увеличивает версию (patch)
   - Обновляет changelog.md
   - Копирует changelog.md → public/changelog/changelog.md
   - Копирует plans.md → public/plans.md

2. **build**:
   - Обновляет build версию в App.jsx
   - Собирает проект в dist/

3. **deploy-to-cloudflare.js**:
   - Проверяет наличие dist/index.html
   - Создает ZIP архив из dist/
   - Деплоит на Cloudflare Pages
   - Удаляет старые деплои
   - Проверяет Web Analytics

## 📚 Дополнительная информация

- [Cloudflare Pages API](https://developers.cloudflare.com/pages/platform/api/)
- [Cloudflare Web Analytics](https://developers.cloudflare.com/analytics/web-analytics/)
