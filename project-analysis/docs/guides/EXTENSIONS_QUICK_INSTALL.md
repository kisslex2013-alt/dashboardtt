# ⚡ Быстрая установка Cursor Extensions

**Время:** 10 минут

---

## 📋 Список расширений для установки

Скопируйте ID и установите каждое:

```
dsznajder.es7-react-js-snippets
bradlc.vscode-tailwindcss
usernamehw.errorlens
formulahendry.auto-rename-tag
esbenp.prettier-vscode
eamodio.gitlens
naumovs.color-highlight
```

---

## 🚀 Как установить

### Шаг 1: Откройте Extensions

- Нажмите `Ctrl + Shift + X`
- Или: меню → Extensions

### Шаг 2: Установите каждое расширение

Для каждого ID из списка выше:

1. Вставьте ID в поле поиска
2. Нажмите "Install"
3. Дождитесь установки
4. Перейдите к следующему

### Шаг 3: Проверка

После установки:

- Откройте любой `.jsx` файл
- Начните печатать `rafce` → должна появиться подсказка
- Нажмите Tab → должен создать компонент автоматически

---

## ✅ Что дает каждое расширение

| Расширение                | Что делает                                          |
| ------------------------- | --------------------------------------------------- |
| **ES7+ React Snippets**   | `rafce` + Tab = готовый компонент за 2 секунды      |
| **Tailwind IntelliSense** | Автодополнение классов, превью цветов при наведении |
| **Error Lens**            | Ошибки прямо в коде, не нужно открывать консоль     |
| **Auto Rename Tag**       | Переименовал `<div>` → автоматически `</div>`       |
| **Prettier**              | Автоформатирование при сохранении файла             |
| **GitLens**               | Git визуально: история, кто менял строку            |
| **Color Highlight**       | Видишь цвета в коде (#FF0000 отображается красным)  |

---

## 🎯 Полезные сниппеты (после установки)

После установки `es7-react-js-snippets` вы можете использовать:

**Компоненты:**

- `rafce` → React Arrow Function Component Export (самый популярный)
- `rafc` → React Arrow Function Component
- `rfc` → React Function Component

**Хуки:**

- `useState` + Tab → useState hook
- `useEffect` + Tab → useEffect hook
- `useMemo` + Tab → useMemo hook
- `useCallback` + Tab → useCallback hook

**Попробуйте:**

1. Откройте новый файл `.jsx`
2. Напечатайте `rafce` + Tab
3. Увидите готовый компонент!

---

## ⚙️ Настройки уже готовы

Файл `.vscode/settings.json` уже создан и настроен:

- ✅ Автоформатирование при сохранении
- ✅ Prettier как форматтер по умолчанию
- ✅ Tailwind IntelliSense активирован
- ✅ Error Lens включен

**Ничего дополнительно настраивать не нужно!**

---

## ❓ Проблемы?

**Расширение не устанавливается:**

- Проверьте интернет соединение
- Перезапустите Cursor
- Попробуйте установить через командную строку: `code --install-extension [ID]`

**Сниппеты не работают:**

- Убедитесь что расширение установлено
- Перезапустите Cursor
- Попробуйте в новом файле `.jsx`

**Автоформатирование не работает:**

- Проверьте что Prettier установлен
- Проверьте что `.vscode/settings.json` существует
- Нажмите `Shift + Alt + F` (форматировать вручную)

---

**Готово!** После установки расширения работают автоматически.
