# 🧩 **CURSOR EXTENSIONS ДЛЯ ZERO CODER**

**Специально для Time Tracker HTML → React Migration**  
**Версия:** v2.0 | **Дата:** 28.10.2025

---

## 🎯 **БЫСТРЫЙ ОТВЕТ**

### **Установи эти 5 расширений СЕЙЧАС:**

1. ⭐ **ES7+ React Snippets** - быстрое создание компонентов
2. ⭐ **Tailwind CSS IntelliSense** - автодополнение Tailwind
3. ⭐ **Prettier** - автоформатирование
4. ⭐ **ESLint** - поиск ошибок
5. ⭐ **Auto Rename Tag** - переименование тегов

**Время установки:** 5 минут  
**Эффект:** Работаешь в 3x быстрее! ⚡

---

## 📋 **ПОЛНЫЙ СПИСОК РАСШИРЕНИЙ**

### **⭐ ОБЯЗАТЕЛЬНЫЕ (Must Have) - 5 штук**

#### **1. ES7+ React/Redux/React-Native snippets**

```
ID: dsznajder.es7-react-js-snippets
Размер: ~100KB
Рейтинг: ⭐⭐⭐⭐⭐ 14M+ установок
```

**Что даёт:**

- ⚡ Snippets для React компонентов
- ⚡ Snippets для хуков
- ⚡ Snippets для импортов

**Примеры использования:**

```javascript
// Напечатай: rafce + Tab
// Получишь полный компонент:
import React from 'react'

const ComponentName = () => {
  return <div>ComponentName</div>
}

export default ComponentName

// ───────────────────────────

// Напечатай: useState + Tab
const [state, setState] = useState(initialState)

// Напечатай: useEffect + Tab
useEffect(() => {}, [])

// Напечатай: useMemo + Tab
const memoizedValue = useMemo(() => computeExpensiveValue(a, b), [a, b])
```

**Все полезные сниппеты:**

```
rafce    → React Arrow Function Component Export
rafcp    → React Arrow Function Component with PropTypes
rafc     → React Arrow Function Component
rfc      → React Function Component
useState → useState hook
useEffect → useEffect hook
useMemo  → useMemo hook
useCallback → useCallback hook
useContext → useContext hook
useRef   → useRef hook
```

**Почему ОБЯЗАТЕЛЬНО:**  
Создание компонента за 2 секунды вместо 2 минут!

---

#### **2. Tailwind CSS IntelliSense**

```
ID: bradlc.vscode-tailwindcss
Размер: ~2MB
Рейтинг: ⭐⭐⭐⭐⭐ 5M+ установок
```

**Что даёт:**

- 🎨 Автодополнение классов Tailwind
- 🎨 Превью цветов при наведении
- 🎨 Lint неправильных классов
- 🎨 Hover подсказки

**Примеры:**

```jsx
// Начни печатать:
className="bg-"

// Увидишь полный список:
bg-white
bg-black
bg-transparent
bg-current
bg-slate-50
bg-slate-100
...
bg-white/10  ← для glassmorphism!
bg-white/20
...

// ───────────────────────────

// Наведи на класс:
className="bg-blue-500"
          ↑ Видишь превью: синий квадрат + HEX код

// ───────────────────────────

// Responsive автодополнение:
className="sm:"  → видишь все sm: варианты
className="md:"  → видишь все md: варианты
className="hover:" → видишь все hover: варианты
className="dark:" → видишь все dark: варианты
```

**Фишки:**

```jsx
// Множественные варианты:
className="
  bg-white/10
  dark:bg-white/5
  hover:bg-white/15
  sm:p-4
  md:p-6
  lg:p-8
"
↑ Автодополнение для каждого!

// Сортировка классов:
Cmd+Shift+P → "Tailwind CSS: Sort Classes"
→ Классы автоматически отсортируются по conventions
```

**Почему ОБЯЗАТЕЛЬНО:**  
Без него Tailwind = ад! Это как Google для классов.

---

#### **3. Prettier - Code formatter**

```
ID: esbenp.prettier-vscode
Размер: ~5MB
Рейтинг: ⭐⭐⭐⭐⭐ 35M+ установок
```

**Что даёт:**

- 📝 Автоформатирование при сохранении
- 📝 Консистентный стиль
- 📝 Форматирование JSX

**Настройка:**

1. **В Cursor Settings (Cmd+,):**

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "[javascript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  },
  "[javascriptreact]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```

2. **Создай .prettierrc в корне проекта:**

```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 80,
  "tabWidth": 2,
  "useTabs": false,
  "jsxSingleQuote": false,
  "bracketSpacing": true,
  "arrowParens": "avoid",
  "endOfLine": "lf"
}
```

**Пример работы:**

```javascript
// Напечатал вот так:
const Button = ({ onClick, children }) => {
  return <button onClick={onClick}>{children}</button>
}

// Нажал Cmd+S → Prettier отформатировал:
const Button = ({ onClick, children }) => {
  return <button onClick={onClick}>{children}</button>
}
```

**Почему ОБЯЗАТЕЛЬНО:**  
Не думаешь об отступах, скобках, точках с запятой!

---

#### **4. ESLint**

```
ID: dbaeumer.vscode-eslint
Размер: ~1MB
Рейтинг: ⭐⭐⭐⭐⭐ 25M+ установок
```

**Что даёт:**

- 🔍 Находит ошибки в реальном времени
- 🔍 Подсказывает best practices
- 🔍 Автофикс многих проблем

**Что увидишь:**

```javascript
// ❌ ОШИБКА (красная волнистая линия):
const [count, setCount] = useState(0)
count = 5 // Assignment to constant variable

// ⚠️ ПРЕДУПРЕЖДЕНИЕ (жёлтая волнистая):
const unused = 'value' // 'unused' is assigned but never used

// ✅ Quick Fix:
// Наведи курсор → Cmd+. → "Fix all auto-fixable problems"
```

**Создай .eslintrc.json в корне:**

```json
{
  "extends": ["react-app", "react-app/jest"],
  "rules": {
    "no-console": "warn",
    "no-var": "error",
    "prefer-const": "error",
    "react-hooks/rules-of-hooks": "error",
    "react-hooks/exhaustive-deps": "warn"
  }
}
```

**Типичные ошибки которые ловит:**

```javascript
// 1. Мутация props
const Component = ({ data }) => {
  data.push('new') // ❌ ESLint: Do not mutate props
}

// 2. Неправильные хуки
if (condition) {
  useState(0) // ❌ ESLint: Hooks can't be inside conditions
}

// 3. Missing dependencies
useEffect(() => {
  console.log(count) // ⚠️ ESLint: 'count' should be in dependencies
}, [])

// 4. Unused variables
const unused = 'value' // ⚠️ ESLint: Unused variable
```

**Почему ОБЯЗАТЕЛЬНО:**  
Ловит 90% ошибок ДО запуска кода!

---

#### **5. Auto Rename Tag**

```
ID: formulahendry.auto-rename-tag
Размер: ~100KB
Рейтинг: ⭐⭐⭐⭐⭐ 13M+ установок
```

**Что даёт:**

- 🏷️ Автопереименование парных тегов
- 🏷️ Работает с HTML и JSX
- 🏷️ Экономит кучу времени

**Как работает:**

```jsx
// Было:
<div>
  <p>Content</p>
</div>

// Меняешь div на section (только открывающий тег):
<section>
  <p>Content</p>
</div>

// Закрывающий тег АВТОМАТИЧЕСКИ меняется:
<section>
  <p>Content</p>
</section>

// ───────────────────────────

// Работает и с React компонентами:
<Button>Click</Button>

// Меняешь на Link:
<Link>Click</Link>
// ↑ Автоматически!
```

**Почему ОБЯЗАТЕЛЬНО:**  
Без него постоянно забываешь менять закрывающий тег!

---

## 🌟 **РЕКОМЕНДУЕМЫЕ (Strongly Recommended) - 5 штук**

#### **6. Error Lens**

```
ID: usernamehw.errorlens
Размер: ~200KB
Рейтинг: ⭐⭐⭐⭐⭐ 3M+ установок
```

**Что даёт:**

- 👁️ Ошибки видны СРАЗУ в строке кода
- 👁️ Не нужно наводить мышкой
- 👁️ Цветовое выделение проблем

**Без Error Lens:**

```javascript
const [count, setCount] = useState(0)
count = 5 // Красная волнистая (нужно навести чтобы увидеть ошибку)
```

**С Error Lens:**

```javascript
const [count, setCount] = useState(0)
count = 5 // ⛔ Assignment to constant variable. [ВИДНО СРАЗУ!]
```

**Настройка:**

```json
{
  "errorLens.enabled": true,
  "errorLens.fontSize": "12",
  "errorLens.onSave": true,
  "errorLens.messageBackgroundMode": "line"
}
```

**Почему РЕКОМЕНДУЕТСЯ:**  
Видишь проблемы не отвлекаясь от кода!

---

#### **7. Color Highlight**

```
ID: naumovs.color-highlight
Размер: ~50KB
Рейтинг: ⭐⭐⭐⭐ 2M+ установок
```

**Что даёт:**

- 🎨 Подсвечивает цвета в коде
- 🎨 Видишь цвет визуально
- 🎨 Работает с hex, rgb, rgba, Tailwind

**Примеры:**

```javascript
// Видишь цвета прямо в коде:
const primary = '#3b82f6' // ← Фон окрашен в синий
const success = 'rgb(16, 185, 129)' // ← Фон окрашен в зелёный
const danger = 'rgba(239, 68, 68, 0.2)' // ← Фон окрашен в красный (прозрачный)

// В Tailwind:
className = 'bg-blue-500' // ← blue-500 подсвечен синим
className = 'text-red-400' // ← red-400 подсвечен красным
className = 'border-green-300' // ← green-300 подсвечен зелёным
```

**Для glassmorphism особенно полезно:**

```jsx
className = 'bg-white/10' // ← Видишь насколько прозрачный!
className = 'bg-blue-500/20' // ← Видишь оттенок и прозрачность!
```

**Почему РЕКОМЕНДУЕТСЯ:**  
Для дизайна с цветами = must have!

---

#### **8. GitLens**

```
ID: eamodio.gitlens
Размер: ~10MB
Рейтинг: ⭐⭐⭐⭐⭐ 24M+ установок
```

**Что даёт:**

- 📜 История изменений прямо в коде
- 📜 Кто и когда менял строку
- 📜 Сравнение версий

**Что увидишь:**

```javascript
const Button = ({ onClick }) => {
  // 👤 You, 2 hours ago - feat: create Button
  return (
    // 👤 You, just now - fix: add onClick
    <button onClick={onClick}> // 👤 You, just now - fix: add onClick Click me</button>
  )
}
```

**Полезные команды:**

```
Cmd+Shift+P → "GitLens: Show File History"
→ Видишь все изменения файла

Cmd+Shift+P → "GitLens: Toggle Line Blame"
→ Включить/выключить аннотации

Cmd+Shift+P → "GitLens: Compare File with Previous Revision"
→ Сравнить с прошлой версией
```

**Почему РЕКОМЕНДУЕТСЯ:**  
Понимаешь ЧТО и ЗАЧЕМ менял!

---

#### **9. Import Cost**

```
ID: wix.vscode-import-cost
Размер: ~500KB
Рейтинг: ⭐⭐⭐⭐ 1M+ установок
```

**Что даёт:**

- 📦 Показывает размер импортов
- 📦 Помогает оптимизировать bundle
- 📦 Предупреждает о тяжёлых библиотеках

**Пример:**

```javascript
import React from 'react' // 2.3kB (gzip) ✅
import { LineChart } from 'recharts' // 45kB (gzip) ⚠️
import { Button } from './Button' // 1.2kB (gzip) ✅

// Тяжёлые импорты выделены:
import moment from 'moment' // 231kB (gzip) ⛔ ТЯЖЁЛЫЙ!
// Может лучше date-fns или day.js?

import lodash from 'lodash' // 71kB (gzip) ⚠️
import _ from 'lodash' // То же самое
// Лучше так:
import debounce from 'lodash/debounce' // 2.3kB ✅
```

**Почему РЕКОМЕНДУЕТСЯ:**  
Bundle не раздувается, приложение быстрее грузится!

---

#### **10. Path Intellisense**

```
ID: christian-kohler.path-intellisense
Размер: ~100KB
Рейтинг: ⭐⭐⭐⭐ 9M+ установок
```

**Что даёт:**

- 📁 Автодополнение путей
- 📁 Видишь структуру проекта
- 📁 Меньше ошибок в импортах

**Пример:**

```javascript
// Начни печатать:
import Button from './c'

// Увидишь dropdown:
./components/
./components/Button.jsx
./components/ui/
./constants.js

// Выбери нужное → Tab → вставится путь
import Button from './components/Button.jsx';
```

**Настройка:**

```json
{
  "path-intellisense.autoSlashAfterDirectory": true,
  "path-intellisense.extensionOnImport": true,
  "path-intellisense.showHiddenFiles": false
}
```

**Почему РЕКОМЕНДУЕТСЯ:**  
Не ошибаешься в путях, быстрее пишешь импорты!

---

## 💎 **ПОЛЕЗНЫЕ (Nice to Have) - 3 штуки**

#### **11. Console Ninja**

```
ID: wallabyjs.console-ninja
Размер: ~2MB
Рейтинг: ⭐⭐⭐⭐ 500K+ установок
```

**Что даёт:**

- 🐛 console.log результаты в редакторе
- 🐛 Не нужно смотреть в браузер
- 🐛 Отладка быстрее

**Пример:**

```javascript
const formatTime = seconds => {
  console.log('input:', seconds) // → input: 3661 [показано справа]

  const hours = Math.floor(seconds / 3600)
  console.log('hours:', hours) // → hours: 1 [показано справа]

  const minutes = Math.floor((seconds % 3600) / 60)
  console.log('minutes:', minutes) // → minutes: 1 [показано справа]

  return `${hours}:${minutes}:${seconds % 60}`
}
```

**Почему NICE TO HAVE:**  
Удобно, но можно жить и без этого!

---

#### **12. Better Comments**

```
ID: aaron-bond.better-comments
Размер: ~100KB
Рейтинг: ⭐⭐⭐⭐ 3M+ установок
```

**Что даёт:**

- 📝 Цветные категоризованные комментарии
- 📝 Легче найти TODO/FIXME
- 📝 Красивее выглядит

**Пример:**

```javascript
// TODO: Добавить валидацию (оранжевый)
// FIXME: Утечка памяти в useEffect (красный)
// NOTE: Это временное решение (синий)
// * Важная информация (зелёный)
// ? Вопрос для обсуждения (фиолетовый)
// ! Deprecated функция (красный жирный)
```

**Почему NICE TO HAVE:**  
Красиво и удобно, но не обязательно!

---

#### **13. Material Icon Theme**

```
ID: pkief.material-icon-theme
Размер: ~2MB
Рейтинг: ⭐⭐⭐⭐⭐ 12M+ установок
```

**Что даёт:**

- 🎨 Красивые иконки для типов файлов
- 🎨 Легче ориентироваться
- 🎨 Приятнее работать

**Что увидишь в проводнике:**

```
⚛️ App.jsx           (React компонент)
⚛️ Button.jsx        (React компонент)
🪝 useTimer.js       (Custom hook)
🛠️ formatters.js    (Utilities)
🎨 styles.css        (Styles)
📦 package.json      (NPM)
⚙️ .cursorrules      (Config)
🔧 vite.config.js    (Vite config)
```

**Почему NICE TO HAVE:**  
Просто красиво, не влияет на функциональность!

---

## ❌ **НЕ НУЖНЫЕ (для твоего случая)**

### **Что НЕ устанавливать и почему:**

#### **1. GitHub Copilot** ❌

```
Причины НЕ устанавливать:
✗ У тебя есть Cursor AI (лучше Copilot!)
✗ Дублирование функциональности
✗ Может конфликтовать с Cursor
✗ Платный ($10/месяц)

Cursor AI уже делает всё что Copilot + больше!
```

---

#### **2. Live Server** ❌

```
Причины НЕ устанавливать:
✗ У тебя Vite dev server (лучше!)
✗ Live Server для статики, не для React
✗ Vite даёт hot module replacement
✗ Vite быстрее

Используй: npm run dev (Vite)
```

---

#### **3. Debugger for Chrome** ❌

```
Причины НЕ устанавливать:
✗ React DevTools в браузере достаточно
✗ console.log работает отлично
✗ Для zero-coder это overkill
✗ Сложная настройка

Используй: React DevTools + console.log
```

---

#### **4. TypeScript extensions** ❌

```
Причины НЕ устанавливать:
✗ Ты используешь JavaScript
✗ TypeScript добавит сложности
✗ Не для zero-coder на старте
✗ Нужно учить новый синтаксис

Начинай с JavaScript, TypeScript потом!
```

---

#### **5. Redux DevTools** ❌

```
Причины НЕ устанавливать:
✗ Нет Redux в проекте
✗ Используешь useState/useContext
✗ Не нужен для Time Tracker
✗ Добавляет ненужный вес

Тебе не нужен Redux для этого проекта!
```

---

## 🎯 **ИТОГОВАЯ РЕКОМЕНДАЦИЯ**

### **Установи ТРИ НАБОРА:**

#### **НАБОР 1: Обязательные (СЕЙЧАС - 5 мин)**

```
1. ⭐ ES7+ React/Redux/React-Native snippets
2. ⭐ Tailwind CSS IntelliSense
3. ⭐ Prettier
4. ⭐ ESLint
5. ⭐ Auto Rename Tag
```

**Эффект:** Работаешь в 3x быстрее! ⚡

---

#### **НАБОР 2: Рекомендуемые (через неделю - 5 мин)**

```
6. 🌟 Error Lens
7. 🌟 Color Highlight
8. 🌟 GitLens
9. 🌟 Import Cost
10. 🌟 Path Intellisense
```

**Эффект:** Работаешь ещё быстрее, меньше ошибок! 🚀

---

#### **НАБОР 3: Опциональные (по желанию - 3 мин)**

```
11. 💎 Console Ninja
12. 💎 Better Comments
13. 💎 Material Icon Theme
```

**Эффект:** Приятнее работать! ✨

---

## 📥 **КАК УСТАНОВИТЬ**

### **Метод 1: Через Extensions панель**

```bash
1. Открой Cursor
2. Cmd+Shift+X (Mac) / Ctrl+Shift+X (Windows)
3. В поиске: "ES7 React snippets"
4. Кликни "Install"
5. Повтори для остальных
```

---

### **Метод 2: Через Command Palette (быстрее)**

```bash
1. Cmd+Shift+P
2. Напечатай: "Extensions: Install Extensions"
3. Вбей ID расширения (например: dsznajder.es7-react-js-snippets)
4. Enter
```

---

### **Метод 3: Все сразу (копируй-вставляй)**

Создай `.vscode/extensions.json`:

```json
{
  "recommendations": [
    "dsznajder.es7-react-js-snippets",
    "bradlc.vscode-tailwindcss",
    "esbenp.prettier-vscode",
    "dbaeumer.vscode-eslint",
    "formulahendry.auto-rename-tag",
    "usernamehw.errorlens",
    "naumovs.color-highlight",
    "eamodio.gitlens",
    "wix.vscode-import-cost",
    "christian-kohler.path-intellisense"
  ]
}
```

Затем:

```
Cmd+Shift+P → "Extensions: Show Recommended Extensions"
→ Кликни "Install All Workspace Extension Recommendations"
```

---

## ⚙️ **ФИНАЛЬНЫЕ НАСТРОЙКИ**

### **Добавь в settings.json:**

```json
{
  // === ФОРМАТИРОВАНИЕ ===
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",

  // === ESLINT ===
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "eslint.validate": ["javascript", "javascriptreact"],

  // === TAILWIND ===
  "tailwindCSS.emmetCompletions": true,
  "tailwindCSS.includeLanguages": {
    "javascript": "javascript",
    "javascriptreact": "javascriptreact"
  },

  // === ERROR LENS ===
  "errorLens.enabled": true,
  "errorLens.fontSize": "12",

  // === PATH INTELLISENSE ===
  "path-intellisense.autoSlashAfterDirectory": true,

  // === AUTO RENAME TAG ===
  "auto-rename-tag.activationOnLanguage": ["html", "javascript", "javascriptreact"],

  // === FORM AT ON SAVE ===
  "[javascript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  },
  "[javascriptreact]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```

---

## 📊 **ЭФФЕКТ ОТ РАСШИРЕНИЙ**

### **Создание компонента:**

**БЕЗ расширений:**

```
1. Создать файл (10 сек)
2. Написать import React (5 сек)
3. Написать функцию (20 сек)
4. Написать PropTypes (30 сек)
5. Написать export (5 сек)
6. Форматировать вручную (20 сек)
7. Искать ошибки (30 сек)

ИТОГО: 2 минуты
```

**С расширениями:**

```
1. Создать файл (10 сек)
2. rafcp + Tab (2 сек) → полная структура
3. Написать JSX с автодополнением (15 сек)
4. Cmd+S (автоформат Prettier) (1 сек)
5. ESLint показал ошибки сразу (0 сек)

ИТОГО: 30 секунд
```

**Экономия: 75%!** ⚡

---

## ✅ **ФИНАЛЬНЫЙ ЧЕКЛИСТ**

### **Установка (делай СЕЙЧАС):**

```
□ Установил 5 обязательных расширений
□ Настроил formatOnSave
□ Создал .prettierrc
□ Создал .eslintrc.json
□ Создал .vscode/settings.json
□ Перезапустил Cursor
□ Протестировал: создал компонент с rafcp
□ Проверил: автоформатирование работает (Cmd+S)
□ Проверил: Tailwind автодополнение работает
□ Проверил: ESLint показывает ошибки
```

### **Дополнительно (через неделю):**

```
□ Установил 5 рекомендуемых
□ Привык к Error Lens
□ Использую Color Highlight для цветов
□ Смотрю GitLens для истории
□ Слежу за Import Cost
□ Использую Path Intellisense
```

---

## 🚀 **ИТОГО**

### **Твой идеальный набор расширений:**

```
⭐ Обязательные: 5 расширений
🌟 Рекомендуемые: 5 расширений
💎 Опциональные: 3 расширения

ВСЕГО: 10-13 расширений
(не перегружаем Cursor!)
```

### **Что получаешь:**

✅ **Скорость:** Работаешь в 3-5x быстрее
✅ **Качество:** Меньше ошибок, красивый код
✅ **Комфорт:** Приятнее работать
✅ **Обучение:** Видишь ошибки, учишься быстрее

---

## 💪 **НАЧНИ ПРЯМО СЕЙЧАС!**

```bash
1. Установи 5 обязательных (5 минут)
2. Создай тестовый компонент (rafcp + Tab)
3. Проверь что всё работает
4. Начинай миграцию с новыми инструментами!
```

**С этими расширениями ты работаешь как PRO!** 🎯

**Удачи с миграцией!** 🚀
