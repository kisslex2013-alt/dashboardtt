# Time Tracker Dashboard - Cursor AI Rules (Enhanced)

## 🎯 КОНТЕКСТ ПРОЕКТА
Это приложение для учета рабочего времени с аналитикой на React + Zustand + Tailwind CSS.
Разработчик: начинающий (zero coder) - объяснять все простым языком.

## 📚 ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Основные библиотеки:
- React 18.2+ (функциональные компоненты + хуки)
- Zustand 4.4+ (state management)
- Tailwind CSS 3.4+ (стилизация)
- Vite 5+ (сборщик)
- Recharts 2.12+ (графики)
- Tone.js 14.7+ (звуки)
- date-fns (работа с датами)
- lucide-react (иконки)
- @headlessui/react (модальные окна)

### НЕ используем:
- Redux (используем Zustand)
- CSS/SCSS файлы (только Tailwind)
- Class компоненты (только функциональные)
- PropTypes (используем TypeScript при необходимости)

## 🏗️ АРХИТЕКТУРА И СТРУКТУРА

### Структура папок:
```
src/
├── components/
│   ├── layout/      # Header, Footer, FloatingPanel
│   ├── entries/     # Компоненты записей времени
│   ├── statistics/  # Статистика и аналитика
│   ├── charts/      # Графики
│   ├── modals/      # Модальные окна
│   ├── settings/    # Настройки
│   └── ui/          # Базовые UI компоненты
├── hooks/           # Кастомные хуки
├── store/           # Zustand хранилища
├── utils/           # Утилиты
├── constants/       # Константы
├── .examples/       # Примеры для AI (не импортировать в код!)
├── App.jsx
└── main.jsx
```

### Принципы организации:
1. **Один компонент = один файл**
2. **Компоненты именуются PascalCase**: `StatisticsDashboard.jsx`
3. **Хуки именуются camelCase с префиксом use**: `useTimer.js`
4. **Утилиты именуются camelCase**: `calculations.js`
5. **Максимум 300 строк на компонент** (если больше - разделить)

---

## 🤖 БЫСТРЫЕ КОМАНДЫ ДЛЯ AI

### Когда пользователь пишет короткую команду, AI должен:

#### "компонент [название]" или "создай компонент [название]"
1. Задать вопросы:
   - В какую папку? (layout/entries/statistics/charts/modals/settings/ui)
   - Какие пропсы нужны?
   - Есть ли состояние?
2. Создать файл по шаблону из `.examples/component.example.jsx`
3. Поместить в правильную папку `src/components/[категория]/`
4. Показать пример использования

#### "хук [название]" или "создай хук [название]"
1. Задать вопросы:
   - Что должен делать хук?
   - Какие параметры принимает?
   - Что возвращает?
2. Создать файл по шаблону из `.examples/hook.example.js`
3. Поместить в `src/hooks/`
4. Показать пример использования

#### "store [название]" или "создай store [название]"
1. Задать вопросы:
   - Какие данные хранить?
   - Какие операции нужны? (CRUD, фильтрация, и т.д.)
   - Нужен ли persist в localStorage?
2. Создать файл по шаблону из `.examples/store.example.js`
3. Поместить в `src/store/`
4. Показать пример использования в компоненте

#### "утилита [название]" или "создай утилиту [название]"
1. Задать вопросы:
   - Что должна делать функция?
   - Какие параметры?
   - Что возвращает?
2. Создать файл по шаблону из `.examples/utility.example.js`
3. Поместить в `src/utils/`
4. Показать пример использования

#### "валидация [название]" или "создай валидацию [название]"
1. Задать вопросы:
   - Какие поля валидировать?
   - Какие правила? (обязательные, формат, диапазон)
2. Создать функцию по шаблону из `.examples/validation.example.js`
3. Добавить в `src/utils/validation.js` или создать новый файл
4. Показать пример использования

#### "расчет [название]" или "создай расчет [название]"
1. Задать вопросы:
   - Что нужно рассчитать?
   - Какие входные данные?
   - Какие единицы измерения?
2. Создать функцию по шаблону из `.examples/calculation.example.js`
3. Добавить в `src/utils/calculations.js` или создать новый файл
4. Показать пример использования

---

## 📋 ПРАВИЛА НАПИСАНИЯ КОДА

### React компоненты:
```javascript
// ✅ ПРАВИЛЬНО
import { useState } from 'react';
import { Button } from '../ui/Button';

export function StatCard({ title, value, icon: Icon }) {
  const [isHovered, setIsHovered] = useState(false);
  
  return (
    <div className="glass-effect rounded-xl p-6">
      <h3>{title}</h3>
      <p>{value}</p>
      {Icon && <Icon className="w-6 h-6" />}
    </div>
  );
}

// ❌ НЕПРАВИЛЬНО
import React from 'react'; // Не нужен в React 18+
const StatCard = (props) => { ... } // Используй деструктуризацию
class StatCard extends React.Component { ... } // Не используем классы
```

### Zustand хранилища:
```javascript
// ✅ ПРАВИЛЬНО
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

export const useEntriesStore = create(
  persist(
    (set, get) => ({
      entries: [],
      addEntry: (entry) => set((state) => ({
        entries: [...state.entries, entry]
      })),
    }),
    { name: 'time-tracker-entries' }
  )
);

// ❌ НЕПРАВИЛЬНО
const entriesStore = createStore(...) // Неверное имя
export default useEntriesStore; // Используем named export
```

### Стилизация с Tailwind:
```javascript
// ✅ ПРАВИЛЬНО - используем Tailwind классы
<div className="glass-effect rounded-xl p-6 hover:scale-105 transition-transform">
  <h2 className="text-xl font-bold mb-4">Заголовок</h2>
</div>

// ✅ ПРАВИЛЬНО - динамические классы через шаблонные строки
<button className={`
  glass-button px-4 py-2 rounded-lg
  ${isActive ? 'bg-blue-500' : 'bg-gray-200'}
  ${disabled ? 'opacity-50 cursor-not-allowed' : 'hover:scale-105'}
`}>

// ❌ НЕПРАВИЛЬНО - инлайн стили (только для динамических значений)
<div style={{ padding: '24px', borderRadius: '12px' }}>

// ❌ НЕПРАВИЛЬНО - отдельные CSS файлы
import './StatCard.css';
```

### Кастомные классы Tailwind:
```css
/* index.css - ТОЛЬКО для глобальных утилит */
@layer components {
  .glass-effect {
    @apply bg-white/80 dark:bg-gray-800/80 backdrop-blur-lg 
           border border-gray-200 dark:border-gray-700;
  }
  
  .glass-button {
    @apply glass-effect px-4 py-2 rounded-lg font-semibold 
           transition-all duration-300 hover:scale-105 active:scale-95;
  }
}
```

---

## 🎨 СТИЛЕВЫЕ СОГЛАШЕНИЯ

### Цветовая палитра:
- **Primary**: blue-500 (`#3B82F6`)
- **Success**: green-500 (`#10B981`)
- **Warning**: yellow-500 (`#F59E0B`)
- **Error**: red-500 (`#EF4444`)
- **Background Light**: gray-100 (`#F3F4F6`)
- **Background Dark**: gray-900 (`#111827`)

### Размеры и отступы:
- Padding карточек: `p-6` (24px)
- Margin между секциями: `mb-6` (24px)
- Border radius: `rounded-xl` (12px)
- Shadows: `shadow-lg`

### Анимации:
- Hover эффекты: `hover:scale-105 transition-transform duration-300`
- Появление: `animate-slide-in` или `animate-fade-in`
- Кнопки: `active:scale-95`

---

## 🔧 ИМЕНОВАНИЕ И СОГЛАШЕНИЯ

### Переменные и функции:
```javascript
// ✅ ПРАВИЛЬНО
const totalEarnings = 10000;
const isLoading = false;
const handleSaveEntry = () => {};
const calculateDuration = (start, end) => {};

// ❌ НЕПРАВИЛЬНО
const TotalEarnings = 10000; // PascalCase только для компонентов
const loading = false; // Булевы без префикса is/has/should
const saveEntry = () => {}; // Обработчики без префикса handle
```

### Пропсы компонентов:
```javascript
// ✅ ПРАВИЛЬНО
<Button 
  onClick={handleClick}
  disabled={isLoading}
  variant="primary"
  icon={SaveIcon}
>
  Сохранить
</Button>

// ❌ НЕПРАВИЛЬНО
<Button 
  click={handleClick}        // onClick, не click
  isDisabled={isLoading}     // disabled, не isDisabled
  buttonVariant="primary"    // variant, не buttonVariant
/>
```

---

## 📦 ИМПОРТЫ

### Порядок импортов:
```javascript
// 1. React и хуки
import { useState, useEffect, useCallback } from 'react';

// 2. Внешние библиотеки
import { create } from 'zustand';
import { Plus, Edit2, Trash2 } from 'lucide-react';

// 3. Внутренние компоненты
import { Button } from '@/components/ui/Button';
import { useEntriesStore } from '@/store/useEntriesStore';

// 4. Утилиты и хелперы
import { calculateDuration } from '@/utils/calculations';
import { formatDate } from '@/utils/dateHelpers';

// 5. Константы
import { DEFAULT_CATEGORIES } from '@/constants/categories';
```

---

## 🎯 ОБРАБОТКА ДАННЫХ

### LocalStorage:
```javascript
// ✅ ПРАВИЛЬНО - через Zustand persist
export const useEntriesStore = create(
  persist(
    (set) => ({ ... }),
    { name: 'time-tracker-entries' }
  )
);

// ❌ НЕПРАВИЛЬНО - прямая работа с localStorage в компонентах
localStorage.setItem('entries', JSON.stringify(entries));
```

### Валидация:
```javascript
// ✅ ПРАВИЛЬНО - валидация в утилитах
export function validateEntry(entry) {
  const errors = {};
  
  if (!entry.date) errors.date = 'Дата обязательна';
  if (!entry.start) errors.start = 'Время начала обязательно';
  if (entry.end <= entry.start) {
    errors.end = 'Время окончания должно быть позже начала';
  }
  
  return { isValid: Object.keys(errors).length === 0, errors };
}
```

---

## 🎭 СОСТОЯНИЕ И ЭФФЕКТЫ

### useState:
```javascript
// ✅ ПРАВИЛЬНО
const [entries, setEntries] = useState([]);
const [isLoading, setIsLoading] = useState(false);
const [formData, setFormData] = useState({
  date: '',
  start: '',
  end: ''
});

// Обновление объекта:
setFormData(prev => ({ ...prev, date: newDate }));

// ❌ НЕПРАВИЛЬНО
setFormData({ date: newDate }); // Потеря остальных полей
```

### useEffect:
```javascript
// ✅ ПРАВИЛЬНО - с зависимостями
useEffect(() => {
  if (activeTimer) {
    const interval = setInterval(() => {
      updateElapsedTime();
    }, 1000);
    
    return () => clearInterval(interval); // Очистка
  }
}, [activeTimer]); // Зависимости

// ❌ НЕПРАВИЛЬНО
useEffect(() => {
  setInterval(...); // Без очистки
}); // Без массива зависимостей
```

---

## 🚨 ОБРАБОТКА ОШИБОК
```javascript
// ✅ ПРАВИЛЬНО
try {
  const data = await fetchData();
  setData(data);
} catch (error) {
  console.error('Ошибка загрузки данных:', error);
  showNotification('Не удалось загрузить данные', 'error');
  setError(error.message);
} finally {
  setIsLoading(false);
}
```

---

## 🧪 КОММЕНТАРИИ И ДОКУМЕНТАЦИЯ

### JSDoc для функций:
```javascript
/**
 * Рассчитывает длительность работы в часах
 * @param {string} start - Время начала в формате HH:MM
 * @param {string} end - Время окончания в формате HH:MM
 * @returns {string} Длительность в часах с 2 десятичными знаками
 */
export function calculateDuration(start, end) {
  // реализация
}
```

### Комментарии в коде:
```javascript
// ✅ ПРАВИЛЬНО - объяснение "почему", не "что"
// Округляем до 15 минут для удобства учета времени
const roundedMinutes = Math.round(minutes / 15) * 15;

// ❌ НЕПРАВИЛЬНО - очевидные комментарии
// Получаем entries из store
const { entries } = useEntriesStore();
```

---

## 📱 АДАПТИВНОСТЬ
```javascript
// ✅ ПРАВИЛЬНО - mobile-first подход
<div className="
  grid grid-cols-1           // Mobile: 1 колонка
  md:grid-cols-2             // Tablet: 2 колонки
  lg:grid-cols-4             // Desktop: 4 колонки
  gap-4
">
```

---

## 📋 ЧЕКЛИСТ ПЕРЕД СОЗДАНИЕМ КОДА

AI должен проверить перед созданием кода:
- [ ] Компонент в правильной папке по структуре
- [ ] Используется функциональный компонент с хуками
- [ ] Named export (не default)
- [ ] Деструктуризация пропсов
- [ ] Стилизация через Tailwind классы
- [ ] Нет инлайн стилей (кроме динамических значений)
- [ ] Нет console.log (кроме ошибок)
- [ ] Есть валидация данных
- [ ] Есть обработка ошибок
- [ ] Есть комментарии на русском для сложной логики
- [ ] Компонент не длиннее 300 строк
- [ ] Есть JSDoc комментарий для функций
- [ ] Есть обучающие комментарии для zero coder

---

## 🤖 ИНСТРУКЦИИ ДЛЯ AI

### КРИТИЧЕСКИ ВАЖНО: Проверка существующего кода

**Перед созданием ЛЮБОГО нового файла AI ОБЯЗАН:**

1. **Проверить через MCP filesystem** существующие файлы в целевой папке
2. **Найти похожие компоненты/хуки/утилиты** которые уже существуют
3. **Переиспользовать** существующий код вместо создания дубликатов
4. **Предложить варианты:**
   - "Похожий компонент уже есть: `ExistingComponent.jsx`. Хотите использовать его или создать новый?"
   - "Эта функциональность есть в `existingUtility.js`. Хотите расширить её или создать новую?"

### При создании компонента:

1. **Спросить:** "В какую папку поместить компонент?" (если не указано)
2. **Проверить** существующие компоненты в этой папке через filesystem MCP
3. **Использовать** шаблон из `.examples/component.example.jsx`
4. Создать файл с именем PascalCase
5. Использовать named export
6. Добавить JSDoc комментарий
7. Использовать деструктуризацию пропсов
8. Применить Tailwind классы
9. Добавить aria-атрибуты
10. Добавить обучающие комментарии для zero coder
11. Показать пример использования

### При создании хука:

1. **Проверить** существующие хуки в `src/hooks/` через filesystem MCP
2. **Использовать** шаблон из `.examples/hook.example.js`
3. Имя начинается с "use"
4. Файл в папке hooks/
5. Возвращает объект с методами
6. Документировать параметры и возврат
7. Добавить обучающие комментарии
8. Показать пример использования

### При создании утилиты:

1. **Проверить** существующие утилиты в `src/utils/` через filesystem MCP
2. **Использовать** шаблон из `.examples/utility.example.js`
3. Чистая функция (pure function)
4. Каждая функция с JSDoc
5. Экспорт именованный
6. Добавить обучающие комментарии
7. Показать пример использования

### При создании Zustand store:

1. **Проверить** существующие stores в `src/store/` через filesystem MCP
2. **Использовать** шаблон из `.examples/store.example.js`
3. Использовать persist если нужно сохранение в localStorage
4. Добавить все CRUD операции
5. Документировать каждый метод
6. Добавить обучающие комментарии
7. Показать пример использования в компоненте

### При ошибке:

1. Объяснить причину простым языком
2. Предложить 2-3 варианта решения
3. Показать исправленный код
4. Объяснить, как избежать в будущем

### Стиль общения:

- Объяснять термины простым языком
- Приводить примеры из реальной жизни
- Использовать эмодзи для наглядности
- Структурировать ответы с заголовками
- Давать пошаговые инструкции
- Всегда добавлять обучающие комментарии в код

---

## 🎓 ОБУЧАЮЩИЕ КОММЕНТАРИИ

Для начинающих разработчиков добавлять обучающие комментарии в КАЖДЫЙ создаваемый файл:

```javascript
/**
 * 🎓 ПОЯСНЕНИЕ ДЛЯ НАЧИНАЮЩИХ:
 * 
 * useState - это хук React для создания состояния в компоненте.
 * Он возвращает массив из 2 элементов:
 * 1. Текущее значение (entries)
 * 2. Функция для обновления (setEntries)
 * 
 * При вызове setEntries компонент перерисуется с новым значением.
 */
const [entries, setEntries] = useState([]);
```

---

## 🎯 ИСПОЛЬЗОВАНИЕ MCP СЕРВЕРОВ

### AI должен активно использовать доступные MCP:

#### **filesystem MCP:**
- Проверять существующие файлы перед созданием новых
- Читать содержимое файлов для понимания контекста
- Избегать дублирования кода

#### **memory MCP:**
- Запоминать предпочтения пользователя
- Запоминать структуру проекта
- Запоминать часто используемые паттерны
- Использовать сохраненный контекст в будущих запросах

#### **git MCP:**
- Показывать статус изменений
- Помогать с commit messages
- Показывать историю изменений файла

#### **brave-search MCP:**
- Искать актуальную документацию библиотек
- Искать решения специфических проблем
- Проверять актуальность версий библиотек

#### **sequential-thinking MCP:**
- Разбивать сложные задачи на шаги
- Планировать архитектуру новых функций
- Решать сложные проблемы пошагово

---

## 📍 ПРИМЕРЫ КОДА (ДЛЯ AI)

**ВАЖНО:** При создании файлов, AI должен смотреть примеры в папке `.examples/`:

- `.examples/component.example.jsx` - эталонный React компонент
- `.examples/hook.example.js` - эталонный React хук
- `.examples/store.example.js` - эталонный Zustand store
- `.examples/utility.example.js` - эталонная утилита
- `.examples/validation.example.js` - эталонная валидация
- `.examples/calculation.example.js` - эталонный расчет

**Инструкция для AI:**
Перед созданием файла определенного типа, прочитай соответствующий пример из `.examples/` через filesystem MCP и используй его как основу, адаптируя под конкретную задачу пользователя.

---

## 🔄 ВЕРСИОНИРОВАНИЕ

Версия .cursorrules: 2.0 (Enhanced)
Последнее обновление: 2024-12-19

Изменения в версии 2.0:
- Добавлены быстрые команды для AI
- Добавлена инструкция по проверке существующего кода
- Добавлена интеграция с .examples/
- Добавлены инструкции по использованию MCP серверов
- Усилены требования к обучающим комментариям
- Добавлен чеклист перед созданием кода

---

**ВАЖНО:** Эти правила являются обязательными для всего проекта Time Tracker Dashboard. 
При любых сомнениях или неясностях - следуй этим правилам.

**Для AI:** Всегда проверяй соответствие создаваемого кода этим правилам перед показом результата пользователю. Используй MCP filesystem для проверки существующего кода. Используй примеры из `.examples/` как основу для нового кода.