# 🎯 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: TIME TRACKER DASHBOARD v2.0

## 📋 ОГЛАВЛЕНИЕ

1. [Обзор проекта](#обзор-проекта)
2. [Технологический стек](#технологический-стек)
3. [Структура проекта](#структура-проекта)
4. [Детальное описание функционала](#детальное-описание-функционала)
5. [Пошаговая инструкция реализации](#пошаговая-инструкция-реализации)
6. [Компоненты и их функции](#компоненты-и-их-функции)
7. [Хранение данных](#хранение-данных)
8. [Стилизация](#стилизация)

---

## 🎯 ОБЗОР ПРОЕКТА

### Что это?

Time Tracker Dashboard - это веб-приложение для учета рабочего времени с расширенной аналитикой, категоризацией и планированием.

### Ключевые возможности:

- ⏱️ Учет времени по категориям с почасовой ставкой
- 📊 Детальная аналитика и графики
- 📅 Сравнение периодов (день, неделя, месяц, год)
- 🎯 План/факт анализ с учетом рабочего графика
- 💡 Интеллектуальные инсайты и рекомендации
- 🔔 Уведомления и звуковые сигналы
- 💾 Экспорт/импорт данных + автобэкапы
- 🌓 Темная/светлая тема
- ⌨️ Горячие клавиши
- 📱 Адаптивный дизайн

---

## 🛠️ ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Обязательные технологии:

```json
{
  "core": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "state": {
    "zustand": "^4.4.0" // Для глобального состояния
  },
  "ui": {
    "tailwindcss": "^3.4.0",
    "@headlessui/react": "^1.7.0", // Для модальных окон
    "lucide-react": "^0.300.0" // Иконки
  },
  "charts": {
    "recharts": "^2.12.0" // Графики
  },
  "audio": {
    "tone": "^14.7.77" // Звуковые уведомления
  },
  "build": {
    "vite": "^5.0.0" // Сборщик
  }
}
```

### Установка:

```bash
npm create vite@latest time-tracker -- --template react
cd time-tracker
npm install zustand @headlessui/react lucide-react recharts tone
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
time-tracker/
├── public/
│   └── sounds/              # Звуковые файлы
├── src/
│   ├── components/          # Все компоненты
│   │   ├── layout/          # Структурные компоненты
│   │   │   ├── Header.jsx
│   │   │   ├── FloatingPanel.jsx
│   │   │   └── Footer.jsx
│   │   ├── entries/         # Работа с записями
│   │   │   ├── EntriesList.jsx
│   │   │   ├── EntryItem.jsx
│   │   │   ├── EntryForm.jsx
│   │   │   ├── EditEntryModal.jsx
│   │   │   └── BatchEditModal.jsx
│   │   ├── statistics/      # Статистика и аналитика
│   │   │   ├── StatisticsDashboard.jsx
│   │   │   ├── PlanFactCompactView.jsx
│   │   │   ├── InsightsPanel.jsx
│   │   │   ├── AnalyticsSection.jsx
│   │   │   └── CompareMode.jsx
│   │   ├── charts/          # Графики
│   │   │   ├── EarningsChart.jsx
│   │   │   ├── CategoryDistribution.jsx
│   │   │   ├── TimeDistribution.jsx
│   │   │   └── TrendsChart.jsx
│   │   ├── modals/          # Модальные окна
│   │   │   ├── TutorialModal.jsx
│   │   │   ├── AboutModal.jsx
│   │   │   ├── WorkScheduleModal.jsx
│   │   │   ├── ConfirmModal.jsx
│   │   │   └── CategoryManager.jsx
│   │   ├── settings/        # Настройки
│   │   │   ├── SettingsPanel.jsx
│   │   │   ├── CategorySettings.jsx
│   │   │   ├── NotificationSettings.jsx
│   │   │   └── ThemeSettings.jsx
│   │   └── ui/              # UI элементы
│   │       ├── Button.jsx
│   │       ├── Input.jsx
│   │       ├── Select.jsx
│   │       ├── Notification.jsx
│   │       └── LoadingSpinner.jsx
│   ├── hooks/               # Кастомные хуки
│   │   ├── useTimer.js
│   │   ├── useLocalStorage.js
│   │   ├── useNotifications.js
│   │   ├── useHotkeys.js
│   │   ├── useBackups.js
│   │   └── useSound.js
│   ├── store/               # Zustand хранилища
│   │   ├── useEntriesStore.js
│   │   ├── useSettingsStore.js
│   │   ├── useUIStore.js
│   │   └── useTimerStore.js
│   ├── utils/               # Утилиты
│   │   ├── calculations.js
│   │   ├── dateHelpers.js
│   │   ├── exportImport.js
│   │   ├── backupManager.js
│   │   ├── soundManager.js
│   │   └── validators.js
│   ├── constants/           # Константы
│   │   ├── categories.js
│   │   ├── hotkeys.js
│   │   └── settings.js
│   ├── App.jsx              # Главный компонент
│   ├── main.jsx             # Точка входа
│   └── index.css            # Глобальные стили
├── tailwind.config.js
├── vite.config.js
├── package.json
└── README.md
```

---

## 🔍 ДЕТАЛЬНОЕ ОПИСАНИЕ ФУНКЦИОНАЛА

### 1. УЧЕТ ВРЕМЕНИ (Entries Management)

#### 1.1 Структура записи (Entry):

```javascript
{
  id: "unique-id-string",
  date: "2024-12-19",           // Дата в формате YYYY-MM-DD
  start: "09:00",               // Время начала HH:MM
  end: "17:00",                 // Время окончания HH:MM
  duration: 8.0,                // Длительность в часах (рассчитывается)
  category: "Дизайн",           // Категория работы
  description: "Описание",      // Описание задачи
  rate: 1000,                   // Ставка за час (₽)
  earned: 8000,                 // Заработано (рассчитывается: duration * rate)
  isManual: true,               // true - ручной ввод, false - таймер
  createdAt: "2024-12-19T10:00:00Z",
  updatedAt: "2024-12-19T10:00:00Z"
}
```

#### 1.2 Операции с записями:

**Добавление записи:**

- Кнопка "+ Новая запись" (горячая клавиша: N или T)
- Модальное окно с полями:
  - Дата (по умолчанию: сегодня)
  - Время начала (обязательно)
  - Время окончания (обязательно)
  - Категория (выбор из списка + иконка)
  - Описание (опционально, multiline)
  - Ставка (подтягивается из категории, можно изменить)
- Автоматический расчет duration и earned
- Валидация:
  - Время окончания > времени начала
  - Дата не в будущем
  - Все обязательные поля заполнены

**Редактирование записи:**

- Клик по записи открывает модальное окно редактирования
- Все поля редактируемые
- Пересчет duration и earned при изменении времени/ставки
- Кнопка "Сохранить" / "Отмена"
- ESC закрывает модалку

**Удаление записи:**

- Кнопка "Удалить" в каждой записи
- Подтверждение не требуется (есть Undo)
- Анимация исчезновения

**Batch-операции:**

- Выбор нескольких записей (чекбоксы)
- Массовое редактирование:
  - Изменение категории
  - Изменение ставки
  - Изменение даты
- Массовое удаление
- Кнопка "Выбрать все"

#### 1.3 Таймер реального времени:

**Функционал:**

- Кнопка "Начать" (горячая клавиша: S или Y)
- Во время работы таймера:
  - Отображение текущего времени работы
  - Категория выбирается перед стартом
  - Плавающая панель с таймером всегда видна
  - Звуковой сигнал каждый час работы
- Кнопка "Стоп":
  - Останавливает таймер
  - Открывает модалку с заполненными полями
  - Возможность отредактировать перед сохранением
  - Время автоматически округляется до 15 минут

**Плавающая панель таймера:**

- Позиция: правый верхний угол (можно перетаскивать)
- Содержимое:
  - Текущее время работы (обновляется каждую секунду)
  - Выбранная категория
  - Кнопка "Стоп"
  - Кнопка "Пауза" (опционально)
- Всегда поверх других элементов (z-index: 9999)
- Полупрозрачный фон (glass effect)
- Анимация пульсации для привлечения внимания

---

### 2. КАТЕГОРИИ

#### 2.1 Дефолтные категории:

```javascript
;[
  { name: 'Разработка', icon: 'Code', rate: 1500, color: '#3B82F6' },
  { name: 'Дизайн', icon: 'Palette', rate: 1200, color: '#8B5CF6' },
  { name: 'Менеджмент', icon: 'Users', rate: 1300, color: '#10B981' },
  { name: 'Маркетинг', icon: 'TrendingUp', rate: 1000, color: '#F59E0B' },
  { name: 'Консультации', icon: 'MessageCircle', rate: 1400, color: '#06B6D4' },
  { name: 'Обучение', icon: 'BookOpen', rate: 800, color: '#EF4444' },
  { name: 'Другое', icon: 'MoreHorizontal', rate: 1000, color: '#6B7280' },
]
```

#### 2.2 Управление категориями:

- Кнопка "Управление категориями" в настройках
- Модальное окно с таблицей:
  - Название категории (редактируемое)
  - Иконка (выбор из lucide-react)
  - Цвет (color picker)
  - Ставка по умолчанию (₽/час)
  - Кнопки "Редактировать" / "Удалить"
- Добавление новой категории
- Нельзя удалить категорию, если есть записи с ней
- Изменение ставки категории предлагает обновить старые записи

---

### 3. СТАТИСТИКА И АНАЛИТИКА

#### 3.1 StatisticsDashboard - Главная панель статистики:

**Карточки (4 штуки):**

1. **Заработано сегодня:**
   - Сумма за текущий день
   - Процент от дневного плана
   - Тренд по сравнению со вчера (↑↓)
   - Анимация при изменении

2. **Отработано часов:**
   - Часы за текущий день
   - Процент от дневного плана (8 часов)
   - Прогресс-бар
   - Тренд по сравнению со вчера

3. **Средняя ставка:**
   - Средневзвешенная ставка за день
   - Тренд по сравнению со вчера
   - Цветовая индикация (выше/ниже среднего)

4. **Эффективность:**
   - Процент выполнения плана
   - Цветовая индикация:
     - Зеленый: ≥ 100%
     - Желтый: 70-99%
     - Красный: < 70%

**Режим сравнения (Compare Mode):**

- Переключатель периодов:
  - День к дню (сегодня vs вчера)
  - Неделя к неделе (текущая vs предыдущая)
  - Месяц к месяцу
  - Год к году
- Все карточки показывают:
  - Текущее значение
  - Предыдущее значение
  - Разницу (абсолютную и %)
  - Стрелки тренда
- Анимация при переключении режимов

#### 3.2 PlanFactCompactView - План/Факт анализ:

**Отображение:**

- Компактная карточка с 3 секциями:
  1. План на день (из настроек рабочего графика)
  2. Факт (отработано сегодня)
  3. Отклонение (план - факт)
- Цветовая индикация:
  - Зеленый: факт ≥ план
  - Желтый: факт 70-99% плана
  - Красный: факт < 70% плана
- Прогресс-бар выполнения плана
- Клик открывает WorkScheduleModal

**Настройка рабочего графика (WorkScheduleModal):**

```javascript
{
  weekDays: {
    monday: { enabled: true, hours: 8, rate: 1000 },
    tuesday: { enabled: true, hours: 8, rate: 1000 },
    wednesday: { enabled: true, hours: 8, rate: 1000 },
    thursday: { enabled: true, hours: 8, rate: 1000 },
    friday: { enabled: true, hours: 8, rate: 1000 },
    saturday: { enabled: false, hours: 0, rate: 1000 },
    sunday: { enabled: false, hours: 0, rate: 1000 }
  },
  monthlyGoal: 40000,      // Цель на месяц (₽)
  dailyGoalAuto: true      // Автоматический расчет дневной цели
}
```

Модальное окно:

- Чекбоксы для каждого дня недели
- Поля для часов и ставки для каждого дня
- Итоговый план на неделю
- Итоговый план на месяц
- Автоматический расчет дневной цели на основе рабочих дней

#### 3.3 InsightsPanel - Интеллектуальные инсайты:

**Алгоритм генерации инсайтов:**

Анализируем последние 30 дней и выдаем до 5 инсайтов:

1. **Самая прибыльная категория:**
   - Категория с максимальным заработком
   - Процент от общего заработка
   - Рекомендация: "Увеличьте долю [категория] для роста дохода"

2. **Пики продуктивности:**
   - Анализ времени дня (утро/день/вечер)
   - Определение самого продуктивного времени
   - Рекомендация: "Планируйте сложные задачи на [время]"

3. **Дни недели:**
   - Анализ продуктивности по дням
   - Выявление самых и наименее продуктивных дней
   - Рекомендация по оптимизации графика

4. **Тренды заработка:**
   - Анализ динамики последних 7/14/30 дней
   - Тренд: растет/падает/стабилен
   - Прогноз на следующую неделю

5. **Рекомендации по ставкам:**
   - Сравнение ваших ставок с средними (если есть данные)
   - Категории, где можно повысить ставку

**Отображение:**

- Карточка с иконкой лампочки
- Список инсайтов с иконками
- Анимация появления (по очереди с задержкой)
- Обновление раз в день

#### 3.4 AnalyticsSection - Графики и детальная аналитика:

**Компоненты графиков:**

1. **EarningsChart - График заработка:**
   - Линейный график за выбранный период
   - Ось X: даты
   - Ось Y: сумма (₽)
   - Точки данных с всплывающими подсказками
   - Легенда: общая сумма, средняя за день
   - Экспорт графика в PNG

2. **CategoryDistribution - Распределение по категориям:**
   - Круговая диаграмма (Pie Chart)
   - Процент времени по каждой категории
   - Легенда с цветами категорий
   - Клик по сегменту - фильтрация записей

3. **TimeDistribution - Распределение времени:**
   - Столбчатая диаграмма по дням недели
   - Показывает часы работы
   - Цветовая кодировка по категориям (stacked bar)
   - Средняя линия

4. **TrendsChart - Тренды:**
   - Мультилинейный график
   - Отображает несколько метрик:
     - Заработок
     - Часы работы
     - Средняя ставка
   - Переключатель метрик
   - Zoom и pan

**Фильтры для графиков:**

- Период: последние 7/14/30/90 дней, весь период
- Категории: выбор конкретных категорий
- Кнопка "Сбросить фильтры"

**Ленивая загрузка:**

- Раздел аналитики не загружается при старте
- Кнопка "Загрузить раздел аналитики"
- После загрузки - кэширование в памяти

---

### 4. ТАБЛИЦА ЗАПИСЕЙ (EntriesList)

#### 4.1 Внешний вид таблицы:

**Заголовок:**

- Кнопки:
  - "+ Новая запись"
  - "Начать таймер"
  - "Импорт/Экспорт"
  - "Очистить всё"
  - "Проверить бэкапы"
- Поиск (live search по описанию и категории)
- Фильтры:
  - По дате (от-до)
  - По категории (мультиселект)
  - Только ручные / только таймер

**Строки таблицы:**

Колонки:

1. Чекбокс (для batch-операций)
2. Дата (группировка по дням)
3. Время (start - end)
4. Длительность (часы с 2 знаками)
5. Категория (иконка + название, цветной бэдж)
6. Описание (truncated, tooltip при наведении)
7. Ставка (₽/час)
8. Заработано (₽)
9. Действия:
   - Кнопка "Редактировать"
   - Кнопка "Удалить"

**Особенности:**

- Группировка по дням с подзаголовками
- Итоги по каждому дню (часы и заработок)
- Цветная индикация:
  - Новые записи (зеленая рамка, исчезает через 3 сек)
  - Отредактированные (желтая рамка на 2 сек)
  - Записи с таймера (иконка ⏱️)
- Сортировка:
  - По дате (по умолчанию: новые сверху)
  - По категории
  - По заработку
  - По длительности
- Виртуализация для больших списков (react-window)
- Пагинация: 50 записей на страницу

#### 4.2 Интерактивность:

- Клик по строке - открытие модального окна редактирования
- Hover эффект с тенью
- Drag-n-drop для изменения порядка (опционально)
- Анимация при добавлении/удалении
- Skeleton loader при загрузке

---

### 5. МОДАЛЬНЫЕ ОКНА

#### 5.1 TutorialModal - Обучающий тур:

**Показывается:**

- При первом запуске приложения
- Можно вызвать из меню "Помощь"

**Содержимое (5 шагов):**

1. **Добро пожаловать:**
   - Название приложения
   - Краткое описание
   - Кнопка "Начать тур"

2. **Учет времени:**
   - Как добавить запись вручную
   - Как использовать таймер
   - Горячие клавиши

3. **Категории и ставки:**
   - Как работают категории
   - Как настроить ставки
   - Автоматический расчет заработка

4. **Аналитика:**
   - Обзор графиков
   - Инсайты
   - План/факт анализ

5. **Импорт данных:**
   - Если есть старые данные
   - Кнопка "Импортировать"
   - Или "Начать с чистого листа"

**Навигация:**

- Кнопки "Назад" / "Далее"
- Индикатор шагов (точки)
- Кнопка "Пропустить тур"
- ESC закрывает

#### 5.2 EditEntryModal - Редактирование записи:

**Поля:**

- Дата (date picker)
- Время начала (time picker)
- Время окончания (time picker)
- Длительность (рассчитывается, но можно изменить вручную)
- Категория (select с иконками)
- Описание (textarea)
- Ставка (number input)
- Заработано (рассчитывается, показывается)

**Функционал:**

- Валидация в реальном времени
- Показ ошибок под полями
- Автоматический пересчет при изменении времени/ставки
- Кнопки:
  - "Сохранить" (Ctrl+Enter)
  - "Отмена" (ESC)
  - "Удалить" (требует подтверждения)
- История изменений (последние 5 изменений)

#### 5.3 WorkScheduleModal - Настройка рабочего графика:

**Интерфейс:**

Таблица дней недели:
| День | Рабочий | Часов | Ставка (₽/час) |
|------|---------|-------|----------------|
| Пн | ✓ | 8 | 1000 |
| Вт | ✓ | 8 | 1000 |
| ... | ... | ... | ... |

**Дополнительные поля:**

- Цель на месяц (₽) - input
- Автоматический расчет дневной цели - checkbox
- Если включен, дневная цель = месячная / количество рабочих дней

**Итоги:**

- Рабочих дней в неделю: X
- Часов в неделю: Y
- План на неделю: Z ₽
- План на месяц: W ₽

#### 5.4 AboutModal - О приложении:

- Название и версия
- Краткое описание
- Список функций
- Горячие клавиши
- Контакты / GitHub
- Кнопка "Закрыть"

#### 5.5 ConfirmModal - Подтверждение действия:

**Используется для:**

- Очистка всех записей
- Удаление категории
- Восстановление из бэкапа

**Содержимое:**

- Заголовок
- Текст предупреждения
- Кнопки:
  - "Подтвердить" (красная)
  - "Отмена" (серая)

---

### 6. УВЕДОМЛЕНИЯ И ЗВУКИ

#### 6.1 Система уведомлений:

**Типы уведомлений:**

- Success (зеленый)
- Error (красный)
- Warning (желтый)
- Info (синий)

**Параметры:**

```javascript
{
  message: "Текст уведомления",
  type: "success" | "error" | "warning" | "info",
  duration: 3000, // мс, 0 = бесконечно
  position: "top-right", // top-left, top-center, bottom-right, etc.
  action: { // опционально
    label: "Отменить",
    onClick: () => {}
  }
}
```

**Отображение:**

- Стек уведомлений в правом верхнем углу
- Анимация появления (slide + fade)
- Прогресс-бар времени до исчезновения
- Кнопка закрытия (X)
- Автоматическое исчезновение
- Максимум 3 уведомления одновременно

**Примеры использования:**

- "Запись добавлена" - success
- "Ошибка сохранения" - error
- "План на день выполнен!" - success
- "Низкая эффективность сегодня" - warning

#### 6.2 Звуковые уведомления:

**Библиотека: Tone.js**

**Звуки:**

1. **Timer Start** - короткий "бип"
2. **Timer Stop** - двойной "бип"
3. **Hourly Alert** - мелодия (каждый час работы)
4. **Plan Completed** - триумфальный звук
5. **New Entry** - тихий клик
6. **Error** - низкий гудок

**Настройки звука:**

- Включить/выключить звуки (глобально)
- Громкость (0-100%)
- Выбор звуковой темы (опционально)

**Реализация:**

```javascript
// soundManager.js
import * as Tone from 'tone'

const sounds = {
  timerStart: () => {
    const synth = new Tone.Synth().toDestination()
    synth.triggerAttackRelease('C5', '8n')
  },
  timerStop: () => {
    const synth = new Tone.Synth().toDestination()
    synth.triggerAttackRelease('C5', '8n', Tone.now())
    synth.triggerAttackRelease('C5', '8n', Tone.now() + 0.1)
  },
  hourlyAlert: () => {
    const synth = new Tone.Synth().toDestination()
    synth.triggerAttackRelease('C5', '8n', Tone.now())
    synth.triggerAttackRelease('E5', '8n', Tone.now() + 0.1)
    synth.triggerAttackRelease('G5', '8n', Tone.now() + 0.2)
  },
}
```

---

### 7. ГОРЯЧИЕ КЛАВИШИ

**Список горячих клавиш:**

| Клавиша                 | Действие                                  |
| ----------------------- | ----------------------------------------- |
| N или T                 | Новая запись                              |
| S или Y                 | Старт/Стоп таймера                        |
| Ctrl+Z                  | Отменить                                  |
| Ctrl+Shift+Z или Ctrl+Y | Повторить                                 |
| Ctrl+S                  | Сохранить (в модалке)                     |
| ESC                     | Закрыть модалку / Отменить редактирование |
| Ctrl+E                  | Экспорт данных                            |
| Ctrl+I                  | Импорт данных                             |
| Ctrl+F                  | Фокус на поиск                            |
| ?                       | Показать справку по клавишам              |

**Особенности:**

- Не работают, если фокус на input/textarea
- Визуальная подсказка при нажатии
- Можно кастомизировать в настройках

---

### 8. ЭКСПОРТ/ИМПОРТ И БЭКАПЫ

#### 8.1 Экспорт данных:

**Формат файла: JSON**

```javascript
{
  version: "1.0",
  exportDate: "2024-12-19T10:00:00Z",
  entries: [...], // все записи
  categories: [...], // категории
  settings: {...}, // настройки
  statistics: { // опционально
    totalHours: 1000,
    totalEarned: 1500000,
    averageRate: 1500
  }
}
```

**Процесс:**

- Кнопка "Экспорт"
- Выбор формата:
  - JSON (полный)
  - JSON (только записи)
  - CSV (для Excel)
- Выбор периода:
  - Все данные
  - Текущий месяц
  - Текущий год
  - Пользовательский диапазон
- Скачивание файла: `time-tracker-export-YYYY-MM-DD.json`

#### 8.2 Импорт данных:

**Процесс:**

- Кнопка "Импорт"
- Выбор файла (JSON или CSV)
- Валидация файла:
  - Проверка структуры
  - Проверка типов данных
  - Проверка дубликатов
- Опции импорта:
  - Заменить все данные
  - Добавить к существующим
  - Обновить существующие (по ID)
- Показ превью:
  - Количество записей
  - Диапазон дат
  - Категории
- Подтверждение
- Импорт с прогресс-баром
- Уведомление об успехе/ошибке

**Обработка ошибок:**

- Неверный формат - показать ошибку
- Отсутствуют поля - попытка восстановления
- Дубликаты - предложить варианты
- Конфликт категорий - предложить маппинг

#### 8.3 Автоматические бэкапы:

**Настройки:**

```javascript
{
  autoBackupEnabled: true,
  backupFrequency: "daily", // daily, weekly, manual
  maxBackups: 10, // максимум копий
  backupLocation: "localStorage" // localStorage или IndexedDB
}
```

**Логика:**

- Создание бэкапа:
  - При закрытии приложения (if enabled)
  - По расписанию (daily в 23:59)
  - Вручную (кнопка "Создать бэкап")
- Хранение: `timeTracker_backup_${timestamp}`
- Ротация: удаление старых, если > maxBackups

**Восстановление:**

- Кнопка "Проверить бэкапы"
- Список доступных бэкапов:
  - Дата создания
  - Размер
  - Количество записей
- Кнопка "Восстановить" для каждого
- Подтверждение (текущие данные будут перезаписаны)
- Восстановление
- Уведомление об успехе

---

### 9. НАСТРОЙКИ (Settings)

#### 9.1 Общие настройки:

```javascript
{
  // Внешний вид
  theme: "light" | "dark" | "auto", // auto = по системе
  animations: true,
  compactMode: false, // компактный вид таблицы

  // Уведомления
  notifications: {
    enabled: true,
    sound: true,
    volume: 80,
    hourlyReminder: true,
    planCompleted: true
  },

  // Работа
  workSchedule: {
    // см. WorkScheduleModal
  },
  dailyGoal: 8000, // ₽
  dailyHours: 8,

  // Категории
  categories: [...],
  defaultCategory: "Разработка",

  // Таймер
  timer: {
    sound: true,
    hourlyAlert: true,
    autoSave: true,
    roundingMinutes: 15 // округление времени
  },

  // Автосохранение
  autoSave: true,
  autoSaveInterval: 30, // секунды

  // Бэкапы
  backup: {
    autoBackupEnabled: true,
    backupFrequency: "daily",
    maxBackups: 10
  },

  // Продвинутые
  advanced: {
    debugMode: false,
    experimentalFeatures: false
  }
}
```

#### 9.2 Интерфейс настроек:

**Структура:**

- Модальное окно или отдельная страница
- Разделы (табы):
  1. Общие
  2. Внешний вид
  3. Уведомления
  4. Рабочий график
  5. Категории
  6. Продвинутые

**Каждая настройка:**

- Label (название)
- Description (описание)
- Input (switch, select, number, text)
- Значение по умолчанию
- Кнопка "Сбросить" для раздела

**Кнопки внизу:**

- "Сохранить" - применить и закрыть
- "Применить" - применить, не закрывая
- "Отмена" - закрыть без сохранения
- "Сбросить всё" - вернуть все к дефолту

---

### 10. UNDO/REDO СИСТЕМА

**Реализация:**

```javascript
// Стек истории
const historyStack = {
  undoStack: [], // массив предыдущих состояний
  redoStack: [], // массив отмененных состояний
  maxSize: 50, // максимум шагов
}

// Добавление действия в историю
function pushToUndo(currentState, actionName) {
  undoStack.push({
    state: JSON.parse(JSON.stringify(currentState)),
    action: actionName,
    timestamp: Date.now(),
  })

  // Очистка redo при новом действии
  redoStack = []

  // Ограничение размера стека
  if (undoStack.length > maxSize) {
    undoStack.shift()
  }
}

// Отмена действия
function undo() {
  if (undoStack.length === 0) {
    showNotification('Нечего отменять', 'info')
    return
  }

  const current = getCurrentState()
  redoStack.push(current)

  const previous = undoStack.pop()
  restoreState(previous.state)

  showNotification(`Отменено: ${previous.action}`, 'success')
}

// Повтор действия
function redo() {
  if (redoStack.length === 0) {
    showNotification('Нечего повторять', 'info')
    return
  }

  const current = getCurrentState()
  undoStack.push(current)

  const next = redoStack.pop()
  restoreState(next.state)

  showNotification(`Повторено: ${next.action}`, 'success')
}
```

**Действия, попадающие в историю:**

- Добавление записи
- Редактирование записи
- Удаление записи
- Batch-операции
- Импорт данных
- Изменение категории
- Изменение ставки

**НЕ попадают в историю:**

- Изменение настроек
- Смена темы
- Фильтры и сортировка
- Открытие/закрытие модальных окон

**Визуальная индикация:**

- Кнопки Undo/Redo в тулбаре
- Disabled состояние, если стек пуст
- Tooltip с названием последнего действия

---

### 11. ТЕМНАЯ ТЕМА

**Реализация с Tailwind CSS:**

```javascript
// Цветовая палитра
const colors = {
  light: {
    background: '#F3F4F6',
    surface: '#FFFFFF',
    primary: '#3B82F6',
    text: '#1F2937',
    textSecondary: '#6B7280',
    border: '#E5E7EB',
  },
  dark: {
    background: '#111827',
    surface: '#1F2937',
    primary: '#60A5FA',
    text: '#F9FAFB',
    textSecondary: '#9CA3AF',
    border: '#374151',
  },
}

// Glass effect
const glassEffect = {
  light: 'bg-white/80 backdrop-blur-lg border border-gray-200',
  dark: 'bg-gray-800/80 backdrop-blur-lg border border-gray-700',
}
```

**Переключение темы:**

- Кнопка в хеадере (иконка солнца/луны)
- Анимация перехода
- Сохранение в localStorage
- Применение класса .dark к <html>

**Авто-тема:**

- Определение системной темы:

```javascript
const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
```

- Отслеживание изменения системной темы

---

### 12. АДАПТИВНЫЙ ДИЗАЙН

**Брейкпоинты:**

- Mobile: < 640px
- Tablet: 640px - 1024px
- Desktop: > 1024px

**Адаптация компонентов:**

1. **Header:**
   - Mobile: бургер-меню, иконки без текста
   - Desktop: полное меню

2. **StatisticsDashboard:**
   - Mobile: карточки в 1 колонку
   - Tablet: 2 колонки
   - Desktop: 4 колонки

3. **EntriesList:**
   - Mobile: карточки вместо таблицы
   - Desktop: полная таблица

4. **FloatingPanel:**
   - Mobile: фиксированная позиция внизу экрана
   - Desktop: перетаскиваемая панель

5. **Графики:**
   - Адаптивная ширина
   - Упрощенные легенды на mobile

---

### 13. ПРОИЗВОДИТЕЛЬНОСТЬ

**Оптимизации:**

1. **Виртуализация списка:**
   - Использовать react-window для EntriesList
   - Рендерить только видимые строки

2. **Мемоизация:**
   - React.memo для компонентов
   - useMemo для тяжелых вычислений
   - useCallback для функций

3. **Ленивая загрузка:**
   - Графики загружаются по требованию
   - Модальные окна - code splitting

4. **Дебаунсинг:**
   - Поиск: 300ms debounce
   - Автосохранение: 2s debounce

5. **IndexedDB вместо localStorage:**
   - Для больших объемов данных (> 1000 записей)
   - Асинхронный доступ

---

## 📝 ПОШАГОВАЯ ИНСТРУКЦИЯ РЕАЛИЗАЦИИ

### ШАГ 1: Инициализация проекта (10 мин)

```bash
# 1. Создаем проект
npm create vite@latest time-tracker -- --template react
cd time-tracker

# 2. Устанавливаем зависимости
npm install zustand @headlessui/react lucide-react recharts tone date-fns

# 3. Устанавливаем dev зависимости
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# 4. Запускаем проект
npm run dev
```

**Настройка Tailwind (tailwind.config.js):**

```javascript
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  darkMode: 'class',
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
      },
      colors: {
        glass: {
          light: 'rgba(255, 255, 255, 0.8)',
          dark: 'rgba(31, 41, 55, 0.8)',
        },
      },
    },
  },
  plugins: [],
}
```

**index.css:**

```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --animations-enabled: 1;
}

@media (prefers-reduced-motion: reduce) {
  :root {
    --animations-enabled: 0;
  }
}

@layer components {
  .glass-effect {
    @apply bg-white/80 dark:bg-gray-800/80 backdrop-blur-lg;
  }

  .glass-button {
    @apply glass-effect px-4 py-2 rounded-lg font-semibold transition-all duration-300 hover:scale-105 active:scale-95;
  }
}
```

---

### ШАГ 2: Создание Zustand хранилищ (30 мин)

**src/store/useEntriesStore.js:**

```javascript
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

export const useEntriesStore = create(
  persist(
    (set, get) => ({
      entries: [],

      addEntry: entry =>
        set(state => ({
          entries: [...state.entries, { ...entry, id: crypto.randomUUID() }],
        })),

      updateEntry: (id, updates) =>
        set(state => ({
          entries: state.entries.map(entry => (entry.id === id ? { ...entry, ...updates } : entry)),
        })),

      deleteEntry: id =>
        set(state => ({
          entries: state.entries.filter(entry => entry.id !== id),
        })),

      clearEntries: () => set({ entries: [] }),

      importEntries: newEntries => set({ entries: newEntries }),
    }),
    {
      name: 'time-tracker-entries',
    }
  )
)
```

**src/store/useSettingsStore.js:**

```javascript
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

const defaultCategories = [
  { name: 'Разработка', icon: 'Code', rate: 1500, color: '#3B82F6' },
  { name: 'Дизайн', icon: 'Palette', rate: 1200, color: '#8B5CF6' },
  { name: 'Менеджмент', icon: 'Users', rate: 1300, color: '#10B981' },
  { name: 'Маркетинг', icon: 'TrendingUp', rate: 1000, color: '#F59E0B' },
  { name: 'Консультации', icon: 'MessageCircle', rate: 1400, color: '#06B6D4' },
  { name: 'Обучение', icon: 'BookOpen', rate: 800, color: '#EF4444' },
  { name: 'Другое', icon: 'MoreHorizontal', rate: 1000, color: '#6B7280' },
]

export const useSettingsStore = create(
  persist(
    set => ({
      theme: 'light',
      animations: true,
      categories: defaultCategories,
      dailyGoal: 8000,
      dailyHours: 8,
      notifications: {
        enabled: true,
        sound: true,
        volume: 80,
      },
      workSchedule: {
        monday: { enabled: true, hours: 8, rate: 1000 },
        tuesday: { enabled: true, hours: 8, rate: 1000 },
        wednesday: { enabled: true, hours: 8, rate: 1000 },
        thursday: { enabled: true, hours: 8, rate: 1000 },
        friday: { enabled: true, hours: 8, rate: 1000 },
        saturday: { enabled: false, hours: 0, rate: 1000 },
        sunday: { enabled: false, hours: 0, rate: 1000 },
      },

      setTheme: theme => set({ theme }),
      toggleAnimations: () => set(state => ({ animations: !state.animations })),
      updateSettings: updates => set(updates),
    }),
    {
      name: 'time-tracker-settings',
    }
  )
)
```

**src/store/useTimerStore.js:**

```javascript
import { create } from 'zustand'

export const useTimerStore = create((set, get) => ({
  activeTimer: null,
  startTime: null,
  elapsedTime: 0,

  startTimer: category =>
    set({
      activeTimer: category,
      startTime: Date.now(),
      elapsedTime: 0,
    }),

  stopTimer: () => {
    const { startTime, elapsedTime } = get()
    const finalElapsed = elapsedTime + (Date.now() - startTime) / 1000

    set({
      activeTimer: null,
      startTime: null,
      elapsedTime: 0,
    })

    return finalElapsed
  },

  updateElapsed: () =>
    set(state => {
      if (!state.activeTimer) return {}
      return {
        elapsedTime: (Date.now() - state.startTime) / 1000,
      }
    }),
}))
```

**src/store/useUIStore.js:**

```javascript
import { create } from 'zustand'

export const useUIStore = create(set => ({
  notifications: [],
  modals: {
    editEntry: { isOpen: false, entry: null },
    tutorial: { isOpen: false },
    about: { isOpen: false },
    workSchedule: { isOpen: false },
  },

  addNotification: notification =>
    set(state => ({
      notifications: [...state.notifications, { ...notification, id: Date.now() }],
    })),

  removeNotification: id =>
    set(state => ({
      notifications: state.notifications.filter(n => n.id !== id),
    })),

  openModal: (modalName, data = null) =>
    set(state => ({
      modals: {
        ...state.modals,
        [modalName]: { isOpen: true, ...data },
      },
    })),

  closeModal: modalName =>
    set(state => ({
      modals: {
        ...state.modals,
        [modalName]: { isOpen: false },
      },
    })),
}))
```

---

### ШАГ 3: Создание утилит (20 мин)

**src/utils/calculations.js:**

```javascript
// Расчет длительности в часах
export function calculateDuration(startTime, endTime) {
  const [startHours, startMinutes] = startTime.split(':').map(Number)
  const [endHours, endMinutes] = endTime.split(':').map(Number)

  const startTotalMinutes = startHours * 60 + startMinutes
  const endTotalMinutes = endHours * 60 + endMinutes

  const durationMinutes = endTotalMinutes - startTotalMinutes
  return (durationMinutes / 60).toFixed(2)
}

// Расчет заработка
export function calculateEarned(duration, rate) {
  return (parseFloat(duration) * parseFloat(rate)).toFixed(2)
}

// Округление времени до N минут
export function roundTime(minutes, roundTo = 15) {
  return Math.round(minutes / roundTo) * roundTo
}

// Статистика за период
export function calculateStats(entries, startDate, endDate) {
  const filtered = entries.filter(entry => {
    const entryDate = new Date(entry.date)
    return entryDate >= startDate && entryDate <= endDate
  })

  const totalHours = filtered.reduce((sum, e) => sum + parseFloat(e.duration), 0)
  const totalEarned = filtered.reduce((sum, e) => sum + parseFloat(e.earned), 0)
  const averageRate = totalHours > 0 ? totalEarned / totalHours : 0

  return {
    totalHours: totalHours.toFixed(2),
    totalEarned: totalEarned.toFixed(2),
    averageRate: averageRate.toFixed(2),
    entriesCount: filtered.length,
  }
}

// Группировка по категориям
export function groupByCategory(entries) {
  return entries.reduce((acc, entry) => {
    if (!acc[entry.category]) {
      acc[entry.category] = {
        hours: 0,
        earned: 0,
        count: 0,
      }
    }
    acc[entry.category].hours += parseFloat(entry.duration)
    acc[entry.category].earned += parseFloat(entry.earned)
    acc[entry.category].count += 1
    return acc
  }, {})
}
```

**src/utils/dateHelpers.js:**

```javascript
import {
  format,
  startOfDay,
  endOfDay,
  startOfWeek,
  endOfWeek,
  startOfMonth,
  endOfMonth,
  startOfYear,
  endOfYear,
  subDays,
  subWeeks,
  subMonths,
  subYears,
} from 'date-fns'

export function formatDate(date) {
  return format(date, 'yyyy-MM-dd')
}

export function formatTime(date) {
  return format(date, 'HH:mm')
}

export function getTodayRange() {
  const today = new Date()
  return {
    start: startOfDay(today),
    end: endOfDay(today),
  }
}

export function getYesterdayRange() {
  const yesterday = subDays(new Date(), 1)
  return {
    start: startOfDay(yesterday),
    end: endOfDay(yesterday),
  }
}

export function getCurrentWeekRange() {
  const today = new Date()
  return {
    start: startOfWeek(today, { weekStartsOn: 1 }),
    end: endOfWeek(today, { weekStartsOn: 1 }),
  }
}

export function getPreviousWeekRange() {
  const lastWeek = subWeeks(new Date(), 1)
  return {
    start: startOfWeek(lastWeek, { weekStartsOn: 1 }),
    end: endOfWeek(lastWeek, { weekStartsOn: 1 }),
  }
}

// ... аналогично для месяца и года
```

**src/utils/exportImport.js:**

```javascript
export function exportToJSON(entries, categories, settings) {
  const data = {
    version: '1.0',
    exportDate: new Date().toISOString(),
    entries,
    categories,
    settings,
  }

  const blob = new Blob([JSON.stringify(data, null, 2)], {
    type: 'application/json',
  })

  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = `time-tracker-export-${new Date().toISOString().split('T')[0]}.json`
  a.click()
  URL.revokeObjectURL(url)
}

export function importFromJSON(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader()

    reader.onload = e => {
      try {
        const data = JSON.parse(e.target.result)

        // Валидация
        if (!data.entries || !Array.isArray(data.entries)) {
          throw new Error('Неверная структура файла')
        }

        resolve(data)
      } catch (error) {
        reject(error)
      }
    }

    reader.onerror = () => reject(new Error('Ошибка чтения файла'))
    reader.readAsText(file)
  })
}
```

---

### ШАГ 4: Создание хуков (30 мин)

**src/hooks/useTimer.js:**

```javascript
import { useEffect } from 'react'
import { useTimerStore } from '../store/useTimerStore'
import { useSoundManager } from './useSound'

export function useTimer() {
  const { activeTimer, startTime, elapsedTime, startTimer, stopTimer, updateElapsed } =
    useTimerStore()
  const { playSound } = useSoundManager()

  useEffect(() => {
    if (!activeTimer) return

    const interval = setInterval(() => {
      updateElapsed()

      // Проверка на каждый час
      const hours = Math.floor(elapsedTime / 3600)
      const prevHours = Math.floor((elapsedTime - 1) / 3600)

      if (hours > prevHours) {
        playSound('hourlyAlert')
      }
    }, 1000)

    return () => clearInterval(interval)
  }, [activeTimer, elapsedTime])

  const start = category => {
    startTimer(category)
    playSound('timerStart')
  }

  const stop = () => {
    const elapsed = stopTimer()
    playSound('timerStop')
    return elapsed
  }

  return {
    activeTimer,
    elapsedTime,
    isRunning: !!activeTimer,
    start,
    stop,
  }
}
```

**src/hooks/useNotifications.js:**

```javascript
import { useUIStore } from '../store/useUIStore'

export function useNotifications() {
  const { addNotification, removeNotification } = useUIStore()

  const showNotification = (message, type = 'info', duration = 3000) => {
    const notification = {
      message,
      type,
      duration,
    }

    addNotification(notification)

    if (duration > 0) {
      setTimeout(() => {
        removeNotification(notification.id)
      }, duration)
    }
  }

  return { showNotification }
}
```

**src/hooks/useHotkeys.js:**

```javascript
import { useEffect } from 'react'

export function useHotkeys(keyMap) {
  useEffect(() => {
    const handleKeyDown = e => {
      const isInputFocused =
        document.activeElement.tagName === 'INPUT' || document.activeElement.tagName === 'TEXTAREA'

      if (isInputFocused) return

      const key = e.ctrlKey || e.metaKey ? `Ctrl+${e.key}` : e.key

      if (keyMap[key]) {
        e.preventDefault()
        keyMap[key]()
      }
    }

    document.addEventListener('keydown', handleKeyDown)
    return () => document.removeEventListener('keydown', handleKeyDown)
  }, [keyMap])
}
```

**src/hooks/useSound.js:**

```javascript
import { useRef } from 'react'
import * as Tone from 'tone'
import { useSettingsStore } from '../store/useSettingsStore'

export function useSoundManager() {
  const { notifications } = useSettingsStore()
  const synthRef = useRef(null)

  const playSound = soundName => {
    if (!notifications.sound) return

    if (!synthRef.current) {
      synthRef.current = new Tone.Synth().toDestination()
    }

    const sounds = {
      timerStart: () => {
        synthRef.current.triggerAttackRelease('C5', '8n')
      },
      timerStop: () => {
        synthRef.current.triggerAttackRelease('C5', '8n')
        setTimeout(() => {
          synthRef.current.triggerAttackRelease('C5', '8n')
        }, 100)
      },
      hourlyAlert: () => {
        synthRef.current.triggerAttackRelease('C5', '8n')
        setTimeout(() => synthRef.current.triggerAttackRelease('E5', '8n'), 100)
        setTimeout(() => synthRef.current.triggerAttackRelease('G5', '8n'), 200)
      },
      newEntry: () => {
        synthRef.current.triggerAttackRelease('C4', '16n')
      },
    }

    if (sounds[soundName]) {
      sounds[soundName]()
    }
  }

  return { playSound }
}
```

---

### ШАГ 5: Создание UI компонентов (40 мин)

**src/components/ui/Button.jsx:**

```javascript
export function Button({
  children,
  variant = 'primary',
  size = 'md',
  icon: Icon,
  onClick,
  disabled,
  className = '',
}) {
  const baseStyles = 'glass-button transition-all duration-300'

  const variants = {
    primary: 'bg-blue-500 hover:bg-blue-600 text-white',
    secondary: 'bg-gray-200 dark:bg-gray-700 hover:bg-gray-300 dark:hover:bg-gray-600',
    danger: 'bg-red-500 hover:bg-red-600 text-white',
    success: 'bg-green-500 hover:bg-green-600 text-white',
  }

  const sizes = {
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-4 py-2 text-base',
    lg: 'px-6 py-3 text-lg',
  }

  return (
    <button
      onClick={onClick}
      disabled={disabled}
      className={`
        ${baseStyles}
        ${variants[variant]}
        ${sizes[size]}
        ${disabled ? 'opacity-50 cursor-not-allowed' : 'hover:scale-105 active:scale-95'}
        ${className}
      `}
    >
      {Icon && <Icon className="inline-block mr-2 w-5 h-5" />}
      {children}
    </button>
  )
}
```

**src/components/ui/Notification.jsx:**

```javascript
import { X } from 'lucide-react'
import { useEffect, useState } from 'react'

export function Notification({ notification, onClose }) {
  const [progress, setProgress] = useState(100)

  useEffect(() => {
    if (notification.duration === 0) return

    const interval = setInterval(() => {
      setProgress(prev => {
        const newProgress = prev - 100 / (notification.duration / 100)
        if (newProgress <= 0) {
          onClose()
          return 0
        }
        return newProgress
      })
    }, 100)

    return () => clearInterval(interval)
  }, [notification.duration, onClose])

  const types = {
    success: 'bg-green-500',
    error: 'bg-red-500',
    warning: 'bg-yellow-500',
    info: 'bg-blue-500',
  }

  return (
    <div className="glass-effect rounded-lg shadow-lg p-4 mb-2 min-w-[300px] animate-slide-in">
      <div className="flex items-start justify-between">
        <p className="text-sm font-medium">{notification.message}</p>
        <button onClick={onClose} className="ml-4">
          <X className="w-4 h-4" />
        </button>
      </div>
      {notification.duration > 0 && (
        <div className="mt-2 h-1 bg-gray-200 dark:bg-gray-700 rounded-full overflow-hidden">
          <div
            className={`h-full ${types[notification.type]} transition-all duration-100`}
            style={{ width: `${progress}%` }}
          />
        </div>
      )}
    </div>
  )
}

export function NotificationContainer() {
  const { notifications, removeNotification } = useUIStore()

  return (
    <div className="fixed top-4 right-4 z-50 space-y-2">
      {notifications.map(notification => (
        <Notification
          key={notification.id}
          notification={notification}
          onClose={() => removeNotification(notification.id)}
        />
      ))}
    </div>
  )
}
```

**src/components/ui/Input.jsx:**

```javascript
export function Input({
  label,
  type = 'text',
  value,
  onChange,
  error,
  required,
  placeholder,
  icon: Icon,
}) {
  return (
    <div className="mb-4">
      {label && (
        <label className="block text-sm font-medium mb-2">
          {label}
          {required && <span className="text-red-500 ml-1">*</span>}
        </label>
      )}
      <div className="relative">
        {Icon && (
          <div className="absolute left-3 top-1/2 -translate-y-1/2">
            <Icon className="w-5 h-5 text-gray-400" />
          </div>
        )}
        <input
          type={type}
          value={value}
          onChange={e => onChange(e.target.value)}
          placeholder={placeholder}
          className={`
            w-full px-4 ${Icon ? 'pl-11' : ''} py-2
            glass-effect rounded-lg
            border ${error ? 'border-red-500' : 'border-gray-300 dark:border-gray-600'}
            focus:outline-none focus:ring-2 focus:ring-blue-500
            transition-colors
          `}
        />
      </div>
      {error && <p className="text-red-500 text-sm mt-1">{error}</p>}
    </div>
  )
}
```

---

### ШАГ 6: Создание основных компонентов (60 мин)

**src/components/layout/Header.jsx:**

```javascript
import { Moon, Sun, Info, HelpCircle } from 'lucide-react'
import { useSettingsStore } from '../../store/useSettingsStore'

export function Header({ onShowTutorial, onShowAbout }) {
  const { theme, setTheme } = useSettingsStore()

  const toggleTheme = () => {
    const newTheme = theme === 'light' ? 'dark' : 'light'
    setTheme(newTheme)
    document.documentElement.classList.toggle('dark')
  }

  return (
    <header className="glass-effect rounded-xl p-4 mb-6 flex items-center justify-between">
      <div>
        <h1 className="text-2xl font-bold">Time Tracker Dashboard</h1>
        <p className="text-sm text-gray-500 dark:text-gray-400">Умный учет рабочего времени</p>
      </div>

      <div className="flex gap-2">
        <button onClick={toggleTheme} className="glass-button p-2 rounded-lg">
          {theme === 'light' ? <Moon className="w-5 h-5" /> : <Sun className="w-5 h-5" />}
        </button>

        <button onClick={onShowTutorial} className="glass-button p-2 rounded-lg">
          <HelpCircle className="w-5 h-5" />
        </button>

        <button onClick={onShowAbout} className="glass-button p-2 rounded-lg">
          <Info className="w-5 h-5" />
        </button>
      </div>
    </header>
  )
}
```

**src/components/layout/FloatingPanel.jsx:**

```javascript
import { Play, Square } from 'lucide-react'
import { useTimer } from '../../hooks/useTimer'

export function FloatingPanel() {
  const { activeTimer, elapsedTime, isRunning, start, stop } = useTimer()

  const formatElapsed = seconds => {
    const hours = Math.floor(seconds / 3600)
    const minutes = Math.floor((seconds % 3600) / 60)
    const secs = Math.floor(seconds % 60)
    return `${hours.toString().padStart(2, '0')}:${minutes.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`
  }

  if (!isRunning) return null

  return (
    <div className="fixed top-20 right-4 glass-effect rounded-xl p-4 shadow-2xl z-50 animate-slide-in">
      <div className="flex items-center gap-4">
        <div>
          <div className="text-sm text-gray-500 dark:text-gray-400">{activeTimer}</div>
          <div className="text-2xl font-mono font-bold">{formatElapsed(elapsedTime)}</div>
        </div>

        <button
          onClick={stop}
          className="glass-button p-3 rounded-lg bg-red-500 hover:bg-red-600 text-white"
        >
          <Square className="w-5 h-5" />
        </button>
      </div>
    </div>
  )
}
```

---

### ШАГ 7: Компоненты статистики (45 мин)

**src/components/statistics/StatisticsDashboard.jsx:**

```javascript
import { TrendingUp, TrendingDown, Clock, DollarSign, Award } from 'lucide-react'
import { useEntriesStore } from '../../store/useEntriesStore'
import { calculateStats } from '../../utils/calculations'
import { getTodayRange, getYesterdayRange } from '../../utils/dateHelpers'

export function StatisticsDashboard() {
  const { entries } = useEntriesStore()

  const todayRange = getTodayRange()
  const yesterdayRange = getYesterdayRange()

  const todayStats = calculateStats(entries, todayRange.start, todayRange.end)
  const yesterdayStats = calculateStats(entries, yesterdayRange.start, yesterdayRange.end)

  const earnedTrend = todayStats.totalEarned - yesterdayStats.totalEarned
  const hoursTrend = todayStats.totalHours - yesterdayStats.totalHours

  const cards = [
    {
      title: 'Заработано сегодня',
      value: `${todayStats.totalEarned} ₽`,
      trend: earnedTrend,
      icon: DollarSign,
      color: 'text-green-500',
    },
    {
      title: 'Отработано часов',
      value: todayStats.totalHours,
      trend: hoursTrend,
      icon: Clock,
      color: 'text-blue-500',
    },
    {
      title: 'Средняя ставка',
      value: `${todayStats.averageRate} ₽/ч`,
      icon: Award,
      color: 'text-purple-500',
    },
  ]

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mb-6">
      {cards.map((card, index) => (
        <div
          key={index}
          className="glass-effect rounded-xl p-6 hover:scale-105 transition-transform duration-300"
        >
          <div className="flex items-center justify-between mb-4">
            <h3 className="text-sm font-medium text-gray-500 dark:text-gray-400">{card.title}</h3>
            <card.icon className={`w-6 h-6 ${card.color}`} />
          </div>

          <div className="flex items-end justify-between">
            <div className="text-3xl font-bold">{card.value}</div>

            {card.trend !== undefined && (
              <div
                className={`flex items-center ${card.trend >= 0 ? 'text-green-500' : 'text-red-500'}`}
              >
                {card.trend >= 0 ? (
                  <TrendingUp className="w-4 h-4" />
                ) : (
                  <TrendingDown className="w-4 h-4" />
                )}
                <span className="text-sm ml-1">{Math.abs(card.trend).toFixed(1)}</span>
              </div>
            )}
          </div>
        </div>
      ))}
    </div>
  )
}
```

---

### ШАГ 8: Таблица записей (60 мин)

**src/components/entries/EntriesList.jsx:**

```javascript
import { Plus, Play, Download, Upload, Trash2 } from 'lucide-react'
import { useState } from 'react'
import { useEntriesStore } from '../../store/useEntriesStore'
import { EntryItem } from './EntryItem'
import { Button } from '../ui/Button'

export function EntriesList({ onAddNew, onStartTimer }) {
  const { entries, clearEntries } = useEntriesStore()
  const [searchQuery, setSearchQuery] = useState('')

  const filteredEntries = entries.filter(
    entry =>
      entry.description?.toLowerCase().includes(searchQuery.toLowerCase()) ||
      entry.category?.toLowerCase().includes(searchQuery.toLowerCase())
  )

  // Группировка по датам
  const groupedEntries = filteredEntries.reduce((acc, entry) => {
    if (!acc[entry.date]) {
      acc[entry.date] = []
    }
    acc[entry.date].push(entry)
    return acc
  }, {})

  return (
    <div className="glass-effect rounded-xl p-6">
      {/* Заголовок и кнопки */}
      <div className="flex items-center justify-between mb-6">
        <h2 className="text-xl font-bold">Записи времени</h2>

        <div className="flex gap-2">
          <Button onClick={onAddNew} icon={Plus}>
            Новая запись
          </Button>
          <Button onClick={onStartTimer} variant="success" icon={Play}>
            Старт таймер
          </Button>
        </div>
      </div>

      {/* Поиск */}
      <input
        type="text"
        placeholder="Поиск по описанию или категории..."
        value={searchQuery}
        onChange={e => setSearchQuery(e.target.value)}
        className="w-full mb-4 px-4 py-2 glass-effect rounded-lg border border-gray-300 dark:border-gray-600"
      />

      {/* Список записей */}
      <div className="space-y-6">
        {Object.entries(groupedEntries)
          .sort(([dateA], [dateB]) => new Date(dateB) - new Date(dateA))
          .map(([date, dateEntries]) => (
            <div key={date}>
              <h3 className="text-lg font-semibold mb-3 text-gray-700 dark:text-gray-300">
                {new Date(date).toLocaleDateString('ru-RU', {
                  weekday: 'long',
                  year: 'numeric',
                  month: 'long',
                  day: 'numeric',
                })}
              </h3>

              <div className="space-y-2">
                {dateEntries.map(entry => (
                  <EntryItem key={entry.id} entry={entry} />
                ))}
              </div>

              {/* Итоги за день */}
              <div className="mt-3 pt-3 border-t border-gray-200 dark:border-gray-700">
                <div className="flex justify-between text-sm font-medium">
                  <span>Итого за день:</span>
                  <span>
                    {dateEntries.reduce((sum, e) => sum + parseFloat(e.duration), 0).toFixed(2)} ч
                    {' | '}
                    {dateEntries.reduce((sum, e) => sum + parseFloat(e.earned), 0).toFixed(2)} ₽
                  </span>
                </div>
              </div>
            </div>
          ))}
      </div>

      {filteredEntries.length === 0 && (
        <div className="text-center py-12 text-gray-500">
          Записей пока нет. Создайте первую запись!
        </div>
      )}
    </div>
  )
}
```

**src/components/entries/EntryItem.jsx:**

```javascript
import { Edit2, Trash2, Clock } from 'lucide-react'
import { useEntriesStore } from '../../store/useEntriesStore'

export function EntryItem({ entry }) {
  const { deleteEntry } = useEntriesStore()

  return (
    <div className="glass-effect rounded-lg p-4 hover:scale-102 transition-transform duration-200 border border-transparent hover:border-blue-500">
      <div className="flex items-start justify-between">
        <div className="flex-1">
          <div className="flex items-center gap-3 mb-2">
            {/* Категория с иконкой */}
            <span className="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200">
              {entry.category}
            </span>

            {/* Время */}
            <span className="text-sm text-gray-500 dark:text-gray-400">
              {entry.start} - {entry.end}
            </span>

            {/* Длительность */}
            <span className="text-sm font-medium">{entry.duration} ч</span>

            {/* Индикатор таймера */}
            {!entry.isManual && <Clock className="w-4 h-4 text-green-500" />}
          </div>

          {/* Описание */}
          {entry.description && (
            <p className="text-sm text-gray-600 dark:text-gray-400 mb-2">{entry.description}</p>
          )}

          {/* Ставка и заработок */}
          <div className="flex items-center gap-4 text-sm">
            <span className="text-gray-500">
              Ставка: <span className="font-medium">{entry.rate} ₽/ч</span>
            </span>
            <span className="text-green-600 dark:text-green-400 font-semibold">
              Заработано: {entry.earned} ₽
            </span>
          </div>
        </div>

        {/* Действия */}
        <div className="flex gap-2 ml-4">
          <button className="p-2 glass-button rounded-lg hover:bg-blue-500 hover:text-white transition-colors">
            <Edit2 className="w-4 h-4" />
          </button>
          <button
            onClick={() => deleteEntry(entry.id)}
            className="p-2 glass-button rounded-lg hover:bg-red-500 hover:text-white transition-colors"
          >
            <Trash2 className="w-4 h-4" />
          </button>
        </div>
      </div>
    </div>
  )
}
```

---

### ШАГ 9: Модальные окна (45 мин)

**src/components/modals/EditEntryModal.jsx:**

```javascript
import { Dialog } from '@headlessui/react'
import { X } from 'lucide-react'
import { useState } from 'react'
import { Input } from '../ui/Input'
import { Button } from '../ui/Button'

export function EditEntryModal({ isOpen, onClose, entry, onSave }) {
  const [formData, setFormData] = useState(
    entry || {
      date: new Date().toISOString().split('T')[0],
      start: '',
      end: '',
      category: '',
      description: '',
      rate: 1000,
    }
  )

  const handleSave = () => {
    // Расчет duration и earned
    const duration = calculateDuration(formData.start, formData.end)
    const earned = calculateEarned(duration, formData.rate)

    onSave({
      ...formData,
      duration,
      earned,
    })

    onClose()
  }

  return (
    <Dialog open={isOpen} onClose={onClose} className="relative z-50">
      <div className="fixed inset-0 bg-black/30 backdrop-blur-sm" aria-hidden="true" />

      <div className="fixed inset-0 flex items-center justify-center p-4">
        <Dialog.Panel className="glass-effect rounded-xl p-6 max-w-md w-full">
          <div className="flex items-center justify-between mb-4">
            <Dialog.Title className="text-xl font-bold">
              {entry ? 'Редактировать запись' : 'Новая запись'}
            </Dialog.Title>
            <button onClick={onClose}>
              <X className="w-5 h-5" />
            </button>
          </div>

          <div className="space-y-4">
            <Input
              label="Дата"
              type="date"
              value={formData.date}
              onChange={value => setFormData({ ...formData, date: value })}
              required
            />

            <div className="grid grid-cols-2 gap-4">
              <Input
                label="Время начала"
                type="time"
                value={formData.start}
                onChange={value => setFormData({ ...formData, start: value })}
                required
              />

              <Input
                label="Время окончания"
                type="time"
                value={formData.end}
                onChange={value => setFormData({ ...formData, end: value })}
                required
              />
            </div>

            {/* Здесь добавить Select для категории */}

            <Input
              label="Описание"
              type="text"
              value={formData.description}
              onChange={value => setFormData({ ...formData, description: value })}
              placeholder="Что вы делали?"
            />

            <Input
              label="Ставка (₽/час)"
              type="number"
              value={formData.rate}
              onChange={value => setFormData({ ...formData, rate: value })}
              required
            />
          </div>

          <div className="flex justify-end gap-2 mt-6">
            <Button variant="secondary" onClick={onClose}>
              Отмена
            </Button>
            <Button onClick={handleSave}>Сохранить</Button>
          </div>
        </Dialog.Panel>
      </div>
    </Dialog>
  )
}
```

---

### ШАГ 10: Графики (30 мин)

**src/components/charts/EarningsChart.jsx:**

```javascript
import {
  LineChart,
  Line,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer,
} from 'recharts'
import { useEntriesStore } from '../../store/useEntriesStore'

export function EarningsChart() {
  const { entries } = useEntriesStore()

  // Подготовка данных за последние 7 дней
  const last7Days = Array.from({ length: 7 }, (_, i) => {
    const date = new Date()
    date.setDate(date.getDate() - i)
    return date.toISOString().split('T')[0]
  }).reverse()

  const chartData = last7Days.map(date => {
    const dayEntries = entries.filter(e => e.date === date)
    const earned = dayEntries.reduce((sum, e) => sum + parseFloat(e.earned), 0)

    return {
      date: new Date(date).toLocaleDateString('ru-RU', { day: 'numeric', month: 'short' }),
      earned: parseFloat(earned.toFixed(2)),
    }
  })

  return (
    <div className="glass-effect rounded-xl p-6 mb-6">
      <h2 className="text-xl font-bold mb-4">График заработка</h2>

      <ResponsiveContainer width="100%" height={300}>
        <LineChart data={chartData}>
          <CartesianGrid strokeDasharray="3 3" />
          <XAxis dataKey="date" />
          <YAxis />
          <Tooltip />
          <Legend />
          <Line
            type="monotone"
            dataKey="earned"
            stroke="#3B82F6"
            strokeWidth={2}
            name="Заработано (₽)"
          />
        </LineChart>
      </ResponsiveContainer>
    </div>
  )
}
```

---

### ШАГ 11: Сборка App.jsx (20 мин)

**src/App.jsx:**

```javascript
import { Header } from './components/layout/Header'
import { FloatingPanel } from './components/layout/FloatingPanel'
import { StatisticsDashboard } from './components/statistics/StatisticsDashboard'
import { EntriesList } from './components/entries/EntriesList'
import { EarningsChart } from './components/charts/EarningsChart'
import { NotificationContainer } from './components/ui/Notification'
import { EditEntryModal } from './components/modals/EditEntryModal'
import { useUIStore } from './store/useUIStore'
import { useEntriesStore } from './store/useEntriesStore'
import { useHotkeys } from './hooks/useHotkeys'
import { useTimer } from './hooks/useTimer'

function App() {
  const { modals, openModal, closeModal } = useUIStore()
  const { addEntry } = useEntriesStore()
  const { start } = useTimer()

  // Горячие клавиши
  useHotkeys({
    n: () => openModal('editEntry'),
    t: () => openModal('editEntry'),
    s: () => start('Разработка'), // можно сделать выбор категории
  })

  const handleSaveEntry = entryData => {
    addEntry(entryData)
    closeModal('editEntry')
  }

  return (
    <div className="min-h-screen bg-gray-100 dark:bg-gray-900 transition-colors duration-300">
      <div className="max-w-7xl mx-auto p-6">
        <Header
          onShowTutorial={() => openModal('tutorial')}
          onShowAbout={() => openModal('about')}
        />

        <StatisticsDashboard />

        <EarningsChart />

        <EntriesList
          onAddNew={() => openModal('editEntry')}
          onStartTimer={() => start('Разработка')}
        />

        <FloatingPanel />

        <NotificationContainer />

        <EditEntryModal
          isOpen={modals.editEntry.isOpen}
          onClose={() => closeModal('editEntry')}
          entry={modals.editEntry.entry}
          onSave={handleSaveEntry}
        />
      </div>
    </div>
  )
}

export default App
```

---

## ✅ ЧЕКЛИСТ ДЛЯ ПРОВЕРКИ

После реализации проверьте:

### Функционал:

- [ ] Добавление записи вручную
- [ ] Редактирование записи
- [ ] Удаление записи
- [ ] Таймер (старт/стоп)
- [ ] Автоматический расчет duration и earned
- [ ] Группировка записей по дням
- [ ] Фильтрация и поиск
- [ ] Экспорт в JSON
- [ ] Импорт из JSON
- [ ] Валидация данных
- [ ] Уведомления
- [ ] Звуковые сигналы
- [ ] Горячие клавиши
- [ ] Темная/светлая тема
- [ ] Графики
- [ ] План/факт анализ
- [ ] Автобэкапы
- [ ] Undo/Redo

### UI/UX:

- [ ] Адаптивный дизайн
- [ ] Анимации и переходы
- [ ] Glass effect
- [ ] Hover эффекты
- [ ] Loading состояния
- [ ] Пустые состояния
- [ ] Ошибки валидации
- [ ] Модальные окна
- [ ] Плавающая панель таймера

### Производительность:

- [ ] Быстрая загрузка
- [ ] Плавные анимации
- [ ] Нет лагов при скролле
- [ ] Оптимизированные рендеры
- [ ] Дебаунсинг поиска

---

## 🎨 КАСТОМИЗАЦИЯ СТИЛЕЙ

Все стили легко настраиваются через:

1. **tailwind.config.js** - цвета, шрифты, размеры
2. **index.css** - глобальные стили, анимации
3. **CSS переменные** - динамические значения

Для изменения визуала достаточно редактировать классы Tailwind в компонентах.

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ МАТЕРИАЛЫ

### Документация:

- React: https://react.dev
- Zustand: https://github.com/pmndrs/zustand
- Tailwind CSS: https://tailwindcss.com
- Recharts: https://recharts.org
- Headless UI: https://headlessui.com
- Tone.js: https://tonejs.github.io

### Полезные ссылки:

- Иконки: https://lucide.dev
- Цвета: https://coolors.co
- Анимации: https://animate.style

---

## 🎯 ФИНАЛЬНЫЕ РЕКОМЕНДАЦИИ ДЛЯ CURSOR

Промт для Cursor AI:

```
Привет! Мне нужно создать приложение Time Tracker Dashboard по следующему техническому заданию.

Я - начинающий разработчик (zero coder), поэтому прошу:

1. СОЗДАВАТЬ все файлы по очереди, строго следуя структуре проекта из ТЗ
2. ОБЪЯСНЯТЬ каждый шаг простым языком
3. ПРОВЕРЯТЬ работоспособность после каждого шага
4. ИСПОЛЬЗОВАТЬ точные имена файлов и компонентов из ТЗ
5. НЕ ПРОПУСКАТЬ ни один функционал из описания
6. ПИСАТЬ комментарии в коде на русском языке
7. ПОКАЗЫВАТЬ как запускать и тестировать приложение

Начнем с Шага 1: Инициализация проекта.
После его завершения я скажу "Готово, идем дальше", и ты перейдешь к Шагу 2.

Готов? Начинай!
```

---

**УСПЕХОВ В РАЗРАБОТКЕ! 🚀**
