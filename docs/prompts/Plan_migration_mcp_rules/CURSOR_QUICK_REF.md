# ⚡ **CURSOR QUICK REFERENCE**

**Шпаргалка для zero-coder | Держи всегда под рукой!**

---

## 🎯 **ЧТО У МЕНЯ ЕСТЬ**

### **.cursorrules** - Автоматические правила

- ✅ Работает сразу после установки
- 🎨 Glassmorphism дизайн по умолчанию
- 📝 JSDoc комментарии автоматически
- ⚛️ React best practices из коробки

### **mcp.json** - MCP серверы

- 📚 @docs-search - документация (работает сразу)
- 🌐 @web-search - интернет (нужен API ключ)
- 🐙 @github - GitHub примеры (нужен токен)
- 💾 @filesystem - локальные файлы (опционально)

---

## ⌨️ **ГОРЯЧИЕ КЛАВИШИ**

```
Cmd/Ctrl + L     Открыть AI чат
Cmd/Ctrl + K     Редактировать код
Cmd/Ctrl + I     Composer (мульти-файлы)
Cmd/Ctrl + /     Добавить в контекст
Tab              Принять предложение AI
Cmd/Ctrl + P     Найти файл
```

---

## 🔍 **MCP КОМАНДЫ**

### **Документация (работает сразу):**

```
@docs-search React useState
@docs-search Tailwind responsive
@docs-search Vite configuration
```

### **Интернет (нужен Brave API ключ):**

```
@web-search react hooks best practices
@web-search tailwind glassmorphism tutorial
@web-search fix useEffect infinite loop
```

### **GitHub (нужен токен):**

```
@github react localStorage hook examples
@github recharts responsive container
@github tailwind components library
```

### **Встроенные (всегда работают):**

```
@codebase где используется useTimer?
@filename.jsx добавить файл в контекст
```

---

## 🎨 **GLASSMORPHISM СТИЛИ**

### **Базовый контейнер:**

```css
backdrop-blur-md
bg-white/10 dark:bg-white/5
border border-white/20
rounded-2xl
shadow-xl
p-6
```

### **С hover:**

```css
transition-all duration-300
hover:bg-white/15
hover:scale-105
```

### **Варианты цветов:**

```css
Primary:  bg-blue-500/20 border-blue-500/30 text-blue-400
Success:  bg-green-500/20 border-green-500/30 text-green-400
Danger:   bg-red-500/20 border-red-500/30 text-red-400
Warning:  bg-yellow-500/20 border-yellow-500/30 text-yellow-400
```

---

## 🚀 **БЫСТРЫЕ ПРОМПТЫ**

### **Создать компонент:**

```
"Создай компонент Button в src/components/Button.jsx
следуя правилам из .cursorrules"
```

### **Конвертировать HTML:**

```
"Конвертируй этот HTML в React:
[вставь HTML]
Используй Tailwind и glassmorphism"
```

### **Исправить баг:**

```
"ПРОБЛЕМА: [описание]
КОД: [код]
ОШИБКА: [из консоли]
Исправь"
```

### **Найти решение:**

```
"@web-search [проблема]
Покажи решение с примером"
```

### **Оптимизировать:**

```
"Оптимизируй @Component.jsx
используя React.memo и useMemo"
```

---

## 📋 **ШАБЛОН КОМПОНЕНТА**

```javascript
import React from 'react'
import PropTypes from 'prop-types'

/**
 * Component description
 * @component
 */
const ComponentName = ({ prop1, prop2 }) => {
  // 1. Hooks
  const [state, setState] = React.useState(null)

  // 2. Handlers
  const handleClick = () => {
    // logic
  }

  // 3. Render
  return (
    <div className="backdrop-blur-md bg-white/10 border border-white/20 rounded-lg p-4">
      {/* content */}
    </div>
  )
}

ComponentName.propTypes = {
  prop1: PropTypes.string.isRequired,
  prop2: PropTypes.func,
}

export default ComponentName
```

---

## 🔧 **TROUBLESHOOTING**

### **.cursorrules не работает:**

```bash
1. Проверь что файл в корне: /project/.cursorrules
2. Перезапусти Cursor
3. Создай тестовый компонент
```

### **MCP не работает:**

```bash
1. Settings → Features → MCP включен?
2. mcp.json добавлен в настройки?
3. Перезапусти Cursor
4. Cmd+Shift+P → "Show Console" (посмотри ошибки)
```

### **@docs-search не находит:**

```bash
1. Проверь интернет
2. Упрости запрос
3. Убедись что MCP включен
```

---

## 📊 **СТРУКТУРА ПРОЕКТА**

```
time-tracker/
├── .cursorrules          ← ПОЛОЖИ СЮДА
├── mcp.json              ← ПОЛОЖИ СЮДА
├── src/
│   ├── components/
│   │   ├── ui/           ← Button, Input, Select
│   │   ├── features/     ← Timer, Statistics, Entries
│   │   └── layout/       ← Header, Footer
│   ├── hooks/            ← useTimer, useLocalStorage
│   ├── utils/            ← formatters, validators
│   └── services/         ← soundService, storageService
```

---

## 💡 **PRO TIPS**

### **Tip 1: Комбинируй источники**

```
"@docs-search React useEffect
Также @web-search useEffect best practices
Покажи @github примеры"
```

### **Tip 2: Добавляй контекст**

```
"@Button.jsx @Input.jsx
Создай Select похожий на эти компоненты"
```

### **Tip 3: Итеративно**

```
Шаг 1: "Создай базовый Timer"
→ [проверка]
Шаг 2: "Добавь кнопку Pause"
→ [проверка]
```

### **Tip 4: Используй для обучения**

```
"@docs-search объясни useMemo простыми словами"
"Почему ты использовал useCallback здесь?"
```

---

## 🎯 **WORKFLOW**

### **1. Создание компонента:**

```bash
1. Cmd+L
2. "Создай компонент [Name] следуя .cursorrules"
3. Проверь результат
4. git commit
```

### **2. Решение проблемы:**

```bash
1. "@docs-search [проблема]"
2. Если не помогло: "@web-search [проблема]"
3. Если нужны примеры: "@github [примеры]"
```

### **3. Миграция HTML:**

```bash
1. Скопируй HTML блок
2. "Конвертируй в React следуя .cursorrules"
3. Проверь в браузере
4. Итерируй если нужно
```

---

## ✅ **CHECKLIST**

### **Базовая настройка:**

```
□ .cursorrules в корне проекта
□ Cursor перезапущен
□ Правила работают (проверил)
```

### **MCP настройка:**

```
□ mcp.json в корне
□ MCP включен в Settings
□ @docs-search работает
```

### **Опциональное:**

```
□ Brave API ключ (web-search)
□ GitHub токен (github)
□ Папки templates (filesystem)
```

---

## 🚨 **ВАЖНО ПОМНИТЬ**

```
✓ Всегда используй Tailwind (НЕ inline styles)
✓ Всегда добавляй PropTypes
✓ Всегда добавляй JSDoc
✓ Всегда glassmorphism дизайн
✓ Всегда dark mode support (dark: классы)
✓ Всегда responsive (sm:, md:, lg:)

✗ НИКОГДА var (только const/let)
✗ НИКОГДА классовые компоненты
✗ НИКОГДА прямые мутации state
✗ НИКОГДА inline styles
```

---

## 📚 **ССЫЛКИ**

### **Документация:**

- React: https://react.dev
- Tailwind: https://tailwindcss.com
- Vite: https://vitejs.dev
- Recharts: https://recharts.org

### **API ключи:**

- Brave Search: https://brave.com/search/api/
- GitHub Token: https://github.com/settings/tokens

### **Твои файлы:**

- .cursorrules - правила кодирования
- mcp.json - MCP конфигурация
- cursor-zero-coder-guide.md - полный гайд
- CURSOR_SETUP_GUIDE.md - инструкция установки

---

## 🎯 **ТИПИЧНЫЕ ЗАДАЧИ**

### **Задача 1: Создать UI компонент**

```
Cmd+L → "Создай компонент Button с:
- 3 варианта (primary, success, danger)
- Glassmorphism стили
- Hover эффект
- PropTypes
Следуй .cursorrules"
```

### **Задача 2: Создать хук**

```
Cmd+L → "Создай хук useTimer для таймера:
- start, pause, stop функции
- useState для isRunning и elapsedTime
- useEffect с интервалом (не забудь cleanup!)
Следуй .cursorrules"
```

### **Задача 3: Найти решение**

```
Cmd+L → "@web-search react useEffect runs on every render fix"
Или: "@docs-search React useEffect dependencies"
```

### **Задача 4: Конвертировать HTML**

```
Cmd+L → "Конвертируй этот HTML в React:
<div class="timer">
  <span id="display">00:00</span>
  <button onclick="start()">Start</button>
</div>

Используй:
- Функциональный компонент
- Tailwind вместо class
- onClick вместо onclick
- Glassmorphism стили
Следуй .cursorrules"
```

---

## 💎 **ЗОЛОТЫЕ ПРАВИЛА**

```
1. Cursor пишет код - ты принимаешь решения
2. Один промпт = одна задача
3. Контекст (@files) решает всё
4. Итерируй малыми шагами
5. Всегда проверяй результат
6. Git после каждой фичи
7. Читай что генерируется
8. Не бойся спрашивать AI
```

---

## 🚀 **НАЧНИ ПРЯМО СЕЙЧАС**

```bash
# 1. Положи файлы в проект
cp .cursorrules /твой-проект/
cp mcp.json /твой-проект/

# 2. Открой Cursor
code /твой-проект

# 3. Настрой MCP (Settings → MCP)

# 4. Перезапусти Cursor

# 5. Первый промпт:
Cmd+L → "Создай компонент Button следуя .cursorrules"

# ГОТОВО! 🎉
```

---

**Распечатай эту страницу и держи рядом с компом!** 📌

**Удачи с миграцией! Ты справишься!** 💪
