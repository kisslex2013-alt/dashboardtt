# 🚀 **УСТАНОВКА CURSOR КОНФИГУРАЦИИ**

**Для zero-coder | Time Tracker HTML → React Migration**

---

## 📦 **ЧТО Я ПОЛУЧИЛ?**

### **2 важных файла:**

1. **`.cursorrules`** - правила для Cursor AI
   - Автоматически настраивает стиль кода
   - Включает best practices
   - Оптимизирован для миграции HTML → React
   - Работает сразу после установки

2. **`mcp.json`** - конфигурация MCP серверов
   - Подключает AI к документации
   - Даёт доступ к поиску в интернете
   - Интегрирует с GitHub
   - Требует минимальной настройки

---

## ⚡ **БЫСТРАЯ УСТАНОВКА (5 минут)**

### **Шаг 1: Установи .cursorrules**

```bash
# 1. Перейди в папку твоего проекта
cd /path/to/time-tracker

# 2. Скопируй файл .cursorrules в корень проекта
# Файл должен быть в корне: /time-tracker/.cursorrules

# 3. Готово! Cursor автоматически прочитает правила
```

**Проверка:**

- Файл `.cursorrules` должен быть в корне проекта
- Открой Cursor в этой папке
- Cursor автоматически применит правила

---

### **Шаг 2: Установи MCP (опционально, но рекомендуется)**

```bash
# 1. Скопируй mcp.json в корень проекта
# /time-tracker/mcp.json

# 2. Открой Cursor Settings (Cmd+, или Ctrl+,)

# 3. Features → Model Context Protocol → Enable ✓

# 4. MCP → Add Configuration File → выбери свой mcp.json

# 5. Перезапусти Cursor
```

---

## 🎯 **ЧТО РАБОТАЕТ СРАЗУ (из коробки)**

### **Из .cursorrules:**

✅ **Автоматический стиль кода**

```javascript
// Cursor будет автоматически создавать компоненты в таком стиле:
import React from 'react'
import PropTypes from 'prop-types'

/**
 * Component description
 * @component
 */
const Button = ({ onClick, children }) => {
  return (
    <button
      onClick={onClick}
      className="backdrop-blur-md bg-white/10 border border-white/20 rounded-lg"
    >
      {children}
    </button>
  )
}

Button.propTypes = {
  onClick: PropTypes.func.isRequired,
  children: PropTypes.node.isRequired,
}

export default Button
```

✅ **Glassmorphism по умолчанию**

```javascript
// Все компоненты будут с glassmorphism эффектом
className = 'backdrop-blur-md bg-white/10 dark:bg-white/5 border border-white/20'
```

✅ **JSDoc комментарии**

```javascript
// Функции автоматически с документацией
/**
 * Format seconds to HH:MM:SS
 * @param {number} seconds - Time in seconds
 * @returns {string} Formatted time
 */
```

✅ **PropTypes валидация**

```javascript
// Компоненты автоматически с PropTypes
ComponentName.propTypes = {
  title: PropTypes.string.isRequired,
  onClick: PropTypes.func,
}
```

---

### **Из mcp.json (после включения):**

✅ **@docs-search** - работает сразу

```
Cmd+L → "@docs-search React useState"
AI найдёт в официальной документации React
```

✅ **sequential-thinking** - работает сразу

```
Cursor будет показывать процесс рассуждения при решении задач
```

✅ **memory** - работает сразу

```
Cursor запоминает твой стиль и предпочтения
```

---

## 🔧 **НАСТРОЙКА MCP СЕРВЕРОВ (опционально)**

### **Сервер 1: docs-search (РЕКОМЕНДУЕТСЯ) ✅**

**Что даёт:** Прямой доступ к документации React, Tailwind, Vite

**Уже настроен!** Работает сразу после включения MCP.

**Использование:**

```
@docs-search React useEffect
@docs-search Tailwind responsive classes
@docs-search Vite configuration
```

---

### **Сервер 2: web-search (ПОЛЕЗНО, нужен API ключ)**

**Что даёт:** Поиск в интернете, Stack Overflow, актуальные решения

**Настройка (10 минут):**

1. **Получи бесплатный API ключ:**
   - Иди на https://brave.com/search/api/
   - Зарегистрируйся (бесплатно)
   - Получи API ключ (2000 запросов/месяц бесплатно)

2. **Добавь ключ в mcp.json:**

```json
"web-search": {
  "env": {
    "BRAVE_API_KEY": "твой_ключ_сюда"  // ← вставь свой ключ
  },
  "enabled": true  // ← измени на true
}
```

3. **Перезапусти Cursor**

**Использование:**

```
@web-search react hooks best practices 2025
@web-search tailwind glassmorphism tutorial
@web-search how to fix useEffect infinite loop
```

---

### **Сервер 3: github (ПОЛЕЗНО, нужен токен)**

**Что даёт:** Поиск примеров кода в GitHub репозиториях

**Настройка (5 минут):**

1. **Создай GitHub токен:**
   - Иди на https://github.com/settings/tokens
   - "Generate new token (classic)"
   - Выбери scope: `public_repo`
   - Создай токен
   - **Сохрани токен!** (он покажется только один раз)

2. **Добавь токен в mcp.json:**

```json
"github": {
  "env": {
    "GITHUB_TOKEN": "твой_токен_сюда"  // ← вставь свой токен
  },
  "enabled": true  // ← измени на true
}
```

3. **Перезапусти Cursor**

**Использование:**

```
@github примеры React useLocalStorage hook
@github recharts responsive chart examples
@github tailwind glassmorphism components
```

---

### **Сервер 4: filesystem (ОПЦИОНАЛЬНО)**

**Что даёт:** Доступ к файлам вне проекта (шаблоны, сниппеты)

**Настройка (опционально):**

1. **Создай папки для шаблонов:**

```bash
mkdir ~/templates
mkdir ~/snippets
```

2. **Добавь туда свои шаблоны** (например, Button.jsx)

3. **Отредактируй mcp.json:**

```json
"filesystem": {
  "args": [
    "-y",
    "@modelcontextprotocol/server-filesystem",
    "/Users/ТВОЙ_USER/templates",  // ← замени на свой путь
    "/Users/ТВОЙ_USER/snippets"    // ← замени на свой путь
  ],
  "enabled": true  // ← измени на true
}
```

4. **Перезапусти Cursor**

**Использование:**

```
@filesystem прочитай ~/templates/Button.jsx
@filesystem покажи мои сниппеты
```

---

## 🎯 **КАК ПРОВЕРИТЬ ЧТО ВСЁ РАБОТАЕТ**

### **Тест 1: .cursorrules работает?**

```bash
# 1. Открой Cursor в папке проекта
# 2. Создай новый файл Test.jsx
# 3. Нажми Cmd+L (открыть чат)
# 4. Напиши: "Создай простой компонент Button"
# 5. Cursor должен создать компонент с:
#    - JSDoc комментариями ✓
#    - PropTypes ✓
#    - Glassmorphism стилями ✓
#    - Tailwind классами ✓
```

**Если всё так → .cursorrules работает! ✅**

---

### **Тест 2: MCP docs-search работает?**

```bash
# 1. Открой чат (Cmd+L)
# 2. Напиши: "@docs-search React useState"
# 3. Cursor должен найти инфу из React.dev
```

**Если нашёл → MCP работает! ✅**

---

### **Тест 3: web-search работает? (если настроил)**

```bash
# 1. Открой чат (Cmd+L)
# 2. Напиши: "@web-search tailwind glassmorphism"
# 3. Cursor должен найти инфу из интернета
```

**Если нашёл → web-search работает! ✅**

---

## 💡 **КАК ИСПОЛЬЗОВАТЬ В РАБОТЕ**

### **Сценарий 1: Создание нового компонента**

```
Ты в Cursor (Cmd+L):

"Создай компонент Timer в src/components/Timer.jsx

ФУНКЦИОНАЛ:
- Отображение времени HH:MM:SS
- Кнопки Start, Pause, Stop
- Использовать хук @useTimer.js

СТИЛЬ:
- Glassmorphism (как в @Button.jsx)
- Dark mode support
- Responsive"

Cursor:
- Прочитает .cursorrules
- Создаст компонент по шаблону
- Применит все правила автоматически
- Добавит JSDoc, PropTypes, glassmorphism стили
```

---

### **Сценарий 2: Решение проблемы**

```
Проблема: useEffect выполняется бесконечно

Шаг 1: "@docs-search React useEffect dependencies"
→ Прочитай официальную документацию

Шаг 2 (если не помогло): "@web-search useEffect infinite loop fix"
→ Найди решения в интернете

Шаг 3 (если нужны примеры): "@github react useEffect cleanup examples"
→ Посмотри как делают другие
```

---

### **Сценарий 3: Конвертация HTML в React**

```
Ты: "Конвертируй этот HTML в React компонент:

<div class="timer-container">
  <h2 id="timer-display">00:00:00</h2>
  <button onclick="startTimer()">Start</button>
</div>

Следуй правилам из .cursorrules"

Cursor:
- Прочитает правила
- Конвертирует в React
- Применит Tailwind вместо CSS классов
- Добавит glassmorphism
- Добавит PropTypes
- Добавит JSDoc
```

---

## 🎨 **ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ**

### **Пример 1: Спросить о React**

```
Cmd+L → "@docs-search как работает useMemo"

Результат: AI найдёт в React.dev и объяснит
```

---

### **Пример 2: Найти решение проблемы**

```
Cmd+L → "@web-search react localStorage not updating"

Результат: AI найдёт решения на Stack Overflow
```

---

### **Пример 3: Найти примеры кода**

```
Cmd+L → "@github примеры recharts responsive container"

Результат: AI покажет реальный код из GitHub
```

---

### **Пример 4: Создать компонент по правилам**

```
Cmd+L → "Создай компонент Input для ввода текста"

Результат: Cursor создаст с учётом всех правил из .cursorrules:
- Функциональный компонент ✓
- JSDoc комментарии ✓
- PropTypes ✓
- Glassmorphism стили ✓
- Tailwind классы ✓
- Dark mode ✓
- Responsive ✓
```

---

## 🔥 **PRO TIPS**

### **Tip 1: Комбинируй источники**

```
"@docs-search React useEffect
Также @web-search useEffect best practices 2025
И покажи @github примеры"

Cursor использует все 3 источника для лучшего ответа!
```

---

### **Tip 2: Ссылайся на .cursorrules**

```
"Создай компонент Button следуя правилам из .cursorrules"

Cursor применит все правила автоматически!
```

---

### **Tip 3: Используй для обучения**

```
"@docs-search объясни React hooks простыми словами"

Cursor найдёт в документации и объяснит понятно
```

---

### **Tip 4: Проверяй актуальность**

```
"@web-search React 18 новые фичи"

Для актуальной информации используй web-search
```

---

## 🚨 **TROUBLESHOOTING**

### **Проблема: .cursorrules не применяется**

**Решение:**

```bash
1. Проверь что файл называется именно .cursorrules (с точкой)
2. Проверь что файл в КОРНЕ проекта
3. Перезапусти Cursor
4. Попробуй создать компонент - правила должны применяться
```

---

### **Проблема: MCP серверы не работают**

**Решение:**

```bash
1. Проверь: Cursor Settings → Features → MCP включен?
2. Проверь: mcp.json добавлен в настройки?
3. Перезапусти Cursor
4. Открой консоль: Cmd+Shift+P → "Show Console"
5. Посмотри на ошибки
```

---

### **Проблема: @docs-search не находит**

**Решение:**

```bash
1. Проверь интернет соединение
2. Попробуй упростить запрос: "@docs-search useState" вместо длинного
3. Убедись что MCP включен
4. Перезапусти Cursor
```

---

### **Проблема: web-search выдаёт ошибку API ключа**

**Решение:**

```bash
1. Проверь что API ключ правильно вставлен (без пробелов)
2. Проверь что ключ валиден (не истёк)
3. Проверь лимиты (2000 запросов/месяц для free tier)
4. Попробуй получить новый ключ
```

---

### **Проблема: GitHub токен не работает**

**Решение:**

```bash
1. Проверь что выбран scope: public_repo
2. Проверь что токен не истёк
3. Создай новый токен если нужно
4. Убедись что токен сохранён правильно (скопирован полностью)
```

---

## 📊 **СРАВНЕНИЕ: ДО и ПОСЛЕ**

### **ДО настройки:**

```
Ты: "Создай компонент Button"

Cursor:
function Button() {
  return <button>Click me</button>
}

// Нет стилей, нет PropTypes, нет комментариев
```

---

### **ПОСЛЕ настройки с .cursorrules:**

```
Ты: "Создай компонент Button"

Cursor:
import React from 'react';
import PropTypes from 'prop-types';

/**
 * Reusable button component with glassmorphism styling
 * @component
 */
const Button = ({ onClick, children, variant = 'primary' }) => {
  const variantClasses = {
    primary: 'bg-blue-500/20 border-blue-500/30 hover:bg-blue-500/30',
    success: 'bg-green-500/20 border-green-500/30 hover:bg-green-500/30',
    danger: 'bg-red-500/20 border-red-500/30 hover:bg-red-500/30'
  };

  return (
    <button
      onClick={onClick}
      className={`
        backdrop-blur-md
        border
        rounded-lg
        px-4 py-2
        transition-all duration-300
        hover:scale-105
        ${variantClasses[variant]}
      `}
    >
      {children}
    </button>
  );
};

Button.propTypes = {
  onClick: PropTypes.func.isRequired,
  children: PropTypes.node.isRequired,
  variant: PropTypes.oneOf(['primary', 'success', 'danger'])
};

export default Button;

// Полный компонент с:
// ✓ JSDoc
// ✓ PropTypes
// ✓ Glassmorphism
// ✓ Варианты
// ✓ Hover эффекты
// ✓ Responsive
```

**ОГРОМНАЯ РАЗНИЦА! 🚀**

---

## 🎯 **ФИНАЛЬНЫЙ ЧЕКЛИСТ**

### **Обязательная настройка (5 минут):**

```
□ .cursorrules скопирован в корень проекта
□ Cursor перезапущен
□ Проверил что правила работают (создал тестовый компонент)
```

---

### **Рекомендуемая настройка (15 минут):**

```
□ mcp.json скопирован в корень проекта
□ MCP включен в Cursor Settings
□ mcp.json добавлен в конфигурацию
□ Cursor перезапущен
□ Проверил @docs-search (работает из коробки)
□ Проверил sequential-thinking (работает из коробки)
□ Проверил memory (работает из коробки)
```

---

### **Опциональная настройка (20 минут):**

```
□ Получил Brave Search API ключ
□ Добавил в mcp.json
□ Включил web-search
□ Проверил что @web-search работает

□ Создал GitHub токен
□ Добавил в mcp.json
□ Включил github
□ Проверил что @github работает

□ Создал папки ~/templates и ~/snippets
□ Добавил свои шаблоны
□ Настроил filesystem в mcp.json
□ Проверил что @filesystem работает
```

---

## 🚀 **ГОТОВ К РАБОТЕ!**

Теперь у тебя:

✅ **Автоматический стиль кода** (.cursorrules)

- Glassmorphism из коробки
- JSDoc для всех функций
- PropTypes для всех компонентов
- Best practices автоматически

✅ **Умный AI** (MCP серверы)

- Доступ к документации (@docs-search)
- Поиск в интернете (@web-search)
- Примеры с GitHub (@github)
- Пошаговое мышление (sequential-thinking)
- Память между сессиями (memory)

✅ **Оптимизация для миграции**

- Все правила настроены для HTML → React
- Шаблоны компонентов готовы
- Стиль кодирования определён

---

## 💪 **СЛЕДУЮЩИЕ ШАГИ**

```bash
1. Проверь что всё установлено (чеклист выше)
2. Открой cursor-zero-coder-guide.md
3. Попробуй создать первый компонент
4. Используй @docs-search для вопросов
5. Начинай миграцию по плану!
```

---

## 📚 **ПОЛЕЗНЫЕ КОМАНДЫ**

### **В чате Cursor (Cmd+L):**

```bash
# Документация
@docs-search [запрос]

# Поиск в интернете (нужен API ключ)
@web-search [запрос]

# GitHub (нужен токен)
@github [запрос]

# Локальные файлы
@filesystem [путь]

# Поиск в проекте (встроенное)
@codebase [запрос]

# Добавить файл в контекст
@filename.jsx
```

---

## 🎉 **ГОТОВ!**

С этой настройкой ты будешь работать **в 10x быстрее**!

Cursor будет:

- 🎨 Автоматически применять правильный стиль
- 📚 Искать в документации
- 🌐 Находить решения в интернете
- 💡 Показывать процесс мышления
- 🧠 Запоминать твои предпочтения

**Удачи с миграцией! Ты справишься!** 🚀

---

**P.S.** Если что-то не работает - проверь Troubleshooting раздел выше или спроси у меня!
