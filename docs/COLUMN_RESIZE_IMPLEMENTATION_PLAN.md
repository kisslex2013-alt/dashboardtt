# ПЛАН ВНЕДРЕНИЯ: Система изменения ширины столбцов

## 📋 АНАЛИЗ ЗАДАЧИ

### Текущая структура

#### 1. Grid в заголовке аккордеона (summary):

```jsx
<div className="grid grid-cols-[1fr_min-content_auto_auto]" style={{ columnGap: '2px' }}>
  {/* Дата (1fr) | Проценты (min-content) | Инсайты (auto) | Итого (auto) */}
</div>
```

- **Столбцы:** Дата, Проценты, Инсайты, Итого
- **Текущие отступы:** percentMargin: 2px, insightsMargin: 80px, totalMargin: 80px
- **Механизм изменения:** `marginLeft` на столбцах

#### 2. Table внутри аккордеона:

```jsx
<table style={{ tableLayout: 'fixed' }}>{/* Чекбокс | Время | Категория | Часы | Доход */}</table>
```

- **Столбцы:** Чекбокс (40px), Время (auto), Категория (auto), Часы (auto), Доход (auto)
- **Текущая ширина:** Только чекбокс имеет фиксированную ширину
- **Механизм изменения:** Нужно использовать `<colgroup>` с `<col>` элементами

## 🔍 АНАЛИЗ МЕТОДА ИЗ columb.md

### ✅ ПОДХОДИТ:

- Скрытый режим активации (не загромождает UI)
- Горячая клавиша для включения/выключения
- Разделители между столбцами
- Перетаскивание границ мышкой
- Сохранение настроек в localStorage
- Визуальные индикаторы режима

### ❌ ТРЕБУЕТ АДАПТАЦИИ:

- Метод работает только с `marginLeft` для grid
- Для table нужен другой подход (colgroup + width)
- Горячая клавиша `Ctrl+Shift+R` может конфликтовать с hard reload браузера
- Нужен универсальный хук для обоих случаев

## 🎯 ОПТИМАЛЬНОЕ РЕШЕНИЕ

### Архитектура

1. **Универсальный хук `useColumnResize`:**
   - Поддержка двух режимов: `'grid'` и `'table'`
   - Для grid: управление `marginLeft` столбцов
   - Для table: управление `width` через colgroup
   - Общий режим активации (Alt+Shift+R)
   - Разные storage keys для grid и table

2. **Компоненты разделителей:**
   - `GridColumnDivider` - для grid (между ячейками)
   - `TableColumnDivider` - для table (в th элементах)

3. **Горячая клавиша:**
   - **Alt+Shift+R** (R = Resize) - избегаем конфликтов с браузером
   - Escape для выхода из режима

## 📝 ПОЭТАПНЫЙ ПЛАН ВНЕДРЕНИЯ

### Этап 1: Создание универсального хука

**Файл:** `src/hooks/useColumnResize.js`

**Функционал:**

- Управление режимом активации (resizeMode)
- Хранение ширины столбцов для grid (marginLeft)
- Хранение ширины столбцов для table (width в px)
- Обработка горячей клавиши Alt+Shift+R
- Сохранение в localStorage (отдельно для grid и table)
- Обработка перетаскивания (drag & drop)
- Сброс к значениям по умолчанию

**Структура данных:**

```javascript
// Для grid (summary)
{
  percentMargin: 2,    // px
  insightsMargin: 80,  // px
  totalMargin: 80      // px
}

// Для table (tbody)
{
  checkbox: 40,        // px
  time: 150,           // px
  category: 200,       // px
  hours: 80,           // px
  income: 100          // px
}
```

### Этап 2: Создание компонентов разделителей

**Файл:** `src/components/ui/ColumnResizers.jsx`

**Компоненты:**

1. `GridColumnDivider` - для grid
   - Вертикальная линия между ячейками
   - Курсор `col-resize`
   - Hover эффект (синий цвет)
   - Увеличенная область захвата

2. `TableColumnDivider` - для table
   - Вертикальная линия в `<th>` элементах
   - Курсор `col-resize`
   - Hover эффект
   - Увеличенная область захвата

### Этап 3: Интеграция в ListView (Grid - summary)

**Файл:** `src/components/entries/views/ListView.jsx`

**Изменения:**

1. Импорт хука и компонентов
2. Добавление хука в компонент ListView
3. Передача `columnWidths.grid` в DayAccordion
4. Использование `marginLeft` из `columnWidths.grid` в столбцах
5. Добавление `GridColumnDivider` между столбцами
6. Добавление индикатора режима и кнопки сброса

### Этап 4: Интеграция в ListView (Table - tbody)

**Файл:** `src/components/entries/views/ListView.jsx`

**Изменения:**

1. Добавление `<colgroup>` с `<col>` элементами
2. Использование `width` из `columnWidths.table`
3. Добавление `TableColumnDivider` в `<th>` элементы
4. Обработка перетаскивания для изменения width

### Этап 5: Интеграция в VirtualizedListView

**Файл:** `src/components/entries/views/VirtualizedListView.jsx`

**Изменения:**

- Аналогичные изменения как в ListView
- Отдельные storage keys для VirtualizedListView (опционально)

### Этап 6: Тестирование и доработка

**Задачи:**

- Проверка работы в обоих режимах
- Проверка сохранения настроек
- Проверка сброса к дефолтным значениям
- Проверка на разных размерах экрана
- Оптимизация производительности

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Горячая клавиша

**Выбрано:** `Alt+Shift+R`

**Причины:**

- Не конфликтует с браузером (Ctrl+Shift+R = hard reload)
- Не конфликтует с существующими горячими клавишами проекта
- Легко запомнить (R = Resize)

**Альтернативы:**

- `Ctrl+Shift+C` (C = Columns)
- `Alt+Shift+C` (C = Columns)

### Storage Keys

```javascript
'listview-grid-column-widths' // Для grid в ListView
'listview-table-column-widths' // Для table в ListView
'virtualized-grid-column-widths' // Для grid в VirtualizedListView (опционально)
'virtualized-table-column-widths' // Для table в VirtualizedListView (опционально)
```

### Значения по умолчанию

**Grid (summary):**

```javascript
{
  percentMargin: 2,    // Минимальное расстояние от даты
  insightsMargin: 80,  // Умеренное расстояние
  totalMargin: 80      // Одинаковое для центрирования
}
```

**Table (tbody):**

```javascript
{
  checkbox: 40,        // Фиксированная ширина чекбокса
  time: 150,           // Время работы (13:36—16:30)
  category: 200,       // Название категории
  hours: 80,           // Часы (2.90ч)
  income: 100          // Доход (1000₽)
}
```

## 🎨 ВИЗУАЛЬНЫЕ ЭЛЕМЕНТЫ

### Индикатор режима

```jsx
{
  resizeMode && (
    <div className="fixed top-4 left-4 bg-blue-500 text-white px-3 py-2 rounded-lg shadow-lg z-50 flex items-center gap-2">
      <span>🔧 Режим изменения столбцов</span>
      <kbd className="px-2 py-1 bg-blue-600 rounded text-xs">Alt+Shift+R</kbd>
      <span className="text-xs opacity-75">или Esc для выхода</span>
    </div>
  )
}
```

### Разделители

- **Обычное состояние:** Прозрачные, видны только при hover
- **Hover:** Синий цвет (#3b82f6)
- **При перетаскивании:** Ярко-синий, более толстый

### Кнопка сброса

```jsx
{
  resizeMode && (
    <button
      onClick={resetWidths}
      className="fixed top-4 right-4 bg-red-500 text-white px-3 py-2 rounded-lg shadow-lg z-50 hover:bg-red-600 transition-colors"
      title="Сбросить к значениям по умолчанию"
    >
      🔄 Сброс
    </button>
  )
}
```

## 📊 ПРЕИМУЩЕСТВА РЕШЕНИЯ

✅ **Универсальность:** Один хук для grid и table  
✅ **Не загромождает UI:** Скрытый режим по умолчанию  
✅ **Интуитивно:** Как в Excel  
✅ **Сохранение настроек:** Автоматически в localStorage  
✅ **Гибкость:** Разные настройки для grid и table  
✅ **Расширяемость:** Легко добавить другие виды (GridView, TimelineView)

## ⚠️ ПОТЕНЦИАЛЬНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: Конфликт с браузерными горячими клавишами

**Решение:** Использовать Alt+Shift+R вместо Ctrl+Shift+R

### Проблема 2: Table-layout fixed требует явных width

**Решение:** Использовать `<colgroup>` с `<col>` элементами

### Проблема 3: Производительность при большом количестве записей

**Решение:** Мемоизация компонентов разделителей, использование useCallback

### Проблема 4: Адаптивность на мобильных устройствах

**Решение:** Отключать режим изменения на мобильных или использовать touch события

## 🚀 ДОПОЛНИТЕЛЬНЫЕ УЛУЧШЕНИЯ (опционально)

1. **Snap к сетке:** Прилипание к 4px, 8px, 12px при перетаскивании
2. **Показ текущего значения:** Отображение ширины при перетаскивании
3. **Экспорт/импорт настроек:** Через JSON файл
4. **Предустановленные профили:** "Компактный", "Стандартный", "Просторный"
5. **Touch поддержка:** Для мобильных устройств
6. **Анимация:** Плавное изменение ширины при перетаскивании

## 📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ/ИЗМЕНЕНИЯ

### Новые файлы:

1. `src/hooks/useColumnResize.js` - универсальный хук
2. `src/components/ui/ColumnResizers.jsx` - компоненты разделителей

### Изменяемые файлы:

1. `src/components/entries/views/ListView.jsx`
2. `src/components/entries/views/VirtualizedListView.jsx`

## ✅ КРИТЕРИИ УСПЕХА

- ✅ Режим активируется по Alt+Shift+R
- ✅ Разделители появляются только в режиме изменения
- ✅ Перетаскивание работает плавно для grid и table
- ✅ Настройки сохраняются в localStorage
- ✅ Сброс к дефолтным значениям работает
- ✅ Режим деактивируется по Escape или повторному Alt+Shift+R
- ✅ Работает на всех размерах экрана
- ✅ Не влияет на производительность

---

**Дата создания:** 8 ноября 2025  
**Статус:** План готов к реализации  
**Приоритет:** Средний
