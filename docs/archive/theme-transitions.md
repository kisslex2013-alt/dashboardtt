# 🎨 5 Концептов Анимации Перехода Темы

Документация по визуальным вариантам анимации перехода при смене темы (от темной к светлой и обратно).

## 📋 Обзор

Реализовано **5 различных концептов** анимации перехода темы, каждый с уникальным визуальным эффектом:

1. **Circle Reveal** - Круговое раскрытие от кнопки переключения
2. **Fade Transition** - Плавное затухание/появление
3. **Wipe** - Горизонтальное стирание
4. **Blur Transition** - Размытие и появление
5. **Rotate Flip** - Вращение с переворотом

## 🎯 Концепты

### 1. Circle Reveal (Круговое раскрытие)

**Описание:** Круг расширяется от кнопки переключения темы, покрывая весь экран.

**Визуальный эффект:**
- Круг начинается от центра кнопки переключения темы
- Радиальный градиент создает эффект раскрытия
- Плавное масштабирование от 0 до 2.5x
- Длительность: 600ms

**Использование:**
- Идеально для акцента на кнопке переключения
- Создает ощущение "взрыва" света/тьмы
- Визуально привлекательный и современный эффект

**Код:**
```css
.theme-transition-circle {
  background: radial-gradient(
    circle at var(--circle-x) var(--circle-y),
    transparent 0%,
    var(--transition-color) 0%
  );
  transform: scale(0);
  animation: circleReveal 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}
```

---

### 2. Fade Transition (Плавное затухание)

**Описание:** Плавное затухание текущей темы и появление новой.

**Визуальный эффект:**
- Overlay появляется с opacity 0 → 1 → 0
- Плавный переход без резких движений
- Длительность: 400ms

**Использование:**
- Классический и универсальный подход
- Не отвлекает от содержимого
- Подходит для всех типов интерфейсов

**Код:**
```css
.theme-transition-fade {
  background: var(--transition-color);
  animation: fadeTransition 0.4s ease-in-out;
}

@keyframes fadeTransition {
  0% { opacity: 0; }
  50% { opacity: 1; }
  100% { opacity: 0; }
}
```

---

### 3. Wipe (Горизонтальное стирание)

**Описание:** Горизонтальное стирание слева направо с использованием clip-path.

**Визуальный эффект:**
- Overlay "стирает" экран слева направо
- Использует clip-path для создания эффекта занавеса
- Длительность: 500ms

**Использование:**
- Динамичный и энергичный эффект
- Создает ощущение "смены сцены"
- Хорошо работает для драматичных переходов

**Код:**
```css
.theme-transition-wipe {
  background: var(--transition-color);
  clip-path: polygon(0% 0%, 0% 0%, 0% 100%, 0% 100%);
  animation: wipeTransition 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes wipeTransition {
  0% { clip-path: polygon(0% 0%, 0% 0%, 0% 100%, 0% 100%); }
  50% { clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%); }
  100% { clip-path: polygon(100% 0%, 100% 0%, 100% 100%, 100% 100%); }
}
```

---

### 4. Blur Transition (Размытие и появление)

**Описание:** Размытие содержимого с последующим появлением новой темы.

**Визуальный эффект:**
- Backdrop-filter создает эффект размытия
- Плавное увеличение blur от 0px до 20px и обратно
- Длительность: 500ms

**Использование:**
- Современный и стильный эффект
- Создает ощущение "фокусировки" на новой теме
- Хорошо работает с glassmorphism дизайном

**Код:**
```css
.theme-transition-blur {
  background: var(--transition-color);
  backdrop-filter: blur(0px);
  animation: blurTransition 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes blurTransition {
  0% { backdrop-filter: blur(0px); opacity: 0; }
  30% { backdrop-filter: blur(20px); opacity: 1; }
  70% { backdrop-filter: blur(20px); opacity: 1; }
  100% { backdrop-filter: blur(0px); opacity: 0; }
}
```

---

### 5. Rotate Flip (Вращение с переворотом)

**Описание:** 3D вращение экрана с эффектом переворота.

**Визуальный эффект:**
- Perspective создает 3D эффект
- Вращение по оси Y от 0° до 180°
- Длительность: 600ms

**Использование:**
- Драматичный и запоминающийся эффект
- Создает ощущение "переворота страницы"
- Подходит для интерактивных приложений

**Код:**
```css
.theme-transition-rotate {
  background: var(--transition-color);
  transform: perspective(1000px) rotateY(0deg);
  animation: rotateFlipTransition 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes rotateFlipTransition {
  0% { transform: perspective(1000px) rotateY(0deg); opacity: 0; }
  25% { opacity: 1; }
  50% { transform: perspective(1000px) rotateY(90deg); opacity: 1; }
  100% { transform: perspective(1000px) rotateY(180deg); opacity: 0; }
}
```

## ⚙️ Настройка

Тип анимации можно изменить через настройки:

```javascript
import { useSetThemeTransitionType } from '../store/useSettingsStore'

const setThemeTransitionType = useSetThemeTransitionType()

// Установить тип анимации
setThemeTransitionType('circle') // 'circle', 'fade', 'wipe', 'blur', 'rotate'
```

## 🎨 Цвета

Каждый тип анимации использует CSS переменные для цветов:

- **Light theme:** `rgba(255, 255, 255, 0.95)` - белый overlay
- **Dark theme:** `rgba(0, 0, 0, 0.95)` - черный overlay

Цвета настраиваются через атрибут `data-theme` на overlay элементе.

## ⚡ Производительность

Все анимации оптимизированы для производительности:

- Используется `will-change` для GPU-ускорения
- `transform` и `opacity` для плавных анимаций
- Поддержка `prefers-reduced-motion` для доступности

## 📱 Адаптивность

Все анимации работают одинаково на всех устройствах:
- Desktop
- Tablet
- Mobile

## 🔧 Технические детали

### Структура файлов:
- `src/hooks/useThemeTransition.js` - хук для управления анимациями
- `src/components/ui/ThemeTransitionOverlay.jsx` - компонент для стилей
- `src/components/layout/Header.jsx` - интеграция в переключатель темы
- `src/store/useSettingsStore.js` - настройки типа анимации

### Порядок выполнения:
1. Пользователь нажимает кнопку переключения темы
2. Создается overlay элемент с соответствующим классом
3. Устанавливается позиция (для circle reveal)
4. Запускается CSS анимация
5. В середине анимации меняется тема
6. После завершения overlay удаляется

## 🎯 Рекомендации по выбору

- **Circle Reveal** - для акцента на кнопке переключения
- **Fade Transition** - для универсального использования
- **Wipe** - для динамичных интерфейсов
- **Blur Transition** - для glassmorphism дизайна
- **Rotate Flip** - для драматичных переходов

## 📝 Примечания

- Все анимации учитывают настройку `prefers-reduced-motion`
- Анимации автоматически отключаются, если `themeTransitionType === 'none'`
- Длительность анимаций оптимизирована для баланса между визуальной привлекательностью и производительностью

