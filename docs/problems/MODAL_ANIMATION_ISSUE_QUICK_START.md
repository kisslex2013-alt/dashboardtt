# 🚀 Быстрый старт для решения проблемы с модальными окнами

## 📋 Краткое описание проблемы

**Проблема:** Модальные окна пропадают после анимации открытия, анимация закрытия не работает.

**Симптомы:**

- ✅ Анимация открытия работает
- ❌ Окно исчезает сразу после открытия
- ❌ Анимация закрытия не работает
- ⚠️ Интерфейс активен в фоне (кнопки работают, но окно невидимо)

---

## 📦 Необходимые файлы

### 🔴 Обязательные (минимум для понимания проблемы):

1. **`time-tracker/src/components/ui/BaseModal.jsx`**
   - Основной компонент с логикой анимаций
   - **Критически важно!**

2. **`time-tracker/tailwind.config.js`**
   - Определения анимаций (keyframes)
   - **Критически важно!**

3. **`time-tracker/package.json`**
   - Версии зависимостей (особенно `@headlessui/react: ^2.2.9`)

### 🟡 Рекомендуемые (для полного контекста):

4. **`time-tracker/src/components/modals/BackupModal.jsx`**
   - Пример использования `BaseModal`
   - Показывает, как открывается/закрывается модальное окно

5. **`time-tracker/src/store/useUIStore.js`**
   - Управление состоянием модальных окон
   - Функции `openModal`, `closeModal`

6. **`time-tracker/src/App.jsx`**
   - Интеграция модальных окон в приложение

### 🟢 Опциональные (для глубокого понимания):

7. **`time-tracker/docs/problems/MODAL_ANIMATION_ISSUE_DETAILED.md`**
   - Подробный отчет со всеми попытками решения
   - Примеры кода и анализ

8. **`time-tracker/docs/problems/ANIMATION_ISSUE_REPORT.md`**
   - Предыдущие проблемы с анимациями
   - Контекст

---

## 🎯 Что нужно сделать

### Целевое поведение:

**При открытии:**

1. Модальное окно появляется снизу вверх (`animate-slide-up`)
2. Overlay появляется (`animate-fade-in`)
3. **Окно остается видимым после анимации** ⚠️ (сейчас пропадает)

**При закрытии:**

1. Модальное окно исчезает вниз (`animate-slide-out`)
2. Overlay исчезает (`animate-fade-out`)
3. Компонент размонтируется после анимации

---

## 🔍 Ключевые моменты для анализа

### 1. Логика состояний в `BaseModal.jsx`:

```javascript
const [shouldMount, setShouldMount] = useState(false) // Рендеринг
const [isAnimating, setIsAnimating] = useState(false) // Анимация открытия
const [isExiting, setIsExiting] = useState(false) // Анимация закрытия
```

**Вопрос:** Почему `isAnimating` сбрасывается после завершения анимации открытия?

### 2. Использование `Dialog` из `@headlessui/react`:

```javascript
<Dialog open={shouldMount} onClose={onClose}>
```

**Вопрос:** Не применяет ли `Dialog` внутренние стили, скрывающие содержимое?

### 3. Применение классов:

```javascript
className={`
  ${!isAnimating && !isExiting ? 'opacity-0 scale-95 translate-y-4' : ''}
  ${isAnimating && !isExiting ? 'animate-slide-up' : ''}
  ${isExiting ? 'animate-slide-out' : ''}
`}
```

**Вопрос:** Правильно ли применяются классы после завершения анимации?

---

## 🛠️ Возможные решения

1. **Использовать `Transition` из `@headlessui/react`** (рекомендуется)
2. **Исправить логику состояний** (после анимации `isAnimating` должен оставаться `true`)
3. **Использовать CSS transitions вместо animations**
4. **Использовать `framer-motion`** (если уже установлен)

---

## 📝 Команды для тестирования

```bash
cd time-tracker
npm run dev
# Открыть http://localhost:5173
# Открыть любое модальное окно (например, "Резервные копии")
```

---

## 📚 Дополнительная информация

Полный отчет: `time-tracker/docs/problems/MODAL_ANIMATION_ISSUE_DETAILED.md`

---

**Версия:** 1.0  
**Дата:** 2025-11-07
