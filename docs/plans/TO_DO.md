# 📋 TO-DO List - Time Tracker Dashboard

## 🎯 Задачи на разработку

---

### 1. 📝 Дополнительная информация в окне "О приложении" (на развитие проекта)

**Описание:** Добавить раздел с информацией о будущих планах развития, roadmap, или возможность пожертвований/поддержки проекта.

**Варианты реализации:**

#### Вариант 1: Раздел "Планы развития"

- Добавить новый раздел в `AboutModal.jsx` после "История изменений"
- Отображать список запланированных функций с иконками статусов:
  - 🔵 В разработке
  - 🟡 Планируется
  - ✅ Завершено
- Можно использовать `Accordion` компонент для свертывания/развертывания

#### Вариант 2: GitHub Issues / Roadmap ссылка

- Добавить кнопку/ссылку на внешний roadmap (GitHub Projects, Trello, etc.)
- Или встроенный виджет с информацией из API (если есть backend)

#### Вариант 3: Информация о поддержке проекта

- Добавить раздел "Поддержать проект"
- Ссылки на:
  - GitHub Sponsors
  - PayPal / Donation
  - Crypto кошельки
- Благодарности спонсорам

**Рекомендуемый подход:** Комбинация Варианта 1 и 3 - показать планы развития + возможность поддержки проекта.

**Файлы для изменения:**

- `src/components/modals/AboutModal.jsx`

---

### 2. 🎨 Иконка в верхнем баре

**Описание:** Добавить иконку приложения в `Header` компонент (обычно слева от названия).

**Варианты реализации:**

#### Вариант 1: Статичная иконка

- Добавить `<img>` или SVG компонент с логотипом
- Использовать тот же фавикон (32x32px) или создать отдельную иконку
- Разместить слева от текста "Time Tracker Dashboard"

#### Вариант 2: Анимированная иконка

- Использовать тот же canvas-генератор, что и для фавикона
- Отображать текущее состояние таймера (активен/на паузе)
- Анимация синхронизирована с фавиконом

#### Вариант 3: Интерактивная иконка

- При клике на иконку - открывается меню с быстрыми действиями:
  - Запуск/остановка таймера
  - Быстрое добавление записи
  - Открытие статистики

**Рекомендуемый подход:** Вариант 2 - анимированная иконка, которая отражает состояние таймера.

**Файлы для изменения:**

- `src/components/layout/Header.jsx`
- Возможно создать новый компонент `src/components/ui/AppIcon.jsx`

---

### 3. 📅 Настройка даты выплаты

**Описание:** Добавить возможность указать дату выплаты зарплаты (например, 25 число каждого месяца) для автоматического расчета доходов за период.

**Варианты реализации:**

#### Вариант 1: Простая настройка в Settings

- Добавить поле "Дата выплаты" (число от 1 до 28)
- Сохранять в `useSettingsStore`
- Использовать для:
  - Фильтрации записей в статистике (период от даты выплаты до даты выплаты)
  - Отображения в Dashboard карточке "Заработано в текущем периоде"

#### Вариант 2: Расширенная настройка с календарем

- Позволить выбрать конкретную дату для каждого месяца
- Поддержка нерегулярных выплат (например, 25 декабря, 15 января)
- Отображение в календаре доходов меткой "Выплата"

#### Вариант 3: Множественные даты выплаты

- Поддержка нескольких дат выплаты (например, аванс 15 числа, зарплата 30 числа)
- Раздельный учет доходов по типам выплат

**Рекомендуемый подход:** Вариант 1 для начала, затем можно расширить до Варианта 2.

**Файлы для изменения:**

- `src/store/useSettingsStore.js` - добавить поле `paymentDate`
- `src/components/modals/SettingsModal.jsx` - добавить поле ввода
- `src/components/statistics/StatisticsOverview.jsx` - использовать для расчета периода
- Возможно создать утилиту `src/utils/paymentPeriod.js` для расчета периода

---

### 4. 🎨 Изменить цвет плашек под проект

**Описание:** Добавить настройку цветовой схемы приложения (primary цвет, accent цвет) для персонализации.

**Варианты реализации:**

#### Вариант 1: Предустановленные темы

- Несколько готовых цветовых схем:
  - Синий (текущий)
  - Зеленый
  - Фиолетовый
  - Оранжевый
  - Красный
- Выбор через dropdown в настройках

#### Вариант 2: Кастомный цвет через Color Picker

- Добавить `<input type="color">` в настройки
- Сохранять выбранный цвет в `useSettingsStore`
- Применять через CSS переменные или Tailwind конфигурацию

#### Вариант 3: Полная кастомизация

- Настройка нескольких цветов:
  - Primary (основной)
  - Accent (акцентный)
  - Success (успех)
  - Warning (предупреждение)
  - Error (ошибка)
- Динамическое обновление Tailwind конфига или CSS переменных

**Рекомендуемый подход:** Вариант 2 - кастомный цвет через Color Picker, который применяется ко всем плашкам.

**Файлы для изменения:**

- `src/store/useSettingsStore.js` - добавить поле `primaryColor`
- `src/components/modals/SettingsModal.jsx` - добавить Color Picker
- `tailwind.config.js` - динамическая генерация цветов
- Или использовать CSS переменные в `src/index.css`

---

### 5. ⚙️ Настройки отключения анимации

**Описание:** Добавить возможность отключать анимации для пользователей, которым они мешают или для улучшения производительности.

**Варианты реализации:**

#### Вариант 1: Глобальный переключатель "Отключить все анимации"

- Одна настройка `disableAnimations: boolean`
- При включении:
  - Устанавливать `prefers-reduced-motion` класс на `<body>`
  - Или использовать CSS переменную `--animation-disabled: 1`
- Все анимации проверяют эту настройку

#### Вариант 2: Гранулярные настройки

- Отдельные переключатели для разных типов анимаций:
  - Анимации переходов (fade, slide)
  - Анимации при наведении (hover effects)
  - Анимация фавикона
  - Анимация таймера
  - Микроанимации (scale, lift)

#### Вариант 3: Умное отключение

- Автоматическое отключение на слабых устройствах
- Определение через `navigator.hardwareConcurrency` или `performance.memory`
- Сохранение предпочтений пользователя

**Рекомендуемый подход:** Вариант 2 - гранулярные настройки, чтобы пользователь мог выбрать что отключить.

**Файлы для изменения:**

- `src/store/useSettingsStore.js` - добавить объект `animations: { transitions, hover, favicon, timer, micro }`
- `src/components/modals/SettingsModal.jsx` - добавить секцию "Анимации"
- `src/styles/animations.css` - добавить проверки настройки перед применением анимаций
- Возможно создать хук `src/hooks/useAnimations.js` для управления

---

### 6. 👁️ Настройки скрытия плавающего таймера

**Описание:** Добавить возможность скрывать `FloatingPanel` (плавающий таймер) когда он не нужен.

**Варианты реализации:**

#### Вариант 1: Простой переключатель

- Настройка `hideFloatingPanel: boolean` в `useSettingsStore`
- В `FloatingPanel.jsx` проверять настройку и условно не рендерить компонент
- Или применять `display: none` / `visibility: hidden`

#### Вариант 2: Автоматическое скрытие

- Автоматически скрывать когда таймер не активен
- Показывать только когда `activeTimer !== null`
- Настройка `autoHideFloatingPanel: boolean` для управления этим поведением

#### Вариант 3: Умное скрытие с контекстом

- Кнопка сворачивания/разворачивания на самой панели
- Сворачивание в маленькую иконку в углу экрана
- Восстановление при клике или при старте таймера
- Настройка позиции минимизированной панели

#### Вариант 4: Комбинированный подход

- Настройка `floatingPanelVisibility: 'always' | 'when-active' | 'hidden'`
- `'always'` - всегда показывать
- `'when-active'` - показывать только когда таймер активен
- `'hidden'` - всегда скрывать

**Рекомендуемый подход:** Вариант 4 - комбинированный, даёт максимум гибкости.

**Файлы для изменения:**

- `src/store/useSettingsStore.js` - добавить поле `floatingPanelVisibility`
- `src/components/modals/SettingsModal.jsx` - добавить dropdown для выбора
- `src/components/layout/FloatingPanel.jsx` - добавить проверку настройки
- Возможно добавить кнопку сворачивания прямо на панели

---

## 📊 Приоритизация задач

**Высокий приоритет:**

1. Настройки скрытия плавающего таймера (#6) - быстро и полезно
2. Настройки отключения анимации (#5) - для доступности и производительности

**Средний приоритет:** 3. Иконка в верхнем баре (#2) - улучшает UX 4. Настройка даты выплаты (#3) - полезная функция для пользователей

**Низкий приоритет:** 5. Изменить цвет плашек под проект (#4) - косметика 6. Дополнительная информация в окне "О приложении" (#1) - информационная

**Технические задачи (качество кода):** 7. Исправить экспорты в App.jsx (#7) - соответствие правилам 8. Проверить экспорты в AnalyticsSection.jsx (#8) - проверка дублирования 9. Настроить ESLint конфигурацию (#9) - автоматическая проверка правил

---

## 🔧 Технические задачи (из отчета проверки систем)

### 7. 📦 Исправить экспорты в App.jsx

**Описание:** Изменить `export default App` на named export для соответствия правилам проекта.

**Проблема:**

- Файл использует `export default App` вместо named export
- Нарушает правило из `.cursorrules` о использовании named exports

**Решение:**

- Изменить `export default App` на `export { App }` в конце файла
- Обновить импорт в `main.jsx` если необходимо (обычно работает и так)

**Файлы для изменения:**

- `src/App.jsx`
- Возможно `src/main.jsx` (проверить импорт)

**Приоритет:** Средний

---

### 8. 📦 Исправить экспорты в AnalyticsSection.jsx

**Описание:** Проверить дублирование экспортов в `AnalyticsSection.jsx`.

**Проблема:**

- Компонент уже экспортируется через `export function AnalyticsSection()`
- В конце файла есть дополнительный `export default AnalyticsSection;`
- Это создает дублирование (хотя `export default` может быть оставлен для `React.lazy()`)

**Решение:**

- Проверить, используется ли `React.lazy()` для этого компонента
- Если используется `lazy()` - оставить `export default` (это нормально)
- Если не используется - удалить `export default` и оставить только `export function`

**Файлы для изменения:**

- `src/components/statistics/AnalyticsSection.jsx`
- Проверить использование в `src/App.jsx`

**Приоритет:** Низкий (если используется `lazy()`, можно оставить)

**Примечание:** В `App.jsx` компонент импортируется через `lazy()`, поэтому `export default` необходим. Оставить как есть или добавить комментарий для ясности.

---

### 9. ⚙️ Настроить ESLint конфигурацию

**Описание:** Создать конфигурационный файл ESLint для автоматической проверки правил кода.

**Проблема:**

- ESLint установлен в `package.json`, но отсутствует конфигурационный файл
- Команда `npm run lint` может не работать корректно без конфигурации
- Нет автоматической проверки правил из `.cursorrules`

**Варианты реализации:**

#### Вариант 1: Flat Config (ESLint 9+)

- Создать `eslint.config.js` (новый формат для ESLint 9+)
- Настроить правила для React, импортов, экспортов
- Добавить проверку соответствия правилам проекта

#### Вариант 2: Стандартная конфигурация

- Использовать готовую конфигурацию для React + Vite
- Добавить правила из `.cursorrules`:
  - Проверка named exports
  - Запрет default exports для компонентов
  - Проверка порядка импортов
  - Проверка использования Tailwind классов вместо inline styles

**Рекомендуемый подход:** Вариант 1 - Flat Config, так как проект использует ESLint 9.36.0

**Файлы для изменения:**

- Создать `eslint.config.js`
- Возможно обновить `package.json` скрипт `lint` для правильной работы

**Правила для проверки:**

- `no-default-export` для компонентов (можно через кастомное правило)
- `import/order` - порядок импортов
- `react-hooks/rules-of-hooks`
- `react-refresh/only-export-components`

**Приоритет:** Низкий (не критично, но полезно для качества кода)

---

## 🔧 Технические заметки

### Общие изменения для всех задач:

- Все новые настройки должны сохраняться через `useSettingsStore` с `persist` middleware
- Добавлять валидацию для пользовательского ввода
- Учитывать темную тему при изменении цветов
- Тестировать на мобильных устройствах

### Структура для новых настроек:

```javascript
// useSettingsStore.js
interface Settings {
  // ... existing settings
  paymentDate: number; // 1-28
  primaryColor: string; // hex color
  animations: {
    transitions: boolean;
    hover: boolean;
    favicon: boolean;
    timer: boolean;
    micro: boolean;
  };
  floatingPanelVisibility: 'always' | 'when-active' | 'hidden';
}
```

---

**Последнее обновление:** 2024-12-19  
**Добавлено задач из проверки систем:** 2024-12-19
