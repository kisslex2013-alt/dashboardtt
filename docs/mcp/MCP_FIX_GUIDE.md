# 🔧 Исправление ошибок MCP серверов

## ❌ Проблема

Два сервера показывают ошибки (красные индикаторы):

1. **`docs-search`** - пакет `@modelcontextprotocol/server-docs` не существует в npm
2. **`git`** - пакет `@modelcontextprotocol/server-git` не существует в npm

## ✅ Решение

### 1. Удалить `docs-search` из конфигурации

**Причина:** Пакет `@modelcontextprotocol/server-docs` не существует в npm registry.

**Решение:**

- Удалить секцию `docs-search` из `mcp.json`
- Использовать встроенные возможности Cursor для поиска в документации
- Или использовать `web-search` (требует API ключ Brave)

### 2. Заменить `git` на альтернативный пакет

**Причина:** Пакет `@modelcontextprotocol/server-git` не существует в npm registry.

**Решение:** Использовать альтернативный пакет `@cyanheads/git-mcp-server`

---

## 📝 Инструкция по исправлению

### Вариант 1: Использовать готовый исправленный файл

1. Откройте глобальный файл конфигурации:

   ```
   c:\Users\kissl\.cursor\mcp.json
   ```

2. Скопируйте содержимое из `time-tracker/mcp.json.fixed`

3. Или просто удалите секции `docs-search` и обновите `git`:

```json
{
  "mcpServers": {
    // УДАЛИТЬ эту секцию полностью:
    // "docs-search": { ... }

    // ОСТАВИТЬ все остальные серверы

    // ОБНОВИТЬ секцию git:
    "git": {
      "command": "npx",
      "args": ["-y", "@cyanheads/git-mcp-server"],
      "env": {
        "GIT_REPOSITORY_PATH": "H:/Backup/Zero-Coding/Cursor AI/TTM_module/time-tracker"
      },
      "description": "Git операции для проекта",
      "enabled": false
    }
  }
}
```

### Вариант 2: Ручное исправление

1. Откройте `c:\Users\kissl\.cursor\mcp.json`

2. **Удалите** секцию `docs-search` полностью:

```json
// УДАЛИТЬ ЭТО:
"docs-search": {
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-docs"],
  ...
}
```

3. **Замените** секцию `git`:

```json
// БЫЛО:
"git": {
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-git", "--repository", "..."],
  ...
}

// СТАЛО:
"git": {
  "command": "npx",
  "args": ["-y", "@cyanheads/git-mcp-server"],
  "env": {
    "GIT_REPOSITORY_PATH": "H:/Backup/Zero-Coding/Cursor AI/TTM_module/time-tracker"
  },
  "description": "Git операции для проекта",
  "enabled": false
}
```

4. **Сохраните** файл

5. **Перезапустите Cursor** полностью

6. **Проверьте** статус в "Tools & MCP":
   - `docs-search` больше не должен быть в списке
   - `git` должен быть зеленым (если включен)

---

## ✅ Альтернативы для docs-search

Поскольку `docs-search` не работает, используйте:

### 1. Встроенный поиск Cursor

- Cursor уже может искать в открытых файлах проекта
- Используйте `Ctrl + F` для поиска в документации проекта

### 2. Web Search (требует API ключ)

- Получите бесплатный Brave API ключ: https://brave.com/search/api/
- Добавьте ключ в `web-search` секцию
- Включите сервер (`enabled: true`)

### 3. GitHub Search

- Используйте `github` сервер для поиска примеров кода
- Требует GitHub токен

---

## 🧪 Проверка после исправления

1. Откройте Cursor Settings (`Ctrl + ,`)
2. Перейдите в "Tools & MCP"
3. Проверьте:
   - ✅ `docs-search` отсутствует в списке
   - ✅ `git` показывает зеленый индикатор (если включен)
   - ✅ Другие серверы работают нормально

---

## 📚 Дополнительная информация

- **Файл с исправлениями:** `time-tracker/mcp.json.fixed`
- **Статус всех систем:** `time-tracker/SYSTEM_STATUS_REPORT.md`

---

**Важно:** После исправления полностью перезапустите Cursor, чтобы изменения вступили в силу.
