# Agent Enforcer - Настройка и использование

## 📋 Описание

`agent-enforcer` - это инструмент для автоматической проверки качества кода, который объединяет различные линтеры, форматтеры и анализаторы статического кода.

## 🚀 Быстрый старт

### Проверка всего проекта:

```bash
npm run quality:check
```

### Подробная проверка с выводом всех проблем:

```bash
npm run quality:check:verbose
```

### Проверка только измененных файлов (git):

```bash
npm run quality:check:modified
```

## 📝 Доступные команды

| Команда                          | Описание                                |
| -------------------------------- | --------------------------------------- |
| `npm run quality:check`          | Базовая проверка качества кода в `src/` |
| `npm run quality:check:verbose`  | Подробная проверка с детальным выводом  |
| `npm run quality:check:modified` | Проверка только измененных файлов (git) |

## ⚙️ Конфигурация

Конфигурация находится в файле `.agent-enforcer.json` в корне проекта.

### Настройка правил

Вы можете настроить уровень серьезности для каждого правила:

```bash
# Установить правило как ошибку
agent-enforcer --error js_ts:no-console

# Установить правило как предупреждение
agent-enforcer --warning js_ts:no-unused-vars

# Игнорировать правило
agent-enforcer --blacklist js_ts:no-console
```

### Временное отключение правил

Для одноразовой проверки без определенных правил:

```bash
agent-enforcer --ignore js_ts:no-console,js_ts:no-debugger
```

## 🔧 Интеграция с Git Hooks

Можно добавить проверку перед коммитом:

```bash
# В .git/hooks/pre-commit
#!/bin/sh
npm run quality:check
```

## 📊 Что проверяется

### JavaScript/TypeScript:

- ✅ Стиль кода (black, isort)
- ✅ Линтинг (flake8, eslint)
- ✅ Проверка типов (mypy, pyright)
- ✅ React Hooks правила
- ✅ Неиспользуемые переменные
- ✅ Отладочный код (console.log, debugger)

### Общие проверки:

- ✅ Длина файлов
- ✅ Длина функций
- ✅ Сложность кода

## 🎯 Рекомендации

1. **Запускайте перед коммитом**: `npm run quality:check:modified`
2. **Исправляйте ошибки сразу**: они блокируют коммит
3. **Предупреждения можно отложить**: но лучше исправить
4. **Используйте verbose режим**: для детального анализа проблем

## ⚠️ Известные проблемы

### Предупреждение "Command not found: npx"

Если вы видите предупреждение о том, что `npx` не найден, это не критично. Инструмент все равно работает, но некоторые проверки могут быть пропущены.

**Решение:**

1. Убедитесь, что Node.js установлен: `node --version`
2. Убедитесь, что npm установлен: `npm --version`
3. `npx` должен быть доступен через npm (он входит в состав npm 5.2+)

## 📚 Дополнительная информация

- Правила проекта описаны в `.cursorrules`
- ESLint конфигурация в `eslint.config.js`
- Конфигурация agent-enforcer в `.agent-enforcer.json`

## 🔄 Обновление

Для обновления agent-enforcer:

```bash
pip install --upgrade agent-enforcer
```
