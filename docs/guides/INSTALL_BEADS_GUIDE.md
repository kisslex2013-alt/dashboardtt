# 📦 Руководство по установке Beads

**Время:** 10-15 минут

---

## 🔍 Шаг 1: Проверка Go

Проверьте установлен ли Go:

```powershell
go version
```

**Если Go установлен:**

- Вы увидите версию, например: `go version go1.21.5 windows/amd64`
- Переходите к Шагу 3

**Если Go не установлен:**

- Переходите к Шагу 2

---

## 📥 Шаг 2: Установка Go (если не установлен)

### Вариант A: Ручная установка (рекомендуется)

1. **Скачайте Go:**
   - Откройте: https://go.dev/dl/
   - Скачайте установщик для Windows (`.msi` файл)
   - Пример: `go1.21.5.windows-amd64.msi`

2. **Установите Go:**
   - Запустите скачанный `.msi` файл
   - Следуйте инструкциям установщика
   - Go установится в `C:\Program Files\Go`

3. **Проверьте установку:**
   - Откройте **новый** терминал (важно!)
   - Выполните: `go version`
   - Должна появиться версия Go

4. **Переходите к Шагу 3**

### Вариант B: Автоматическая установка (через Chocolatey)

Если у вас установлен Chocolatey:

```powershell
choco install golang -y
```

После установки перезапустите терминал и проверьте: `go version`

---

## 📦 Шаг 3: Установка Beads

### Автоматическая установка (рекомендуется)

Используйте скрипт:

```powershell
cd "H:\Backup\Zero-Coding\Cursor AI\TTM_module\time-tracker"
.\install-beads.ps1
```

Скрипт:

- Проверит Go
- Установит Beads
- Инициализирует Beads в проекте

### Ручная установка

Если скрипт не работает, установите вручную:

```powershell
# Установка Beads
go install github.com/steveyegge/beads@latest

# Проверка установки (может потребоваться перезапуск терминала)
bd --version
```

---

## 🔧 Шаг 4: Настройка PATH (если нужно)

Если после установки Beads команда `bd` не найдена:

### Временное решение (для текущей сессии):

```powershell
$env:PATH += ";$env:USERPROFILE\go\bin"
bd --version
```

### Постоянное решение:

1. Откройте "Переменные среды" (Environment Variables)
2. Найдите переменную `PATH` в "Пользовательские переменные"
3. Добавьте: `%USERPROFILE%\go\bin`
4. Перезапустите терминал

---

## ✅ Шаг 5: Инициализация Beads в проекте

```powershell
cd "H:\Backup\Zero-Coding\Cursor AI\TTM_module\time-tracker"
bd init --prefix ttm
```

Это создаст:

- `.beads/` папку
- `.beads/issues.jsonl` файл (не игнорируется в Git)

---

## 🧪 Шаг 6: Тестирование

Проверьте что все работает:

```powershell
# Проверка версии
bd --version

# Создание тестовой задачи
bd add "Тестовая задача: проверить установку Beads"

# Просмотр задач
bd list

# Получение следующей задачи
bd next
```

---

## 📚 Дальнейшие шаги

1. **Читайте `SETUP-GUIDE.md`** - полная инструкция по использованию
2. **Используйте Beads в работе:**
   - `bd add "описание задачи"` - добавить задачу
   - `bd list` - список задач
   - `bd next` - следующая задача
   - `bd done <ID>` - завершить задачу

3. **Интеграция с AI:**
   - AI может использовать Beads для управления задачами
   - См. раздел в `.cursorrules`

---

## 🆘 Проблемы и решения

### "go: команда не найдена"

- Go не установлен или не в PATH
- Установите Go (Шаг 2)
- Перезапустите терминал

### "bd: команда не найдена"

- Beads не в PATH
- Добавьте `%USERPROFILE%\go\bin` в PATH (Шаг 4)
- Перезапустите терминал

### "Permission denied" при установке

- Запустите терминал от имени администратора
- Или используйте ручную установку Go

### "bd init" уже выполнен

- Это нормально, Beads уже инициализирован
- Можно продолжать работу

---

## ✅ Чеклист установки

- [ ] Go установлен (`go version` работает)
- [ ] Beads установлен (`bd --version` работает)
- [ ] Beads инициализирован в проекте (`.beads/` папка существует)
- [ ] Тестовая задача создана (`bd add "тест"` работает)
- [ ] Задачи просматриваются (`bd list` показывает задачи)

---

**Готово!** Beads установлен и готов к использованию! 🎉
