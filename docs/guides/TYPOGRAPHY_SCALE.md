# 📐 Typography Scale System

> Документация по системе типографики для Time Tracker Dashboard

## 📖 Содержание

1. [Введение](#введение)
2. [Размеры шрифтов](#размеры-шрифтов)
3. [Вес шрифта](#вес-шрифта)
4. [Интерлиньяж (Line Height)](#интерлиньяж-line-height)
5. [Letter Spacing](#letter-spacing)
6. [Готовые классы](#готовые-классы)
7. [Примеры использования](#примеры-использования)
8. [Цветовая система](#цветовая-система)
9. [Best Practices](#best-practices)

---

## Введение

Typography Scale System - это централизованная система типографики, обеспечивающая консистентность дизайна во всем приложении.

### Принципы:

- ✅ **Модульная шкала 1.125** (Major Second) для гармоничных размеров
- ✅ **CSS переменные** для легкой кастомизации
- ✅ **Готовые классы** для быстрой разработки
- ✅ **Адаптивность** - автоматическое масштабирование на мобильных
- ✅ **Accessibility** - оптимальная читаемость
- ✅ **Тёмная тема** - отдельные цвета для dark mode

---

## Размеры шрифтов

### CSS Переменные

```css
--font-size-xs: 0.75rem;    /* 12px */
--font-size-sm: 0.875rem;   /* 14px */
--font-size-base: 1rem;     /* 16px */
--font-size-lg: 1.125rem;   /* 18px */
--font-size-xl: 1.25rem;    /* 20px */
--font-size-2xl: 1.5rem;    /* 24px */
--font-size-3xl: 1.875rem;  /* 30px */
--font-size-4xl: 2.25rem;   /* 36px */
--font-size-5xl: 3rem;      /* 48px */
```

### Использование в коде

```css
/* В CSS */
.my-heading {
  font-size: var(--font-size-2xl);
}

/* В Tailwind (используйте стандартные классы Tailwind) */
<h1 className="text-2xl">Заголовок</h1>
```

### Применение

| Размер | Пиксели | Применение |
|--------|---------|------------|
| `xs` | 12px | Captions, мелкие метки, вспомогательный текст |
| `sm` | 14px | Второстепенная информация, подписи |
| `base` | 16px | Основной текст, параграфы |
| `lg` | 18px | Крупный основной текст, lead параграфы |
| `xl` | 20px | Небольшие заголовки, заголовки карточек |
| `2xl` | 24px | Заголовки секций |
| `3xl` | 30px | Заголовки страниц |
| `4xl` | 36px | Герои, крупные заголовки |
| `5xl` | 48px | Display заголовки (редко) |

---

## Вес шрифта

### CSS Переменные

```css
--font-light: 300;      /* Light (редко используется) */
--font-normal: 400;     /* Normal - основной текст */
--font-medium: 500;     /* Medium - акценты */
--font-semibold: 600;   /* Semibold - подзаголовки */
--font-bold: 700;       /* Bold - заголовки */
```

### Использование

```css
/* В CSS */
.my-text {
  font-weight: var(--font-medium);
}

/* В Tailwind */
<p className="font-medium">Средний вес</p>
```

### Рекомендации

- **Normal (400)**: Основной текст, параграфы
- **Medium (500)**: Акценты, важная информация, метки
- **Semibold (600)**: Подзаголовки, названия карточек
- **Bold (700)**: Заголовки, кнопки

---

## Интерлиньяж (Line Height)

### CSS Переменные

```css
--leading-none: 1;        /* Tight, для заголовков */
--leading-tight: 1.25;    /* Для больших заголовков */
--leading-snug: 1.375;    /* Для средних заголовков */
--leading-normal: 1.5;    /* Для основного текста */
--leading-relaxed: 1.625; /* Для длинного текста */
--leading-loose: 2;       /* Очень расслабленный */
```

### Применение

| Line Height | Значение | Применение |
|-------------|----------|------------|
| `none` | 1.0 | Однострочные заголовки, логотипы |
| `tight` | 1.25 | Большие заголовки (h1, h2) |
| `snug` | 1.375 | Средние заголовки (h3, h4) |
| `normal` | 1.5 | Основной текст, параграфы (оптимально) |
| `relaxed` | 1.625 | Длинный текст, статьи |
| `loose` | 2.0 | Особые случаи |

### Примеры

```css
/* Заголовок */
h1 {
  line-height: var(--leading-tight);
}

/* Основной текст */
p {
  line-height: var(--leading-normal);
}

/* Длинная статья */
article {
  line-height: var(--leading-relaxed);
}
```

---

## Letter Spacing

### CSS Переменные

```css
--tracking-tighter: -0.05em;  /* Для больших заголовков */
--tracking-tight: -0.025em;   /* Для заголовков */
--tracking-normal: 0;         /* Для основного текста */
--tracking-wide: 0.025em;     /* Для кнопок, меток */
--tracking-wider: 0.05em;     /* Для uppercase текста */
--tracking-widest: 0.1em;     /* Для акцентов */
```

### Применение

```css
/* Большой заголовок */
.hero-title {
  letter-spacing: var(--tracking-tight);
}

/* Кнопка */
.button {
  letter-spacing: var(--tracking-wide);
}

/* Uppercase метка */
.label-uppercase {
  text-transform: uppercase;
  letter-spacing: var(--tracking-wider);
}
```

---

## Готовые классы

### Заголовки

```html
<!-- H1 - 36px, bold, tight line height -->
<h1 className="heading-1">Главный заголовок</h1>

<!-- H2 - 30px, bold, tight line height -->
<h2 className="heading-2">Заголовок секции</h2>

<!-- H3 - 24px, semibold, snug line height -->
<h3 className="heading-3">Подзаголовок</h3>

<!-- H4 - 20px, semibold, snug line height -->
<h4 className="heading-4">Заголовок карточки</h4>

<!-- H5 - 18px, semibold, normal line height -->
<h5 className="heading-5">Небольшой заголовок</h5>

<!-- H6 - 16px, semibold, normal line height -->
<h6 className="heading-6">Минимальный заголовок</h6>
```

### Основной текст

```html
<!-- Крупный текст - 18px -->
<p className="body-large">
  Крупный основной текст для вводных параграфов
</p>

<!-- Обычный текст - 16px -->
<p className="body-base">
  Основной текст приложения, параграфы
</p>

<!-- Мелкий текст - 14px -->
<p className="body-small">
  Второстепенная информация
</p>

<!-- Очень мелкий текст - 12px -->
<p className="body-xs">
  Вспомогательный текст, примечания
</p>
```

### Специализированные классы

```html
<!-- Метка поля формы -->
<label className="text-label">
  Название поля
</label>

<!-- Подпись -->
<span className="text-caption">
  Подпись к изображению или пояснение
</span>

<!-- Overline (надзаголовок) -->
<div className="text-overline">
  Категория / Раздел
</div>

<!-- Текст кнопки -->
<button>
  <span className="text-button">Нажмите здесь</span>
</button>

<!-- Ссылка -->
<a href="#" className="text-link">
  Ссылка с подчеркиванием
</a>
```

---

## Примеры использования

### Пример 1: Карточка статистики

```tsx
function StatCard({ title, value, description }) {
  return (
    <div className="bg-white dark:bg-gray-800 rounded-lg p-4">
      {/* Overline */}
      <div className="text-overline mb-2">
        Статистика
      </div>

      {/* Значение */}
      <div className="heading-2 mb-1">
        {value}
      </div>

      {/* Заголовок */}
      <h3 className="heading-4 mb-2">
        {title}
      </h3>

      {/* Описание */}
      <p className="body-small">
        {description}
      </p>
    </div>
  )
}
```

### Пример 2: Форма с метками

```tsx
function EntryForm() {
  return (
    <form className="space-y-4">
      {/* Поле с меткой */}
      <div>
        <label htmlFor="category" className="text-label block mb-2">
          Категория
        </label>
        <select id="category" className="body-base w-full">
          <option>Development</option>
        </select>
        <p className="text-caption mt-1">
          Выберите категорию задачи
        </p>
      </div>

      {/* Кнопка */}
      <button className="bg-blue-500 text-white px-4 py-2 rounded">
        <span className="text-button">Сохранить</span>
      </button>
    </form>
  )
}
```

### Пример 3: Модальное окно

```tsx
function Modal({ title, children }) {
  return (
    <div className="bg-white dark:bg-gray-800 rounded-lg p-6">
      {/* Заголовок модального окна */}
      <h2 className="heading-3 mb-4">
        {title}
      </h2>

      {/* Контент */}
      <div className="body-base">
        {children}
      </div>

      {/* Футер с кнопками */}
      <div className="flex gap-3 mt-6">
        <button className="text-button px-4 py-2">
          Отмена
        </button>
        <button className="text-button px-4 py-2 bg-blue-500 text-white">
          Подтвердить
        </button>
      </div>
    </div>
  )
}
```

### Пример 4: Использование CSS переменных напрямую

```tsx
// В styled-components или inline styles
const CustomHeading = styled.h1`
  font-size: var(--font-size-4xl);
  font-weight: var(--font-bold);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
  color: var(--text-primary);

  @media (max-width: 640px) {
    font-size: var(--font-size-2xl);
  }
`

// В inline styles (не рекомендуется, используйте классы)
<h1 style={{
  fontSize: 'var(--font-size-3xl)',
  fontWeight: 'var(--font-bold)',
  lineHeight: 'var(--leading-tight)',
  color: 'var(--text-primary)'
}}>
  Заголовок
</h1>
```

---

## Цветовая система

### Семантические цвета текста

```css
/* Светлая тема */
--text-primary: rgb(17, 24, 39);      /* Основной текст */
--text-secondary: rgb(75, 85, 99);    /* Второстепенный */
--text-tertiary: rgb(156, 163, 175);  /* Вспомогательный */
--text-disabled: rgb(209, 213, 219);  /* Отключенный */
--text-link: rgb(59, 130, 246);       /* Ссылки */
--text-link-hover: rgb(37, 99, 235);  /* Ссылки hover */

/* Темная тема */
--text-primary: rgb(243, 244, 246);   /* Основной текст */
--text-secondary: rgb(209, 213, 219); /* Второстепенный */
--text-tertiary: rgb(107, 114, 128);  /* Вспомогательный */
--text-disabled: rgb(75, 85, 99);     /* Отключенный */
--text-link: rgb(96, 165, 250);       /* Ссылки */
--text-link-hover: rgb(59, 130, 246); /* Ссылки hover */
```

### Классы цветов

```html
<!-- Основной цвет текста -->
<p className="text-color-primary">
  Основной текст
</p>

<!-- Второстепенный цвет -->
<p className="text-color-secondary">
  Второстепенная информация
</p>

<!-- Вспомогательный цвет -->
<p className="text-color-tertiary">
  Вспомогательный текст
</p>

<!-- Отключенный -->
<p className="text-color-disabled">
  Недоступный текст
</p>
```

---

## Best Practices

### ✅ Рекомендации

1. **Используйте готовые классы** вместо inline стилей:
   ```tsx
   // ✅ Хорошо
   <h1 className="heading-1">Заголовок</h1>

   // ❌ Плохо
   <h1 style={{ fontSize: '36px', fontWeight: 700 }}>Заголовок</h1>
   ```

2. **Следуйте иерархии** заголовков:
   ```tsx
   // ✅ Хорошо
   <h1 className="heading-1">Главная</h1>
   <h2 className="heading-2">Секция</h2>
   <h3 className="heading-3">Подсекция</h3>

   // ❌ Плохо (пропуск уровня)
   <h1 className="heading-1">Главная</h1>
   <h3 className="heading-3">Секция</h3>
   ```

3. **Используйте семантические теги**:
   ```tsx
   // ✅ Хорошо
   <h2 className="heading-2">Заголовок</h2>
   <p className="body-base">Текст</p>

   // ❌ Плохо
   <div className="heading-2">Заголовок</div>
   <div className="body-base">Текст</div>
   ```

4. **Учитывайте контраст** для accessibility:
   ```tsx
   // Используйте семантические цвета
   <p className="text-color-primary">Читаемый текст</p>

   // Избегайте низкоконтрастных комбинаций
   ```

5. **Адаптивность**:
   ```tsx
   // Классы автоматически адаптируются на мобильных
   <h1 className="heading-1">
     {/* 36px на desktop, 30px на mobile */}
     Заголовок
   </h1>
   ```

### ❌ Чего избегать

1. **Не смешивайте системы**:
   ```tsx
   // ❌ Плохо
   <h1 className="heading-1 text-4xl">Заголовок</h1>
   ```

2. **Не используйте произвольные размеры**:
   ```tsx
   // ❌ Плохо
   <p style={{ fontSize: '17px' }}>Текст</p>

   // ✅ Хорошо
   <p className="body-large">Текст</p>
   ```

3. **Не игнорируйте line-height**:
   ```tsx
   // ❌ Плохо
   <p className="text-base">
     {/* Стандартный Tailwind line-height может быть не оптимальным */}
   </p>

   // ✅ Хорошо
   <p className="body-base">
     {/* Оптимизированный line-height из системы */}
   </p>
   ```

---

## Миграция существующего кода

### Постепенная миграция

1. **Найдите произвольные размеры шрифтов**:
   ```bash
   # Поиск inline styles
   grep -r "fontSize:" src/

   # Поиск произвольных text-* классов
   grep -r "text-\[" src/
   ```

2. **Замените на системные классы**:
   ```tsx
   // Было
   <h1 className="text-3xl font-bold">Заголовок</h1>

   // Стало
   <h1 className="heading-2">Заголовок</h1>
   ```

3. **Обновите custom компоненты**:
   ```tsx
   // Button.tsx - было
   const Button = ({ children }) => (
     <button className="text-sm font-medium">
       {children}
     </button>
   )

   // Button.tsx - стало
   const Button = ({ children }) => (
     <button className="text-button">
       {children}
     </button>
   )
   ```

---

## Расширение системы

### Добавление новых размеров

Если нужны дополнительные размеры:

```css
/* В src/index.css, в блоке :root */
:root {
  /* Существующие... */

  /* Новые размеры */
  --font-size-6xl: 4rem;    /* 64px */
  --font-size-7xl: 5rem;    /* 80px */
}

/* Новый класс */
.heading-hero {
  font-size: var(--font-size-6xl);
  font-weight: var(--font-bold);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tighter);
  color: var(--text-primary);
}
```

### Добавление вариантов темы

```css
.theme-compact {
  --font-size-base: 0.875rem;  /* Уменьшенный base */
  --leading-normal: 1.4;        /* Более сжатый интерлиньяж */
}

.theme-large {
  --font-size-base: 1.125rem;  /* Увеличенный base */
  --leading-normal: 1.6;        /* Более расслабленный */
}
```

---

## Accessibility

### WCAG Compliance

1. **Минимальный размер шрифта**: 16px (--font-size-base)
2. **Контраст текста**: Используйте семантические цвета
3. **Line height**: Минимум 1.5 для основного текста
4. **Адаптивность**: Автоматическое масштабирование

### Screen Readers

```tsx
// Используйте семантические теги
<h1 className="heading-1">Заголовок страницы</h1>
<p className="body-base">Основной текст</p>

// Не забывайте про aria-labels для нетекстовых элементов
<button aria-label="Закрыть" className="text-button">
  <X />
</button>
```

---

## Примеры визуальной иерархии

### Dashboard Header

```tsx
<header>
  <div className="text-overline">Time Tracker</div>
  <h1 className="heading-1">Dashboard</h1>
  <p className="body-large text-color-secondary">
    Обзор вашей продуктивности
  </p>
</header>
```

### Карточка с данными

```tsx
<div className="card">
  <div className="text-caption text-color-tertiary">Сегодня</div>
  <div className="heading-2">7.5ч</div>
  <h3 className="heading-5 mb-2">Отработано</h3>
  <p className="body-small text-color-secondary">
    +15% по сравнению со вчера
  </p>
</div>
```

---

**Дата создания:** 2025-11-16
**Версия:** 1.0
**Статус:** Реализовано

## Следующие шаги

- [ ] Миграция существующих компонентов на новую систему
- [ ] Добавление Storybook для визуализации всех стилей
- [ ] Создание UI Kit документации
- [ ] Настройка линтера для контроля использования системы
