# 💡 Анализ лучшего решения для анимации изменения размера модального окна

## 📅 Дата создания: 2025-01-05

## 🔍 Исследование решений

### Вариант 1: Framer Motion (layout prop)

**Плюсы:**

- Встроенная поддержка анимации высоты
- Автоматическое измерение размеров
- Хорошая интеграция с React

**Минусы:**

- Может конфликтовать с существующими framer-motion аккордеонами
- Дополнительная зависимость (уже есть в проекте)
- Сложнее контролировать timing

**Вывод:** ⚠️ Возможно, но может создать конфликты

---

### Вариант 2: CSS Transition + useLayoutEffect (Текущий подход)

**Плюсы:**

- Полный контроль над анимацией
- Нет дополнительных зависимостей
- Работает с любым контентом

**Минусы:**

- Сложная синхронизация React и DOM
- Нужно правильно измерять высоту
- Проблемы с уменьшением размера

**Вывод:** ✅ Подходит, но нужны улучшения

---

### Вариант 3: ResizeObserver

**Плюсы:**

- Автоматическое отслеживание изменений размера
- Не зависит от React lifecycle

**Минусы:**

- Реагирует на все изменения размера (включая аккордеоны)
- Нужна фильтрация событий
- Сложнее изолировать от аккордеонов

**Вывод:** ⚠️ Возможно, но сложнее фильтровать события

---

## ✅ Рекомендуемое решение: Улучшенный вариант 5 (fade-smooth)

### Ключевые принципы

1. **Использование useLayoutEffect**
   - Синхронное выполнение после обновления DOM
   - Измерение высоты до paint

2. **Правильная последовательность**
   - Измерение текущей высоты
   - Установка фиксированной высоты
   - Fade-out контента (250ms)
   - Изменение контента (React)
   - Ожидание полного рендера (тройной RAF)
   - Измерение новой высоты
   - Установка новой высоты через RAF (для гарантии transition)
   - Fade-in контента (50ms)
   - Возврат к auto (350ms)

3. **Изоляция от аккордеонов**
   - Реагировать только на `activeTab`
   - Игнорировать изменения `expandedVersions`, `expandedSections`, `expandedTechs`
   - Использовать `previousTabRef` для отслеживания изменений

4. **Гарантия transition**
   - Transition должен быть активен ДО изменения высоты
   - Использовать `requestAnimationFrame` перед установкой новой высоты
   - Transition активен всегда когда `height !== 'auto'`

5. **Правильная очистка**
   - Хранить все таймеры в массиве
   - Очищать все таймеры в cleanup функции
   - Проверять `isAnimatingRef` перед выполнением операций

---

## 🎯 Улучшения по сравнению с предыдущей версией

### Проблема 1: Уменьшение размера не анимируется

**Решение:**

- Использовать `requestAnimationFrame` перед установкой новой высоты
- Гарантировать применение transition ДО изменения высоты
- Transition активен всегда когда `height !== 'auto'`

### Проблема 2: Конфликт с аккордеонами

**Решение:**

- `useLayoutEffect` с зависимостью только `[activeTab, isOpen]`
- Проверка изменения `activeTab` перед запуском анимации
- Игнорирование изменений состояний аккордеонов

### Проблема 3: Рывки и нестабильность

**Решение:**

- Упрощенная логика измерения
- Правильная последовательность таймеров
- Использование `requestAnimationFrame` для синхронизации

---

## 📝 Псевдокод решения

```javascript
useLayoutEffect(() => {
  // 1. Проверки
  if (!isOpen || !contentRef.current) return
  if (activeTab === previousTabRef.current) return

  // 2. Измерение текущей высоты
  const currentHeight = contentRef.current.scrollHeight
  const savedHeight = heightRef.current || currentHeight

  // 3. Установка фиксированной высоты и fade-out
  setContentHeight(`${savedHeight}px`)
  setIsFadingOut(true)
  isAnimatingRef.current = true

  // 4. Ожидание fade-out (250ms)
  setTimeout(() => {
    // 5. Ожидание полного рендера (тройной RAF)
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        requestAnimationFrame(() => {
          // 6. Измерение новой высоты
          const newHeight = contentRef.current.scrollHeight

          // 7. Установка новой высоты через RAF (для гарантии transition)
          requestAnimationFrame(() => {
            setContentHeight(`${newHeight}px`)

            // 8. Fade-in контента (50ms)
            setTimeout(() => {
              setIsFadingOut(false)

              // 9. Возврат к auto (350ms)
              setTimeout(() => {
                setContentHeight('auto')
                isAnimatingRef.current = false
                previousTabRef.current = activeTab
              }, 350)
            }, 50)
          })
        })
      })
    })
  }, 250)
}, [activeTab, isOpen])
```

---

## 🎨 CSS стили

```css
.modal-content-size-animation {
  position: relative;
  transition: height 0.35s cubic-bezier(0.4, 0, 0.2, 1);
}

.content-wrapper-fade {
  transition: opacity 0.25s ease-in-out;
}

.content-wrapper-fade.fade-out {
  opacity: 0;
  pointer-events: none;
}

.content-wrapper-fade.fade-in {
  opacity: 1;
  pointer-events: auto;
}

.content-wrapper-fade.fade-in > div:first-child {
  animation: fadeInUp 0.35s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

---

## ✅ Итоговое решение

**Подход:** Улучшенный вариант 5 с правильной последовательностью и синхронизацией

**Ключевые особенности:**

1. useLayoutEffect для синхронного измерения
2. Правильная последовательность: fade-out → изменение контента → fade-in
3. Тройной RAF для гарантии полного рендера
4. RAF перед установкой новой высоты для гарантии transition
5. Изоляция от аккордеонов через зависимости useLayoutEffect
6. Правильная очистка таймеров

**Статус:** Готов к реализации
