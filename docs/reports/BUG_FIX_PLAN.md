## 📋 План исправления бага: Неверный расчет заработка за день при остановке таймера

### Анализ
- **Цель:** Исправить отображение заработка за день в модальном окне редактирования записи при остановке таймера
- **Scope:** 
  - Исправление логики расчета заработка за день
  - Учет только сохраненных записей (исключение записей из таймера до сохранения)
  - Обновление отображения при изменении данных формы
- **Зависимости:** 
  - `src/components/modals/EditEntryModal.tsx` - компонент модального окна
  - `src/store/useEntriesStore.ts` - хранилище записей
  - `src/hooks/useEntryForm.js` - хук для работы с формой
- **Риски:** 
  - Возможные проблемы с производительностью при большом количестве записей
  - Необходимость проверки всех сценариев использования

### Шаги выполнения

#### Шаг 1: Анализ текущей логики
- **Файлы:** `src/components/modals/EditEntryModal.tsx`
- **Действия:** 
  - Изучить функцию `getDailyEarnings`
  - Понять, как она рассчитывает заработок
  - Определить, какие записи учитываются
- **Проверка:** Проверить логику в коде

#### Шаг 2: Исправление логики расчета
- **Файлы:** `src/components/modals/EditEntryModal.tsx`
- **Действия:** 
  - Изменить фильтрацию записей: исключить записи с `isManual: false` (созданные таймером, но не сохраненные)
  - Убедиться, что учитываются только сохраненные записи за день
  - Обновить логику для учета изменений в форме
- **Проверка:** Проверить, что расчет корректный

#### Шаг 3: Тестирование
- **Файлы:** Все затронутые файлы
- **Действия:** 
  - Протестировать сценарий: запустить таймер, остановить, проверить отображение заработка
  - Протестировать сценарий: добавить несколько записей за день, проверить расчет
  - Протестировать сценарий: изменить сумму в форме, проверить обновление
- **Проверка:** Все сценарии работают корректно

### Edge Cases
- [ ] Что если нет записей за день? (должно показывать 0)
- [ ] Что если есть только записи из таймера (не сохраненные)? (должно показывать 0)
- [ ] Что если редактируем существующую запись? (должно учитывать новое значение из формы)
- [ ] Что если удаляем запись? (должно пересчитать заработок)

### Тестирование
- [ ] Unit тесты (если есть)
- [ ] Ручная проверка всех сценариев
- [ ] Проверка edge cases

### Результат
После исправления:
- Заработок за день будет рассчитываться только из сохраненных записей
- Записи из таймера (до сохранения) не будут учитываться
- Отображение будет обновляться при изменении данных в форме

### Файлы для изменения:
- `src/components/modals/EditEntryModal.tsx` - основная логика расчета
- Возможно `src/hooks/useEntryForm.js` - если нужно обновить логику формы

### Примечания:
- Записи с `isManual: false` создаются таймером, но еще не сохранены пользователем
- После сохранения запись получает `isManual: true` и должна учитываться в расчете
- Необходимо убедиться, что логика работает корректно во всех сценариях

