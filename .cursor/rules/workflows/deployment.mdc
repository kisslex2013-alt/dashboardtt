---
description: Правила деплоя проекта на Cloudflare Pages - ГЛАВНЫЙ скрипт деплоя
alwaysApply: true
---

# 🚀 Правила деплоя проекта

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Главный скрипт деплоя

**ГЛАВНЫЙ ЕДИНСТВЕННЫЙ скрипт деплоя данного проекта:**

```
scripts/deploy-to-cloudflare-wrangler.js
```

### Правила работы со скриптом деплоя:

1. **НЕ МЕНЯЙ** файл `scripts/deploy-to-cloudflare-wrangler.js` **БЕЗ ЯВНОГО РАЗРЕШЕНИЯ** пользователя
2. **НЕ СОЗДАВАЙ** альтернативные скрипты деплоя без разрешения
3. **НЕ ИЗМЕНЯЙ** логику деплоя в этом скрипте без разрешения
4. Если нужно что-то изменить - **СНАЧАЛА СПРОСИ** пользователя

### Команда для деплоя:

```bash
npm run deploy
```

Эта команда вызывает `scripts/deploy-to-cloudflare-wrangler.js` через Wrangler CLI.

### Альтернативные скрипты:

- `scripts/deploy-to-cloudflare.js` - **НЕ ИСПОЛЬЗУЕТСЯ** (старый скрипт через API)
- Другие скрипты деплоя - **НЕ СОЗДАВАТЬ** без разрешения

### Процесс деплоя:

1. Сборка проекта: `npm run build`
2. Деплой: `npm run deploy`
3. Скрипт автоматически:
   - Проверяет наличие `dist/index.html`
   - Проверяет/создает проект Cloudflare Pages
   - Деплоит через Wrangler CLI
   - Устанавливает production деплой
   - Очищает старые деплои
   - Проверяет Web Analytics

### Если нужно изменить скрипт:

**ОБЯЗАТЕЛЬНО** спроси пользователя перед любыми изменениями:
- "Можно ли изменить скрипт деплоя для [причина]?"
- "Нужно ли добавить [функциональность] в скрипт деплоя?"

**ПРАВИЛО:** "NEVER modify deploy-to-cloudflare-wrangler.js without explicit user permission. Always ask first."
