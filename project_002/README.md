# 📦 Пакет правил и настроек для Cursor IDE

Этот пакет содержит все правила, конфигурации и скрипты для работы с Cursor IDE и системой контроля качества кода.

## 📁 Содержимое

- **`.cursor/`** - Все правила Cursor IDE:
  - `rules/` - Основные правила проекта
  - `rules/agents/` - Правила для специализированных агентов
  - `rules/workflows/` - Workflow для различных задач
  - `rules/templates/` - Шаблоны для разработки
  - Дополнительные MD файлы с документацией

- **`.cursorrules`** - Основной файл правил для Cursor IDE (в корне проекта)

- **`.agent-enforcer.json`** - Конфигурация для agent-enforcer (проверка качества кода)

- **`.enforcer/`** - Дополнительные настройки enforcer

- **`scripts/`** - Вспомогательные скрипты:
  - `sync-enforcer-rules.js` - Синхронизация правил между .cursor и .enforcer
  - `enforcer-to-beads.js` - Интеграция с Beads (создание задач из проблем качества)
  - `quality-check-modified.js` - Проверка качества измененных файлов
  - `install-beads.ps1` - Установка Beads (PowerShell скрипт)

## 🚀 Установка в новый проект

### Шаг 1: Скопируйте содержимое

Скопируйте все содержимое этой папки в корень вашего нового проекта:

```bash
# Windows PowerShell
Copy-Item -Path "project_002\*" -Destination "C:\path\to\your\project\" -Recurse -Force

# Linux/Mac
cp -r project_002/* /path/to/your/project/
```

### Шаг 2: Установите зависимости

Убедитесь, что у вас установлены необходимые инструменты:

```bash
# Установка agent-enforcer (если еще не установлен)
npm install -g agent-enforcer

# Установка Beads (опционально, для интеграции с задачами)
# Запустите scripts/install-beads.ps1 или установите вручную
```

### Шаг 3: Настройте package.json

Добавьте следующие скрипты в `package.json`:

```json
{
  "scripts": {
    "quality:check": "agent-enforcer check src/ || true",
    "quality:check:verbose": "agent-enforcer check src/ --verbose || true",
    "quality:check:modified": "node scripts/quality-check-modified.js || true",
    "quality:sync-rules": "node scripts/sync-enforcer-rules.js",
    "quality:to-beads": "node scripts/enforcer-to-beads.js"
  }
}
```

### Шаг 4: Проверьте работу

```bash
# Проверка синхронизации правил
npm run quality:sync-rules

# Проверка качества кода
npm run quality:check
```

## 📋 Основные правила

### Планирование

Всегда создавайте план перед началом работы. При использовании триггерных фраз ("по правилам", "структурированно", "с планированием", "workflow") применяется полный workflow.

### React Patterns

- Хуки только на верхнем уровне
- Обязательные dependency arrays
- Очистка в useEffect
- Уникальные key props (не индексы)

### Zustand Stores

- Экспортируйте только кастомные хуки (атомарные селекторы)
- Разделяйте actions от state

### Tailwind CSS

- Mobile-first подход
- Группировка классов по категориям
- НЕ используйте cursor-pointer на интерактивных элементах

## 🔧 Настройка под ваш проект

1. **Обновите `.agent-enforcer.json`** под ваш стек технологий
2. **Адаптируйте правила в `.cursor/rules/`** под специфику проекта
3. **Настройте скрипты** в `scripts/` под вашу структуру проекта

## 📚 Дополнительная документация

- Основные правила: `.cursor/rules/000-core.mdc`
- Workflow планирования: `.cursor/rules/workflows/planning.mdc`
- Уроки и исправления: `.cursor/rules/lessons-learned.mdc`

## ⚠️ Важно

- Не копируйте файлы с секретами (`.cursor/mcp.json` - только `.cursor/mcp.json.example`)
- Проверьте пути в скриптах - они могут требовать адаптации
- Убедитесь, что структура проекта соответствует ожиданиям правил

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи agent-enforcer
2. Убедитесь, что все зависимости установлены
3. Проверьте синхронизацию правил: `npm run quality:sync-rules`

---

**Версия:** 1.0  
**Дата создания:** 2025-01-19  
**Проект-источник:** Dashboardtt

